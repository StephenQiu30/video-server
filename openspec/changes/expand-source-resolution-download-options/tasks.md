# Tasks

## 1. OpenSpec And Docs

- [x] 1.1 新增多视频源与清晰度下载 OpenSpec 变更。
- [x] 1.2 更新 M1 产品范围、架构/执行计划和测试验收文档。

## 2. Backend

- [x] 2.1 `POST /api/parse` 响应增加来源识别字段和清晰度预设字段。
- [x] 2.2 解析适配层生成推荐、1080p、720p、480p、360p 短 selector。
- [x] 2.3 任务创建限制 `format_id` 长度，避免超过数据库字段。
- [x] 2.4 补充单元测试覆盖清晰度预设和 selector 传递边界。

## 3. Frontend

- [x] 3.1 前端类型同步解析响应增强字段。
- [x] 3.2 工作台默认展示清晰度预设、来源和不可用说明。
- [x] 3.3 创建任务时禁用不可用清晰度，不展示复杂原始格式。
- [x] 3.4 工作台改为先选择清晰度、再创建单个下载任务，避免默认推荐格式被误认为直接下载。

## 4. Compliance And Validation

- [x] 4.1 修正合规 smoke，允许视频源名称但继续禁止绕过能力。
- [x] 4.2 运行 OpenSpec、后端测试、前端 lint/build 和合规 smoke。
