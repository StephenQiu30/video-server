# Tasks

## 1. OpenSpec

- [x] 1.1 新增 Ant Design Pro 蓝白下载器 UI 简化变更。
- [x] 1.2 修订变更说明，明确 `/` 与 `/workspace` 共用标准工具页，不新增重复 OpenSpec 变更。

## 2. Frontend

- [x] 2.1 工作台改为单列居中下载器页面，删除左右分栏。
- [x] 2.2 成功任务改为蓝白轻量信息展示，不再使用大块绿色面板。
- [x] 2.3 合规页改为单列上下结构。
- [x] 2.4 全局样式收敛到 Ant Design Pro 蓝白基调。
- [x] 2.5 抽出复用下载器页面，让 `/` 与 `/workspace` 共用同一套链接解析、任务创建和下载状态逻辑。
- [x] 2.6 删除说明型 SaaS 首页，首页直接进入下载器工具体验。

## 3. Validation

- [x] 3.1 运行 OpenSpec 校验。
- [x] 3.2 运行前端 lint 和 build。
- [x] 3.3 浏览器检查 `/`、`/workspace`、`/tasks`、`/compliance` 桌面和移动端。
