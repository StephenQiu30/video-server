# 全站样式一致性检查 — 2026-09-25

本文件记录本轮检查与修复证据；唯一视觉标准仍为 design.md。

## 范围与结论

对 19 个页面路由的实现进行检索，并在当前 localhost:8101 实例检查：首页、下载记录、处理记录、剧本文档、平台状态、账户、用户管理、文件管理、AI 服务、平台目录、下载分析、操作日志、下载详情、剧本详情、分析详情、使用指南、登录、注册、解析结果。另检查匿名首页。

已修复本轮发现的页头尺寸、面包屑、列表布局、分页冗余和长文本越列问题。此结论限于下列实际查看的状态，不表示穷尽所有数据、错误状态、浏览器和辅助技术。

## 发现与处理

| 问题 | 原因 | 修复 |
| --- | --- | --- |
| 处理记录与其他列表不一致 | 单独用 ItemGroup 和网格模拟表头，保留旧批量工具栏 | 改用共享 DataTable，官方 Table + TanStack；无边框、垂直居中；勾选后才显示工具栏 |
| 页头与普通表单操作高度混用 | 多个消费者显式指定 lg，输入仍为默认尺寸 | 下载、剧本、账户、认证、平台编辑等改用默认按钮尺寸；首页既定 56px 入口保留 |
| 使用指南的导航与页头不同 | 手工面包屑、独立居中容器、营销字号 | 官方 Breadcrumb 接入 PageNavigation；统一 32px 导航行、24px 下间距、PageHeader 和 inner-page |
| 部分标题过大 | 登录、缺失资源、分析区保留 clamp 自定义字号 | 恢复 PageHeader、Empty 默认层级；AI 分析区和指南章节使用 20px 标题 |
| 管理员页重复总数 | 顶部汇总与分页各自拼接文案 | 移除用户、文件、平台目录、AI 配置与日志冗余总数，保留共享分页 |
| 管理员用户名/邮箱挤入下一列 | 固定列宽中的长连续文本不能断行 | 在用户名和邮箱内容上允许断词；未改变表格基础样式 |
| 报告内表格仍有分隔线 | Markdown 和改写结果漏用共享无边框规则 | 统一 table-borderless |
| 平台状态刷新外观不同 | 使用 ghost，与其他页头刷新不同 | 使用官方 outline 默认尺寸 |

新增的官方 Breadcrumb 仅做项目格式适配、Phosphor SSR 入口适配；保留当前页禁用链接语义，并注明静态 lint 例外。首次构建暴露客户端图标入口调用 createContext 的服务端错误，改用 SSR 入口后构建通过。

## 浏览器证据

截图在本机 `/tmp/framefetch-style-audit/`，包含业务数据，不提交图片到仓库。均读取保存后的图像核对；加载中截图、主题切换瞬间缩放异常截图及登录自动跳转后的首页截图不作为对应页面证据。

- 亮色桌面：01–16 系列涵盖主要业务和管理员页面；18-login.png 为独立未登录浏览器的登录页；19-public-home.png 为匿名首页。
- 修复后代表截图：after-activity.png、after-calendar.png、after-select.png、after-users.png、after-guide-dark.png。
- 390×844 窄屏：mobile-home/history/activity/users/documents/providers；dark-mobile-files/ai/catalog/logs/register/guide；dark-mobile-video/screenplay/command；final-mobile-users.png 为最终长文本修复。
- 共享导航实测：桌面 32px 高、下间距 24px、标题 30px；390px 用户页导航 32px，文档可视宽与滚动宽均为 375px（其余 15px 为浏览器滚动条）。宽表在自身容器内横向滚动。
- 处理记录全选选中 4 条可下载链接，其他类型与不可下载记录不可选；取消选择后工具栏收起。未触发真实批量下载或删除。
- 日历正常打开并切换到次月；状态选择框覆盖内容区域，无透明穿透；Command K 展示链接、本地视频、剧本文档三个操作，Escape 关闭。未进行生产数据写操作。
- 所查看的解析结果已过期，验证了过期提示与返回导航；本轮未更新真实解析，因此不将该状态视为画质选择全流程验收。

## 工程验证

- format:check：通过。
- lint（含 TypeScript）：通过。
- 单元测试：91 个文件，480 项通过。增加混合记录同 ID 的选择隔离回归；确认勾选与取消仅发起 GET，不创建任务。
- 生产构建：通过。
- Docker：仅重建并重启 frontend，浏览器复核当前实例；未修改后端或数据。

## 验收边界

已覆盖主要页面及代表性桌面、窄屏和明暗主题组合，未执行每个页面的全部主题×尺寸×业务状态笛卡尔组合。真实上传、模型分析、下载创建、批量删除及角色变更未作为本轮视觉检查执行；已有工程测试不替代这些真实业务验收。

## 首页标题与留白调整 — 2026-09-25 后续

按用户截图反馈，将首页 PageHeader 使用独立 lg 层级：桌面标题 36px、手机 30px、说明 16px。首页外层改用 inner-page，上下留白与业务页面共用桌面 32px / 手机 24px，移除原桌面 96px 的额外留白。

重建后的真实浏览器已验证 1440px 桌面及 390px 手机；实测字号与 padding 符合上述值，手机文档 scrollWidth 与 clientWidth 均为 375px，无页面横向溢出，亮暗主题正常。format:check、lint、480 项测试及生产构建通过，仅重建 frontend。
