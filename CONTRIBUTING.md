# Contributing

`video-server` 当前处于全面重新设计阶段。

## 可接受的贡献

1. 完善 Codex 协作规范、Subagent 或 Skill。
2. 按 `PRD -> Design -> Plan -> Acceptance` 新建经过评审的执行文档。
3. 在文档门禁通过后，提交由失败测试约束的最小实现。

## 不接受的贡献

1. 从 Git 历史或备份中恢复旧产品实现、旧接口、旧依赖或旧部署方式。
2. 在需求和设计未批准前预选技术栈或创建产品脚手架。
3. 引入兼容层、双轨实现、临时日志、缓存、生成物或一次性状态文件。

## 提交与验证

1. 遵循 `AGENTS.md` 的提交类型、TDD 顺序和 PR 门禁。
2. 当前空仓没有产品验证命令。
3. 新验证方式必须随新设计和实现一并进入仓库。
