# Long-running video analysis can legitimately hold a delivery for many minutes.
heartbeat = 60
consumer_timeout = 3600000

# Command envelopes only contain IDs and bounded metadata, never video payloads.
max_message_size = 1048576

# Keep the management and Prometheus endpoints on their standard internal ports.
management.tcp.port = 15672
prometheus.tcp.port = 15692
