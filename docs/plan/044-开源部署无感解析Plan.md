# 开源部署无感解析 Plan

日期：2026-09-22。状态：实施中，P9.01 已进入代码完成待验收。本文接管 BACKLOG P9 的执行细节，保留 P9.01–P9.14 编号；文档拆分不关闭任何业务任务。

[PRD](../prd/044-开源部署无感解析PRD.md) 定义 FR／NFR／AC；[Design](../design/044-开源部署无感解析需求与系统设计.md) 定义技术方案；[BACKLOG P9](../../BACKLOG.md#p9-开源部署无感解析执行计划044) 只提供入口和任务导航。**任务状态、承接者、阻塞和证据仅在本文维护**，不在 BACKLOG 复制勾选项。

## 1. SDD 工作方式

1. 从目标任务读取 PRD 条目、Design 章节和依赖；核对当前代码、工作区已有改动及调用方。未通过开始条件，不进入依赖实现。
2. 先为业务规则、故障或安全边界建立可判定的测试／验证场景，再实现最小闭环；代码生成只基于服务端 OpenAPI。
3. 按任务列出的开发步骤实现；新事实改变需求时先改 PRD，改变技术决策时先改 Design，同步本文的影响范围与验收。
4. 运行任务验证和所触及模块的仓库门禁；记录预期／实测、commit、版本和脱敏证据。代码完成不等于平台实测完成。
5. 检查全部关联 FR、NFR、AC 后更新唯一状态；发布依赖跨任务门禁，不能因一个后端任务完成就删除客户端仍依赖的接口。

开始条件（DoR）：依赖产物可用且必要验收通过；本任务 FR／NFR／AC 无未归属项；现有代码和未提交原型已盘点；真实样本、环境、预算与承接者已确定。遇到外部阻断记录事实和可继续的独立工作，不用假数据替代平台通过。

完成条件（DoD）：实现和被替代路径处理符合 Design；契约及受影响客户端同步；全部关联验收通过；必要运行手册更新；最小充分检查和脱敏证据完整。仅“已验收”可勾选。

状态使用：未开始 → 进行中 → 代码完成待验收 → 已验收；阻塞时写明阻塞点、恢复条件与负责人。每个任务标题下的勾选项及状态字段是唯一事实；步骤编号只标识开发顺序，不另建进度表。估时与日期在承接者和环境明确后填写，不用未经评估的日期承诺进度。

## 2. 里程碑与发布依赖

| 里程碑 | 任务范围 | 退出条件 |
| --- | --- | --- |
| M1 契约与执行基础 | P9.01 → P9.02／P9.03 | 类型与错误语义、持久接单、访客生命周期和故障夹具验证通过 |
| M2 首平台与产品闭环 | P9.04、P9.06、P9.07、P9.08、P9.09、P9.10／P9.12 | 空状态公开解析、真实文件交付、必要授权、双客户端及标准部署互通；平台扩展不妨碍先验证首条链路 |
| M3 覆盖与运行维护 | P9.05、P9.11、P9.13；P9.09 完整准入 | 计划平台均有处置，Web 会话收敛，升级回滚和部署门禁通过 |
| M4 综合验收 | P9.14 | 全部关联验收、容量、恢复、真实平台与客户端证据闭环 |

里程碑用于管理，不代替每项依赖，也不代表可跳过后续发布门禁。P9.10 缓存工作不等待 P9.11 会话替换。P9.02、P9.07、P9.08 的开发契约先冻结供 Web／App 联调；P9.10 与 P9.12 完成前，不发布删除旧同步业务入口的破坏性替换。这是开发／发布依赖的区分，不增加第二套长期生产执行器。

执行主线：P9.01 → P9.02／P9.03 → P9.04 → P9.05。P9.06／P9.07／P9.08／P9.09 按各自依赖推进，P9.13 在覆盖与部署证据具备后收口。所有任务均有独立 NFR，不能把安全和恢复推迟到 P9.14。

## 3. 执行任务

<a id="p9-01"></a>

### P9.01 统一身份、错误和契约语义

- [ ] **P9.01**；状态：代码完成待验收；承接者：Codex；职责：后端架构／Provider／Web 与 App 契约；依赖：无。
  - 功能需求：FR-03、FR-06、FR-07、FR-13。分开 public、guest 与 account；盘点 040 未提交授权原型；明确唯一意图、操作、上下文唯一约束和错误动作，不直接将 fresh cookies 转成账号授权。
  - 非功能需求：NFR-06、NFR-08、NFR-09。准入、owner 和作用域先确定；只有 FastAPI/Pydantic 是契约编辑入口，不新增手写 schema 或第二套 Provider 框架。
  - 交付：类型／错误和策略变更、代码现状对照、生成契约及客户端迁移范围；明确稳定错误在任务状态与 HTTP 之间的职责。
  - 验收：AC-03、AC-06、AC-07；匿名、访客、账号及未知错误夹具不会隐式升权；模糊空响应不产生必需登录动作；源码、schema、生成类型一致。

设计定位：Design §3.1／3.2／3.3。改动范围：backend/app 的 providers 服务、schemas、core/error_codes 及契约测试。验证入口：V-B、V-C（见 §4）。

1. **P9.01.S1**：盘点已提交与未提交路径，逐项记录复用／替代关系和当前调用方；固定身份、预算和错误职责。
2. **P9.01.S2**：补齐 public／guest／account 类型、owner／作用域约束以及任务失败与 HTTP 错误映射。
3. **P9.01.S3**：用匿名失败、明确账号要求、权限拒绝等反例固化契约，生成客户端并记录后续迁移范围。

执行记录（2026-09-22）：

- 承接范围：完成 P9.01.S1–S3 的服务端与 Web 契约实现；复核并复用 040 授权原型中的显式账号动作，删除“仅因平台名或模糊错误就进入授权”的推断。
- 实际变更：提交 `8b0be2fe`、审查修复 `ca919b5c`。新增 `guest` 访问模式、游客准备／可用状态与 `provider_guest_context_required` 稳定错误；`public_session` 只映射 guest，guest Runner 与 operator Runner 独立路由且缺失时不得互相回退；账号动作仅在该平台具备 account 路线、状态明确为 `access_required` 且最近探测明确失败时出现。fresh cookies 和匿名空响应分别归入游客准备或平台暂时不可用，配置缺失、验证失败与策略拒绝不再触发账号授权。状态投影按选中的策略／运行上下文判定，不再因平台“具备账号能力”就显示为账号线路。
- 审查修复：浏览器同步必须携带服务端管理员先创建的待处理事务，Native Host 校验事务、平台、来源和期限后才接收快照；同平台账号授权按部署级单飞，禁止不同管理员或不同来源并发覆盖；管理员的浏览器动作可从真实状态响应到达；卸载清除加密快照与来源标记；系统／验证错误不再提示不存在的账号动作。
- 契约与客户端：FastAPI/Pydantic 仍是唯一编辑入口，已从运行中的 `/openapi.json` 重新生成 Web `src/api`；Flutter 当前生成快照及迁移由 P9.12 承接，在其通过前不得发布依赖新枚举／错误的破坏性服务端替换。
- 确定性验证：后端 `ruff check`、`ruff format --check`、`mypy app` 通过；`pytest` 为 1942 passed、2 skipped（隔离 MinIO 未提供、Linux `O_PATH` 平台项）；Web `format:check`、`lint`、361 tests 与生产 `build` 通过；浏览器扩展脚本语法检查通过；从临时 code-first 服务执行 `openapi:check` 通过。开发 Compose 解析通过；生产 Compose 在提供必填 `SITE_URL=https://example.invalid` 后解析通过。测试覆盖 public／guest／account 分流、guest 不回退 operator、跨 owner／来源竞争、无服务端事务的浏览器同步、卸载清理、权限拒绝、模糊验证失败不展示账号动作及生成枚举一致性。
- 未验收边界：本项尚未勾选。P9.03 仍需实现 guest 生命周期，P9.06 仍需完成 owner／用途／主体绑定和乱序续接，P9.12 仍需同步 App 并做设备验证；这些项及真实平台样本未完成前，不宣称 AC-03／AC-06／AC-07 或跨客户端发布门禁已整体通过。

<a id="p9-02"></a>

### P9.02 持久解析意图与后台执行

- [ ] **P9.02**；状态：进行中；承接者：Codex；职责：downloads 后端／数据库／Worker；依赖：P9.01。
  - 功能需求：FR-02、FR-06。先保存 intent 与 outbox，再执行准备／解析；提供创建、查询、取消和原任务恢复；沿用 040 单一意图，不另建 parse_task。
  - 非功能需求：NFR-02、NFR-03、NFR-08。唯一约束、CAS、lease/fencing、有限预算、重投去重；事务内不等待上游。
  - 交付：当前态 schema.sql、ORM、事务用例、Worker 操作、生成 API；删除被替代同步创建路径前完成客户端切换门禁。
  - 验收：AC-02、AC-06、AC-11；50 次同 key 并发仅一意图，异输入冲突；接单／消息／落库断点不丢任务，旧 Worker 不可迟到覆盖，取消不复活。

设计定位：Design §3.2／3.3。改动范围：backend/sql/schema.sql、models、repositories/downloads、services/downloads、workers/outbox 与下载 Worker。验证入口：V-B、V-C、V-R（见 §4）。

1. **P9.02.S1**：定义当前态意图字段、唯一约束和事务所有者，覆盖空库及已有库幂等初始化。
2. **P9.02.S2**：实现持久接单与 outbox、查询／取消、租约执行、预算与可恢复调度；禁止事务内等上游。
3. **P9.02.S3**：注入提交／发布／执行／结果断点和迟到写入，验证幂等与 fencing；生产旧入口的删除等待双客户端发布门禁。

执行记录（2026-09-22）：

- 完成 P9.02.S1 和 S2 的仓储事务部分：新增单一 `download_intents` 当前态 SQL／ORM 与业务快照，输入只保存加密 envelope；owner/key、job 引用和 outbox aggregate/event/version 唯一约束；接单／查询／取消、租约领取／心跳、有限重试、失联恢复及结果原子落库。
- 事务所有者为 `IntentRepository`；无上游调用发生在事务内。inspection／formats 插入复用既有实现，与意图 ready 在同一事务提交，取消／旧 fence／旧 version／过期租约无法迟到写入。排队、重投和 Worker 接管均沿用原截止，最多三次执行。
- 确定性验证：178 项关联 PostgreSQL 集成、downloads 仓储／模型／服务测试通过；Ruff、格式检查和 mypy 通过。新增 16 项集成场景包含 50 次同 key 接单、20 次竞争领取、跨 owner 访问、异输入／策略冲突、outbox 写失败、结果保存失败、取消／完成竞争、重复扫尾、迟到 Worker、预算不重置及非法状态约束。空 schema 初始化及有在途意图的重复初始化通过；SQL 与 ORM 列集合一致。
- 本机已在现有项目 PostgreSQL 中幂等执行当前 `schema.sql` 并确认意图表存在；未另起基础服务。
- 后续完成 S2 的 API／Worker 接线：创建／查询／取消接口、分享输入规范化与加密、202 持久接单；新增有界解析消费槽，复用既有 Outbox、RabbitMQ 传输与恢复扫尾。复用 `InspectMedia.prepare` 计算结果，意图仓储原子提交，未创建第二套解析器。Web 客户端从运行中的 OpenAPI 生成。
- 准入复用 owner 事务锁和资源配额：同 key 重放不重复计量；每次新解析占一个每日任务、零下载字节，活跃解析占并发名额，取消后释放。用户确认创建下载时，另按已有下载资源规则计量。Worker 新增指纹密钥的生产校验与进程密钥白名单。
- S3 证据：真实 PostgreSQL／RabbitMQ 隔离命名空间验证未连接队列仍持久接单、发布失败后恢复、broker 已确认但数据库未确认造成的重复投递只落一份结果；真实 Uvicorn／HTTP 断开会终止实际子进程并等待清理。另覆盖 Worker 中断后原意图恢复、执行中取消无结果、三次暂时失败封顶、跨 owner 不可见。真实平台尚未用于本组测试。
- 审查修复：生产 Worker 拒绝默认指纹密钥；Runner 读取请求体后等待标准 ASGI 断连事件，避免轮询 `is_disconnected()` 的取消范围吞掉任务取消而挂起响应。真实网络与 TestClient 两种运行方式的 10 项检查通过。
- 本次检查：Ruff／格式／mypy 通过；后端全量 1967 passed、3 skipped（RabbitMQ 专项另以本机现有 broker 单独通过，隔离 MinIO 未提供，Linux O_PATH 不适用 macOS）；Web 361 tests、格式／lint／构建通过；新 API 已从运行中的 `/openapi.json` 生成。开发／生产 Compose 使用各自现有 env 解析通过；现有 PostgreSQL 已重复执行当前态 SQL。以上是本地证据，远端 CI 按提交 SHA 另行核验。
- 尚未完成：双客户端切换及部署进程级发布验收、授权等待与同意图续接（P9.06）、下载交接／引用清理（P9.07）。P9.02 保持未勾选，不把受控上游测试作为全部平台或 AC-02／AC-06／AC-11 整体验收。

<a id="p9-03"></a>

### P9.03 公开访客上下文生命周期

- [ ] **P9.03**；状态：进行中；承接者：Codex；职责：Provider／Runner／安全边界；依赖：P9.01。
  - 功能需求：FR-04、FR-05。空状态创建、验证、复用、刷新、冷却、回收；区分 guest 与 account，重用现有加密与租约原语。
  - 非功能需求：NFR-02、NFR-06、NFR-07、NFR-11。管理写入与执行只读分离，单平台材料、互斥维护、原子版本、时间／资源上限；无普通浏览器 Profile 依赖。
  - 交付：上下文记录与策略、平台管理过程、只读执行租约及必要浏览器边界；无意义的空 Provider 方法不创建。
  - 验收：AC-04、AC-05、AC-12；并发刷新只有一个维护者，迟到发布不覆盖，失效可重建；无跨平台／账号泄露、无限浏览器或平台失败时补充风暴。

设计定位：Design §3.1／3.2／3.5。改动范围：providers 持久域、services/providers、workers/runner 及受控出口。验证入口：V-B、V-R（见 §4）。

1. **P9.03.S1**：明确 guest/account 存储作用域、版本、加密来源和执行只读租约；先补跨平台隔离用例。
2. **P9.03.S2**：实现空状态准备、验证、原子发布、单飞刷新与冷却回收；浏览器只按平台需要启用。
3. **P9.03.S3**：验证同一上下文竞争、过期重建、迟到发布、资源耗尽和秘密清理；记录维护预算。

执行记录（2026-09-22）：

- S1 与 S2 的持久控制部分：独立 `provider_guest_contexts` 当前态表，作用域绑定平台、Profile 版本、客户端及出口；不会使用账号来源表的 provider 唯一键。复用既有 Fernet 原语，guest 密文的绑定域与 account 分离，跨平台／客户端／出口／版本／期限不能互换。
- 维护领取使用 PostgreSQL 短事务，实现全局最多 2 个、同平台最多 1 个准备者，租约最长 60 秒；发布检查 owner／fence／revision／有效租约。失败指数冷却 30–300 秒，不延长旧材料期限；失联准备进入冷却；过期材料清理；撤销留下不可自动复活的记录。
- 新增 8 项集成检查：30 个并发准备只有一个持有者、跨作用域平台限额与全局限额、刷新失败保留原期限、过期回收、迟到发布、撤销空记录、冷却退避、账号／访客密文隔离、空库与重复当前态 SQL 保留有效租约。
- 本组关联回归 61 项通过（guest、已有 account 来源恢复、当前态数据库及意图），Ruff／格式／mypy 通过。维护材料与状态尚未接入运行服务；不会因这组仓储检查通过就宣布平台可用。
- 平台只读实测：本机现有无凭据 Runner 经受控出口，从空 Cookie 访问抖音首页仅取得 `__ac_nonce`；第一方 `iesdouyin.com` 首页返回 301 并签发 `ttwid`。复用该访客材料和现有移动分享页请求头，既有固定公开样本返回 200／包含 `_ROUTER_DATA`；同材料调用 Web detail API 仍返回 403。因此不得把 Cookie 存在或首页成功视为下载成功，也不应新增 Web detail API 重试路线。本项未进行完整媒体文件验收。
- 后续接通：独立 `provider-guest` 管理进程从空 jar 请求固定第一方页面，仅允许 `ttwid`；独立 guest Runner 只读最长 90 秒租约，操作级 Cookie jar 位于 tmpfs。意图按部署中 guest endpoint 选择公开访客路线，暂不可用在原预算中重试，不进入账号授权。开发／生产 Compose 同步配置；API、Worker 不等待访客平台健康。
- 恢复与审查：损坏密文按 fence/revision 条件失效并冷却重建；维护含数据库总预算 25 秒，HTTP 15 秒；版本切换／撤销／跨平台材料／账号 Cookie／数据库挂起均有回归。`review-agent` 只读复核后修复总预算缺口；业务路线选择留在装配层，业务服务不反向导入 Runner。
- 验证：Ruff／格式／mypy 通过；全量 **1988 passed、3 skipped**（RabbitMQ 专项前组单独通过、隔离 MinIO 未提供、macOS 无 Linux O_PATH）。开发／生产 Compose 解析通过，现有 PostgreSQL 幂等应用最新 SQL；arm64 镜像构建及新服务启动成功，未另建基础栈。观察到维护进程约 60 MiB、Runner 空闲约 67 MiB，分别受 256 MiB／1 GiB 上限约束；不是峰值容量测试。
- 剩余：执行层已有真实抖音三样本与重建容器后完整文件证据（见 P9.04），但同意图端到端、跨平台冷／热／失效矩阵及最终 Web／App 验收仍未完成，P9.03 保持未勾选。

<a id="p9-04"></a>

### P9.04 首个公开平台纵向闭环

- [ ] **P9.04**；状态：进行中；承接者：Codex；职责：平台适配／下载准备／QA；依赖：P9.02、P9.03。
  - 功能需求：FR-01、FR-03、FR-04、FR-08。以抖音公开样本接通分享输入、访客准备、解析和媒体验证；先定位实际响应，不重复增加同功能分享页解析器。
  - 非功能需求：NFR-01、NFR-06、NFR-08、NFR-11。统一总预算和受控出口；保留必要参数；恢复不扩大权限。
  - 交付：专用适配缺口修复、结构变化／访客失效的脱敏夹具、同一意图执行证据。
  - 验收：AC-01、AC-03、AC-04、AC-08；标准空状态下至少 3 个合格公开样本完成冷／热／恢复，用户不导出 Cookie；metadata 与 media 分开记录。失败时保持本项未完成，不拿旧账号来源替代。

设计定位：Design §3.1／3.2。改动范围：分享输入规范化、已有抖音插件、Runner 与平台样本。验证入口：V-B、V-P（见 §4）。

1. **P9.04.S1**：准备合规公开样本和分享文案夹具，记录现有请求失败阶段及必要参数。
2. **P9.04.S2**：接通同一意图的公开路线、访客准备、解析结果与有界媒体验证，复用现有提取器。
3. **P9.04.S3**：在空 guest、热状态和进程恢复下逐样本验证，保留 metadata／media 分层证据；未通过不扩称支持。

执行记录（2026-09-22）：复用已有官方移动分享页插件，没有新增签名或账号路线。新建 guest 卷、数据库从无该作用域记录自动初始化后，公开样本 `7674644830270473609`、`6961737553342991651`、`6982497745948921092` 均完成 metadata 与实际下载；首轮总耗时 6.62／7.06／10.98 秒。文件经 ffprobe 确认双流、大小 2,991,195／4,076,260／3,870,741 bytes，时长 14.070／19.736／45.451 秒。强制重建管理进程和 guest Runner 后，三样本再次通过，耗时 6.57／7.08／7.44 秒，大小和时长一致。验收专用临时文件已清理；未导入账号 Cookie，未读取普通浏览器。此处是 Runner 执行层证据，不是同意图 Web 流程或各样本完整冷／热／失效矩阵，P9.04 未关闭。

<a id="p9-05"></a>

### P9.05 跨机制验证与重点平台扩展

- [ ] **P9.05**；状态：未开始；承接者：待分配；职责：平台适配／QA；依赖：P9.04。
  - 功能需求：FR-03、FR-04、FR-05、FR-06、FR-08。先覆盖小红书、微博、YouTube、B 站，再完成 PRD 批次 C 的 TikTok、快手、X、Instagram、Reddit 处置；复用已有 PO token provider。
  - 非功能需求：NFR-01、NFR-04、NFR-06、NFR-08。不同平台不共享账号状态；平台故障冷却不影响其他路线；不采用无限 UA／引擎轮询。
  - 交付：逐平台访问说明、已验证媒体类别、样本及失败原因；必要 query、图集、分离音视频回归。
  - 验收：AC-03、AC-04、AC-05、AC-06、AC-08、AC-17；每个平台至少 3 个有权样本的冷／热／重启记录；所有计划平台都有结果，核心未通过不关闭，不仅挑成功样本。

设计定位：Design §3.1／3.5。改动范围：各平台可信插件、访问策略、bgutil 集成与平台样本。验证入口：V-B、V-P（见 §4）。

1. **P9.05.S1**：按 PRD 批次 B／C 逐平台列出公开输入、媒体类型和访问前提；不可共用账号假设。
2. **P9.05.S2**：逐平台实现必要准备和错误动作，覆盖图集、必要 query 与分离音视频，不复制完整生命周期。
3. **P9.05.S3**：逐项执行冷／热／重启样本并登记限制，单平台冷却时验证其他平台；核心失败保持任务未完成。

执行记录：尚未实施；提交／测试与实测证据／阻塞项均待执行时按 §6 填写。

<a id="p9-06"></a>

### P9.06 必要授权与原意图续接

- [ ] **P9.06**；状态：实施中（维护事务已持久化，内容授权绑定及续接未完成）；承接者：当前实现任务；职责：授权后端／Provider／客户端动作；依赖：P9.02、P9.03。
  - 功能需求：FR-06、FR-07。仅明确账号权限且有受控动作时等待授权；完成后后端续接，公开默认路线不调用宿主 Agent；复核原 P7 修复。
  - 非功能需求：NFR-03、NFR-06、NFR-07、NFR-11。owner／用途／主体／版本绑定，可靠 ACK，等待期限，撤销和取消 CAS。
  - 交付：统一授权关联、恢复事件和 next_action；按来源能力展示动作，不保留必失败按钮。
  - 验收：AC-06、AC-07、AC-12；关闭页面后正确授权仍续接；跨 owner／账号切换／撤销／迟到完成不能复活任务；普通用户不能修改共享来源。

设计定位：Design §3.1／3.2／3.3。改动范围：provider_authorization 既有原型、providers 服务、授权事件和客户端动作契约。验证入口：V-B、V-C、V-R（见 §4）。

1. **P9.06.S1**：核对未提交授权原型的 owner、主体、用途与版本约束，建立迟到和跨用户攻击用例。
2. **P9.06.S2**：实现 action_required 与受控授权引用、服务端完成事件及原意图续接；限定等待期限。
3. **P9.06.S3**：验证关闭页面、重复完成、撤销／取消竞争和共享来源写权限；缺入口时不给不可执行按钮。

执行记录（2026-09-22）：

- 先区分两种事实：既有本机 Agent 的 `source_available` 仅证明部署来源可读，不是某一内容的授权；其回执没有受控账号主体与用途版本，禁止直接用于恢复下载意图。维护来源仍仅允许管理员发起，普通用户不能写共享身份。
- 来源维护事务从 Redis 迁入 PostgreSQL `provider_authorizations`，保存 owner、平台、显式来源、固定维护用途、操作截止与独立 24 小时保留截止。平台 advisory lock 与 pending 唯一索引跨副本互斥，终态行锁更新阻止取消／过期被迟到完成覆盖。
- API 生命周期启动有界后台协调，关闭页面后仍处理结果／超时；数据库已接单但队列尚未发布时，按原 ID 和截止幂等补投。完成结果先落库，再写终态队列标记并清理响应；标记保留至结果清理，迟到发布不会重新打开已取消操作。清理先处理对应队列文件再删除数据库记录，不扫描或删除无关来源材料。
- Web 软导航与组件卸载只停止观察，不再取消持久事务；明确“取消授权”仍取消服务端操作。响应晚于卸载也不会发出误取消。agent-browser 在生产构建和受控接口／连接器回执夹具中验证：授权弹窗打开后浏览器后退不发 DELETE，重新进入并明确取消只发一次 DELETE；390px 截图 `/tmp/framefetch-authorization-mobile.png`。此测试未读取真实浏览器 Cookie。
- 已有库已幂等应用当前态 SQL；空 schema 和重复执行保留终态测试通过。事务／Agent 队列／运行时相关 48 项测试及 Web 授权相关 29 项测试通过，覆盖服务重建、10 个 API 实例并发、数据库失败保留响应、取消乱序、队列缺投恢复、后台生命周期与有界清理。完整后端 2003 项通过、4 项环境相关跳过，Ruff／Mypy 通过；前端 73 文件／373 项、lint／类型检查／format／生产构建通过。API 镜像构建、已有业务容器替换及 `/health/ready` 通过；切换前核对旧 Redis pending 来源维护事务为 0。
- 剩余：账号主体验证、来源／grant 版本与用途绑定、`action_required` 暂停预算和同意图服务端续接。P9.06 不提前勾选，维护来源可读不能冒充 AC-07 整体验收。

<a id="p9-07"></a>

### P9.07 结果、下载交接与存量业务

- [ ] **P9.07**；状态：进行中；承接者：Codex；职责：downloads／媒体验证／存储；依赖：P9.02、P9.04。
  - 功能需求：FR-08、FR-09、FR-16。ready 展示规格，明确下载确认才交接既有 job；链接过期同权限重解析；保留上传、历史、分析和文档。
  - 非功能需求：NFR-03、NFR-07、NFR-08、NFR-11。原子幂等交接、制品校验、取消子进程组、私有缓存隔离与安全 GC。
  - 交付：意图到 job 唯一关系、结果复核、取消竞态处理、既有业务回归。
  - 验收：AC-08、AC-09、AC-16；双击只有一个 job，取消与交接竞态收敛；视频／图集完整文件验证；AI 失败不改下载成功，活跃工作区不误删。

设计定位：Design §3.2／3.3。改动范围：downloads 服务与仓储、Runner 文件验证、存储清理和既有分析关联。验证入口：V-B、V-C、V-R、V-P（见 §4）。

1. **P9.07.S1**：实现 inspection 有效性与语义规格确认，重解析核对内容标识和权限。
2. **P9.07.S2**：原子交接已有 job，完成并发确认／取消处理及完整媒体校验；保持 AI 任务独立。
3. **P9.07.S3**：回归旧历史、上传、在途下载、AI 失败与 GC；记录完整文件、单 job 和取消子进程证据。

执行记录（2026-09-22）：

- 复用现有下载创建入口及 `JobRepository` 事务，按 inspection 唯一索引找到意图并锁定；ready → handed_off、唯一 job、配额与 outbox 同事务提交。双击即使使用不同 HTTP 幂等键，也只返回原 job；已取消／失效的意图不能通过旧创建入口绕过。用户主动历史重试仍创建独立资源，不覆盖原意图引用。
- 原意图取消在同一事务内取消已交接的活跃 job，清除执行租约，现有 Worker heartbeat 收敛和终止子进程。删除下载时先按统一顺序锁定意图／job，释放 job 引用并标记原意图 expired，防止外键阻止清理或删除后复活。
- 下载前允许同一 guest 作用域的材料 revision 更新，仍固定平台／权限／引擎／Profile／客户端／出口；复用现有重新解析、内容标识检查、语义格式选择及完整文件校验。账号上下文不适用该规则。
- 验证：新增 50 次并发确认、取消竞争、outbox 失败回滚、删除引用与历史重试检查；60 项初始关联检查、24 项后续关联检查通过；全量 **1993 passed、3 skipped**，Ruff／格式／mypy 通过。现有 PostgreSQL 已幂等加载唯一索引；arm64 业务镜像构建后重建 API／Outbox／下载 Worker／guest Runner，启动前确认无既有活跃任务。
- 部署发现并修复的缺口：真实下载角色缺少新队列权限，Outbox 的 topic write 列表缺少 `download.intent.requested`，导致接单无法执行。只给这两个现有角色补对应队列／routing key 权限，未给管理员权限；CI 夹具补完整队列绑定，运行手册补双层 ACL，并新增真实受限下载角色的拓扑检查（1 passed）。首次验收意图超时；第二条在权限修复后沿原意图恢复，144.27 秒 ready、149.40 秒 succeeded，未重置预算或重新提交；修复后的第三条 4.07 秒 ready、10.18 秒 succeeded，并落入 MinIO。两次成功的验收下载及对象已按正常删除用例清理。
- 接口生成：发现当前生成器忽略 202，仅在生成阶段选择服务端 202 schema，生成明确响应类型；未改 HTTP 状态或手写 DTO。Web 362 tests、lint／格式／生产 build 通过，提交 `990b5df6`；队列部署修复 `ecf425a1`。
- 剩余：Web／App 的同意图用户路径、结果过期的产品动作、账号授权续接和完整生命周期验收仍由 P9.06／P9.10／P9.12／P9.14 完成，P9.07 不提前勾选。

<a id="p9-08"></a>

### P9.08 能力目录与管理员诊断

- [ ] **P9.08**；状态：实施中（访客与账号部署状态分离，候选目录未完成）；承接者：当前实现任务；职责：Provider／Canary／管理端；依赖：P9.01、P9.03。
  - 功能需求：FR-10、FR-14。引擎候选、策略准入、部署状态、阶段证据分层；历史域名阻断逐项复核，长尾按 PRD 批次 D 纳入。
  - 非功能需求：NFR-07、NFR-08、NFR-12。证据绑定版本／上下文／出口和时效，单链接失败不污染平台；只展示非秘密诊断。
  - 交付：构建能力清单、证据投影、查询接口、可执行管理员动作和前端展示。
  - 验收：AC-10、AC-18；不把 1,751 extractor 或 24 标签当成功数量，不让旧版本成功为新版本背书；从任务能定位真实失败阶段。

设计定位：Design §2／3.1／3.3／3.5。改动范围：Provider Registry、Canary、管理端 API 与平台状态组件。验证入口：V-B、V-C、V-W（见 §4）。

1. **P9.08.S1**：生成绑定引擎版本的候选清单，关联策略／部署／证据，逐项记录历史硬阻断处置。
2. **P9.08.S2**：实现平台健康和单内容结果独立投影，提供不含秘密的失败阶段、证据时效与恢复动作。
3. **P9.08.S3**：用旧版本成功、内容删除、受限来源等夹具验证目录不误报，管理端可定位任务原因。

执行记录（2026-09-22）：

- 修正能力基线将 guest 配置错误关联到 operator 配置的问题：API、应用工厂与发布就绪检查分别传入访客 Runner 和账号 Runner 配置。仅有访客组件的新部署可以显示公开访客路线；仅有账号来源不能冒充访客组件就绪。显式部署策略仍优先，无显式策略时可使用已配置的访客路线。
- 访客组件已配置时基线为待验证，不显示账号授权要求；`download_available` 保持 false，真实媒体可用性仍由匹配上下文及有效期的证据决定。新增三项部署组合回归，Ruff／Mypy 通过，能力／证据／Canary／运行时接口共 77 项测试通过。
- 引擎候选清单、历史硬阻断逐项处置和完整管理端诊断尚未完成，P9.08 保持未勾选。
- 修正默认执行路线与能力投影不一致：共享公开路线选择规则在已配置且 Profile 准入 guest 时优先使用 `public_session`，同时保留显式请求／部署策略优先级；普通同步解析入口与平台基线使用同一规则。访客失败不退回匿名重试，也不升级至账号。路由／Canary 71 项回归、Ruff／Mypy 通过。
- 真实 Web 访客路线验收：独立普通用户登录，在生产构建浏览器中粘贴分享文案、解析、创建任务、获取文件，三个抖音样本 `7674644830270473609`、`6961737553342991651`、`6982497745948921092` 均完成，数据库上下文均为 `public_session / guest`。三个浏览器文件 SHA-256 均与服务端 Artifact 一致，ffprobe 均有 H.264 视频与 AAC 音频，大小分别 2,991,195／4,076,260／3,870,741 字节，时长 14.070／19.736／45.451 秒；job 接单至完成分别 5.916／6.174／5.905 秒（不含用户选择等待，不能作为全链路 p95）。没有账号 Cookie 导入或授权弹窗。
- 本轮 Web 使用已经自动准备好的 guest 上下文，不冒充全新空库冷启动或跨平台验收；既有三样本空 guest 与重建证据仍见 P9.04。390px 深色下载详情无横向溢出；axe 无违规，播放器／标题对比度有自动待判读项，已检查当前截图，仍不据此宣称所有媒体及全部可访问性场景通过。证据 `/tmp/framefetch-p9-browser-results.json`、`/tmp/framefetch-live-douyin-parsed.png`、`/tmp/framefetch-live-douyin-mobile-dark-ready.png`。

<a id="p9-09"></a>

### P9.09 标准空部署与自动准备

- [ ] **P9.09**；状态：未开始；承接者：待分配；职责：部署／Runner／运维；依赖：P9.03、P9.04；平台完整准入还依赖 P9.05。
  - 功能需求：FR-04、FR-05、FR-11。标准模式打包必需引擎、runtime 与访客组件，后台初始化；精简模式声明差异；核心不等待所有平台成功。
  - 非功能需求：NFR-04、NFR-05、NFR-09、NFR-11。开发／生产一致、amd64／arm64、资源隔离、备份恢复；无新基础设施堆叠。
  - 交付：固定镜像、标准／精简能力清单、资源预算、空状态启动和故障恢复手册；不覆盖用户环境文件。
  - 验收：AC-04、AC-11、AC-12、AC-17；无账号文件或普通 Profile 可启动准备；访客丢失能重建；依赖故障不伪装身份失效。已有存储复用隔离数据，不另起本机基础栈。

设计定位：Design §3.5。改动范围：backend/Dockerfile、两份业务 Compose、类型配置、docs/operations。验证入口：V-D、V-R、V-P（见 §4）。

1. **P9.09.S1**：固定标准／精简模式依赖和版本、amd64／arm64 构建入口，复用既有基础设施。
2. **P9.09.S2**：加入有界后台准备、独立资源限制与职责级 readiness；空状态不要求宿主账号文件。
3. **P9.09.S3**：验证双架构、重建、组件故障与备份还原，测定容器资源上限并写运行手册；完整准入等待 P9.05。

执行记录：尚未实施；提交／测试与实测证据／阻塞项均待执行时按 §6 填写。

<a id="p9-10"></a>

### P9.10 Web 单次提交与页面连续性

- [ ] **P9.10**；状态：实施中（查询缓存基础已完成，意图入口及完整验收未完成）；承接者：当前实现任务；职责：Web 产品／前端；依赖：P9.02、P9.07、P9.08。
  - 功能需求：FR-01、FR-02、FR-06、FR-12。观察同一意图，后台准备与必要动作清晰；根布局查询缓存、草稿、筛选和已加载数据恢复。
  - 非功能需求：NFR-06、NFR-09、NFR-10、NFR-12。owner 隔离、生成客户端、局部加载、键盘／读屏、请求与导航职责分开。
  - 交付：统一入口与任务观察、TanStack Query 业务缓存迁移、删除页面级重复恢复决策；已修复 AuthProvider 行为保持。
  - 验收：AC-01、AC-13、AC-14、AC-18；桌面及 390px 明暗主题真实浏览器记录，切页保留内容，断网不跳登录，换账号旧响应不可覆盖。不得仅删 skeleton 掩盖闪屏。

设计定位：Design §3.3／3.4。改动范围：frontend/src 的根布局、身份投影、业务查询缓存、intake／下载页面及 src/api 生成输出。验证入口：V-C、V-W（见 §4）。

1. **P9.10.S1**：先复现导航闪屏／重连问题，固定 owner 隔离、草稿与已有数据寿命的浏览器断言。
2. **P9.10.S2**：接入统一查询缓存和 intent 观察，处理必要动作及局部加载，删除页面级重复重试。
3. **P9.10.S3**：验证桌面／390px、明暗主题、键盘和读屏，覆盖断网、换账号和迟到响应；保留录制证据。

执行记录（2026-09-22）：

- 根布局引入独立 QueryClient，身份代际更换时取消请求并清理缓存；无跨 SSR 请求的共享实例。下载历史、剧本文档列表／详情、平台状态、分析 Skill、管理统计已迁移，queryFn 直接使用生成 API 的 AbortSignal。保留分页／筛选切换时已有数据，网络刷新失败采用局部反馈。
- 下载创建／导入／重试／取消／删除及详情快照更新定向失效历史；文档导入／删除失效列表并清理已删除详情，避免导航回来读取失效记录。迟到的 Skill 清单不会覆盖已输入提示词。
- 自动验证：`pnpm lint`、`pnpm format:check`、`pnpm build` 通过；72 个测试文件、368 项测试通过。新增导航缓存、后台失败保留数据、身份切换取消和迟到响应、StrictMode、输入不被迟到默认值覆盖的回归测试。
- agent-browser 使用生产构建与受控接口夹具验证 UI：历史→文档→历史，逐帧观察为 0 帧历史骨架、0 帧登录跳转；阻断历史 API 后仍保留记录并显示刷新失败。桌面／390px、浅色／深色布局已检查无横向溢出。发现反馈文字和头像回退文字对比度不足并修复，四种尺寸／主题组合的 axe WCAG 2A／2AA 复验均为 0 违规、0 待判读项。
- 本机录制：`/tmp/framefetch-query-continuity-verified.webm`；最终截图：`/tmp/framefetch-query-mobile-light-final.png`、`/tmp/framefetch-query-desktop-light-final.png`。夹具仅用于 UI 故障与导航复现，不是上游平台解析、认证协议或真实媒体验收证据。
- 剩余：持久 intent 观察／同意图继续、筛选恢复、下载／分析详情缓存、全部异常与身份场景的真实浏览器验收。P9.10 保持未勾选。
- 响应丢失恢复接口：新增当前 owner + 幂等键的只读意图查询，使用现有唯一索引，不创建工作、不改变 version／截止、不新增 outbox／配额；陌生 owner 与未接单都返回相同 404。后端 29 项意图／交接集成测试、Ruff／Mypy 及自动生成客户端的类型检查和 13 项契约测试通过。Web 接入尚未完成。
- 草稿连续性：输入原文、来源选择和文件来源声明移入身份代际隔离的根布局 Context，软导航保留，身份变更即时清空；不写 URL、localStorage 或 sessionStorage，刷新／关闭应用后不保留未提交原文。新增路由卸载恢复、换身份清除和独立应用实例隔离测试；前端 73 个文件、371 项测试以及 lint／format／类型检查／生产构建通过。agent-browser 在生产构建中验证输入→文档页→返回、来源选择恢复，390px 明暗主题无横向溢出且 axe 均为 0 违规、0 待判读；截图 `/tmp/framefetch-draft-mobile-dark.png`。此证据覆盖 UI 草稿，不替代已提交意图的持久恢复验收。

<a id="p9-11"></a>

### P9.11 Web 持久会话协议收敛

- [ ] **P9.11**；状态：未开始；承接者：待分配；职责：认证后端／Web；依赖：P9.01；不阻塞 P9.10 的业务缓存实施。
  - 功能需求：FR-12、FR-16。按 041 完成 PostgreSQL 不透明 Web Session、期限和撤销；原生 Bearer 独立；显式说明切换时一次重新登录。
  - 非功能需求：NFR-03、NFR-06、NFR-07、NFR-09。CSRF、HttpOnly、跨标签、身份代际和存储边界；移除旧 Web 双 JWT 刷新路径，不保留平行 Web 认证。
  - 交付：schema／会话用例／同源协议／客户端切换和回滚边界，已有私有数据与原生访问保持。
  - 验收：AC-14、AC-16、AC-19；数据库短故障返回服务不可用而非401；Redis 重启不丢 Web 会话事实；撤销、过期、跨标签／CSRF 与原生隔离通过。

设计定位：Design §3.4；另见 041。改动范围：auth 服务与仓储、当前态 schema、认证依赖和 Web 请求层。验证入口：V-B、V-C、V-W、V-R（见 §4）。

1. **P9.11.S1**：明确不透明 Web Session 的期限、撤销、Cookie、CSRF 与原生 Bearer 边界，先补失效／不可用区分测试。
2. **P9.11.S2**：实现 PG 持久会话及 Web 同源协议；同次切换移除 Web 双 JWT 路径。
3. **P9.11.S3**：验证 PG 短故障、Redis 重启、跨标签和撤销；明确一次重新登录、切换及回滚步骤。

执行记录：尚未实施；提交／测试与实测证据／阻塞项均待执行时按 §6 填写。

<a id="p9-12"></a>

### P9.12 Flutter 契约同步与恢复

- [ ] **P9.12**；状态：未开始；承接者：待分配；职责：video-app 独立仓库／服务端契约；依赖：P9.02、P9.07、P9.08。
  - 功能需求：FR-02、FR-06、FR-13、FR-16。生成新契约、观察 intent、必要动作和前后台恢复，不复制平台准备逻辑。
  - 非功能需求：NFR-06、NFR-09、NFR-10。既有 Riverpod／Dio／go_router；原生认证和用户隔离；不保存 Provider 凭据。
  - 交付：App 对应设计与台账链接、冻结 schema 和生成客户端、Controller／页面迁移；仅暂存该仓库本任务文件。
  - 验收：AC-02、AC-14、AC-15、AC-16；format／analyze／单元／Widget／设备流程；iOS、Android 恢复同任务。不能因当前主要修 Web 而跳过破坏性 API 的 App 发布门禁。

设计定位：Design §3.3／3.4。改动范围：video-app 独立仓库的生成 API、Riverpod Controller、Dio／go_router 与相关页面。验证入口：V-C、V-A（见 §4）。

1. **P9.12.S1**：在 App 仓库盘点现有改动，关联本任务；从冻结服务端 schema 生成客户端并确认发布版本。
2. **P9.12.S2**：实现同一意图观察、必要动作、前后台和断网恢复，保持原生认证及 owner 隔离。
3. **P9.12.S3**：完成静态／Widget 和 iOS／Android 设备流程；记录两仓库 commit 和 schema 标识，未通过阻断破坏性 API 发布。

执行记录：尚未实施；提交／测试与实测证据／阻塞项均待执行时按 §6 填写。

<a id="p9-13"></a>

### P9.13 引擎维护、更新和回滚

- [ ] **P9.13**；状态：未开始；承接者：待分配；职责：维护者／CI／发布运维；依赖：P9.05、P9.08、P9.09。
  - 功能需求：FR-10、FR-11、FR-15。候选构建、确定性测试、低频平台 canary、稳定版本发布与部署更新策略；上下文维护和代码升级分开。
  - 非功能需求：NFR-04、NFR-08、NFR-09、NFR-12。固定来源／digest、版本绑定、可观察回滚，不启动时安装 master，不执行远端不可信插件。
  - 交付：发布门禁、适配版本证据、稳定通道和维护窗口配置、数据兼容及失败回滚运行手册。
  - 验收：AC-10、AC-17、AC-18、AC-20；回归阻断发布，镜像失败可回滚，旧任务不混用版本；不可逆数据变化阻断自动升级并给出明确方案。

设计定位：Design §3.5。改动范围：版本锁定、候选构建／发布工作流、Canary 和运维手册。验证入口：V-B、V-C、V-D、V-P（见 §4）。

1. **P9.13.S1**：定义可信上游、固定 digest 和稳定通道，区分运行上下文自动维护与代码升级。
2. **P9.13.S2**：实现候选构建、确定性回归、低频 canary、维护窗口和已验证版本更新。
3. **P9.13.S3**：注入坏镜像／引擎回归及不兼容数据变化，验证阻断或回滚，在途任务不混用版本。

执行记录：尚未实施；提交／测试与实测证据／阻塞项均待执行时按 §6 填写。

<a id="p9-14"></a>

### P9.14 全链路、容量与恢复验收

- [ ] **P9.14**；状态：未开始；承接者：待分配；职责：QA／后端／Web／App／运维；依赖：P9.01–P9.13。
  - 功能需求：FR-01–FR-16，核对全流程与存量能力，不再新增需求范围。
  - 非功能需求：NFR-01–NFR-12，按 PRD 参考环境和明确分母测量，不把单测数量当高可用／并发证据。
  - 交付：AC-01–AC-20 追溯矩阵、真实平台样本结果、受控并发／故障注入、双架构部署、Web／设备证据、备份恢复及剩余限制；运行指导归入 operations。Web 端最终验收使用 `agent-browser` 保存逐平台操作、网络请求、错误动作和截图证据。
  - 验收：所有门槛逐项通过才勾选。核心平台空状态失败、重复任务、跨用户泄露、客户端契约断裂或预算无界均阻断发布；未完成性能采样不得声称达到 p95。单宿主恢复边界和不能满足的宿主故障连续服务要求必须公开。P9.01–P9.13 完成后，必须从当时发布的 Provider Registry 生成完整平台清单，使用有权公开样本逐项执行 `agent-browser` 验收；缺少合格样本、账号或平台不可达必须记为未验收，不能从清单静默删除。

设计定位：Design 全部章节。改动范围：跨仓库验收、容量／恢复记录、发布检查与运行手册。验证入口：V-B、V-C、V-W、V-A、V-D、V-R、V-P（见 §4）。

1. **P9.14.S1**：核对每项 FR／NFR／AC 的任务、测试和实测证据，缺失项退回责任任务。
2. **P9.14.S2**：在 PRD 指定口径下完成并发、故障注入、真实平台、双架构和客户端验收；使用 `agent-browser` 逐项覆盖分享文案／URL 输入、解析、格式选择、下载、有限恢复、登录态保持、切页无闪屏、桌面与 390px 视口，分别记录 metadata、真实媒体、失败和限制。
3. **P9.14.S3**：确认发布门禁、备份及回滚证据，更新逐项结果和最终判定；未达标不得关闭或宣称高可用。

执行记录：尚未实施；提交／测试与实测证据／阻塞项均待执行时按 §6 填写。

## 4. 验证入口与执行边界

以下是执行时的入口，不是本次已运行结果。先跑与改动对应的场景，再完成受影响模块的 AGENTS 门禁；有新失败或实质变更才重复扩测。各任务关联的 AC 是最终判定，命令退出码不能替代真实结果。

| 编号 | 范围与入口 | 证据要求 |
| --- | --- | --- |
| V-B | `backend/`：`uv sync --frozen --dev`，`uv run ruff check app tests`、`uv run ruff format --check app tests`、`uv run mypy app`、`uv run pytest`；先运行本任务相关用例 | 记录具体测试节点和结果；数据变更覆盖空库／已有当前态库；需要数据库时只用既有服务的隔离数据 |
| V-C | 从运行的 code-first schema 更新客户端；`frontend/`：`pnpm openapi:check`；服务端契约测试及 App 现有生成流程 | 记录 schema 与两仓库 commit；生成目录含未跟踪文件也算漂移；API 地址和端口按当前配置，禁止手改生成文件 |
| V-W | `frontend/`：`pnpm format:check`、`pnpm lint`、`pnpm test`、`pnpm build`；最终使用 `agent-browser` 执行真实浏览器导航／故障交互 | 桌面／390px、主题、焦点、导航和请求记录；无闪屏与无误退出须有运行证据；浏览器验收在 P9.01–P9.13 完成后执行，不以中途冒烟替代最终结果 |
| V-A | `video-app/`：`dart format --output=none --set-exit-if-changed .`、`flutter analyze`、`flutter test`，按设备执行 `flutter test integration_test` | iOS／Android 实际目标和结果；缺设备列为未验收，不以编译通过替代 |
| V-D | 校验业务与生产 Compose 配置、双架构构建与标准／精简运行；实际命令记录所用配置和镜像 | 不输出完整解析后的 Secret；不覆盖已有 .env；本机复用既有 PG／RabbitMQ／Redis／MinIO，空业务状态用隔离数据 |
| V-R | 对事务边界、消息重投、租约失效、进程退出、取消和备份恢复注入故障；按 PRD 测试预算执行受控负载 | 接单／结果数、时序、旧 Worker 写入、资源峰值、RPO／RTO 及失败结果；不得在现有生产数据上破坏性演练 |
| V-P | 按执行时 Provider Registry 的完整平台清单，低频执行有权公开样本的冷／热／重启与真实媒体校验；Web 用户路径由 `agent-browser` 驱动 | 每个平台分列输入样本类型、metadata、media、完整文件、浏览器操作与失败原因；样本量不足只报原始耗时；缺少合格样本或必要授权标为未验收；禁止把竞品源码研究当平台实测 |

## 5. 需求到任务的追溯矩阵

下表只维护归属，不复写需求文本和数值。FR／NFR／AC 的定义链接指向 PRD；开发责任来自 P9.01–P9.13，P9.14 对所有项做综合复核。任务调整时同步本表，不能只留下最终 QA 为无人承接的需求兜底。

| 需求／验收 | 开发及专项验收任务 |
| --- | --- |
| [FR-01](../prd/044-开源部署无感解析PRD.md#fr-01) | [P9.04](#p9-04)、[P9.10](#p9-10) |
| [FR-02](../prd/044-开源部署无感解析PRD.md#fr-02) | [P9.02](#p9-02)、[P9.10](#p9-10)、[P9.12](#p9-12) |
| [FR-03](../prd/044-开源部署无感解析PRD.md#fr-03) | [P9.01](#p9-01)、[P9.04](#p9-04)、[P9.05](#p9-05) |
| [FR-04](../prd/044-开源部署无感解析PRD.md#fr-04) | [P9.03](#p9-03)、[P9.04](#p9-04)、[P9.05](#p9-05)、[P9.09](#p9-09) |
| [FR-05](../prd/044-开源部署无感解析PRD.md#fr-05) | [P9.03](#p9-03)、[P9.05](#p9-05)、[P9.09](#p9-09) |
| [FR-06](../prd/044-开源部署无感解析PRD.md#fr-06) | [P9.01](#p9-01)、[P9.02](#p9-02)、[P9.05](#p9-05)、[P9.06](#p9-06)、[P9.10](#p9-10)、[P9.12](#p9-12) |
| [FR-07](../prd/044-开源部署无感解析PRD.md#fr-07) | [P9.01](#p9-01)、[P9.06](#p9-06) |
| [FR-08](../prd/044-开源部署无感解析PRD.md#fr-08) | [P9.04](#p9-04)、[P9.05](#p9-05)、[P9.07](#p9-07) |
| [FR-09](../prd/044-开源部署无感解析PRD.md#fr-09) | [P9.07](#p9-07) |
| [FR-10](../prd/044-开源部署无感解析PRD.md#fr-10) | [P9.08](#p9-08)、[P9.13](#p9-13) |
| [FR-11](../prd/044-开源部署无感解析PRD.md#fr-11) | [P9.09](#p9-09)、[P9.13](#p9-13) |
| [FR-12](../prd/044-开源部署无感解析PRD.md#fr-12) | [P9.10](#p9-10)、[P9.11](#p9-11) |
| [FR-13](../prd/044-开源部署无感解析PRD.md#fr-13) | [P9.01](#p9-01)、[P9.12](#p9-12) |
| [FR-14](../prd/044-开源部署无感解析PRD.md#fr-14) | [P9.08](#p9-08) |
| [FR-15](../prd/044-开源部署无感解析PRD.md#fr-15) | [P9.13](#p9-13) |
| [FR-16](../prd/044-开源部署无感解析PRD.md#fr-16) | [P9.07](#p9-07)、[P9.11](#p9-11)、[P9.12](#p9-12) |
| [NFR-01](../prd/044-开源部署无感解析PRD.md#nfr-01) | [P9.04](#p9-04)、[P9.05](#p9-05) |
| [NFR-02](../prd/044-开源部署无感解析PRD.md#nfr-02) | [P9.02](#p9-02)、[P9.03](#p9-03) |
| [NFR-03](../prd/044-开源部署无感解析PRD.md#nfr-03) | [P9.02](#p9-02)、[P9.06](#p9-06)、[P9.07](#p9-07)、[P9.11](#p9-11) |
| [NFR-04](../prd/044-开源部署无感解析PRD.md#nfr-04) | [P9.05](#p9-05)、[P9.09](#p9-09)、[P9.13](#p9-13) |
| [NFR-05](../prd/044-开源部署无感解析PRD.md#nfr-05) | [P9.09](#p9-09) |
| [NFR-06](../prd/044-开源部署无感解析PRD.md#nfr-06) | [P9.01](#p9-01)、[P9.03](#p9-03)、[P9.04](#p9-04)、[P9.05](#p9-05)、[P9.06](#p9-06)、[P9.10](#p9-10)、[P9.11](#p9-11)、[P9.12](#p9-12) |
| [NFR-07](../prd/044-开源部署无感解析PRD.md#nfr-07) | [P9.03](#p9-03)、[P9.06](#p9-06)、[P9.07](#p9-07)、[P9.08](#p9-08)、[P9.11](#p9-11) |
| [NFR-08](../prd/044-开源部署无感解析PRD.md#nfr-08) | [P9.01](#p9-01)、[P9.02](#p9-02)、[P9.04](#p9-04)、[P9.05](#p9-05)、[P9.07](#p9-07)、[P9.08](#p9-08)、[P9.13](#p9-13) |
| [NFR-09](../prd/044-开源部署无感解析PRD.md#nfr-09) | [P9.01](#p9-01)、[P9.09](#p9-09)、[P9.10](#p9-10)、[P9.11](#p9-11)、[P9.12](#p9-12)、[P9.13](#p9-13) |
| [NFR-10](../prd/044-开源部署无感解析PRD.md#nfr-10) | [P9.10](#p9-10)、[P9.12](#p9-12) |
| [NFR-11](../prd/044-开源部署无感解析PRD.md#nfr-11) | [P9.03](#p9-03)、[P9.04](#p9-04)、[P9.06](#p9-06)、[P9.07](#p9-07)、[P9.09](#p9-09) |
| [NFR-12](../prd/044-开源部署无感解析PRD.md#nfr-12) | [P9.08](#p9-08)、[P9.10](#p9-10)、[P9.13](#p9-13) |
| [AC-01](../prd/044-开源部署无感解析PRD.md#ac-01) | [P9.04](#p9-04)、[P9.10](#p9-10) |
| [AC-02](../prd/044-开源部署无感解析PRD.md#ac-02) | [P9.02](#p9-02)、[P9.12](#p9-12) |
| [AC-03](../prd/044-开源部署无感解析PRD.md#ac-03) | [P9.01](#p9-01)、[P9.04](#p9-04)、[P9.05](#p9-05) |
| [AC-04](../prd/044-开源部署无感解析PRD.md#ac-04) | [P9.03](#p9-03)、[P9.04](#p9-04)、[P9.05](#p9-05)、[P9.09](#p9-09) |
| [AC-05](../prd/044-开源部署无感解析PRD.md#ac-05) | [P9.03](#p9-03)、[P9.05](#p9-05) |
| [AC-06](../prd/044-开源部署无感解析PRD.md#ac-06) | [P9.01](#p9-01)、[P9.02](#p9-02)、[P9.05](#p9-05)、[P9.06](#p9-06) |
| [AC-07](../prd/044-开源部署无感解析PRD.md#ac-07) | [P9.01](#p9-01)、[P9.06](#p9-06) |
| [AC-08](../prd/044-开源部署无感解析PRD.md#ac-08) | [P9.04](#p9-04)、[P9.05](#p9-05)、[P9.07](#p9-07) |
| [AC-09](../prd/044-开源部署无感解析PRD.md#ac-09) | [P9.07](#p9-07) |
| [AC-10](../prd/044-开源部署无感解析PRD.md#ac-10) | [P9.08](#p9-08)、[P9.13](#p9-13) |
| [AC-11](../prd/044-开源部署无感解析PRD.md#ac-11) | [P9.02](#p9-02)、[P9.09](#p9-09) |
| [AC-12](../prd/044-开源部署无感解析PRD.md#ac-12) | [P9.03](#p9-03)、[P9.06](#p9-06)、[P9.09](#p9-09) |
| [AC-13](../prd/044-开源部署无感解析PRD.md#ac-13) | [P9.10](#p9-10) |
| [AC-14](../prd/044-开源部署无感解析PRD.md#ac-14) | [P9.10](#p9-10)、[P9.11](#p9-11)、[P9.12](#p9-12) |
| [AC-15](../prd/044-开源部署无感解析PRD.md#ac-15) | [P9.12](#p9-12) |
| [AC-16](../prd/044-开源部署无感解析PRD.md#ac-16) | [P9.07](#p9-07)、[P9.11](#p9-11)、[P9.12](#p9-12) |
| [AC-17](../prd/044-开源部署无感解析PRD.md#ac-17) | [P9.05](#p9-05)、[P9.09](#p9-09)、[P9.13](#p9-13) |
| [AC-18](../prd/044-开源部署无感解析PRD.md#ac-18) | [P9.08](#p9-08)、[P9.10](#p9-10)、[P9.13](#p9-13) |
| [AC-19](../prd/044-开源部署无感解析PRD.md#ac-19) | [P9.11](#p9-11) |
| [AC-20](../prd/044-开源部署无感解析PRD.md#ac-20) | [P9.13](#p9-13) |

## 6. 验收记录与变更管理

每个任务完成步骤或遇到阻塞时，在该任务“执行记录”处维护以下信息；不要创建另一张状态表。无需为尚未执行的步骤预填“通过”。

```text
承接者 / 当前状态 / 更新日期：
关联步骤、FR / NFR / AC：
实际变更与提交（跨仓库时分别列出）：
环境、架构、镜像、schema、平台/出口/上下文版本：
验证命令或操作 / 时间 / 脱敏证据位置：
预期 / 实测 / 通过或失败 / 未测：
资源、耗时、样本量与统计口径：
阻塞或剩余风险 / 责任人 / 恢复条件：
需求或设计调整及影响任务：
验收结论与复核者：
```

代码与确定性测试链接到对应 commit；可公开的短实测结论记录在任务下，运行指导进入 `docs/operations/`。大体积录制保存在受控制品位置并附稳定引用；Cookie、完整签名 URL、个人媒体、原始日志和临时报告不得提交。证据失效、版本变化或复核失败时重开受影响任务，不能保留错误“已验收”。

PRD、Design、Plan 的有效内容持续维护，不因本次文档提交就删除。需求变更先更新 PRD，再审查 Design 和依赖任务；开发步骤变化只改 Plan。版本替代和已失效过程稿通过 Git 历史追溯，不复制多份当前规格。

## 7. 旧台账归并

| 旧范围 | 当前唯一执行任务 | 保留／调整 |
| --- | --- | --- |
| R1–R13、P1–P5 | P9.01 复核；P9.14 做变更相关回归 | 保留已有完成记录，不为架构整齐重写 |
| R14、P6.1／P6.2／P6.5 | P9.08、P9.09、P9.14 | 启动健康与平台成功分开；空 guest 自动准备才满足新目标 |
| R15／R17、P6.3／P6.4 | P9.06、P9.10 | 宿主授权仅保留必要的受控边界，不作为公开默认流程 |
| R16 | P9.01、P9.03、P9.05 | 补 guest 语义和有界恢复，不能由匿名失败直接要求登录 |
| R18／R19、P7／P8.2 | P9.01、P9.03、P9.06 | 既有控制修复复用；账号来源与访客初始化分开 |
| R20、P8.1／P8.3 | P9.02、P9.06、P9.07 | 同一意图，不建立第二套执行或授权状态 |
| R21、P8.4／P8.5 | P9.01、P9.10、P9.12 | 契约、交互和双客户端发布同步 |
| R22、P8.6／P8.7 | P9.05、P9.07、P9.14 | 扩平台、媒体证据和有限恢复；不再只验三个有账号来源的平台 |
| 041 Web 会话／缓存 | P9.10、P9.11 | 部分身份修复已有代码；剩余缓存／协议按独立任务验收 |
| 042 能力治理／并发 | P9.08、P9.13、P9.14 | 能力清单、可观察维护和真实容量证据 |
| 043 来源分发 | P9.03、P9.09 的可复用原语 | 保留已有来源能力，不作为新部署 guest 初始化验收 |

旧 P1–P8 完成记录保留在 BACKLOG；其未完成改造由本文任务接管，不重复勾选。PRD 管理需求，Design 管理实现决策，本 Plan 管理执行事实。
