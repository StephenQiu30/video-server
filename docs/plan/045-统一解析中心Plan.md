# 045 统一解析中心 Plan

日期：2026-09-24。状态：Web／Backend 本地实现与受控验收完成；部署、真实业务数据、App 和产品验收待完成。

[PRD](../prd/045-统一解析中心PRD.md) 唯一维护需求/验收标准；[Design](../design/045-统一解析中心设计.md) 维护实现方案；本文件维护任务状态和证据。承接 044 解析历史与 023/028 文档/Skill 能力，不重建已有流程。

## 1. 执行顺序

| 顺序 / 任务 | 工作项与产物 | 前置依赖 | 验收 |
| --- | --- | --- | --- |
| 045-S1 | 核对部署 SHA、当前接口实际记录类型、Web/App 调用方；冻结四类口径、状态、来源与删除规则 | 无 | FR-01/02/08/09；记录源码与运行差异 |
| 045-S2 | 扩展 HistoryRecordKind/判别联合、查询过滤与动作/可用性字段，生成 OpenAPI；补契约失败用例 | S1 | AC-03/06/08；未知枚举发布检查 |
| 045-S3 | Document + screenplay 查询覆盖、owner scoped join、稳定游标、标题过滤与索引评估 | S2 | AC-01/05/11；含已删除/源丢失记录 |
| 045-S4 | 剧本 analysisId 精确恢复、独立分析详情入口、来源关联校验、运行记录读取 | S2/S3 | AC-02/04/07/09；同源多次分析与删除后报告 |
| 045-S5 | 解析中心页签/筛选/导航、状态和错误语义、所有类型轮询、身份隔离缓存失效 | S3/S4 | AC-01/03/06/10；所有入口找回 |
| 045-S6 | 真实 PostgreSQL 混合数据性能与隔离测试、Web 回归、桌面/390px 明暗浏览器交互 | S5 | AC-05/08/12；性能执行计划 |
| 045-S7 | 配套部署、历史对账、真实四类流程、小范围启用与回退验证 | S6 | AC-01—12 完整证据；产品验收签署 |
| 045-S8 | App 采用新契约及独立页面/导航/设备验证 | S2；有旧接口消费者时必须前置于 S7 | 独立 App 验收，不与 Web 混记 |

## 2. 可执行清单

- [x] S1 保存版本/接口基线；不得用截图推定源码缺失，也不得用源码推定已部署。
- [x] S2 先定 API schema，后生成 Web 类型；不手写 API 平行类型。
- [x] S3 覆盖 parse/video_analysis/document_parse/screenplay_analysis；旧记录无需数据复制。
- [x] S3 每分支鉴权、软删除、来源关联、同时间/相同跨类型 UUID/三页游标测试。
- [x] S4 历史 analysisId 不回退 latest；来源删除、报告缺失、ID 不匹配分别处理。
- [x] S4 重试同任务新 run、改配置新任务、重复幂等键行为与运行列表权限验证（复用既有领域用例与相关回归）。
- [x] S5 全类型状态更新、筛选恢复、异步缓存/账号切换/迟到响应回归。
- [ ] S6 scoped 用例与完整项目门禁通过；性能不足记录原因并修复。
- [ ] S7 实际部署确认、真实用户流程与回退证据；产品验收人/日期记录。
- [ ] S8 App 独立任务明确是否影响服务端启用；未验收时不能宣称全端完成。

## 3. 测试数据和场景

为测试账号建立：链接 ready/handed_off/action_required/failed/expired；视频 AI running/retry_wait/succeeded；剧本文档 uploading/verifying/ready/failed；剧本 AI 分析与改写各一项；同一剧本两次不同配置；源已删除但报告存在；分析已软删除；另一 owner 的同名数据。

关键回归：页面重载与跨浏览器不增加写请求；三页翻页中插入新记录不重复；后台任务终态后列表停止轮询；删除后旧链接不给其他结果；账号 A 请求延迟到 B 登录后返回不得显示；运行历史每条都属于所查任务与 owner。

受控 UI 夹具只能证明交互。真实 AI 和真实报告验证使用用户有权处理的短视频与小剧本，检查既有模型凭据可用性并按正常任务授权执行；不能用 fixture 或 HTTP 200 代替结果验收。无需为本功能重做全平台下载矩阵。

## 4. 检查入口

后端按 AGENTS：Ruff check/format、mypy、pytest；接口契约测试包含 discriminator 全覆盖、分页筛选、响应敏感字段过滤。SQL 有变更才增加空库/已有库幂等门禁。

前端按 AGENTS：pnpm format:check、pnpm lint、pnpm test、pnpm build；从运行源 OpenAPI 重生成请求与类型并核对差异。浏览器覆盖桌面和 390px、明暗、键盘返回焦点、筛选 URL、空/错/刷新失败，记录真实测试与夹具边界。

性能数据固定十万条混合记录、10 并发、热身后至少 1,000 次列表请求，记录环境、分布、p50/p95、错误率及 SQL EXPLAIN；首次页、深游标、状态筛选、标题搜索分开测，不用平均值掩盖慢查询。

## 5. 工作量估算与发布边界

以现有执行能力复用为前提，单工程师 Web/Backend 预计 7–10 个工作日：基线与契约 1 日，查询/测试 1.5–2 日，详情/运行历史 1.5–2 日，列表交互 1.5–2 日，联调/性能/发布 1.5–3 日。此为规划估算；老客户端升级、历史关联损坏或运行环境差异可能延长。App 另估 2–4 日并以真实设备条件校准。

第一交付切片 S2–S4 解决“记录缺失和结果找错”，S5–S7 完成可发布解析中心。切片合入不是整体验收，不能只加两个页签就关闭任务。

## 6. 本次证据与后续验收登记

本次完成 Backend + Web 本地实现。源码基线 `2921d25b`；正在运行的 8111 API `openapi.json` 有旧历史路由，但缺新增的分析来源及运行记录端点，不能用其验收新功能。`video-app` 源码未发现调用本聚合历史接口，仍需独立 App 设计、生成与设备验收。

- Backend 在原联合查询上增加剧本文档、剧本 AI、状态/类型/时间/标题/Skill/素材过滤；每源先过滤和取前 K 条，再统一按创建时间/ID/类型排序，旧记录立即可见。报告摘要、来源可用性和运行记录分别由已有表读取。来源/报告状态不改变原任务状态。
- Web 增加分类/筛选 URL、独立 AI 详情、按 analysisId 精确读取、运行代次列表，以及上传/分析后的缓存失效。删除或素材不匹配不回退 latest。
- 2026-09-24 分页交互调整：解析中心和分析运行记录显示页码、上一页/下一页；保留稳定的服务端游标与 URL 恢复，已访问页可直接返回，未知总页数不虚构。刷新非首页时只导航首页，避免同时重取旧页。三页前进/返回、筛选重置的前端回归 5 项通过；仅含本次改动的隔离树通过格式、lint、类型检查和生产构建，全量测试 439 通过、1 项既有剧本报告断言失败。agent-browser 使用受控身份/API 夹具在桌面及 390px 验证页码点击、URL 恢复和筛选重置；390px 无横向溢出，浅色/深色 WCAG 2A/2AA 自动检查无违规。该夹具不代表真实业务数据验收。
- 独立 PostgreSQL 测试覆盖四类、跨类型同 UUID 游标、owner 隔离、源删除及软删除。接口契约和 Swagger 断言已扩展；OpenAPI 从当前 FastAPI 工厂导出并重新生成 Web 客户端。
- 100,000 条混合测试记录、10 并发、四类查询各 250 次（合计 1,000 次）测得 p95：首次页 450.81ms、深游标 76.39ms、状态 58.53ms、标题 65.4ms；数据与结果在 `/tmp/framefetch-045-performance.json`。该环境的首次页已接近 500ms 门槛，后续真实数据分布仍需复测。
- agent-browser 在本地新构建 8102 上，使用受控身份/API 夹具验证四类列表、剧本筛选参数、新建入口、独立剧本分析详情、源缺失禁用重试及运行记录；读取过程中无 POST。解析中心及分析详情在桌面/390px、浅色/深色的 axe WCAG 2A/2AA 自动检查均 0 违规、0 待判读，390px 无横向溢出。截图 `/tmp/framefetch-045-center-mobile-light.png`。这些夹具不代表真实模型输出或部署验收。

| 验收类别 | 当前状态 | 完成时填写 |
| --- | --- | --- |
| 文档一致性 | 已检查 | 三份规格及导航链接有效；需求/任务/AC 对应审查；git diff --check 通过 |
| API/查询/前端工程门禁 | 已执行，见下方失败说明 | 提交 SHA、命令与结果 |
| 浏览器行为 | 受控夹具通过 | 真实登录／新 API 部署待验收 |
| 真实四类任务 | 未执行 | 脱敏任务 ID、结果、时间 |
| 性能/回退 | 本地性能通过；回退未演练 | 部署后回退结果 |
| 产品验收 | 未执行 | 验收人、角色、日期、AC 逐项结论 |
| App 设备验收 | 未执行 | 独立仓库提交、设备、结果 |

完整门禁中发现与本次改动无关、在原工作树隔离副本可复现的既有失败：后端 CLI schema 大小门禁、DOCX subject 和剧本报告章节断言 3 项（2121 通过、4 跳过）；包含用户既有未提交改动的前端工作树为 4 项失败（434 通过），分别涉及管理员、下载历史及剧本报告。仅含本次暂存内容的隔离前端树通过格式、lint、类型检查与构建；全量测试 438 通过、1 项剧本报告旧断言失败。045 专项回归及指定旧记录后新建任务的回归通过；不能将完整门禁记为绿色。后续需在不覆盖用户工作区修改的条件下分别修复并复验。

## 7. 系统操作日志实施（2026-09-25）

用户确认完整系统日志，包含管理员操作。FR-04/11/12/13 与 AC-13—16 对应本次变更。

- [x] 请求持久化、管理员全局查询、任务状态事务事件与删除后日志保留。
- [x] 主导航移除解析中心，管理员日志入口、个人结果路径同步迁移。
- [x] 新增 5 项 PostgreSQL/API 集成测试全部通过，覆盖真实事务、软删除、平台标识和日志完成失败；架构测试 3 项通过。此前认证/下载契约、主应用和日志组合测试 12 项通过。
- [x] 前端全量 463 项通过，最终日志/导航/处理记录回归 20 项通过；格式、lint、类型和生产构建通过。
- [x] 空隔离 schema、重复执行和已有本地项目数据库应用验证。
- [x] 本地 API/前端镜像构建并更新，两个容器 healthy；桌面/390px 明暗主题、管理员入口、筛选空态、真实 401 日志详情、10/20/50 条切换、Escape 焦点恢复通过。390px 页面无横向溢出；个人处理记录入口 URL `/history/activity` 与标题核验通过。权限探测生成两条真实的未登录拒绝记录，未对真实业务资源执行删除或权限变更。

新增任务不改变前述性能、App 与产品签署的未完成事实。
