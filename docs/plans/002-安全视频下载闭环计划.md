# 002 安全视频下载闭环计划

- 状态：Completed（MVP 核心受控直链 MP4 闭环）

## 实施顺序

1. 先实现 Domain：语义格式、DownloadJob 状态机、lease、错误分类与纯单测。
2. 建立 PostgreSQL 当前态 SQL、repository 与 transactional outbox。
3. 建立无凭据 Media Runner、HMAC、受控子进程组、工作目录与资源限制。
4. 配置 egress proxy，并用私网/重定向/DNS 用例验证。
5. 实现 inspect、格式规范化和 re-inspect 语义重匹配。
6. 实现 Worker 领取、心跳、进度、取消、硬超时、remux、ffprobe、SHA 和 MinIO 上传。
7. 实现 API/session/idempotency/problem details 与前端完整下载流程。
8. 使用真实 PostgreSQL/RabbitMQ/MinIO/yt-dlp/FFmpeg 完成受控直链 MP4 核心 E2E。
9. 后续扩展第三方站点、真实分离流与故障注入矩阵，不阻塞 MVP 核心结论，但必须单独记录范围和证据。

## 提交切片

每个步骤按 `test → implementation → docs/evidence` 分离，禁止用 Mock-only 测试宣称下载闭环完成。

## 当前证据与剩余工作（2026-08-06）

- 步骤 1—7 的代码和受控自动化已完成；后端与前端全量门禁均通过。
- 当前数据库定义集中在唯一的 `backend/sql/schema.sql`；默认完整与仅依赖两份 Compose 均可解析，代码进程使用统一 runtime 镜像。
- CI 不设置覆盖率数字硬门槛，现有门禁聚焦状态机、消息契约、SSRF/进程安全、API 与用户流程。
- 步骤 8 已完成：受控直链由 `Generic` 解析并经代理绑定的 ffprobe 补全，选择 `360p MP4 H264/AAC`；下载任务成功，签名 URL 取回文件，DB/文件 SHA-256 一致，ffprobe 为 `640x360 / H.264 / AAC / 3.702993s`。
- 真实浏览器已完成解析和下载任务页验证，console/overlay 无错误；002 Acceptance 已按 MVP 核心范围判定 Passed。
- 步骤 9 尚未完成：第三方站点/Extractor、真实分离流、重定向/格式漂移和故障注入仍是明确的扩展验证项。
