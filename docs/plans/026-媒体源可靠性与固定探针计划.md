# 026 媒体源可靠性与固定探针计划

- 状态：Completed for implementation；external Provider recovery ongoing
- 日期：2026-08-17

1. 将解析路由拆为 Strategy、责任链和错误 Policy，补 Operator readiness。
2. 实现 YouTube/TikTok 通用浏览器 Cookie 桥接及兼容包装。
3. 将私有封面改为认证 Blob 读取，更新 CSP 和组件状态测试。
4. 实现 `RetryFormatRecoveryPolicy`，在兼容边界内自动改选当前规格。
5. 修复 Provider canary 的共享 `/work` 挂载，增加格式轮换有界重检和稳定错误翻译。
6. 建立 22 Provider × metadata/media 固定矩阵、Registry 覆盖测试和脱敏 CLI。
7. 重建 Compose，运行真实矩阵与浏览器 E2E；将未通过平台保留为真实降级状态。
8. 运行后端、前端、Compose、文档与仓库全量门禁。
