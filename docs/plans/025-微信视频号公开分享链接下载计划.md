# 025 微信视频号公开分享链接下载计划

- 状态：In Progress，匿名公开媒体链路已实现，真实 canary 待完成
- 日期：2026-08-29

## Phase 1：C 端边界收敛

- [x] 移除元宝 Cookie、浏览器导出、宿主机会话服务和微信专用 Operator Runner。
- [x] Profile 升级为 `wechat-channels-public-v2`，仅允许 anonymous。
- [x] 状态改为 `degraded`，并展示公开媒体限制与上传行动。

## Phase 2：可审计提取

- [x] 保留严格 `/sph/{id}` URL 准入。
- [x] 仅请求第一方公开预览与 feed 接口。
- [x] 仅接受响应直接提供的 H.264/H.265 clear 媒体。
- [x] 保留腾讯媒体 host/path allowlist 和 DRM/加密 fail-closed。
- [x] 无直接媒体稳定分类为 `content_entitlement_unknown`。

## Phase 3：验证与发布

- [ ] 项目自有/明确授权真实样本完成 metadata 和 media Range canary。
- [ ] 完整下载通过 FFprobe、SHA-256、Artifact 和 AI 入口验证。
- [ ] 用 `agent-browser` 复验桌面/移动端成功、受限和上传路径。
- [ ] 真实 canary 证据足够后由发布人决定是否提升 Provider 状态。
