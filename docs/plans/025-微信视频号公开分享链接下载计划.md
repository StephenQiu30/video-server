# 025 微信视频号公开分享链接下载计划

- 状态：In Progress，代码与自动化测试已完成，真实 canary 待运维会话
- 日期：2026-08-27

## Phase 1：Provider 与安全准入

- [x] `weixin.qq.com/sph/{id}` 从静态 export-required 分流到 Runner。
- [x] 新增严格 normalizer，拒绝 query、fragment、其他路径与非默认端口。
- [x] 注册 `wechat_channels-public-v1`、`access_required`、匿名 + operator Profile。
- [x] Cookie domain 限制为 `yuanbao.tencent.com`。
- [x] 权益防火墙允许该 Provider 使用隔离 operator mode。

## Phase 2：可信 extractor

- [x] 匿名访问第一方预览页与 `get_feed_info(shortUri)`。
- [x] 公开预检失败时禁止 operator fallback。
- [x] 检查 `hy_user`、`hy_token` 后调用元宝 resolver。
- [x] 严格验证 playable host/path/query。
- [x] 调用视频号 feed API并提取 H.264/H.265 URL。
- [x] 限制腾讯媒体 host/path。
- [x] 递归拒绝 decodeKey/DRM/encryption。
- [x] 复用现有 FFprobe、大小/时长和下载前重解析。

## Phase 3：契约、配置与文档

- [x] 稳定映射 link unavailable、credential required/expired 和 DRM。
- [x] Provider API 状态改为 registered/access-required。
- [x] 浏览器导出器要求视频号会话双 Cookie。
- [x] 独立 `wechat-channels-operator` Profile、只读 Secret 与本地自动 Cookie 桥接。
- [x] `.env`、Backend README 与运维手册增加配置示例。
- [x] 更新前端受控会话错误文案。
- [x] 新增 025 Research/Design/PRD/Plan/Acceptance，并标记 024 视频号旧结论被取代。

## Phase 4：验证

- [x] extractor、Profile、Cookie、错误、Application 与 API 相关测试。
- [x] 全量 backend lint/type/test、frontend lint/type/test/build。
- [x] Docker Compose 配置与镜像测试。
- [x] `agent-browser` 桌面 1280×900 与移动 390×844 验收。
- [ ] 授权公开样本真实 metadata/media/Artifact canary。
- [ ] 会话过期、链接失效、受保护媒体紧急停用演练。

## Phase 5：发布

- [ ] 检查无 Secret、无无关改动，提交 main 并推送。
- [ ] CI 全绿。
- [ ] 真实 canary 通过后由发布人显式批准 Provider；此前保持 `access_required`。
