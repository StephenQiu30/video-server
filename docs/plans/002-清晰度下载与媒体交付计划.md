---
layer: Plan
status: backlog
version: "0.2.0"
canonical_path: docs/plans/002-清晰度下载与媒体交付计划.md
purpose: "交付选定规格的下载、合并、校验与资产交付"
inputs:
  - "REQ-DL-001"
  - "REQ-DL-002"
  - "REQ-ASSET-001"
  - "docs/plans/001-URL异步解析与清晰度目录计划.md"
outputs:
  - "docs/acceptance/000-MVP验收总览.md"
---

# Plan 002：清晰度下载与媒体交付

## 1. 前置门禁

Plan 001 状态为 `done`，其 Acceptance 全部通过；格式快照和 Job 事件契约稳定。Design/Acceptance 必须先冻结 `download` operation 的签名 policy、current rights 和错误契约。

## 2. 范围

- 创建下载时重新读取 current rights statement 并要求显式确认；锁定 current ACTIVE signed policy，只有 `decision=allow` 且 `permitted_operations` 含 `download` 才创建 Job。
- 下载前重新解析并验证远端视频身份、DRM/download-disabled 信号和 `format_key`；Plan 001 的 probe-only dossier 必须 fail closed。
- Celery media queue 执行 yt-dlp 下载、必要的 FFmpeg stream copy 合流。
- 进度、取消、有限重试、磁盘/时长/字节配额和失败清理。
- ffprobe 验证分辨率、时长、音轨、容器和非空输出。
- S3-compatible multipart 上传、SHA-256、短期签名地址和 24 小时生命周期。
- SSE 断线恢复和刷新后的任务恢复。

## 3. 非目标

主动升频、默认转码、AI、PDF、Cookie、DRM、直播、播放列表和永久媒体库。

## 4. 核心验收

- 选择规格与最终媒体一致率 100%。
- 已知总量进度单调；未知总量不伪造百分比。
- 取消后 2 秒内更新状态、5 秒内停止继续拉取。
- 重试不生成重复资产；大文件不经过 API 或 Next.js。
- 签名链接过期可重签，不重复下载。
- probe-only policy 100% 返回 canonical `SOURCE_DOWNLOAD_NOT_AUTHORIZED` 且不创建 Job；operator-owned `download` grant 有真实 origin-control 正向 canary。

## 5. 变更记录

| 版本 | 日期 | 变更说明 |
| --- | --- | --- |
| 0.2.0 | 2026-07-18 | 分离 probe/download grant 并要求创建下载时重验 current rights/policy |
| 0.1.0 | 2026-07-18 | 初始化下载与资产交付计划 |
