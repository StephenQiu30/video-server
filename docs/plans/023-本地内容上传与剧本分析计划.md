# 023 本地内容上传与剧本分析计划

- 状态：In Progress，Phase A 已完成
- 日期：2026-08-14
- 对应设计：`docs/design/023-本地内容上传与剧本分析设计.md`
- 对应需求：`docs/prd/023-本地内容上传与剧本分析需求.md`
- 对应验收：`docs/acceptance/023-本地内容上传与剧本分析验收.md`

## 1. 交付策略

按“先统一边界与上传基础设施，再交付本地视频，随后交付剧本文档和 Skill”的顺序实施。每个阶段必须有独立 feature flag、自动化和回归证据；未完成剧本能力不能阻塞已验证的 MP4 上传，后续 `019` Edge Agent 也只能复用本计划的上传/验证端口。

本计划不创建新仓库、平行前端、第二套任务系统或运行时 Skill 安装器。数据库、API、Worker、Web 和文档均在现有 `video-server` 职责内完成。

## 2. Phase A：范围收敛与当前态契约

- [x] 更新 `019` 四件套，把 `browser_import`、quarantine 和浏览器 MP4 校验的交付所有权转到 `023`；`019` 只保留 Edge 设备与平台 Adapter。
- [x] 定义 `ContentKind`、`DownloadSourceKind.BROWSER_IMPORT`、`AnalysisInputKind` 和 `AnalysisResultContract` 领域枚举与转换规则。
- [x] 更新 `download_jobs`，使远程下载要求 inspection/format，浏览器导入要求两者为空，并用 SQL CHECK 保证。
- [x] 为现有下载和 Analysis 数据幂等回填 `source_kind=remote_provider`、`input_kind=video` 和 `result_contract=video-visual-analysis`。
- [x] 把 Analysis 创建、快照、持久化和公开 View 改为联合输入/结果契约，保持 Domain/Application 不依赖数据库、MinIO、CLI 和解析器；Document repository 与执行分派仍由 Phase D/E 完成。
- [x] 增加默认关闭的 `MEDIA_IMPORT_ENABLED`、`DOCUMENT_IMPORT_ENABLED`、视频/文档独立容量上限、上传会话 TTL 和稳定错误码。

验证：领域状态机、SQL CHECK、空/已有数据卷幂等、现有下载/分析 repository 与 OpenAPI 契约回归。

Phase A 证据（2026-08-14）：PostgreSQL 17 临时实例先应用上一提交的 `schema.sql` 并写入旧下载/分析行，再连续两次应用当前 SQL；回填值为 `remote_provider,video,video-visual-analysis`，非法下载来源形态和双 Analysis 输入均被 CHECK 拒绝。后端 Ruff/format/mypy 与全量测试通过（609 passed、1 Windows 不适用项 skipped）；OpenAPI 客户端已重新生成。因本机已有 Next.js dev server 锁定工作区原生模块，根 `ci.py all` 的 `npm ci` 无法原地清理，已改在隔离临时目录完成干净 `npm ci`、lint/typecheck、format、119 项测试和 production build，随后删除临时目录；工作区依赖已用 lockfile 恢复并再次通过 lint/typecheck。

## 3. Phase B：公共上传会话与 quarantine

- [x] 在 `application/imports/` 定义资源创建、attempt、上传会话、complete、取消和状态查询用例与 ports。
- [x] 扩展 `object_storage.py`，提供确定 key 的 multipart 创建、part 签名、complete、abort、HEAD、copy/promote 和 list/reconcile 能力。
- [x] 配置 quarantine 独立前缀/身份、短 lifecycle、part 大小/数量和上传会话 TTL。
- [x] 新增 `content.import.verify.requested` outbox、quorum queue、DLQ、publisher confirm、manual ack 和有界回灌。
- [x] 建立 `workers/imports/` 的 consumer、lease/heartbeat、恢复扫描、workspace、清理和 orphan reconciliation。
- [x] 确保普通日志、消息、WebSocket 和指标只包含内部 ID、kind、attempt、状态、大小和稳定错误码。

验证：过期会话、缺 part、重复 complete、重复消息、取消、对象存储失败、Worker 在 copy/DB commit 前后崩溃和 TTL 清理。

Phase B 存储切片证据（2026-08-14）：真实 MinIO 已完成“创建会话 → 预签名 PUT → complete → HEAD → 幂等 promote → 精确清理”，并接受同时覆盖完整 quarantine 对象与未完成 multipart 的 1 天 lifecycle。当前运行时收敛为唯一 `MINIO_ACCESS_KEY` / `MINIO_SECRET_KEY`，API、Import、Analysis 等应用进程不再配置重复的分角色凭据；浏览器仍只获得受限预签名上传会话，不接收通用 AK/SK。此处只证明存储层；应用契约与数据库聚合见下段，Import Worker 仍未完成，因此上传基础设施验收保持未勾选。

Phase B 应用切片证据（2026-08-14）：`application/imports` 已提供视频/剧本资源创建、确定 quarantine key 的 attempt、受限 part 会话、complete + HEAD、状态查询和先提交状态后清理的取消用例；repository port 明确要求 `mark_verifying` 与 `content.import.verify.requested` 在同一事务。重复 complete 可在 multipart 已完成但数据库未提交时通过 HEAD 恢复；数据库失败时保留 quarantine，过期会话保持资源可刷新；repository 返回的越界 cleanup key 被拒绝。文件名、声明哈希、object key、upload ID 和预签名 URL 均从 dataclass `repr` 隐藏。数据库实现与真实 Outbox 证据见 Phase C 聚合切片；Import Worker 执行/恢复仍属后续切片。

Phase B 消息切片证据（2026-08-14）：新增严格的 `content.import.verify.requested` v1 payload/parser，只允许 `resource_id/content_kind/attempt/version`；RabbitMQ 初始化建立独立只读 import 用户、`video.import` quorum 主队列与 quorum DLQ、5 次 delivery limit、at-least-once dead-letter、TTL/长度/字节上限和受控回灌白名单。Import consumer 使用 manual ack，非法消息直接 dead-letter，执行异常与显式 retry 只允许一次客户端重投后进入 DLQ，shutdown cancellation 会 requeue。真实 RabbitMQ 4 已确认队列类型/策略/权限，并通过 Outbox 身份 publisher confirm 发布、Import 身份读取、严格解析和 ack；Phase C 聚合切片进一步证明 repository 在状态事务中只写一条验证事件，因此该消息基础项完成。consumer 的业务 lease/heartbeat、执行恢复和孤儿协调仍留在下一项。

## 4. Phase C：浏览器 MP4 导入

- [x] 建立 `media_imports/media_import_attempts` ORM、repository 和当前态 SQL。
- [x] 实现 `POST/GET /api/media-imports`、upload-session、complete 与现有 download cancel 级联。
- [x] 实现无外网视频 verifier：BMFF/magic、大小、SHA-256、ffprobe、视频轨、时长、codec、尺寸和外部引用检查。
- [x] 允许合法无声 MP4；拒绝假扩展名、损坏、多文件、playlist、无视频轨和超限输入。
- [x] 实现 deterministic Artifact 晋升、`download_job` 原子成功、任务事件和崩溃恢复。
- [x] 下载 View/API/history/analytics 增加 `source_kind/source_label`，本地上传不计 Provider canary。
- [x] 首页加入“本地上传”入口和上传进度；成功后导航现有下载详情。

验证：真实短 MP4 → Artifact → director-breakdown → PostgreSQL result → MinIO MD/DOCX → WebSocket → 桌面/移动浏览器；现有 URL 下载全量回归。

Phase C 聚合切片证据（2026-08-14）：新增 `media_imports/media_import_attempts` 当前态表、ORM 与 owner-scoped repository；资源与 `browser_import` download job 共用稳定 ID，inspection/format 保持为空，创建资源不发送远程 `download.requested`。attempt 分配使用行锁、确定 quarantine key 和复合主键；刷新、激活、失败、过期、取消与重复取消均按状态前置条件收敛。`mark_verifying` 在同一事务更新资源/attempt/download 投影并写入唯一 `content.import.verify.requested` Outbox；普通 Download Worker 的 claim、queued/retry/stale 恢复均显式排除浏览器导入。PostgreSQL 17 已连续两次应用当前 schema，并通过真实并发同 key 创建（一新一重放）和并发完成（仅一条验证事件）；后端 repository/backend 门禁通过（672 passed、1 个 Windows 不适用项 skipped）。本切片尚未开放 API，也未实现视频 verifier、Artifact 晋升或浏览器 E2E。

Phase C API 切片证据（2026-08-14）：新增受登录 owner 隔离的 create/get/upload-session/complete 路由、严格请求/响应 schema、稳定 RFC 9457 错误与独立速率限制；create 只接受文件声明和版本化权利确认，客户端不能提交 bucket、object key、MIME、part size 或 S3 参数，multipart URL 响应使用 `no-store`。API composition 使用 quarantine 专用身份，feature flag 默认关闭；现有 download 详情跳过浏览器导入的远程 presentation，download cancel 会调用同一 Import 聚合并先提交取消事实、再容错清理对象，retry 仍拒绝浏览器来源。四个 operationId 已通过当前 FastAPI schema 生成前端客户端。完整后端门禁通过（681 passed、1 个 Windows 不适用项 skipped）；前端 lint/typecheck、格式、119 项测试与隔离目录的干净 `npm ci + Next.js production build` 通过。此切片仍不代表 verifier、Artifact 晋升、history/analytics 或真实浏览器上传 E2E 完成。

Phase B/C Import Worker 切片证据（2026-08-14）：新增仅连接内部 DB/queue/storage 网络、只读根文件系统、无 capabilities、`no-new-privileges`、pids 与 tmpfs 上限的 `worker-import`；消息按资源版本和上传 attempt 领取双租约，心跳同步 download 投影，过期租约由 `FOR UPDATE SKIP LOCKED` 扫描原子回写 Outbox。私有工作区只接受确定 task ID 和根目录直属文件，终态/中断有界清理；`downloads/*` 协调只删除超过宽限期、符合 Artifact key 且数据库无记录的孤儿。视频 verifier 重算大小/SHA-256，完整遍历 BMFF box、拒绝外部 data reference，并用无 Shell、仅 `file` 协议、净化环境、有界 stdout/stderr/超时的 ffprobe 验证 MP4、视频轨、正时长、codec 和尺寸；合法无声 H.264 MP4 真实验证通过。Artifact 使用 `downloads/{resource_id}/{attempt}/video.mp4` 确定 key，晋升后在同一 PostgreSQL 事务写 Artifact、Import/attempt ready 和 download succeeded，数据库提交前失败会保留两份可恢复对象。真实 PostgreSQL 17 并发领取只产生一个 owner，并发恢复只产生一条回灌事件；临时 PostgreSQL + MinIO + 真实 ffmpeg MP4 闭环得到 `ready`、单一 Artifact、相同 SHA-256 且 quarantine 已删除。完整后端门禁通过（712 passed、1 个 Windows 不适用项 skipped），三套 Compose 门禁通过；临时容器和文件均已清理。本证据只覆盖 Worker；历史/统计见下段，浏览器 UI 与拉片 Analysis/报告/WebSocket E2E 仍未完成。

Phase C 历史/统计投影切片证据（2026-08-14）：下载历史查询将远程 inspection/format 改为可选关联，并按 `source_kind` 读取本地导入文件名、固定 MP4 展示、Artifact 可用性与过期时间；文件名搜索、状态汇总和 owner 隔离同时覆盖 `browser_import`。管理员统计将本地上传纳入总量、成功数、用户数、Artifact 字节和真实媒体时长，使用独立 `browser_import / 本地视频上传` 维度，不伪造 extractor，也不触碰 Provider canary。远程 Provider 分类与历史行为保持原路径。新增 SQLite 聚合/历史回归后，完整后端 Ruff、format、mypy 与 715 项测试通过（714 passed、1 个 Windows 不适用项 skipped）。本证据仍不代表浏览器 UI 或拉片 Analysis/报告/WebSocket E2E 完成。

Phase C 浏览器上传切片证据（2026-08-14）：首页已在同一内容入口提供“链接解析 / 本地上传”Tabs，保留原链接下载流程；本地上传只接受单个 MP4 和版本化权利声明，在浏览器分块增量计算 SHA-256，并以有界并发完成预签名 multipart、聚合进度、取消和兄弟请求失败联动中止。`@noble/hashes` 固定为 `2.2.0`，MIT 许可与来源记录在前端 NOTICE，production dependency audit 为 0。开源 MinIO 使用精确来源的 `MINIO_API_CORS_ALLOW_ORIGIN`，应用 CSP 仅追加配置所得的公开存储 origin；真实预检证明允许来源返回 `204 + PUT`，非允许来源不返回 CORS 放行头，真实签名 PUT 返回 `200 + ETag`。真实桌面浏览器上传 12,236-byte 合成 H.264/AAC MP4，任务 `4e9bc2a4-5043-41c5-8d77-80be6f37bd8e` 完成 create/session、OPTIONS 204、PUT 200、complete 200、Outbox/RabbitMQ、Import Worker、ffprobe/hash、Artifact 晋升并进入现有下载详情与 AI 分析入口；1280px 详情和 390×844 上传入口无浏览器错误或页面级横向溢出。验收中发现并修复导入限流策略缺项、开发 MinIO 来源缺项和 CSP 未放行公开存储 origin。最终后端 Ruff、format、mypy 和全量测试通过（718 passed、1 个 Windows 不适用项 skipped）；Node 24 隔离环境完成干净 `npm ci`、production audit 0、lint/typecheck、format、126 项测试、production build，43 个 OpenAPI operation 生成目录零漂移，repository 门禁通过。本切片没有执行 `director-breakdown`，因此 PostgreSQL Analysis result、MD/DOCX 报告和 WebSocket 仍不计入 Phase C 完整通过。

Phase C 通过即可在 `DOCUMENT_IMPORT_ENABLED=false` 时独立发布本地视频上传。

## 5. Phase D：剧本文档导入与规范化

- [x] 建立 `documents/document_import_attempts/document_artifacts` ORM、repository、生命周期锁和当前态 SQL。
- [x] 实现 Document create/list/get、upload-session、complete、cancel/delete API 与严格 schema。
- [x] 复用 Phase B 上传会话和 Import Worker，通过 `content_kind` 路由到文档 verifier。
- [x] DOCX verifier 增加 ZIP 条目、大小、压缩比、路径穿越、宏和外部关系门禁，使用现有 `python-docx` 读取允许内容。
- [x] 引入固定版本 `pypdf`，拒绝加密、附件、扫描/空文本和低质量 PDF；完成许可证/SBOM 更新。
- [x] 实现 TXT/Markdown/Fountain 编码、控制字符、行长和字符上限。
- [x] 实现 UTF-8/LF/NFC 规范化、稳定 SHA-256、语言检测、场景标题/scene ID 和缺少场景标题质量警告。
- [x] 完成动作、人物、括注和对白结构解析，并以中英文人工真值 fixture 校验。
- [x] 原始与规范化对象使用 deterministic key，ready/failed/expired 与清理状态原子收敛。
- [x] 新增 `/documents` 列表和详情的格式、字符、场景、语言、警告与有界预览。

验证：每种首期格式的中/英文 fixture；同一字节重复规范化稳定；ZIP bomb、路径穿越、宏、外部关系、加密/扫描/乱码 PDF、NUL、超长行和超限输入 fail closed。

Phase D 持久化切片证据（2026-08-14）：新增独立 `documents`、`document_import_attempts`、`document_artifacts` 与 `analysis_document_locks` ORM/当前态表，Document 不创建或伪造 download job；owner + idempotency key 唯一，source format、上传/终态、ready 派生字段、Artifact kind/status/deleted shape 和生命周期外键均由 SQL CHECK/FK 约束。Document repository 复用公共导入端口，完成 owner 隔离、幂等创建、确定 quarantine key、attempt 刷新/激活、complete 与 `content.import.verify.requested` 同事务、失败/过期/取消和精确 cleanup ref；源码按职责拆分且单文件不超过 200 行。临时空库和现有数据 PostgreSQL 17 均连续两次应用当前 schema，四张新表、关键 CHECK 与 `analysis_jobs.document_id → documents.id` 外键存在；并发同 key 创建得到一新一重放、只存一行，测试记录已按两个预检资源 ID 精确清理且余量为 0。完整后端 Ruff、format、mypy 和全量测试通过（723 passed、1 个 Windows 不适用项 skipped）。该持久化切片提交时尚未公开 Document API，也未实现 verifier、规范化或 Artifact 晋升，因此对应产品验收保持未通过。

Phase D API 切片证据（2026-08-14）：新增 owner-scoped Document create/list/get、upload-session、complete、cancel 和 delete 共 7 个稳定 operationId；创建返回 `201 + Location`，请求/响应使用严格且有界 schema，文档导入与上传会话使用独立速率策略，默认关闭的 feature flag 继续 fail closed。列表和详情只返回安全元数据，不返回对象存储 key、原始正文或预签名 URL。删除采用 prepare → 对象清理 → finish 两阶段流程，活动 Analysis 锁阻止删除；清理前对 quarantine/original/normalized key 做 Document/attempt 级精确白名单校验，失败和重试均不扩大删除范围。OpenAPI 已生成 50 个前端操作，后端 Ruff/format/mypy 与全量测试通过（734 passed、1 个 Windows 不适用项 skipped），前端 Node 24 隔离门禁通过 lint/typecheck、format、126 项测试和 production build，仓库门禁通过。verifier、规范化、安全预览和 Artifact 晋升仍未实现，对应验收保持未通过。

Phase D 文本 verifier 切片证据（2026-08-14）：Import Worker 现按可信 `content_kind` 分派视频和剧本文档，Document 使用独立 claim/heartbeat/lease/recovery repository。TXT、Markdown、Fountain 只接受严格 UTF-8（允许 BOM），拒绝 NUL/C0/C1 控制字符、超长行、超字符数、大小与 SHA-256 漂移，并确定性执行 NFC、LF 和单一尾部换行规范化；场景标题生成稳定 scene ID，无标题正文退化为单场景并只记录受控 warning。原始字节与 `screenplay.md` 分别使用 `documents/{id}/{attempt}/original|screenplay.md` 确定 key，先晋升对象再原子提交 Document ready、语言/字符/场景/文本哈希和两份 Artifact；提交失败保留可恢复副本，过期 lease 重投，孤儿清理只匹配严格 UUID/正整数 attempt 路径。真实 PostgreSQL 17 + MinIO Fountain 预检 `370880c0-eb53-477b-9ec0-055239667ea2` 得到 mixed、2 scenes、两份 Artifact 且规范化哈希一致，数据库、quarantine 和最终对象测试余量均为 0。后端全量门禁与仓库/Compose 门禁通过。该文本切片提交时 DOCX/PDF 安全提取、动作/人物/括注/对白结构、详情预览仍待后续切片。

Phase D DOCX verifier 切片证据（2026-08-14）：在读取正文前检查 OOXML ZIP magic、大小写不敏感重复路径、绝对/反斜线/`.`/`..`/盘符路径、符号链接、加密 entry、条目数、单项/总解压大小和压缩比；要求最小 OPC 部件，拒绝宏/VBA、`.bin`、ActiveX、嵌入对象、DTD/ENTITY 以及任意外部关系。通过门禁后仅由现有 `python-docx 1.2.0` 读取 body 段落和表格单元格，不读取页眉页脚、批注或外部资源，随后复用统一文本规范化与 Artifact 晋升。真实 PostgreSQL 17 + MinIO DOCX 预检 `e492c5bf-3817-4ee7-b6c5-0a5c444be28d` 得到 ready、mixed、2 scenes、original/normalized 两份 Artifact 且文本哈希一致；数据库与对象存储测试余量均为 0。PDF、安全预览和人物/对白结构仍待后续切片。

Phase D PDF verifier 切片证据（2026-08-14）：固定 `pypdf 6.16.0`（上游 commit `2b60c99973df8d7f959cd46658604d881be3de3a`），以 strict 模式拒绝加密、附件/关联文件、XFA、JavaScript、URI、启动和远程动作，并在提取前限制页数、对象图及每页/总解压 content stream；扫描/空文本、低文本页面覆盖率和过高替换字符比例以稳定错误码失败，不包含 OCR。依赖与用途记录在独立 CycloneDX 1.6 SBOM 和随镜像发布的 BSD-3-Clause NOTICE。真实 PostgreSQL 17 + MinIO 中英混合 PDF 预检 `f184c17a-7f30-48ac-b5e7-37f21927f998` 得到 ready、mixed、2 scenes、85 characters、original/normalized 两份 Artifact 且规范化哈希一致；数据库与对象存储测试余量均为 0。后端全量门禁通过（776 passed、1 个 Windows 不适用项 skipped）。安全预览和人物/对白结构仍待后续切片。

Phase D 剧本结构切片证据（2026-08-14）：规范化阶段在不改变原文、SHA-256 和既有 scene ID 的前提下，为每个场景生成 `heading/action/character/parenthetical/dialogue` 有序字符区间；支持 Fountain 英文大写人物、`@` 强制人物、中英文括注、中文人物，以及 DOCX 表格提取形成的 `人物<TAB>对白`。数据库只在 normalized Artifact metadata 保存类型和偏移，不复制正文；持久化前拒绝越界、倒序或重叠区间，领域层限制单文档最多 5,000 场景和 20,000 元素以阻断元数据放大。中英文人工真值逐元素切回原文一致，真实 PostgreSQL 17 + MinIO Fountain 预检 `51dbf046-3888-4043-883f-df421eaf25df` 得到 ready、mixed、2 scenes，两场均按五类元素往返恢复；数据库与对象存储测试余量均为 0。后端全量门禁通过（780 passed、1 个 Windows 不适用项 skipped）。文档详情安全预览仍待后续切片。

Phase D 安全预览 API 切片证据（2026-08-14）：`GET /api/documents/{id}` 使用独立详情响应，只对 owner 所属且 ready 的 Document 返回 normalized `screenplay.md` 纯文本预览；列表不读取或返回正文。应用层在存储读取前绑定 Artifact bucket、`documents/{id}/{attempt}/screenplay.md` 确定 key、大小与文本 SHA-256，跨 owner 统一 404；MinIO 只执行从 offset 0 开始、最大 256 KiB 的有界 range read，配置默认进一步限制为 32 KiB/12,000 字符，并以严格 UTF-8 边界和 `preview_truncated` 表达截断。真实 PostgreSQL 17 + MinIO 预检 `17b114a0-adea-41f9-909c-d315c6e8d260` 证明跨 owner 在零对象读取时被拒绝、授权请求只读取前 40 字节且 HTML-like 内容保持普通字符串，数据库与对象存储余量均为 0。后端全量门禁通过（783 passed、1 个 Windows 不适用项 skipped）；生成客户端通过 production audit 0、lint/typecheck、format、126 项测试和 production build。

Phase D 前端列表/详情切片证据（2026-08-14）：新增受保护的 `/documents` 与 `/documents/detail?documentId=...`，只经生成 OpenAPI 客户端访问 owner-scoped 列表和详情；列表支持分页、刷新、首次 loading、empty 和失败态，详情展示状态、格式、语言、场景、字符、保留期、受控错误、质量警告及截断提示，并仅把 `preview` 放入 React 文本节点和 `<pre>`。桌面和移动导航新增“剧本文档”；1280×900、390×844 的浅色/深色生产构建完成真实浏览器复验，长文件名和正文无页面级横向溢出，移动 Sheet 的 Escape/焦点恢复和跳过链接可用，浏览器 console/error 为空。巡检发现的窄栏告警标题挤压已通过内容分组修复并复验；前端完整门禁为 lint/typecheck、format、37 个测试文件/133 项测试、production build 和 production audit 0。

## 6. Phase E：Analysis 联合输入与结果契约

- [x] `analysis_jobs` 增加 `input_kind/document_id/result_contract` 和二选一 SQL 约束，现有 artifact 外键改为条件必填。
- [x] 新增 Document owner/readiness/expiry 查询和 `analysis_document_locks`；更新 retry/delete/report lifecycle。
- [x] 新增 `/api/documents/{id}/analyses` 和 latest analysis 接口，复用现有 Analysis 资源查询、取消、重试、删除与导出。
- [x] 把公开 Analysis Result 改为带 `kind` discriminator 的视频/剧本联合契约，一次性更新 schema、ORM serialization、OpenAPI 和前端生成类型。
- [x] 拆分 `VideoAnalysisExecutor` 与 `ScreenplayAnalysisExecutor`；公共 Worker 只负责编排和生命周期。
- [x] 剧本执行器复核只读 `input/screenplay.md`，由父 Worker 在内存生成确定性 chunks，并禁止 FFmpeg、通用 Shell、网络、MCP、插件、浏览器和 subagent。
- [x] 实现 `screenplay-analysis` 与 `screenplay-rewrite` schema、解析、大小、scene evidence 和 chunk 覆盖校验。
- [x] 报告 renderer 根据 result kind 生成唯一 canonical Markdown，并复用现有 Markdown/DOCX 发布链。

验证：非法联合输入、过期/删除/哈希漂移、未知 kind/contract、悬空 scene 引用、未知字段、超大结果、取消、重试和报告一致性。

Phase E 联合输入持久化切片证据（2026-08-14）：Phase A 已落地并验证 `analysis_jobs.input_kind/document_id/result_contract`、现有视频数据幂等回填、视频/剧本二选一输入与 contract SQL CHECK；本切片把 Analysis repository 从视频专用扩展为不可变输入联合校验。新增 Document 分析投影只返回 owner、状态、文本哈希与过期元数据；创建和手动重试在同一事务内重新校验 Document 与 normalized Artifact 均为 ready、未删除、owner 一致、未过期，且两处 SHA-256 同时等于任务锁定哈希。视频继续写 `analysis_artifact_locks`，剧本改写/分析写 `analysis_document_locks`，取消、终态失败、删除、恢复终态和报告发布完成统一释放对应输入锁。真实 PostgreSQL 17 预检 `document=423759df-7e77-409b-9525-a739d0acb4ee / job=8ce1b960-304d-4ef0-89f5-2958b7b5072a` 得到锁计数 `1→0→1→0`（创建→终态失败→手动重试→取消），清理余量为 0；后端全量门禁通过（787 passed、1 个 Windows 不适用项 skipped）。剧本创建 API、公开联合结果、执行器和 screenplay 报告尚未实现，因此 Phase E 其余项保持未勾选。

Phase E 结果持久化与报告切片证据（2026-08-14）：领域结果已收敛为 `VideoAnalysisResult | ScreenplayAnalysisResult | ScreenplayRewriteResult`，三者分别固定 `video_visual_analysis/screenplay_analysis/screenplay_rewrite`，数据库 JSON 只接受当前带 `kind` 的严格形态，不保留缺字段 legacy parser。幂等 SQL 会按任务 `result_contract` 为既有视频报告补齐 `kind`，并以“字段存在且值在白名单”的 CHECK 拒绝缺失/未知值；从上一提交 schema 建立的 PostgreSQL 17 临时库已验证旧 JSON 升级、当前 schema 连续应用两次、约束拒绝缺失 kind，最终 `migrated_kind=video_visual_analysis / constraint_count=1`。报告 renderer 已按三类结果分派，剧本改写正文在 canonical Markdown 中阻断 HTML 与远程 Markdown 链接，现有 Markdown/DOCX 发布链继续只消费同一快照。该切片全量后端门禁为 798 passed、1 个 Windows 不适用项 skipped；Worker 模型输出 parser 仍待后续切片。

Phase E 公开联合结果切片证据（2026-08-14）：`AnalysisResponse.result` 已一次性切换为 OpenAPI `oneOf + discriminator(kind)`，三个分支分别固定单值 enum；视频分支保留全部拉片字段，剧本分析公开结构、人物、场景与证据，改写分支只公开语言、场景统计、术语表和修改摘要，不返回内部 chunks/改写正文。锁定版 `@umijs/openapi` 对命名 `oneOf` 会退化为 `Record<string, any>`，实现已改为等价内联联合形态并由生成器产出三个强类型 TypeScript 分支，前端通过穷尽 switch 编译测试防止退化。后端全量门禁为 800 passed、1 skipped；隔离 Node 24 前端门禁为 209 files lint/format、38 个测试文件/135 项测试、15 页 production build，官方 npm registry production audit 0。剧本 Analysis API 与 Worker 尚未完成，因此端到端验收仍保持未勾选。

Phase E 剧本 Analysis API 切片证据（2026-08-14）：新增 `POST /api/documents/{id}/analyses` 与 `GET /api/documents/{id}/analysis` 两个 owner-scoped operation；创建只接受 screenplay 兼容 Skill 和 `zh-CN/en-US`，锁定 Document/normalized 双 SHA-256、双过期时间、编译指令哈希、result contract、输出语言与规范化 custom prompt，并以 `201 + Location` 返回统一 Analysis 资源。latest 查询按 owner + Document 隔离，现有查询、取消、原任务重试、删除和 MD/DOCX 导出继续复用 `/api/analyses/{id}`。该切片新增默认关闭的 `SCREENPLAY_ANALYSIS_ENABLED`，在执行器当时尚未完成时返回 503，后续已由受限执行链接管。OpenAPI 已由 52-operation 当前 schema 生成；后端 Ruff/format/mypy 与全量测试通过（822 passed、1 skipped），隔离 Node 24 前端通过 209 files lint/format、38 个测试文件/135 项测试及 15 页 production build，repository/Compose 门禁通过。

Phase E 执行分派安全切片证据（2026-08-14）：现有视频源加载、工作区、Provider 调用、视频结果解析和清理已封装为 `VideoAnalysisExecutor`，公共 `AnalysisExecution` 只保留消息 claim、lease monitor、按 `AnalysisInputKind` 分派、统一 validating/publish 与失败收敛。未注册 `ScreenplayAnalysisExecutor` 时 screenplay/未知输入以非重试 `analysis_cli_unsupported` 终态失败；负例确认不会调用视频 Artifact loader、视频 analyzer 或任何视频工作区路径，因而不存在剧本回落到 FFmpeg/MCP 的可能。后端 Ruff/format/mypy 与全量测试通过（823 passed、1 skipped）；受限剧本执行器仍未实现，所以“拆分两个执行器”计划项保持未勾选。

Phase E 剧本输入工作区切片证据（2026-08-14）：Analysis repository 为 Worker 提供只读 normalized Document 投影，执行前再次绑定 job 的 owner、document ID、输入 SHA-256 与最短过期时间，并严格解析最多 5,000 个有序 scene range。独立 `LocalScreenplayArtifactLoader` 只接受当前 Document 下确定的 `documents/{id}/{attempt}/screenplay.md`，以默认 2 MiB 的 `ANALYSIS_MAX_SCREENPLAY_BYTES` 限制输入，下载后复核 bucket、路径、大小、SHA-256、严格 UTF-8、NFC、LF、单一尾换行、字符数和场景范围，再以只读 `input/screenplay.md` 交给后续执行器；失败/取消清空精确临时工作区。该路径不调用视频 loader、FFmpeg 或 MCP。后端全量门禁通过（826 passed、1 skipped）；CLI adapter、Prompt、schema 和结果发布仍属下一切片。

Phase E 剧本单次分析执行切片证据（2026-08-14）：新增独立 `ScreenplayAnalysisExecutor`，只处理 `screenplay-analysis` 契约；公共 Worker 继续只负责任务 claim、lease/heartbeat、按 input kind 分派、validating/publish 与失败收敛。执行器在调用前复核 normalized source，把默认单次容量限制在 120,000 字符与 120 个源场景，CLI Schema 另限 28,000 字节以守住 Windows 子进程参数边界；超限不下载、不调用模型，`screenplay-rewrite` 仍以 `analysis_cli_unsupported` 非重试失败。Claude adapter 固定单轮 `--tools ""`，同时拒绝 Bash/Read/Write/Edit/WebFetch/WebSearch/Agent，不暴露 FFmpeg、MCP、插件、浏览器或 subagent；Codex 因当前 CLI 没有可验证的全工具关闭模式而 fail closed。剧本正文按 JSON 字符串标记为不可信数据，输出由独立严格 schema 和领域 parser 再次校验输出语言、未知字段、总量、全部源场景精确顺序和 scene evidence 白名单后才发布。全量后端 Ruff、format、mypy 与测试通过（844 passed、1 skipped）；功能开关仍默认关闭，确定性分块、改写契约与真实中英文 Provider E2E 属于后续切片。

Phase E 剧本改写分块计划切片证据（2026-08-14）：可信父 Worker 的纯应用层 planner 已按 normalized scene range 生成确定性 source chunks；标题页等首场景前缀归入第一场，后续场景要求首尾连续且最后覆盖完整文本。单场景超限时依次选择后半区最靠后的段落、行或空格边界，无可用自然边界才按 Unicode 字符硬切；每块保存绝对 start/end、场景内连续 part_no、原文和 UTF-8 SHA-256，最终强制逐字符拼回完整输入。覆盖缺口、尾部截断、重复 scene ID 和块数超限分别以输入完整性或资源错误 fail closed。后端全量门禁通过（850 passed、1 skipped）；本切片尚未调用模型或发布改写结果，glossary、逐块 schema/调用和原子合并仍属后续切片。

Phase E 剧本改写模型契约切片证据（2026-08-14）：新增 glossary 与单块改写两类独立严格 Schema/Prompt/parser。Glossary 精确绑定源/目标语言，只接受六类术语、case-insensitive 唯一 source 和有界 style rules；单块输出必须原样回传 source scene ID、part_no、源块 SHA-256 和目标语言，正文保留经 NFC/LF 校验的原始空白，change summary 有界。剧本、相邻上下文、术语字段、Skill 和用户要求都以 JSON 数据边界传入，不能改变身份字段或工具策略。Claude 的 glossary/chunk 调用继续固定 `--tools ""`、单轮和双重工具拒绝；Provider resolver 只有同时提供两种受限调用才声明 rewrite 可用，Codex fail closed。后端全量门禁通过（865 passed、1 skipped）；该切片当时尚未把这些调用编排进 Analysis 生命周期，后续执行编排证据见下段。

Phase E 剧本改写执行编排切片证据（2026-08-14）：新增 `ScreenplayRewriteExecutor` 与契约路由，公共 Worker 在功能开关开启时同时装配并预检 analysis/rewrite 两种能力。父 Worker 先下载并复核一次 normalized screenplay，生成 glossary 后按确定性计划顺序调用每个 chunk；每次输出立即绑定 scene ID、part、源 SHA-256 和 target language，最终再按完整计划验证 100% 覆盖、顺序、glossary 术语出现次数和累计输出上限。只有全部块通过才构造单一结果进入公共 validating/publish，身份漂移、术语漂移、块数或总输出超限均不发布部分结果并清理工作区。默认限制为 8,000 字符/块、128 块、前后各 1,000 字符上下文和 400,000 输出字符；功能开关继续默认关闭，等待 Claude 中英文真实 E2E。后端全量门禁通过（872 passed、1 skipped）。

## 7. Phase F：Skill 迁移与剧本能力

- [x] 将 Skill loader 改为标准 YAML frontmatter + namespaced product metadata，支持一层 allowlisted references。
- [x] 迁移现有五个视频 Skill，显式声明 `video + video-visual-analysis`，删除 legacy parser/fixture。
- [x] 新建 `screenplay-analysis`，结合故事结构、人物、场景、对白和证据优先级规则。
- [x] 新建 `screenplay-structure-review`，聚焦 beats、场景推进、转折、连续性和节奏。
- [x] 新建 `screenplay-rewrite`，支持 `zh-CN/en-US` 跨语言改写和同语言润色。
- [x] 为每个外部启发来源记录 URL、固定 commit、许可证、NOTICE 和本地修改说明；不得复制未授权示例文本。
- [x] Registry 拒绝符号链接、路径穿越、绝对引用、scripts、allowed-tools 扩权、未知 input kind 和未知 result contract。
- [x] `GET /api/analysis-skills?input_kind=`、前端 hook 和 configurator 只展示输入兼容项。
- [x] 创建任务指纹加入编译后 Skill SHA-256、result contract、输出语言和 custom prompt。

验证：标准格式静态校验、重复 ID、非法 metadata/references、供应链负例、视频/剧本 Skill 过滤和任务快照重放。

Phase F Skill 目录切片证据（2026-08-14）：八个内置 Skill 已统一为 Agent Skills YAML frontmatter 与 namespaced 产品 metadata，视频/剧本分别稳定过滤为 5/3 个，结果契约在 registry 和创建任务两处按输入类型 fail closed；三个剧本 Skill 由 `skill-creator` 初始化并通过其 UTF-8 模式静态校验，详细规则只从显式一层 Markdown reference 编译。Registry 负例覆盖重复 ID/YAML key、非法名称、未知字段/输入/结果契约、绝对与穿越引用、未列出 reference、scripts、allowed-tools 与目录/文件符号链接。`NOTICE.md` 固定记录四个 GitHub 来源的 URL、commit、许可证和本地改写/排除范围，运行时不存在 clone/pull/install 或外部工具加载路径。OpenAPI 由当前 50-operation schema 隔离生成并逐文件 SHA-256 核对；后端 813 passed、1 个 Windows 不适用项 skipped，隔离 Node 24 前端 209 files lint/format、38 个测试文件/135 项测试、15 页 production build 通过。根 `scripts/ci.py all` 的提交/Actions/Compose/后端/20 项仓库检查通过，但其主工作区 `npm ci` 被外部进程占用的 `lightningcss` 原生文件锁阻断；等价前端门禁已在 lockfile 隔离安装中完成。该目录提交时尚未持久化编译指令 SHA-256，已由下段切片补齐。

Phase F 指令快照切片证据（2026-08-14）：`analysis_jobs` 现在同时保存编译后的完整 Skill 指令与其小写 SHA-256，创建任务会校验 catalog 哈希，并在幂等指纹中使用输入哈希、input kind、result contract、Skill ID、指令哈希、输出语言和规范化 custom prompt；Outbox 与公开 API 均不暴露指令或哈希。当前 PostgreSQL schema 通过受控 `pgcrypto.digest` 为旧任务按原指令回填哈希，再设置 NOT NULL 与十六进制 CHECK。真实 PostgreSQL 17 从提交 `01a231e` schema 建库、写入旧任务并连续两次应用当前 schema，得到 `migrated_hash=true / constraint_count=1 / extension_count=1`，非法哈希被拒绝，临时数据库已删除。后端全量门禁通过（814 passed、1 个 Windows 不适用项 skipped），Phase F 全部计划项完成。

## 8. Phase G：剧本分析与中英文改写闭环

- [ ] 实现容量内单次剧本分析与超限时“场景级 → 全局摘要”父 Worker 编排。
- [x] 实现 glossary 提取、按场景 chunk、单场景 part、相邻上下文和源块 SHA-256。
- [x] 实现逐块受限 CLI 调用、100% 覆盖校验和确定性合并。
- [x] 增加同一 run/attempt 内已验证 chunk 的安全恢复；跨 attempt/run 继续禁止复用临时输出。
- [x] 改写公开 JSON 只保存统计、glossary 和修改摘要，完整正文只进入 canonical Markdown/DOCX。
- [x] 文档详情增加结构/人物/场景/对白/修改建议视图，以及改写正文、glossary、摘要和导出视图。
- [x] 增加 AI 数据披露、Agent 离线、资源超限、部分块失败、取消、重试和过期文案。
- [ ] 完成中文剧本分析、英文剧本分析、中→英、英→中和同语言润色真实 E2E。

验证：scene/chunk 无缺失重复、顺序稳定、保留术语一致、语言正确、Markdown/DOCX 内容一致、普通日志和 API 无正文泄漏。

Phase G 文档详情闭环切片证据（2026-08-14）：前端 Analysis 服务与任务 Hook 已支持 owner-scoped Document create/latest，创建继续使用幂等键，执行中复用 Analysis WebSocket、降级轮询、取消、原任务重试和删除。ready 文档详情新增全宽剧本工作区，只展示三个 screenplay Skill；配置器按 Skill contract 把主操作切换为“开始剧本分析/改写”，并明确披露发送到活动云端模型的数据及无工具边界。分析结果按结构、人物、场景、对白、修改建议和安全 Markdown 报告组织；改写结果只从公开统计/glossary/摘要和 canonical Markdown 正文呈现，术语使用语义 table，完整报告可导出 Markdown/DOCX。隔离锁定依赖门禁通过 218 files lint/typecheck/format、39 个测试文件/140 项测试、15 页 production build；官方 npm registry production audit 为 0。1280×900 与 390×844 fixture 浏览器冒烟证明两类结果无页面级横向溢出，分析“场景”和改写“改写正文”页签可通过语义角色切换，浏览器 error 为空。主工作区 `npm ci` 仍被外部进程占用的 `lightningcss` 原生文件锁阻断，未改动依赖或 lockfile。真实 Provider 和带 owner/API 数据的完整浏览器流程仍待后续证据。

Phase G 同 attempt chunk 恢复切片证据（2026-08-14）：`ScreenplayRewriteExecutor` 把单块调用与身份/语言/SHA-256 解析校验置于同一个 lease monitor 内，对 Provider 限流、CLI 超时/通用失败、CLI 无效结构和 parser 校验失败执行指数退避的有界当前块重试。此前已验证输出只存在本次 `_rewrite_chunks` 局部列表中，不重复调用、不写数据库或工作区；当前块耗尽次数后仍走公共任务级失败，新 attempt 从 glossary 和完整确定性计划重新开始。自动化覆盖四类可恢复错误、持续身份漂移、不可恢复资源上限、无部分发布和工作区清理；默认每块最多 2 次调用、首次等待 1 秒。后端锁定依赖、Ruff、584 files format、407 source files mypy 与全量测试通过（878 passed、1 skipped）。

## 9. Phase H：安全、运行边界与发布

- [x] 收敛 PostgreSQL 单一数据库边界，并删除 Compose 对应用类型化安全默认值的重复展开。
- [ ] 验证正文/metadata/文件名 Prompt Injection 不能联网、读取 Home/仓库/相邻任务/Secret 或扩大工具。
- [ ] 验证 DOCX/PDF 解析器、FFmpeg、CLI 和 Import Worker 的 CPU/内存/pids/磁盘/wall-clock 限制。
- [ ] 验证删除/过期覆盖 multipart、quarantine、原文件、规范化文本、chunks、结果和报告，同时不删除仍被任务锁定的输入。
- [ ] 完成数据库空/已有卷、RabbitMQ、MinIO ACL/lifecycle、默认/production Compose 和健康接口验证。
- [ ] 执行后端 Ruff/format/mypy/pytest、前端 lint/format/test/build 和 OpenAPI 契约检查。
- [ ] 使用真实浏览器检查 1280px、1024px、390×844、键盘、屏幕阅读器名称、焦点恢复和页面级溢出。
- [ ] 更新 README、运行手册、设计 QA、SECURITY/隐私披露、SBOM/NOTICE 与 Acceptance 证据。
- [ ] main 推送后的 Required CI 全绿后才允许把 feature flags 作为默认开启。

Phase H PostgreSQL/容器边界切片证据（2026-08-14）：运行时配置和引擎仅接受 `postgresql+asyncpg`，JSON/索引/统计/就绪检查删除 SQLite 方言分支，开发锁文件移除 `aiosqlite`。原 SQLite repository/integration 测试全部改为真实 PostgreSQL 17 随机独立 schema，并禁止 search path 回退到部署 `public` schema；GitHub Backend quality 增加 PostgreSQL 17 service。迁移同时修正了 SQLite 曾掩盖的 aware UTC 断言、外键插入顺序及 readiness 硬编码 `public` schema，完整后端门禁通过 Ruff、586 files format、407 source files mypy 和 `881 passed, 1 skipped`。Compose 使用公共应用/Runner anchor，基础文件由 652 行收敛为 517 行，生产覆盖由 189 行收敛为 170 行；上传预算、Artifact TTL/GC、租约、批量和 verifier 上限回归类型化配置默认值，Secret、连接、固定容器路径、feature flags、网络隔离、初始化任务及生产 quorum 策略保持显式。默认、`environment` Profile、production override 均通过解析，Runner anchor 展开结果和 `video-server:local` runtime 镜像构建通过，镜像内不存在 `aiosqlite`。本证据只完成配置/构建和自动化门禁，不替代本 Phase 剩余的真实安全、完整运行边界、浏览器与 Required CI 验收。

## 10. 代码落点

```text
backend/app/domain/imports/                    上传/导入状态与规则
backend/app/domain/documents/                  Document、格式、规范化值对象
backend/app/application/imports/               上传会话、complete、取消用例与 ports
backend/app/application/documents/             文档查询、生命周期和预览用例
backend/app/application/analysis/              联合输入、Skill/契约选择与公开 views
backend/app/infrastructure/document_parsing/   DOCX/PDF/Text/Fountain verifier
backend/app/infrastructure/analysis_skills/    标准 loader、引用与供应链校验
backend/app/infrastructure/ai_cli/              视频/剧本 Prompt、Schema 与执行器
backend/app/workers/imports/                    Import consumer、恢复与清理
frontend/src/app/documents/                    文档列表与详情路由
frontend/src/components/                       上传、文档与剧本结果组件
frontend/src/hooks/                            导入、文档与分析状态流程
frontend/src/services/video/                   OpenAPI 生成代码
```

不在仓库顶层新增 `uploader/`、`document-server/`、`skills-market/` 或独立前端。

## 11. 每阶段质量门禁

- 所有后端公开操作有稳定唯一 operationId；创建资源返回 `201 + Location`。
- API、Workers、Application、Domain 依赖方向通过 architecture tests。
- 当前态 SQL 在空数据卷和已有当前态数据卷幂等执行。
- 每个新队列具备 quorum、confirm、manual ack、DLQ、有界回灌和幂等 consumer。
- 所有对象路径、上传权限、TTL、删除和孤儿恢复有正/负例。
- 所有外部依赖固定兼容版本并更新 lock、SBOM、许可证/NOTICE。
- 每个 Phase 完成时同步更新 Design/PRD/Plan/Acceptance，不等最终发布再补文档。

## 12. 开工前输入

Phase A–F 可使用仓库自建 fixture 开始。真实验收前需要：

- 项目自有或明确授权的短 MP4；
- 可提交为 fixture 的合成中/英文短剧本，以及不能入库但可本地验收的完整剧本；
- 内容权利声明和 AI 云端处理披露文本；
- 外部 Skill 来源 commit、许可证和 NOTICE 责任人确认；
- 足够完成中英双向真实调用的活动 AI Provider 配额。

缺少真实完整剧本或 Provider 配额时，代码和自动化可以推进，但 Acceptance 中的中英双向 E2E 必须保持未通过，四件套不得归档。
