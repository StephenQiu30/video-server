# 003 AI 分析与思维导图计划

- 状态：In Progress（LangChain 双 Provider 与自动化门禁已完成，真实视频 E2E 待验）

## 实施顺序

1. 实现 AnalysisJob 状态机、Transcript 与 AnalysisDocument schema/validator。
2. 增加 `analysis_jobs`、`analysis_results`、outbox 与 artifact retention lock。
3. 实现 MinIO 输入读取、FFmpeg 音频提取和确定性分块。
4. 实现独立 ASR 客户端，以及 LangChain DeepSeek/Ollama 文本分析适配器、稳定错误映射和受控 Provider fixture。
5. 实现结构化输出、证据校验、最多一次修复请求与 transcript-free 结果持久化。
6. 实现 analysis API 和 Worker 的 lease/heartbeat/cancel。
7. 前端实现 Analysis Panel、章节时间展示与思维导图。
8. 使用受控短视频执行真实 ASR/LLM E2E，并记录模型、schema、prompt 和成本证据。

## 当前证据与剩余工作（2026-08-06）

- 步骤 1—7 已落地；后端自动化覆盖分析状态机、SQLAlchemy repository/outbox、FFmpeg 预处理、ASR、LangChain DeepSeek/Ollama adapter、Worker、HTTP API 和敏感字段隔离。
- 前端全量门禁通过，覆盖 Analysis Panel、API、轮询/取消和结构化结果展示。
- PostgreSQL 当前结构只定义在 `backend/sql/schema.sql`；分析 API、Outbox、Worker 与 Media Runner 通过根 Dockerfile 的统一 runtime 镜像交付，运行入口由两份根 Compose 管理。
- CI 不设置覆盖率数字硬门槛；当前 ASR 与 LangChain adapter 测试使用受控 fake client。
- 步骤 8 尚未执行，未产生真实 ASR + DeepSeek/Ollama、模型成本或真实视频证据，因此 003 Acceptance 保持 Pending。
