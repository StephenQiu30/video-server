# 038 本地 Agent 与 Codex App 插件计划

## 切片 1：本机控制面与插件启动链路（已完成）

- [x] 固定 loopback HTTP v1、随机令牌和私有 endpoint。
- [x] 实现 Agent 存活探测、陈旧状态恢复、并发启动锁和停止清理。
- [x] 实现 Codex/Claude Code 安装、版本与登录诊断。
- [x] 提供 Codex 插件清单、stdio MCP、Skill 和仓库内市场清单。
- [x] MCP 初始化时自动尝试拉起 Agent。
- [x] 为鉴权、隐私字段和 MCP 生命周期补充确定性测试。

## 切片 2：设备配对与服务端任务端口（待实施）

- [ ] 服务端提供一次性配对码、设备撤销和最小权限设备凭证。
- [ ] Agent 保存设备凭证，不复用本机 endpoint 令牌。
- [ ] 定义任务 lease、heartbeat、取消和结果提交契约。
- [ ] 保持任务状态事实在 PostgreSQL，Agent 断线后 lease 可恢复。

## 切片 3：Codex / Claude 业务执行（待实施）

- [ ] 将 037 统一 AI 请求映射到本地 CLI 执行策略。
- [ ] 子进程禁用 FrameFetch 插件，限制工作区、时长和输出大小。
- [ ] 实现 Codex 与 Claude Code 的取消、超时、结构化结果校验和错误分类。
- [ ] 以授权媒体样本完成一条真实分析与报告回传。

## 切片 4：发行与升级（待实施）

- [ ] 生成独立 Agent 可执行包和 macOS/Windows 安装流程。
- [ ] 增加签名、公证、版本协商、可回滚自动更新。
- [ ] 发布正式插件市场条目并验证全新设备安装。
