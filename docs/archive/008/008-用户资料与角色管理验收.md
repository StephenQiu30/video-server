# 008 用户资料与角色管理验收

- 状态：Accepted
- 日期：2026-08-09
- 关联 Design：`docs/archive/008/008-用户资料与角色管理设计.md`
- 关联 PRD：`docs/archive/008/008-用户资料与角色管理需求.md`

## 逐项验收

- [x] A1：注册用户名唯一，首位或配置邮箱正确获得管理员角色。
- [x] A2：个人资料更新后 `/api/auth/me` 与导航显示一致。
- [x] A3：普通用户访问管理员接口返回 403。
- [x] A4：管理员查询、角色调整、停用和会话撤销可用。
- [x] A5：管理员自我降级或停用返回稳定冲突错误。
- [x] A6：历史、资料和用户管理页使用 Next.js App Router、Radix UI 与 Tailwind CSS，符合 009 方案 3 的 `#FAFAFA` 画布、Geist、80px 无边框 Header、导航/主体统一的 1376px 隐形网格与 80/32/16px 响应式 gutter、近黑主操作、无边框填充控件和发丝 Separator；无可见 PageShell/Card/ring 外壳，窄屏无溢出。
- [x] A7：方案 3 合并后的后端与前端全量门禁、生产构建和 Compose 解析通过。

## 验收证据

- 后端：当前 `main` 的 GitHub Actions 运行 `31765542909` 中 Backend quality 与契约/集成测试通过。
- 前端：同一运行的 Frontend quality 通过；本轮 `npm run build` 再次成功导出 12 条业务/错误路由，认证与用户服务继续由 `@umijs/openapi` 生成。
- 浏览器：2026-08-14 使用独立管理员与普通用户会话复验账户和用户管理。普通用户直接访问 `/admin/users`、`/admin/analytics` 均重定向回首页，账户菜单无管理入口；管理员用户表真实显示 11 个账户且当前管理员自编辑按钮禁用。
- 布局与可访问性：1440×900 与 390×844 的账户、用户管理页均无横向溢出；移动导航 Sheet 正常；全路由 axe serious/critical 为 0。
- 运行边界：GitHub Actions Runtime boundary、Required CI 全部成功；本轮统一镜像重建、Compose 启动和 `/health/ready` 200 通过。本地验收不等同于生产部署。
