# 004 下载历史验收

- 状态：Accepted
- 日期：2026-08-07
- 关联 Design：`docs/archive/004/004-下载历史设计.md`
- 关联 PRD：`docs/archive/004/004-下载历史需求.md`

## 冻结前置条件

- [x] 当前登录账户可稳定派生并复用 `owner_hash`。
- [x] `download_jobs`、`media_inspections`、`media_formats` 已由当前 schema 初始化。
- [x] API 与前端均通过现有项目依赖启动，不引入第二套路由或数据库访问方式。

## 逐项验收

- [x] A1：同一账户可创建任务并在历史中看到；另一账户看不到该任务。
- [x] A2：默认排序、分页、状态筛选和标题搜索均返回正确 `total`。
- [x] A3：inspection 过期后历史仍返回标题和格式，且响应不包含敏感字段。
- [x] A4：历史页统计、空状态、加载失败、重试和窄屏布局可用；页面符合 009 方案 3 的 `#FAFAFA` 画布、Geist、80px 无边框 Header、导航/主体统一的 1376px 隐形网格与 80/32/16px 响应式 gutter、64px 桌面顶部留白、近黑主操作、无边框填充控件与发丝 Separator；页内无可见 PageShell/Card/ring 外壳，真实空态、历史数据态与移动端证据已补齐。
- [x] A5：成功记录可进入任务详情，并沿用短时 URL 获取文件。
- [x] A6：后端测试、前端测试、lint、格式、类型检查和构建通过。

## 证据记录

### 自动化证据

- `uv run pytest tests/unit/infrastructure/test_download_store.py tests/integration/api/test_download_routes.py tests/contract/test_swagger_docs.py -q`：退出码 0，14 passed；覆盖 owner 隔离、标题/状态筛选、公开字段、解析记录过期后仍可读和 OpenAPI operation。
- `uv run ruff check app tests`：退出码 0。
- `npm run lint`：退出码 0，包含 Biome lint 与 TypeScript typecheck。
- `npm run format:check`：退出码 0。
- `npm test -- --run`：退出码 0，11 files / 34 tests passed。
- `npm run build`：退出码 0，产出 Next.js 静态导出目录 `frontend/out`。

### 当前结论

下载历史的 owner 隔离、筛选/搜索/分页、过期 inspection 可读、公开字段白名单、详情/取件入口和工程门禁均由既有自动化覆盖。2026-08-14 在真实 Compose（PostgreSQL、MinIO 与统一 FastAPI 静态交付）上复验桌面 1440×900 和移动 390×844：空态可读，移动端 `scrollWidth = clientWidth = 390`，页面错误与产品控制台错误为空。

当前 `main` 的 GitHub Actions 运行 `31765542909` 中 Repository rules、Frontend quality、Backend quality、Runtime boundary 与 Required CI 全部成功；本轮统一镜像重建和 readiness 200 再次通过。历史数据态与任务跳转已有 `design-qa.md` 记录，故本文状态同步为 `Accepted`。
