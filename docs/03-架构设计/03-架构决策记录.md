# 架构决策记录

更新时间：2026-05-02

## 当前状态

关键技术已由项目负责人在 2026-05-02 手动确认。当前已生效决策见下表，详细候选和说明见 [02-技术选型候选.md](02-技术选型候选.md)。

| ADR | 状态 | 决策 |
| --- | --- | --- |
| ADR-001 | 已接受 | 前端采用 React + Umi + TypeScript + Ant Design Pro |
| ADR-002 | 已接受 | 后端采用 FastAPI + Python 3.12 |
| ADR-003 | 已接受 | 下载内核采用 yt-dlp，音视频处理采用 FFmpeg / ffprobe |
| ADR-004 | 已接受 | 后台任务采用 RQ + Redis |
| ADR-005 | 已接受 | 数据库采用 PostgreSQL |
| ADR-006 | 已接受 | 文件存储采用 MinIO / S3 兼容对象存储 |
| ADR-007 | 已调整 | M1 采用本地单用户模式，JWT 用户系统延后到上线级 SaaS |
| ADR-008 | 已接受 | 首版不做 AI 摘要和平台专用解析，只预留扩展口 |
| ADR-009 | 已接受 | 项目结构采用默认推荐 monorepo |
| ADR-010 | 已接受 | 下载限制采用 2GB、2 小时、全局并发 2、单用户并发 1、保留 24 小时 |
| ADR-011 | 已接受 | M1 实现变更使用 OpenSpec `bootstrap-mvp-foundation` 管理 |

## ADR-001：前端技术栈

## 状态

已接受。

## 决策

前端采用 React + Umi + TypeScript，并使用 Ant Design Pro。实现时优先复用 ProLayout、ProTable、ProForm 和任务工作台模式。

## 影响

前端更适合构建任务台、表格、表单和管理型工作流。Umi 与 Ant Design Pro 的常见组合降低了工作台搭建成本。

## ADR-002：后端技术栈

## 状态

已接受。

## 决策

后端采用 FastAPI + Python 3.12。

## 影响

后端可以直接集成 yt-dlp、RQ、PostgreSQL 客户端和对象存储 SDK，并通过 OpenAPI 文档支撑前后端并行。

## ADR-003：下载与音视频处理

## 状态

已接受。

## 决策

下载内核采用 yt-dlp，音视频处理采用 FFmpeg / ffprobe。所有调用必须通过下载内核适配层进入，不允许散落在业务控制器中。

## 影响

首版依赖成熟下载生态，但必须接受平台规则变化造成的解析失败。系统需要保留清晰的失败原因和升级路径。

## ADR-004：任务队列

## 状态

已接受。

## 决策

后台任务采用 RQ + Redis。

## 影响

下载任务由 Worker 执行，API 只负责创建任务和查询状态。后续可以扩展多队列、优先级和多 Worker。

## ADR-005：数据库

## 状态

已接受。

## 决策

数据库采用 PostgreSQL。

## 影响

任务、用户、历史、元数据和后续审计可以使用统一关系数据库管理。部署需要在 Docker Compose 中提供 PostgreSQL 服务。

## ADR-006：文件存储

## 状态

已接受。

## 决策

文件存储采用 MinIO / S3 兼容对象存储。

## 影响

MVP 即按对象存储接口设计，适合私有部署和后续迁移到云对象存储。需要在任务完成、过期清理和下载授权中处理对象 key。

## ADR-007：M1 访问模式

## 状态

已调整。

## 决策

M1 阶段采用本地单用户模式：解析、任务和下载链接接口自动归属到默认本地用户，用于优先跑通核心下载闭环。JWT 注册登录、用户隔离、管理员和配额能力归入生产化计划，不作为当前本地 MVP 的默认 API 或前端入口。

## 影响

本地 MVP 不再被登录注册阻塞，前端默认展示首页、工作台、任务历史和合规边界。公网或小范围内测 SaaS 前仍必须重新设计并启用鉴权、限流、配额和管理员兜底，并通过 `production-saas-readiness` 单独验收。

## ADR-009：项目目录结构

## 状态

已接受。

## 决策

项目结构采用默认推荐 monorepo：`apps/web`、`apps/api`、`apps/worker`、`packages/shared`、`infra/docker`。

## 影响

前端、后端、Worker、共享契约和基础设施边界清晰，适合 M1 并行推进。

## ADR-010：下载限制

## 状态

已接受。

## 决策

MVP 默认限制为：单任务最大 2GB、最长 2 小时、全局并发 2、单用户并发 1、文件保留 24 小时、预签名 URL TTL 15 分钟。

## 影响

默认值偏保守，优先保护本地和私有部署资源。后续可通过环境变量调整，并在生产化阶段引入用户配额。

## ADR-011：OpenSpec 变更管理

## 状态

已接受。

## 决策

M1 MVP 骨架实现使用 OpenSpec 变更 `bootstrap-mvp-foundation` 管理。

## 影响

后续实现必须围绕 proposal、design、specs、tasks 推进，完成后运行 `openspec validate --all`，并在交付前更新任务状态。

## ADR-008：首版延后能力

## 状态

已接受。

## 决策

AI 摘要二期再做；平台专用解析首版不做，只预留插件口。

## 影响

MVP 聚焦视频解析、格式选择、任务下载、状态反馈、历史和合规提示，避免过早扩大范围。

## ADR 命名规范

后续如需拆分为单独 ADR 文件，使用以下命名：

```text
adr-001-use-react-umi-ant-design-pro.md
adr-002-use-fastapi-for-backend-api.md
adr-003-use-yt-dlp-and-ffmpeg.md
```

## ADR 模板

```markdown
# ADR-编号：决策标题

## 状态

待确认 / 已接受 / 已废弃 / 已替代

## 背景

说明为什么需要做这个决策。

## 候选方案

列出候选方案和取舍。

## 决策

说明最终选择。

## 影响

说明对开发、部署、成本、安全和后续扩展的影响。
```
