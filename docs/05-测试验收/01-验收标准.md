# 验收标准

更新时间：2026-05-02

## MVP 验收标准

| 模块 | 标准 |
| --- | --- |
| 链接解析 | 至少 3 个公开可访问视频样例能解析出标题、封面、时长和格式 |
| 任务创建 | 用户能选择格式并创建下载任务 |
| 任务状态 | 支持 queued、running、succeeded、failed、canceled |
| 下载结果 | 至少 1 个样例能完成下载、合并和文件获取 |
| 错误反馈 | 不支持平台、网络失败、格式不可用、文件过大、超时均有明确提示 |
| 合规提示 | 下载前和错误提示中体现授权和合法使用边界 |
| 安全 | URL、日志、文件名做基础脱敏和清洗 |
| 清理 | 下载文件有保留期限和清理策略 |

## M1 当前验收状态

| 验收项 | 当前状态 | 证据或说明 |
| --- | --- | --- |
| 后端代码可编译 | 已通过 | `python3 -m compileall apps packages` |
| OpenSpec 变更有效 | 已通过 | `openspec validate --all --json` |
| 本地脚本语法 | 已通过 | `bash -n scripts/*.sh` |
| URL 脱敏、文件名清洗、健康检查和 Worker 工具函数测试 | 已通过 | `PYTHON_BIN=.venv/bin/python ./scripts/dev_test.sh` 执行通过，结果为 `11 passed` |
| 本地开发配置 | 已完成 | `.env.example` 使用 `127.0.0.1`，复用本地 Python、PostgreSQL、Redis、MinIO / S3 |
| Docker 部署配置 | 已完成基础配置 | `APP_ENV_FILE=../../.env.production.example docker compose --env-file .env.production.example -f infra/docker/docker-compose.yml config` 已通过；生产 Compose 覆盖 Web、API、Worker、PostgreSQL、Redis、MinIO，并固定 `video-downloader-*` 容器名 |
| 完整 API smoke | 已通过 | `BASE_URL=http://127.0.0.1:8000 PYTHON_BIN=.venv/bin/python ./scripts/smoke_api.sh` 已通过 |
| 公开视频解析样例 | 已通过 | `scripts/smoke_parse_samples.sh` 使用 3 个 Creative Commons / Public Domain Vimeo 样例跑通解析 |
| 小文件下载样例 | 已通过 1 个样例 | `scripts/smoke_download_task.sh` 使用 `https://commons.wikimedia.org/wiki/File:%22Movbild-fizika%22_falo_en_Big_Buck_Bunny.webm` 跑通创建任务、Worker 下载、上传 MinIO、生成下载链接 |
| FFmpeg / ffprobe | 已通过本地验收 | 本机 `ffmpeg -version`、`ffprobe -version` 均可执行；当前小文件下载样例可读取到时长 `5.262000` 秒、大小 `614710` 字节 |
| 前端操作验收 | 已完成构建级验收 | `apps/web` 已初始化 Ant Design Pro / Umi 顶部布局工作台；`npm run lint` 与 `npm run build` 已通过 |
| 合规负向验收 | 已通过基础脚本 | `scripts/smoke_compliance_negative.sh` 已覆盖未登录、重复邮箱、密码错误、跨用户任务访问、未完成任务下载链接和 Cookie / 平台绕过运行时代码扫描 |
| Web 镜像构建 | 已通过 | 生产 Web 镜像由 `infra/docker/web.prod.Dockerfile` 构建，Nginx 配置内置在生产镜像中，不保留独立 nginx 配置文件 |
| 管理员与免费配额 | 已通过基础脚本 | `scripts/smoke_admin_quota.sh` 已覆盖管理员识别、用户列表、配额调整、普通用户拒绝访问管理员接口、每日任务额度为 `0` 时创建任务返回 `429` |

## 技术验收标准

- 关键技术栈已由项目负责人确认。
- 下载内核通过适配层调用。
- Worker 只能写入受控目录。
- 公网部署前必须启用鉴权、限流和任务配额。
- 测试样例不得使用付费、会员、隐私或盗版链接。

## M1 放行标准

M1 不能只看代码存在，必须满足以下放行条件：

- 用户可以完成注册、登录并获取 JWT。
- 用户只能看到和操作自己的下载任务。
- 解析接口必须通过下载内核适配层调用 yt-dlp，不允许绕过适配层。
- 任务必须覆盖 queued、running、succeeded、failed、canceled 状态。
- 下载成功后文件必须进入私有 MinIO / S3 bucket。
- 下载链接必须由后端校验任务归属后生成短期预签名 URL。
- `/ready` 必须能同时证明 PostgreSQL、Redis、MinIO / S3、FFmpeg / ffprobe 在本地可用。
- 至少 3 个公开视频样例能解析成功。
- 至少 1 个小文件样例能完成下载、上传和下载链接获取，并补充 ffprobe 媒体校验证据。
- DRM、付费墙、会员内容、私密链接、Cookie 托管和平台专用绕过不得作为 M1 能力出现。
- 本地开发默认复用已有 Python、PostgreSQL、Redis、MinIO / S3；上线容器验收后续单独规划。
