# 付费内容识别与授权获取验收

## 本期验收条件

- B 站充电标记在归一化前被阻断；原始试看格式不能继续作为完整格式。
- 抖音付费、已购买、试看及字段异常可区分；分享页、回退、图文入口不因异常处理跳过检查。
- 新原因跨 runner、应用层和下载执行保持一致；inspection 不包含格式或伪造授权。
- 非 downloadable UI 不呈现可下载格式及按钮，普通公开结果保持原行为。
- 插件在真实 yt-dlp 子进程加载，不能只以直接调用辅助函数作为集成依据。

## 验证状态

2026-09-07，本地验证：

- `ruff check app tests` 通过；`mypy app --platform linux` 通过（519 个源文件）。
- 本期插件、限制结果、HTTP 客户端、排队前复核、命令、Provider 注册表及数据库契约定向回归 222 项通过。
- Windows 可执行的后端完整回归 1485 项通过、2 项按环境跳过、1 项既有权限语义用例取消选择；另有 4 个依赖 POSIX 信号或权限语义的测试模块未在 Windows 收集。
- 前端 `npm run lint`、`npm run format:check`、`npm run openapi:check`、`npm test`（56 文件 / 229 项）和 `npm run build` 均通过。
- 本机、生产及 GitHub CI 三套 Compose 配置均可静态解析。

脱敏夹具验证只证明逻辑，不代表生产平台成功率。未修改运行中容器，也未部署应用。

## 图文备用解析回归修复验证

2026-09-07：已修复公开抖音图文在 React Flight 备用解析成功后，被旧版 router data 非致命警告误判为 extractor_regression 的问题。

- 新增 `tests/unit/workers/runner/test_inspection_gallery_warnings.py`，保留真实 `_search_json`，只替换网络响应，覆盖“实际警告 + 成功 assets”组合；修复前可复现失败，修复后通过。
- 新增 16 项测试：有效图文、五类 ContentRestriction 继续阻断、空/畸形 assets、未知媒体类型、私网和非 HTTP(S) 地址仍被后续校验拒绝。
- 本轮相关回归 175 项及图文/集合/元数据回归 35 项全部通过，共 210 项；`ruff check app tests` 与 Linux 目标 `mypy app --platform linux` 通过。
- 本次仅修复后端判断及补充测试，未改前端，未部署运行中容器；不扩展真实付费下载验收结论。

## 待外部条件

未提供真实已充电/已购买账号及可验证导出授权样本，付费完整版端到端未验收。
未覆盖全部番剧、课程、抖音钻石商品入口；不得宣传“支持所有 VIP 视频下载”。
未来授权连接器须另行验证购买前后、过期/撤销、试看/完整版、音画完整性与 DRM 拒绝路径。
