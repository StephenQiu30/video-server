---
layer: Acceptance
status: draft
result: pending
version: "2.0.0"
canonical_path: docs/acceptance/000-MVP验收总览.md
purpose: "冻结 MVP 全链路验收 ID、方法与证据要求"
inputs:
  - "docs/prd/001-授权视频下载与AI知识化MVP.md"
  - "docs/design/005-邮箱身份与持久化基础设施设计.md"
  - "docs/acceptance/002-邮箱身份与持久化基础设施验收.md"
outputs: []
---

# MVP 验收总览

## 1. 验收规则

每项只能记录 `passed`、`failed` 或 `blocked`，并附命令、请求响应、日志、媒体探针、评测表、PDF 截图或删除审计证据。缺少证据即不是 `passed`。

## 2. 验收数据集

- `DS-P1-RESOLVE-12` 与 `CANARY-W3C-SINTEL-1` 是 Plan 001 门禁；定义见细化 Acceptance。
- `DS-MVP-SOURCES-60` 是 Plan 005 最终兼容集：直链、非加密 HLS、非加密 DASH 各 20 个，不反向扩大 Plan 001。
- Plan 001 负向集按安全向量逐项 100% 阻断；最终 MVP 另保留 DRM、SSRF、禁用下载、政策阻断各 10 个的回归集。
- AI 基准视频：中英文、5 至 120 分钟，共 30 个，覆盖教程、演讲、访谈、评测和会议。
- PDF 基准：短、中、长文档，中文、英文、混排和大思维导图。

所有线上样本必须记录授权依据；CI 使用自有或明确授权 fixture。

## 3. 验收矩阵

| ID | 预期 | 方法 | 当前结果 |
| --- | --- | --- | --- |
| `AC-POL-001` | 权利确认不能覆盖政策阻断 | API/策略测试 | pending |
| `AC-POL-002` | 未评审或过期来源默认禁用 | 时钟与策略测试 | pending |
| `AC-AUTH-001` | 邮箱注册/验证/登录/注销/找回可用，generic response 不泄露账户存在性 | API/SMTP/枚举测试 | pending |
| `AC-AUTH-002` | Argon2、数据库会话、安全 Cookie/CSRF、UUID owner 隔离与撤销全部满足契约 | 认证/IDOR/会话故障测试 | pending |
| `AC-DATA-001` | PostgreSQL 是全部业务状态的唯一事实，事务/outbox/owner FK 可恢复 | migration/并发/故障注入 | pending |
| `AC-REDIS-001` | Redis 全失后可从 PostgreSQL/outbox 重建，session/Job/资产事实不丢失 | wipe/replay/恢复测试 | pending |
| `AC-MAIL-001` | post-commit callback 崩溃可由幂等扫描补单；PG mail outbox 经 SMTP 重试且 generic response 不泄露账户存在性 | callback/SMTP 故障/重放/枚举测试 | pending |
| `AC-OBJ-001` | MinIO 私有对象只在 HEAD/SHA-256 后 READY，孤儿/错配可补偿；生产维护来源已批准 | 集成/故障注入/对账 | blocked |
| `AC-DR-001` | PostgreSQL PITR、Redis 重建、MinIO 独立备份恢复达到 RPO≤5 分钟/RTO≤2 小时 | 灾备演练/审计 | blocked |
| `AC-CONTRACT-001` | 身份/会话/资产 OpenAPI 可重复生成并作为 Web 唯一 HTTP 基线 | 生成/diff/schema fixture | pending |
| `AC-SRC-001` | Plan 001 数据集 100% + production canary；最终线上目标 ≥95% | worker 集成/canary | pending |
| `AC-SRC-002` | 真实规格准确且 p95 ≤8 秒 | fixture/基准 | pending |
| `AC-SRC-003` | API、事件和日志无真实媒体 URL | 契约/泄漏扫描 | pending |
| `AC-SEC-001` | 私网、metadata、危险重定向阻断 100% | 安全测试 | pending |
| `AC-API-001` | 幂等创建且 PostgreSQL 为唯一状态真相 | 并发/故障注入 | pending |
| `AC-DL-001` | probe-only dossier 100% 拒绝下载；operator-owned download grant 正向成功率 ≥99%，可取消和有限重试 | policy/E2E/故障注入 | pending |
| `AC-DL-002` | 分辨率、音轨和容器一致率 100% | ffprobe | pending |
| `AC-ASSET-001` | 大文件直达对象存储且生命周期生效 | 网络/清理审计 | pending |
| `AC-UP-001` | 本地合法文件可独立进入 AI | E2E | pending |
| `AC-AI-001` | 普通话 CER、英语 WER 均 ≤15% | 固定评测集 | pending |
| `AC-AI-002` | 忠实度 ≥4.2/5，完整度 ≥4.0/5，严重幻觉 0 | 人工双评 | pending |
| `AC-AI-003` | Schema 100%，事实证据 ≥95%，节点证据 ≥90% | Schema/评测 | pending |
| `AC-PDF-001` | 基准 PDF 100% 成功且无排版缺陷 | 渲染/截图/文本抽取 | pending |
| `AC-PRI-001` | 同意、私有性、立即删除和自动过期符合契约 | 隐私/时钟测试 | pending |
| `AC-OBS-001` | 每个失败有稳定错误码、终态和审计事件 | 故障矩阵 | pending |

## 4. 评测与性能规则

AI 基准 manifest 固定 30 个授权样本、参考转写、等权关键事实和内容要点。转写先做 Unicode NFKC、英文小写、数字保留、标点/多余空白移除；中文按字符、英文按空格词计算全语料 micro CER/WER。

忠实度：5=全部主张有证据，4=无事实错误且仅轻微措辞偏差，3=有非关键无依据陈述，2=有关键错误，1=主体结论失真。完整度按参考要点覆盖：5=100%，4=90–99%，3=75–89%，2=50–74%，1<50%。两名评审独立打分；任一维度分差>1时，第三人独立评分并以三人该维度的中位数作为样本最终分，否则取原两人均值；再对30样本做宏平均。捏造主体、数字、因果、结论或行动项即严重幻觉，任一出现即 AC 失败。

PDF corpus 固定 12 份：中英文/混排、短中长内容和小/大思维导图交叉覆盖。阻断缺陷包括乱码、文字/图形越界、裁切、重叠、空白关键页、不可选择正文、标题层级丢失或大纲缺失；任一阻断缺陷即失败。

持久化恢复必须从可记录的故障时点开始：先恢复 PostgreSQL 至目标时间，再启动空 Redis 并重放 outbox，最后恢复/挂载 MinIO 并按 key/version/SHA-256 与 PostgreSQL 对账。任何把 Redis 快照或 MinIO listing 反向当作用户、Job、资产状态真相的流程都失败。系统默认目标为 RPO≤5 分钟、RTO≤2 小时。MinIO 生产验收还必须记录维护发行版或书面风险接受、安全补丁责任和固定 digest；当前缺失，因此 `AC-OBJ-001`、`AC-DR-001` 明确为 `blocked`。

- 清晰度目录 p95 ≤8 秒。
- 下载取消 2 秒内更新、5 秒内停止拉取。
- 5 至 60 分钟视频 AI 处理 p95 不超过视频时长的 0.5 倍。
- PDF 生成 p95 ≤10 秒。

清晰度 p95 使用 Plan 001 的 12 个 fixture，在 warm worker、cold format cache、并发 1 下运行 30 次，从 POST 接收至 Source 首次可读。其余性能目标在对应 Plan 冻结硬件、并发、缓存与样本起止时点；环境不一致时不得比较。

## 5. 当前状态

产品尚未完成：`AC-OBJ-001`、`AC-DR-001` 因 MinIO 生产维护/风险接受与恢复证据缺失为 `blocked`，其余全局条目为 `pending`。纯领域、rights catalog、Resolution 原子创建和 attestation history 已有局部 Green，但邮箱身份、UUID owner、完整基础设施、来源解析、下载、AI 与 PDF 均未形成全链证据。Plan 000/001 的细化验收分别见 `002-邮箱身份与持久化基础设施验收.md` 与 `001-URL解析与清晰度目录验收.md`。

## 6. 变更记录

| 版本 | 日期 | 变更说明 |
| --- | --- | --- |
| 2.0.0 | 2026-07-18 | 因邮箱 UUID/数据库会话替换 installation 身份而保持 draft；加入 PG/Redis/MinIO/SMTP 与灾备验收矩阵 |
| 1.0.0 | 2026-07-18 | 独立复审通过，冻结 MVP 验收方法；执行结果仍 pending |
| 0.4.0 | 2026-07-18 | 冻结第三评审仲裁为三人中位数 |
| 0.3.0 | 2026-07-18 | 区分 Plan/MVP 数据集并冻结 CER/WER 与 AI 评分算法 |
| 0.2.0 | 2026-07-18 | 补充主体验收、AI 双评、PDF 缺陷和性能测量规则 |
| 0.1.0 | 2026-07-18 | 初始化全链路验收矩阵 |
