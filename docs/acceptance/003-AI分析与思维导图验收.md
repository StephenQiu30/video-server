# 003 AI 分析与思维导图验收

- 状态：In Progress
- 结论：Pending

## 自动化门禁

- [x] AnalysisJob 状态机、lease、幂等、取消与下载失败隔离。
- [x] 音频提取/分块、绝对时间偏移、资源上限和临时文件清理。
- [x] 使用受控 SDK fake 验证 ASR/LLM 成功、可重试、不可重试和非法响应错误。
- [x] transcript 单调性、证据 ID、章节范围和导图深度/节点数校验。
- [x] Prompt injection fixture 保持为不可信转录数据，不能改变严格 schema。
- [x] artifact retention lock 在创建、重试和终态释放时的竞争测试。
- [x] Analysis API owner 404、幂等创建、取消、Problem Details 与敏感字段隔离；前端 Analysis Panel 契约测试。
- [ ] 使用真实 OpenAI API 执行 ASR 与结构化 Responses E2E。

## 当前自动化证据（2026-08-06）

- 后端全量门禁通过，包括 Analysis Domain、持久化、音频预处理、OpenAI adapter、Worker 和 HTTP API。
- 前端全量门禁通过，包括分析创建、轮询、取消、结构化结果和思维导图展示。
- API 响应不返回 artifact id、schema version、完整 transcript 或 provider 原始 payload；自动化使用 fake provider，不代表真实 OpenAI 调用成功。
- 002 已通过的受控视频下载只证明分析输入制品可获得；本次没有调用真实 OpenAI ASR/Responses API，不能作为 003 的真实 Provider E2E 证据。
- CI 没有覆盖率数字硬门槛；验收依据核心契约与安全场景，而非覆盖率百分比。

## 真实用户验收

- [ ] 受控视频生成摘要、关键观点、行动项、章节和思维导图。
- [ ] 所有观点和节点都引用本次真实转录证据，带时间的内容可定位到视频时间。
- [ ] AI 失败时下载仍可获取，重试不会产生重复结果。
- [ ] 浏览器、API 和日志均不出现 provider secret。

真实 OpenAI 与受控视频端到端验收尚未完成，结论保持 Pending；不得以 fake provider 结果改写为 Passed。
