# 008 用户资料与角色管理验收

- 状态：In Progress（A1–A5 业务/安全能力已验收；A6–A7 方案 3 UI 与本轮门禁待重新取证）
- 日期：2026-08-09
- 关联 Design：`docs/design/008-用户资料与角色管理设计.md`
- 关联 PRD：`docs/prd/008-用户资料与角色管理需求.md`

## 逐项验收

- [x] A1：注册用户名唯一，首位或配置邮箱正确获得管理员角色。
- [x] A2：个人资料更新后 `/api/auth/me` 与导航显示一致。
- [x] A3：普通用户访问管理员接口返回 403。
- [x] A4：管理员查询、角色调整、停用和会话撤销可用。
- [x] A5：管理员自我降级或停用返回稳定冲突错误。
- [ ] A6：历史、资料和用户管理页使用 Next.js App Router、Radix UI 与 Tailwind CSS，符合 009 方案 3 的 `#FAFAFA` 画布、Geist、72px 无边框 Header、1456/1376px 两级隐形网格与 40/80/32/16px 响应式 gutter、近黑主操作、无边框填充控件和发丝 Separator；无可见 PageShell/Card/ring 外壳，窄屏无溢出。待完整数据态浏览器证据。
- [ ] A7：方案 3 合并后的后端与前端全量门禁、生产构建和 Compose 解析通过。待本轮实际结果。

## 验收证据

- 后端：`ruff check`、`ruff format --check`、`mypy app` 通过，`pytest` 355 项通过。
- 前端：旧基线的 Biome、TypeScript、Vitest 和 Next.js production build 证据仅保留为历史记录；方案 3 合并后的实际命令与计数待填写。认证与用户服务仍由 `@umijs/openapi` 生成。
- 浏览器：旧基线已验证管理员资料更新、普通用户路由 403、管理员停用普通用户和停用后登录态失效；方案 3 下的功能/视觉浏览器回归待本轮证据。
- 布局：Partial；账户与管理页面已采用 1456/1376px 两级隐形网格、近黑主操作、无边框填充控件、发丝 Separator 与 Radix 覆盖层；完整下载历史数据态与 390px 跨路由矩阵仍待补齐。
- 部署配置：Pending；待方案 3 合并后的 Compose 解析结果；验收过程仍不因配置解析声称已部署。
