# 023 本地内容上传与剧本分析验收

<!-- acceptance: pending -->

- 状态：Pending，剧本文档真实浏览器上传、规范化预览与 Codex/Claude 受限执行链已完成，真实中英文模型质量 E2E 仍待完成
- 日期：2026-08-15
- 对应设计：`docs/design/023-本地内容上传与剧本分析设计.md`
- 对应需求/计划：`docs/prd/023-本地内容上传与剧本分析需求.md`、`docs/plans/023-本地内容上传与剧本分析计划.md`

## 1. 验收边界

验收分为上传基础设施、本地视频、剧本文档、Skill/供应链、剧本分析、剧本改写、安全、API/UI 和真实运行九个门禁。任一自动化或 fake Provider 成功不能替代真实 MP4、真实文本解析和中英双向模型 E2E。

本编号只验收浏览器本地上传；设备配对、Edge Import、微信视频号和红果继续由 `019` 独立验收。本地 MP4 成功不能提升任何 Provider canary。

### 1.1 已完成的前置证据

- [x] `ContentKind`、`DownloadSourceKind`、`AnalysisInputKind`、`AnalysisResultContract` 与稳定导入错误码已进入 Domain，视频/文档容量独立；Document 导入与剧本分析当前默认开启且仍可显式关闭。
- [x] `download_jobs` 已具备 `source_kind` 回填与远程/浏览器二选一 SQL CHECK；Analysis 已具备 `input_kind/document_id/result_contract` 回填和联合输入 SQL CHECK。
- [x] PostgreSQL 17 已验证“上一提交 schema + 旧数据 → 当前 schema → 再次应用”的幂等路径，并验证非法来源形态和双 Analysis 输入 fail closed。
- [x] 后端 Ruff/format/mypy 与 609 项全量测试通过（1 个 Windows 不适用项 skipped）；OpenAPI 客户端已重新生成。前端在隔离临时目录完成干净 `npm ci`、lint/typecheck、format、119 项测试和 production build；工作区因已有 Next.js dev server 锁定原生模块，根 `ci.py all` 的原地 `npm ci` 未通过，不能把“完整根门禁”勾为完成。

以上只证明 Phase A 契约，不证明 multipart、quarantine、Import Worker、Document 或剧本分析可用；下列产品验收项继续保持未勾选。

## 2. 上传基础设施

- [x] 浏览器 multipart 直传不经过 API 完整文件内存/磁盘中转。
- [x] 上传会话只允许 owner 对应资源、attempt 和确定 quarantine key，无法更换 bucket/key、扩大大小或延长权限。
- [x] quarantine 对公开下载、Analysis Worker 和其他 owner 不可读，短 lifecycle 生效。
- [x] complete 的对象 HEAD、资源状态和 `content.import.verify.requested` outbox 原子提交。
- [x] 过期会话、缺 part、错误 ETag、重复 complete、重复消息和并发 complete 状态稳定。
- [x] Worker 在下载、验证、晋升、数据库提交和清理前后崩溃均无重复最终资源或假成功。
- [x] 成功、失败、取消、过期和中断均有界 abort multipart 并清理 quarantine、part、最终孤儿和工作区。
- [x] 队列使用 quorum、publisher confirm、manual ack、DLQ、有界回灌、lease/heartbeat 和恢复扫描。

## 3. 本地视频上传与拉片

- [x] 有效本地 MP4 创建 `browser_import` download job，inspection/format 为空且 SQL CHECK 正确。
- [x] 服务端重算大小/SHA-256，并验证 BMFF、视频轨、正时长、codec、尺寸和配置上限。
- [x] 合法无声 MP4 可以通过；系统不转码、不修复、不改变原始字节。
- [x] 假扩展名、损坏容器、无视频轨、外部引用、多文件、playlist、超大小/时长和哈希不一致 fail closed。
- [x] 成功只生成一个现有 Artifact，来源显示“本地视频上传”，不写 Provider canary。
- [x] 下载历史可按本地文件名搜索并显示 Artifact 可用性；管理员统计以独立本地上传维度计数、计用户、计字节和时长，不归因给任何 Provider。
- [x] 真实桌面浏览器完成 create/session、CORS preflight、multipart PUT、complete、Import Worker、Artifact 晋升和下载详情跳转；390×844 上传入口无页面级横向溢出，最终浏览器 console/error 为空。
- [ ] 上传视频完成 `director-breakdown`：完整视频 Agent、结构化结果、PostgreSQL、MinIO MD/DOCX、WebSocket 和浏览器 E2E。
- [ ] Analysis 失败、取消或删除不改变 upload/download 成功和原始 Artifact 状态。
- [ ] 原 URL inspection、格式选择、下载、历史、取消、重试和五个视频 Skill 全量回归通过。

## 4. 剧本文档导入

- [x] 独立 Document/attempt/Artifact ORM、当前态 SQL、owner-scoped 幂等 repository 和 Analysis 生命周期锁已建立；Document 不创建 download job，真实 PostgreSQL schema 重放与并发同 key 创建通过。
- [x] Document create/list/get、upload-session、complete、cancel/delete 均使用严格 owner-scoped API、稳定 operationId 和独立速率策略；两阶段删除受活动任务锁和精确对象 key 白名单保护，OpenAPI 生成客户端与全量后端/前端/仓库门禁通过。
- [ ] DOCX、可提取文字 PDF、TXT、Markdown、Fountain 各有中/英文有效 fixture 并进入 ready。
- [x] Document 独立于 download job/video artifact，原始与 normalized 对象各自有稳定 SHA-256、状态和保留期；真实 PostgreSQL + MinIO Fountain 预检生成两份 Artifact 且清理余量为 0。
- [x] 同一文本原始字节重复规范化得到相同 UTF-8 文本 SHA-256、场景顺序和 scene ID。
- [x] 场景标题、动作、人物、括注和对白的中英文 fixture 解析符合人工真值。
- [ ] 无明确场景但有正文时产生单场景质量警告；空文本、乱码超限和不可恢复结构失败。
- [x] DOCX zip bomb、路径穿越、符号链接、加密容器、宏/嵌入内容、DTD/ENTITY、外部可加载资源关系、超多条目和超解压大小均被拒绝；解析器不解引用的标准超链接可正常导入；真实 DOCX 经 PostgreSQL + MinIO 进入 ready 并完成零余量清理。
- [x] 加密/附件/扫描/空文字/低质量 PDF 被拒绝，不自动 OCR、不执行脚本或外部资源。
- [x] TXT/Markdown/Fountain 的 NUL、控制字符、超长行、非法编码和超字符数被拒绝。
- [x] 文档详情只显示有界纯文本预览，不渲染上传 HTML/OOXML/PDF 活动内容。
- [ ] 文档不可原地替换；上传修订稿创建新的 Document 和 SHA-256。

有界预览验收证据（2026-08-14）：详情接口仅在 owner 校验和 ready 状态通过后，从绑定 bucket 与确定 key 的 normalized Artifact 做有界 range read；默认限制 32 KiB/12,000 字符，严格处理 UTF-8 截断并返回 `preview_truncated`，列表不读取正文。真实 PostgreSQL 17 + MinIO 预检 `17b114a0-adea-41f9-909c-d315c6e8d260` 证明跨 owner 在零对象读取时返回 404、授权请求只读取前 40 字节、HTML-like 文本不被后端解释，清理后数据库与对象余量均为 0。前端详情仅通过 React 文本节点和 `<pre>` 呈现预览；单元测试证明 `<script>` 形态内容不生成脚本节点，生产浏览器复验无执行、无产品 console/error。

## 5. Analysis 联合输入与生命周期

- [x] 现有 Analysis 数据幂等回填为 video；数据库拒绝双输入、无输入和 kind/外键不一致。
- [ ] 视频只使用 Artifact，screenplay 只使用 Document normalized SHA-256。
- [ ] Document owner、ready、expiry、deleted 和 hash 漂移检查 fail closed，跨 owner 返回 404。
- [ ] `analysis_document_locks` 在运行、可重试和报告发布期间阻止输入清理，终态后按策略释放。
- [ ] 视频/剧本共享 lease、heartbeat、retry wait、取消、原任务重试、删除、报告发布和 WebSocket 语义。
- [ ] 创建时 Skill 指令、指令 SHA-256、result contract、output language、custom prompt 和 input SHA-256 固定；配置变化不改变重试。
- [x] 视频与剧本 result 使用当前带 `kind` discriminator 的联合 OpenAPI，不存在 legacy 双轨 parser。
- [ ] Markdown 预览、Markdown 下载和 DOCX 内容来自同一 canonical Markdown。

联合输入持久化证据（2026-08-14）：Analysis repository 已按 `input_kind/result_contract` 分派视频 Artifact 与 screenplay Document，剧本路径在创建和手动重试事务中同时复核 owner、ready、deleted、Document/normalized 双过期时间及双 SHA-256，并只写 `analysis_document_locks`。真实 PostgreSQL 17 预检的锁计数按创建、终态失败、重试、取消收敛为 `1→0→1→0`，测试 Document/Analysis 和锁记录余量为 0；全量后端 787 passed、1 skipped。该切片当时尚未完成公开剧本 Analysis 与 Worker，后续执行和前端证据见本文件下文。

结果持久化证据（2026-08-14）：三类领域结果已使用固定 `kind` 严格序列化，未知 kind、缺失 kind、未知嵌套字段、悬空 scene evidence、重复/交错改写块和结果契约错配均 fail closed；既有视频 JSON 由当前态 SQL 幂等补齐。PostgreSQL 17 已从上一提交 schema 和无 kind 视频报告开始，验证当前 schema 连续应用两次后仅保留一个 kind CHECK，且缺失 kind 更新被拒绝。剧本分析与改写 canonical Markdown 已接入同一报告发布入口；该切片全量后端 798 passed、1 skipped。

公开联合结果证据（2026-08-14）：OpenAPI 已把 `AnalysisResponse.result` 定义为按 `kind` 判别的 `VideoAnalysisResultResponse | ScreenplayAnalysisResultResponse | ScreenplayRewriteResultResponse`；生成 TypeScript 保留三个 literal kind，未退化为 `any`，前端视频结果组件显式只接收视频分支。剧本改写公开响应不含 chunks 或 rewritten text，完整正文继续只来自 canonical Markdown。后端全量 800 passed、1 skipped；前端隔离门禁为 209 files、38 个测试文件/135 项测试、15 页静态构建和 production audit 0。该项只证明当前公开契约，不替代真实剧本创建/执行 E2E。

剧本 Analysis API 证据（2026-08-14）：创建与 latest 接口已按 owner、Document readiness、Document/normalized 双过期时间和双 SHA-256 fail closed；仅接受 screenplay Skill 与 `zh-CN/en-US`，创建快照和幂等指纹覆盖输入哈希、result contract、Skill 指令哈希、输出语言和 custom prompt。两个新 operationId 已进入 52-operation 生成客户端，通用 Analysis 查询/取消/重试/删除/导出保持复用。该切片当时的开关默认关闭，后续在受限执行链和入口齐备后已改为默认开启；后端 822 passed、1 skipped，前端 lint/typecheck、format、135 tests、production build 与 repository/Compose 门禁通过。

执行分派安全证据（2026-08-14）：视频执行已进入独立 `VideoAnalysisExecutor`，公共执行层按持久化 `input_kind` 精确选择执行器。当前未注册的 screenplay 与未知类型直接以 `analysis_cli_unsupported` 非重试失败；单元负例证明未调用视频 Artifact loader、视频 analyzer 或视频工作区。全量后端 823 passed、1 skipped。该证据只证明 fail-closed 分派，不代表受限剧本执行器已可用。

剧本输入工作区证据（2026-08-14）：Worker 的 normalized Document 投影会重新绑定 owner、document ID、输入哈希与过期时间；独立 materializer 只下载确定的 `screenplay.md`，并校验 bucket、key、容量、SHA-256、严格 UTF-8、NFC/LF、字符数和 scene range 后生成只读 `input/screenplay.md`。`ANALYSIS_MAX_SCREENPLAY_BYTES` 默认 2 MiB，失败和取消均清理精确工作区。全量后端 826 passed、1 skipped；该证据不包含模型调用或结果 E2E。

剧本单次分析执行证据（2026-08-14）：独立 screenplay executor 只接受 `screenplay-analysis`，从 Worker 重新校验的 normalized source 构造请求，并在 120,000 字符、120 场或 28,000 字节 CLI Schema 上限外于模型调用前以 `analysis_resource_limit` 终止。Claude CLI 固定 `--max-turns 1`、`--tools ""`，命令和 policy 双重拒绝文件、Shell、网络与 Agent 工具。该切片当时尚不支持 Codex 与 `screenplay-rewrite`，后续已补齐 Codex analysis/glossary/rewrite chunk 与完整改写编排。严格 schema 与领域 parser 要求全部源 scene ID 恰好一次且保持顺序，所有结构、人物、对白、优点和修改建议证据只能引用该白名单；未知字段、语言漂移、场景漂移和悬空证据负例均不发布结果。本证据仍不替代真实中英文 Provider 质量 E2E。

Codex 剧本执行证据（2026-08-15）：Codex adapter 已通过 screenplay analysis、glossary 和 rewrite chunk 三类结构化单元测试；命令参数固定禁止网络与 web search，只授予当前任务工作区最小读写权限，Prompt 经标准输入传递，结果必须通过既有严格 Schema 与领域 parser。

## 6. Skill 与供应链

- [x] `GET /api/analysis-skills?input_kind=video` 只返回五个视频 Skill；`screenplay` 只返回三个剧本 Skill。
- [x] 现有视频 Skill 已迁移到标准 frontmatter，视频分析结果无行为回归。
- [x] `screenplay-analysis`、`screenplay-structure-review`、`screenplay-rewrite` 的 ID、显示名、默认提示和顺序稳定。
- [x] Skill 只能绑定 allowlisted input kind 和 result contract，用户 Prompt/正文不能改变。
- [x] 重复 ID、非法名称、未知 metadata、绝对/穿越引用、符号链接、未知 contract 和输入类型均使服务启动/CI fail closed。
- [x] 外部 scripts、allowed-tools、MCP、插件和网络要求不会进入子进程。
- [x] 每个受 GitHub 启发的 Skill 有来源 URL、固定 commit、许可证/NOTICE 和本地修改记录。
- [x] 固定 commit 更新会触发重新审查、fixture 和真实 E2E，不存在运行时 clone/pull/install 路径。

## 7. 剧本分析

- [ ] 中文和英文完整剧本分别完成一次 `screenplay-analysis` 真实 Provider E2E。
- [ ] `screenplay-structure-review` 对人工植入的节拍、场景推进、转折、连续性和节奏问题给出可执行结论。
- [x] 结果包含 logline、synopsis、结构/转折/节奏、人物、场景、对白、优点和优先修改项。
- [x] 每个人物、场景、对白和修改建议只引用存在的源 scene ID，无悬空、重复或模型自造源场景。
- [ ] 未知字段、超长字符串、超大数组、非法 ID、悬空引用和超大 JSON 被拒绝且不保存部分结果。
- [x] 超过单次预算的 fixture 使用“场景级 → 全局摘要”父 Worker 编排，最终结论仍覆盖全部场景。
- [ ] fake CLI 覆盖正常、非法 JSON、Schema 失败、限流、额度、超时、取消和进程树清理。

## 8. 中英文改写

- [ ] 中文剧本完成中→英，英文剧本完成英→中，另有一次同语言润色真实 Provider E2E。
- [ ] glossary 覆盖主要人物名、地点、专有名词、称谓和保留术语，并通过 schema 校验。
- [x] 每个输出块携带正确 scene ID、part、源 SHA-256 和 target language。
- [x] 合并前 scene/chunk 覆盖率为 100%，无缺失、重复、顺序漂移和跨源哈希复用。
- [ ] 同一人物与保留术语在全部场景中遵循 glossary；冲突会阻止报告发布或返回明确质量失败。
- [x] 单场景超限时按动作/对白段落细分，合并后仍保持一个源场景的相对顺序。
- [x] 任一块失败时不发布部分改写；原任务重试保持同一输入、Skill、语言和 glossary 规则。
- [x] 可恢复的当前块失败只在同一 run/attempt 内有界重试；已验证块不重复调用，跨 attempt/run 不保存或复用临时模型输出。
- [x] 完整改写正文只确定性合并一次，公开 JSON 没有第二份全文，Markdown/DOCX 内容一致。
- [ ] 输出页面明确标注 AI 改写和目标语言，不宣称法律、行业或逐字翻译准确性。

确定性分块计划证据（2026-08-14）：父 Worker planner 已保证标题页前缀与全部场景按源顺序逐字符覆盖，超限场景优先在段落/行/空格边界拆分并保持场景内连续 part_no；每块 SHA-256 按实际 UTF-8 原文计算，合并 source chunks 必须精确等于 normalized 全文。覆盖缺口、尾部截断和块数超限负例全部 fail closed；全量后端 850 passed、1 skipped。该证据只覆盖模型调用前的 source plan，不代表 glossary、逐块改写、合并发布或真实 Provider E2E 已通过。

改写模型契约证据（2026-08-14）：glossary 输出已按任务源/目标语言、六类术语、唯一 source 与有界 style rules 严格解析；逐块输出同时由 Schema 和 parser 绑定 scene ID、part_no、源 SHA-256、目标语言，NFC/LF 改写正文保留原始空白。Fake Claude 证明 glossary 与 chunk 都使用单轮、空工具集，正文和相邻上下文只经 JSON 标记的标准输入传递，manifest 不写模型正文；缺少任一受限方法的 Provider 会返回 `analysis_cli_unsupported`。全量后端 865 passed、1 skipped。该切片当时尚未进入执行编排，后续覆盖与原子发布证据见下段。

改写执行编排证据（2026-08-14）：Worker 已通过契约路由把 `screenplay-rewrite` 交给独立 executor，先生成严格 glossary，再按确定性计划顺序处理全部块；结果发布前精确比较每个 `(scene ID, part, source SHA-256)`，验证场景/块覆盖与顺序，并按完整源文/输出的精确字符串计数阻断 glossary target 缺失。自动化负例证明第二块身份漂移、术语缺失、块数超限和累计输出超限都不会留下部分发布，且工作区始终清理；功能关闭时不装配执行器，开启时 analysis/rewrite 能力都必须在消费队列前预检。后端全量门禁为 872 passed、1 skipped。该确定性下限不能替代跨场景语义一致性审计；真实中→英、英→中和同语言 Claude E2E 尚未执行，因此相关质量项与整体验收继续保持未通过。

长剧本改写长度回归证据（2026-08-17）：改写 glossary 不再以完整规范化剧本的 120,000 字符作为一次性输入硬门槛，而是默认按 20,000 字符安全边界分段提取，再由服务端去重合并为唯一 glossary；长文本回归验证了分段请求不超过配置上限、拼接后逐字符等于原文，且仍按原有确定性 source chunk 顺序完成改写和原子发布。该证据证明应用层长度阈值已从“整本拒绝”改为“有界扩展调用”，不替代真实 Provider 对长剧本的上下文、耗时和翻译质量 E2E。

同 attempt chunk 恢复证据（2026-08-14）：当前块调用、严格 parser 与恢复循环共同运行在同一个 lease monitor 内；Provider 限流、CLI 超时/通用失败、CLI 无效结构和身份等 parser 校验失败可按配置有界重试当前块，之前已验证块不会重复调用。自动化证明持续身份漂移在耗尽 2 次调用后不发布部分结果，资源上限等不可恢复错误只调用一次；输出只存在本次 executor 局部内存，finally 继续清理 attempt 工作区，新 attempt/run 没有 checkpoint 可复用。后端全量门禁为 878 passed、1 skipped。

## 9. 安全、隐私与资源

- [ ] 剧本文本、metadata、文件名和 Prompt Injection 无法联网、读取 Home/仓库/相邻任务/Secret 或扩大工具权限。
- [x] 剧本执行器没有 FFmpeg、通用 Shell、MCP、插件、浏览器和 subagent；视频权限保持当前最小边界。
- [ ] CLI 子进程环境没有数据库、RabbitMQ、MinIO、签名或无关 Provider Secret。
- [ ] 上传/解析/分析的 CPU、内存、pids、磁盘、文件数、文本、chunk、输出和 wall-clock 上限均有故障注入证据。
- [ ] 取消、超时、lease 丢失、Agent shutdown 和 Worker 崩溃终止完整进程组并清理 workspace/chunks。
- [ ] API、WebSocket、RabbitMQ、数据库普通字段、日志、trace 和指标扫描无完整正文、Prompt、预签名 URL、模型原始响应、人物名和场景内容。
- [ ] 创建 AI 任务前披露剧本文本和必要上下文会发送到活动云端模型 Provider。
- [ ] 删除/过期清理 quarantine、原始文档、normalized 文本、chunks、result 和报告，不越过活动任务锁。

## 10. API、前端与可访问性

- [ ] 所有新公开操作 operationId 唯一稳定；创建资源返回 `201 + Location`；严格 schema 拒绝额外字段。
- [x] OpenAPI 生成前端服务无手工漂移，认证、幂等键、错误映射和重试入口一致。
- [x] 首页“链接解析 / 本地视频 / 剧本文档”可键盘切换，不破坏现有首页网格、格式选择和返回路径；视频与剧本选择文件后可直接上传，不出现独立权利勾选、重复说明或底部提示；桌面与移动导航均按“首页、下载记录、剧本文档、平台状态”排列。
- [ ] MP4 上传成功进入现有下载详情；剧本文档进入 `/documents/detail`，直接访问和站内返回均稳定。（剧本分支已验证，MP4 分支未在本轮复验）
- [ ] 文档列表/详情覆盖 loading、empty、uploading、verifying、ready、failed、cancelled、expired 和 retry。
- [x] 剧本分析/改写覆盖 Agent 离线、执行、分块进度、失败、取消、重试、成功、导出和删除。
- [x] 所有文本按纯文本或安全 Markdown 呈现，不执行上传 HTML、脚本、命令和外部链接指令。
- [ ] 1280px、1024px、390×844 无页面级横向溢出；长文件名、错误、glossary 和正文安全换行/局部滚动。
- [ ] 唯一 h1、顺序标题、label、按钮、焦点环、Escape/焦点恢复、44px 目标、对比度和 aria-live 满足 WCAG 2.2 AA。
- [ ] 明暗主题、上传进度、覆盖层滚动锁、导航固定槽位和 reduced motion 无视觉回归。

剧本前端闭环证据（2026-08-14）：ready 文档详情已接入 owner-scoped latest/create，并复用通用 Analysis WebSocket、降级轮询、取消、同 ID 重试、删除和报告有效性判断。三个 screenplay Skill、目标语言与 4,000 字符补充要求可访问；界面在提交前披露规范化剧本、任务指令、glossary/有界上下文会发送到活动云端模型，并明确受限执行器无文件、Shell、网络、浏览器、插件或其他 Agent 权限。分析视图提供结构/人物/场景/对白/修改建议及 scene evidence，改写视图提供场景统计、语义 glossary 表、摘要和唯一 canonical Markdown 正文；资源超限与不完整输出使用剧本专用中文文案，不暗示已有部分报告。隔离前端门禁为 218 files、39 个测试文件/140 项测试、15 页 production build，官方 registry production audit 0。1280×900 与 390×844 fixture 浏览器冒烟证明两类结果没有页面级横向溢出，分析“场景”和改写“改写正文”页签可按语义角色切换，浏览器 error 为空；该证据不替代真实 owner/API 流程、1024px、完整键盘与屏幕阅读器复验，因此响应式与完整 WCAG 条目保持未通过。

剧本文档真实入口证据（2026-08-15）：使用新建 owner 从 `/documents` 打开“上传剧本”，选择 Markdown 后直接通过 Homebrew MinIO multipart 直传，不经过额外确认步骤。页面立即跳转 `/documents/detail?documentId=...`，Import Worker 收敛后显示“可以核对”、规范化纯文本预览、Markdown 格式、中英混合语言、1 个退化场景、7,786 字符和质量警告，同页剧本 Skill 选择与“开始剧本分析”入口可用。本轮未点击分析，因此不将该证据等同于真实 Provider 质量 E2E。

超限英文剧本真实分析证据（2026-08-15）：一份明确授权的英文 DOCX 经 Homebrew MinIO 导入后得到 139,724 字符、129 个连续源场景，超过 120,000 字符和 120 场单次预算。可信 Worker 以 115/14 场景两块完成严格分析，再用仅含已验证 chunk JSON 的汇总调用生成全局结论，最终发布简体中文 Markdown/DOCX 报告。PostgreSQL 核对源场景、结果场景和唯一结果场景均为 129，顺序差异与非规范结果 ID 均为 0；浏览器显示“已完成”、129 源场景及结构、人物、场景、对白、修改建议和完整报告视图。该证据满足英文超限分析及结果结构/引用验收，不代表中文剧本分析或任何改写方向已完成。

## 11. 工程与运行门禁

- [x] `cd backend && uv run ruff check app tests`
- [x] `cd backend && uv run mypy app`
- [x] `cd backend && uv run pytest`
- [x] `cd frontend && npm run lint`
- [x] `cd frontend && npm test`
- [x] `cd frontend && npm run build`
- [x] `cd frontend && npm run openapi:check`
- [ ] 按 `.github/workflows/ci.yml` 直接执行完整代码级门禁
- [x] 空 PostgreSQL 数据卷和已有当前态数据卷幂等执行 `schema.sql`。
- [x] 运行时、开发依赖和持久化测试只支持 PostgreSQL；测试使用随机隔离 schema，CI 提供 PostgreSQL 17。
- [x] 环境 Compose、业务 Compose、production Compose、MinIO ACL/lifecycle 和 RabbitMQ 拓扑通过运行边界烟测。
- [x] Compose 不重复展开应用已有的上传预算、TTL/GC、租约、批量和 verifier 默认值；公共应用/Runner 骨架收敛且镜像可构建。
- [ ] main 推送后的 GitHub Required CI 全绿。
- [ ] `design-qa.md` 更新且 final result 为 passed，无剩余 P0/P1/P2 差异。

PostgreSQL/容器瘦身证据（2026-08-14）：配置与 engine 对非 `postgresql+asyncpg` URL fail closed，`aiosqlite` 已从依赖和锁文件删除，Repository/集成测试在 PostgreSQL 17 每例独立 `test_*` schema 运行且不回退到 `public`；readiness 同样按当前 schema 验证表集合。后端门禁为 `881 passed, 1 skipped`，Ruff/format/mypy 全绿。基础 Compose `652→517` 行，生产覆盖 `189→170` 行；三种配置解析、Runner anchor 展开、runtime 镜像构建和镜像内无 SQLite driver 检查通过。该证据不改变真实 Provider E2E、Prompt Injection、安全资源预算、浏览器和 Required CI 的未完成状态。

## 12. 真实证据记录模板

```text
feature flags / app commit / CI run:
authorized MP4 / rights basis / size / sha256 / ffprobe summary:
media import / download / artifact / analysis / report / WebSocket:
authorized Chinese screenplay / format / bytes / normalized sha256 / scenes:
authorized English screenplay / format / bytes / normalized sha256 / scenes:
analysis jobs / Provider / model / reports:
zh→en / en→zh / same-language rewrite jobs:
scene and chunk coverage / glossary audit / human format review:
negative document and Prompt Injection fixtures:
multipart / crash recovery / orphan cleanup evidence:
desktop / tablet / 390px / keyboard / accessibility evidence:
secret and content leakage scan:
SBOM / licenses / Skill source commits / NOTICE:
```

记录不得包含未公开剧本正文、完整 Prompt、预签名 URL、Provider Secret、用户身份或本机认证路径。

## 13. 当前结论

当前仓库已完成 Phase A 当前态契约、公共上传/quarantine/可靠消息、浏览器 MP4 API/UI、Import Worker、可信视频校验、Artifact 原子晋升及 history/analytics 本地来源投影，并以真实 PostgreSQL、MinIO、RabbitMQ、ffmpeg/ffprobe 和桌面/移动浏览器证明“本地 MP4 → Artifact → 现有详情与分析入口”闭环。Document 聚合、五类文档安全 verifier、确定性规范化、场景结构、原始/normalized Artifact、公开控制面、有界预览和 `/documents` 列表/详情/上传入口已经建立；三个剧本 Skill、Codex/Claude 受限分析与改写调用、确定性分块、glossary/chunk 契约、原子执行编排、同 attempt chunk 恢复及剧本配置/状态/两类结果前端也已落地。真实英文超限 DOCX 已通过 Homebrew MinIO 导入、分块分析、受控汇总、严格合并与报告发布闭环。尚未完成的是中文真实 Provider 分析、三类改写质量 E2E、glossary 人工质量审计及完整桌面/移动浏览器证明。本验收保持 `pending`；不得宣称本地视频拉片分析或剧本中英文改写已经完整验收，也不得归档 `023` 四件套。
