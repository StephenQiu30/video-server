# 020 用户制品持久化与保留策略验收

- 状态：Accepted
- 日期：2026-08-13
- 关联 Plan：`docs/plans/archive/020-用户制品持久化与保留策略计划.md`

## 验收清单

- [x] AC-020-01：视频与报告默认保留 7 天。
- [x] AC-020-02：30 天配置可生效。
- [x] AC-020-03：短时 URL 与对象保留期解耦。
- [x] AC-020-04：详情与历史显示实际到期状态。
- [x] AC-020-05：旧一天对象幂等延长且不覆盖长策略。
- [x] AC-020-06：GC 与分析锁保护无回归。
- [x] AC-020-07：Compose 配置进入正确运行角色。

## 验证记录

- `python3 scripts/ci.py all` 通过：仓库/Compose 配置、Ruff、格式、严格 mypy、581 项后端测试、前端 lint/typecheck/格式、115 项前端测试、生产构建与生产依赖审计全部通过。
- 隔离 PostgreSQL 17 对 `backend/sql/schema.sql` 连续应用三次均成功；旧一天对象变为 `604800` 秒，已有 30 天对象保持 `2592000` 秒。
- 开发与生产 Compose 解析后均确认：API 的短时 URL 为 `300` 秒，Download Worker 的对象保留为 `604800` 秒，Report Worker 的报告保留为 `604800` 秒。
- 配置测试确认默认 7 天、显式 30 天均合法，一天配置会 fail fast；发布测试确认报告按 7 天写入，下载执行测试确认视频按 7 天写入。
- 下载详情与历史回归测试确认继续展示 API 返回的实际到期时间，过期状态和重新下载入口无回归。
- `agent-browser` 对本地完整 Compose 执行真实链接验收：2,848,148 字节 MP4 下载、完整性校验、上传 MinIO 与详情页刷新后持久化均通过；页面显示“保留至 2026年8月20日”，数据库确认 `expires_at - created_at = 604800` 秒，MinIO 对象仍存在。
- 运行回归确认 API、Download Worker 与 Media Runner 均使用共享 `/work`；Vimeo DRM 样例直接返回 `drm_protected`/422，不再误报 `worker_lost` 或触发无效重试。

## 最终结论

`final result: passed`
