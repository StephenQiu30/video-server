# Acceptance 003 异步任务与数据持久化

## 状态与冻结输入

- 状态：Blocked（2026-07-20）；迁移/状态单元与真实依赖健康已执行，重投递并发/逐依赖恢复证据缺失。
- Design：[003 异步任务与数据库](../design/003-异步任务与数据存储设计.md)。
- PRD：[003 异步任务与数据持久化需求](../prd/003-异步任务与数据持久化需求.md)。
- Plan：[003 异步任务与数据持久化计划](../plans/003-异步任务与数据持久化计划.md)。
- 冻结规则：实现开始后只填写实际证据、结果和结论；标准变化必须先重新确认上游文档。

## 1. 阶段开始门禁

- Acceptance 001 已 Accepted，Plan 003 仍为 Ready，用户已明确授权本阶段。
- PostgreSQL、RabbitMQ、MinIO 与 Worker 真实环境可停启；四表、状态机和消息契约仍与 Design 一致。
- 当前 branch、基线 commit、已有改动和 `S003-T01` 空库迁移 Red 已记录。
- 任一项不满足时本阶段为 `blocked`，不得开始 S003 或 S002。

## 2. 逐任务验收

| 任务 | 必须通过 | 最低证据 |
| --- | --- | --- |
| `S003-T01` | 空库/重复 upgrade 可重复，只有四张业务表且约束/索引准确 | migration、schema/索引/约束快照 |
| `S003-T02` | 状态迁移矩阵和乐观并发只以 PostgreSQL 为事实 | 单元测试、并发事务记录 |
| `S003-T03` | 会话组合 FK、幂等键和唯一 artifact 在并发下成立 | 冲突/重放并发测试 |
| `S003-T04` | RabbitMQ exchange/queue/routing、publisher confirm 与消息体准确 | broker 声明、消息捕获 |
| `S003-T05` | 重复/终态/重投递消息幂等，成功后 manual ack | 投递计数、ack 与行锁证据 |
| `S003-T06` | 发布窗口、heartbeat、到期对象和墓碑能由 housekeeping 收敛 | 可控时间与故障注入 |
| `S003-T07` | PostgreSQL/RabbitMQ/MinIO/Worker 重启后可恢复且不重复完成 | 逐依赖重启与恢复记录 |

当前任务未 `passed`，下一任务不得开始。

## 3. DAC/AC 验收矩阵

| 标准 | 通过条件与证据 |
| --- | --- |
| `S003-DAC-01` / `S003-AC-01` | 空库/重复迁移与 schema 快照一致 |
| `S003-DAC-02` / `S003-AC-02` | 状态和并发事务矩阵全部通过 |
| `S003-DAC-03` / `S003-AC-03` | RabbitMQ 拓扑、消息体和配置准确 |
| `S003-DAC-04` / `S003-AC-04` | 崩溃窗口、重投递和 heartbeat 收敛准确 |
| `S003-DAC-05` / `S003-AC-05` | 无 Celery、Result Backend 或第二状态事实 |
| `S003-DAC-06` / `S003-AC-06` | URL、对象和元数据按冻结边界到期清理 |
| `S003-DAC-07` / `S003-AC-07` | 四个真实单元逐项重启后恢复 |

## 4. 阶段验证

- 对新空库重复执行 `uv run alembic upgrade head` 并导出 schema/索引/约束。
- `uv run pytest tests/unit/state tests/unit/repositories -q`。
- `uv run pytest tests/integration/postgres tests/integration/rabbitmq tests/integration/recovery -q`。
- 捕获 RabbitMQ 声明和消息，逐项重启 PostgreSQL、RabbitMQ、MinIO、Worker。
- 完整覆盖率、Ruff、格式与 mypy 门禁全部通过。

## 4.1 实际执行证据与结论

- `docker compose ... run --rm api uv run alembic upgrade head` 重复执行退出码 0；PostgreSQL 中确认 `alembic_version` 及四张业务表 `media_sources`、`media_formats`、`download_jobs`、`artifacts`。
- `tests/unit/state` 与仓储相关测试、全量 `uv run --frozen pytest -q` 退出码 0（`43 passed`）；RabbitMQ `rabbitmq-diagnostics -q ping` 成功，MinIO health 200，API readiness 200。
- RabbitMQ 消息捕获、manual ack/重投递、并发事务、崩溃窗口及 PostgreSQL/RabbitMQ/MinIO/Worker 逐项重启恢复未形成可复核集成测试/记录，故 `S003-T04` 至 `S003-T07` 的强制真实证据为 `blocked`，阶段结论为 `Blocked`。

## 5. 证据与结论填写规则

执行后逐项填写命令、退出码、证据、结果和复核人。全部任务及 S003 DAC/AC 为 `passed` 才能改为 `Accepted` 并允许开始 S002；任一失败为 `Rejected`，依赖或证据缺失为 `Blocked`。
