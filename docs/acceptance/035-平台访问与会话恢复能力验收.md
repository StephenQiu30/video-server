# 035 平台访问与会话恢复能力验收

- 状态：基础修复、策略/诊断与持久冷却已实现；平台 G1–G3/G5 尚未完成，不宣称 035 完整通过。
- 更新日期：2026-09-15；各次实验保留原日期。
- 关联：[Design](../design/035-平台访问与会话恢复能力设计.md) · [PRD](../prd/035-平台访问与会话恢复能力需求.md) · [Plan](../plans/035-平台访问与会话恢复能力计划.md)。

## 1. 当前裁决

| 门 | 状态 | 原因 |
| --- | --- | --- |
| G0 设计评审 | 已获推进指令 | 用户明确要求按计划开始全面实施；未据此推断平台实测通过 |
| G1 根因区分 | 部分执行，未通过完整门禁 | YouTube 同 Runner 引擎/应用 metadata 对照均未取得格式；完整 Worker/Artifact 对照未完成 |
| G2 重启与新机 | 部分执行，未通过 | 单一样本三次同机 Runner 重建通过；未完成全栈或干净目标机器交付 |
| G3 自动来源 | 尚未整体通过 | 元宝专用持久来源有原机证据；普通平台访客/账号维护、主体核验和撤销仍待逐平台证明 |
| G4 实现批准 | 已获得，条件门禁保留 | 已实施证据明确的代码质量切片；自动来源仍需 G3 Go |
| G5 发布验收 | 未完成 | 生产已有多项修复及原 Chrome 局部验证；手机、新机和全平台完整矩阵尚未通过 |

## 2. 必须通过的行为测试

下列项目都是验收规格，不是已通过的测试报告。

| ID | 场景 | 必须观察到的结果 | 对应需求 | 状态 |
| --- | --- | --- | --- | --- |
| T01 | 已配置账号路径，但默认 public | 只调匿名 Runner；账号不可用不干扰 public | R01/R02 | 未执行 |
| T02 | 显式策略及幂等重放 | 只调被授权策略；改变策略与同幂等键冲突，不能返回旧模式结果 | R01/R02 | 未执行 |
| T03 | 未知策略/缺 endpoint/跨 Provider redirect | 无越权外部调用；缺依赖准确报错，跨平台重新 admission | R01 | 未执行 |
| T04 | 访客来源含账号字段；private/DRM/未知权益；跨域跳转 | 拒绝、无 Artifact、无 Cookie/私有头泄漏；032 例外不扩到其他平台 | R02/R07 | 未执行 |
| T05 | 有效只读来源同机重建与不同目录 | 同修订读取成功；坏权限、符号/硬链接、跨域、非法格式均拒绝 | R03 | 未执行 |
| T06 | 清空可丢弃缓存、证明过期/服务不可用 | 同 binding 安全重建；一次修复上限；不能当作账号过期 | R04 | 未执行 |
| T07 | 两个维护者/两个操作竞争一个来源 | 只有一名持有者；超时不发新副本，不产生第二写者 | R05 | 未执行 |
| T08 | 发布更新在写/rename/fsync/通知阶段崩溃 | 重启得到完整旧版或完整新版；未提交/过期写者不能发布 | R05 | 未执行 |
| T09 | 更新/撤销/账号替换与领取并发 | 原子期望修订校验；新主体 epoch；旧 context 拒绝，不静默重绑 | R05/R06 | 未执行 |
| T10 | 运行中撤销、网络中断、旧来源备份恢复 | 新租约立即拒绝；目标 60 秒内终止旧操作；无法证明备份新鲜度时重新授权/登记，不能仅靠上游验证复活旧授权 | R05 | 未执行 |
| T11 | 来源/客户端/出口/引擎变化；音视频分流 | context 不一致拒绝；相同绑定下请求一致；过期媒体地址不复用 | R06/R07 | 未执行 |
| T12 | 3 次冷启动 + 新机器无旧缓存恢复 | 同样本解析、下载、制品获取全部成功；不修改解析器或重导有效来源 | R03/R11 | 部分：三次 Runner 重建；全栈/新机未完成 |
| T13 | Worker/Runner 停止、租约回收、重复消息 | 既有 fencing 生效；一个业务成功，不重复扣配额/发布 Artifact | R08 | 未执行 |
| T14 | 429/repair 后重启、人工重试与删除 | 同一 job 的 retry_at/repair 预算跨重启保留；新 job 有新预算但不能绕过路径 cooldown，删任务也不解除冷却 | R08 | 冷却数据库子场景通过；真实进程/平台整项未完成 |
| T15 | 完整视频、缺分段、缺轨、试看、损坏 ZIP | 只交付完整合法制品；ffprobe/时长/轨道校验；部分结果失败 | R09 | 未执行 |
| T16 | Web、390px 与真实手机恢复交互 | 相同策略/错误/重新解析语义；仅取本部署 Artifact；Secret 不进客户端 | R10 | 未执行 |
| T17 | 同出口换机、另出口迁移、旧 Artifact | 分开报告变量；旧机停写；历史文件可取；新地址真机验证 | R11 | 未执行 |
| T18 | 候选引擎发布、回滚、旧任务 | 候选有同样本证据；启动不在线升级；旧 context 不放宽 | R12 | 未执行 |
| T19 | 单条成功、样本删除、证据过期、目录自定义 | 不推导全平台/全部形态成功；stale 不冒充失败；目录不能扩大能力 | R13 | 未执行 |
| T20 | 缺实验/实现批准、任一 No-Go | 保持未实施，不更新支持声明；逐平台保留裁决 | R14 | 未执行 |
| T21 | 实现切片及复杂度评审 | 关联代码质量 ID，提供保留/修改/删除/不引入清单；相关问题有修复或明确延期裁决，关闭有回归和 commit，新结构无必要性则不引入 | R15 | 首批已执行，完整计划未完成 |

所有安全负例要求业务日志、异常、trace、诊断包、命令行和 API 响应中均没有原始 URL 中的敏感参数、Cookie、token、账号和私有出口值。测试仅用合成凭据；真实材料不提交。

## 3. 全目录覆盖和初始策略候选

以下“先前观察”来自 2026-09-12 固定 Runner 探针，不是 035 验收；不覆盖 API/Worker/MinIO/客户端全链路，也不证明新机可用。默认路由的最新规则见第 13 节；默认 public 是尝试方式，不是可用性验收。

| 平台 | 初始策略候选 | 先前观察 / 本次设计的特殊门禁 |
| --- | --- | --- |
| YouTube | public + 按批准的 operator_public | 既有 Operator 不可达；POT/引擎/出口冷启动分别验证 |
| 哔哩哔哩 | public | 单条 media 成功；不推导会员、课程或所有合集可用 |
| 抖音 | public；public_session 待 G3 | 既有 Operator 不可达；不能直接引入签名/浏览器模拟方案 |
| TikTok | public | 单条 media 成功；保持既有第一方 Player 路径 |
| 小红书 | public；public_session 待 G3 | 既有 Operator 不可达；分享 token 与画质/媒体类型分别测，不生成分享 token |
| 快手 | public | 单条 media 成功；公开分享页边界不变 |
| Vimeo | public | 单条 media 成功；密码/private 不开放 |
| X | public 候选；operator_public | 既有 Operator 不可达；不能从账号成功推导 public 可用 |
| Instagram | public 候选；operator_public | 既有 Operator 不可达；图集/合集必须独立验收 |
| Facebook | public 候选；operator_public | 既有 Operator 不可达；公开 Reel 与其他帖子分别记录 |
| Twitch | public | clip/VOD 样本成功；live 不扩展 |
| Reddit | public 候选；operator_public | 既有 Operator 不可达；私密/quarantine 不开放 |
| Pinterest | public 候选；operator_public | 既有 Operator 不可达；video pin 与图片不混算 |
| 微博 | public | 单条 media 成功；短链与 canonical 分开测 |
| 优酷 | public；032 personal_entitled | 普通样本成功，VIP 未验收 |
| 腾讯视频 | public；032 personal_entitled | metadata 成功；长视频 media 未完成，不能记成已证实平台失败/通过 |
| 微信公众号文章 | discovery → 用户选择 → 对应媒体策略 | 非直接 Runner 固定矩阵；文章发现与原生视频交付单独验证 |
| 微信视频号 | 现有专项路径，不承诺新来源 | 无桌面动态来源 No-Go；不接受普通文件冒充完整动态会话 |
| Snapchat | public | Spotlight 单条 media 成功 |
| LinkedIn | public | 公开单条 media 成功；账号内容不扩展 |
| Telegram | public | 公开频道帖单条 media 成功；私密频道不扩展 |
| Kick | public | clip 样本 media 成功；live 不扩展 |
| Tumblr | public | 单条 media 成功 |
| 红果官方分享 | public | 首轮格式失败、同样本重测成功；偶发性保留记录 |

Generic 与批准实例型路由也必须验证无凭据、重定向与 SSRF 边界，但不因出现在枚举中增加“已支持平台”数量。未来 Registry/目录变化必须同步此表；平台 key 与 capability 双向覆盖检查不能只核对固定列表长度。

每个宣称支持的 `single_video/short_video/clip_or_vod/audio_video_split/subtitles/image_or_carousel` 需对应样本/结果；只有单视频探针时，其他能力明确“未验证”。发现 live/playlist 枚举不能视为产品开放。

## 4. 证据模板

每条实验记录至少包含以下内容，未取得结果的字段留“未执行”，不得填推测值：

```text
experiment_id / requirement_ids / executed_at_utc
provider_key / profile_version / access_policy_id / capability
sample_target_id + version / sample_authorization_reference
host_role + OS + architecture / image_digest / engine_bundle
source_kind / opaque_revision / egress_generation (不含真实账号/IP)
control_case / changed_variable / attempt_budget / timeout_budget
inspection_outcome / first_byte_ms / media_outcome / artifact_outcome
bytes / media_duration / codecs / ffprobe_result / checksum
restart_or_restore_steps / old_host_write_fenced / persisted_budget
Web_result / mobile_device_result / screenshot_or_video_reference
stable_error / error_stage / manual_cancellation / cleanup_result
decision: Go | Conditional | No-Go | Inconclusive
reviewer / unresolved_items / approved_next_scope
```

选择样本后不能为求通过静默换 URL/账号/格式；样本删除或权益变化只能升级版本并保留原因。长样本预算必须在执行前声明，本轮腾讯的主动取消不记为上游失败。正式实验数量与存储/流量成本先批准；23 个媒体样本 × 3 个冷启动循环已是 69 次下载，不能把该数量隐含在普通健康检查中。

## 5. Go/No-Go 规则

- 所有选定平台：安全负例零越界，3 个冷启动循环均完成完整制品，目标机器至少一次恢复成功，真实客户端获取结果明确。
- G1 只有直接引擎成功而应用失败才支持“改应用链路”；两者都失败先定位平台/引擎/来源，不先重构架构。
- G3 自动访客来源必须证明无账号材料和正常第一方建立流程；账号来源必须证明同主体更新与撤销。材料、接口或证据缺失就 No-Go，继续 manual_file/用户重新授权。
- 一项可自动恢复不代表所有来源可自动恢复；用户可批准部分平台实现，但不得据此关闭其他平台的缺口。
- 账号/权益/DRM/地域等限制维持原边界。任何“只有绕过限制才能成功”的实验结果都不能作为 Go。
- 单测、源码审查、第三方 README 和官方 issue 修复记录都是证据输入，不能单独通过 G2/G3/G5。

## 6. 本轮文档交付验证

初版静态检查（2026-09-12）：6 个文档、122 个相对链接均存在，14 个需求与 20 个行为测试 ID/引用合法，平台表含 24 个目录项；未发现尾随空白或缺失末尾换行。追加代码质量要求后，规格为 15 个需求、21 项验收，全部尚未执行；文档表完整不证明 Registry/capability 的真实双向覆盖。

035 初版仅文档与修改范围检查；初次代码审查的 20 项既有 Web 测试不作为修复证据。2026-09-12 首批实施结果追加到 [代码质量登记](../code-quality.md)：后端、Web、App 回归和候选构建、合成浏览器恢复交互。它们不替代 T01–T20 的平台实验；本轮未执行平台探针、重启、新机器、手机安装或真实会话更新，不回填其他任务的测试次数。

## 7. 第二批候选实现证据

- I1 与 I4 策略/诊断已有实现。T01/T02 的路由与幂等负例、T03 的未准入/未配置拒绝、Web 策略变化重解析通过自动化测试；不能据此关闭含真实客户端或跨平台重定向的整项验收。
- 原部署配置的九个 operator 端点在现有 API 容器内均 DNS 不可达；候选核心 readiness 正常。没有输出端点、凭据或私人地址，没有修改真实 `.env.prod`。
- E01 有界 metadata 对照：固定 `youtube-me-at-the-zoo` 样本（jNQXAC9IVRw），现有 media-runner、同固定引擎 commit `3a08beaf031ab68f966401ead017ac81fe8486cf`、公开无会话、同出口。应用 Router→Runner 5.272 秒返回 `MediaInspectionVerificationFailed`；同容器直接引擎 3.139 秒退出 0，但 formats=0、duration=null、login_required=true。退出 0 不是下载成功，未产生 Artifact；尚未完成 E01 的全业务链与 E02。
- 状态缓存测试覆盖 200 个读取者、真实等待中的竞争、刷新取消、失效重取与错误恢复；只证明进程内受控行为，不是跨进程数据库压力或生产延迟结果。
- agent-browser 候选窗口已到实际登录页；复用本地账号会话仍显示未登录，已请用户自行登录，不提取或代填密码。
- 本批最终确定性回归：后端 1709 passed / 2 skipped（隔离 MinIO 未提供、macOS 无 Linux O_PATH）；Ruff/mypy 531 文件通过。Web 62 文件/268 测试及 lint/typecheck/format/生产构建通过；App analyze 与 201 测试通过。两个业务 Compose 静态解析通过。review-agent 只读审查无可确认新业务缺陷；仍缺登录后的真实业务链、冷启动、设备与多机证据。

## 8. 持久冷却与审查修复证据

- 当前后端 1735 passed / 2 skipped，Ruff/format 与 mypy 534 源文件通过；跳过原因同上。Web 62 文件/268 测试、lint/typecheck/format 与生产构建通过。App analyze、202 测试、Android debug APK 与 iOS simulator 构建通过；Kotlin 插件和部分媒体插件 Swift Package Manager 警告仍存在，构建不等于真机安装/播放。
- 已有 PostgreSQL 隔离 schema 中验证：空结构/重复 SQL 保留冷却、独立 Repository 重建、20 个竞争者仅一个半开租约、旧结果不能清新限流、关闭后 version 单调、过期崩溃租约恢复、删任务/新任务不清冷却、领取前拒绝不增 attempt、领取后 429 保留 retry_at。
- 本地有界准入测量：20 并发/200 请求全部为预期冷却拒绝，p50=8.24ms、p95=14.20ms、max=18.75ms；这是数据库准入路径，不是全 HTTP、下载吞吐或多机容量。持锁故障在 1 秒 lock_timeout 内失败关闭，SQL statement_timeout=3 秒。没有第三方压力测试。
- review-agent 初审发现 CQ-010/CQ-011 两项 P1；回归先失败，再修复剩余租约预算、签名截止传递和冻结 context 访问前校验。补充临期不发请求、截止取消 Runner 工作/清理 workspace、签名 HTTP 传递测试；复核 `No findings`，不表示无任何潜在缺陷。
- T14 的持久冷却/删除/attempt 子场景已通过确定性数据库测试；尚未用真实平台执行停进程/重投/最终制品整项测试。未准入自动修复，所以不添加 repair_count，T06–T10 不因此完成。
- 部署前只读核对：47 个下载均终态（44 成功、1 失败、2 取消）、导入 0、12 个分析均终态；项目数据库当时尚无新冷却表。原部署未重启；后续部署结果另记，不以挂载源码后的旧进程代表新实现已运行。

## 9. 同版镜像与固定矩阵（2026-09-13）

项目数据库仅应用当前 schema 中新增的 provider_route_cooldowns 建表语句，未重写历史任务。候选 API/Runner/前端复用已有基础设施；独立镜像 `video-server:035-validated`（候选标签，不表示发布通过）构建自 `4f50f4d5`，镜像 ID `sha256:e94f0908c5834829a6a2a569c7b5fe89172c5f2363e2915094462b8f5d03b421`。三个镜像进程均健康且没有源码挂载。原 `video-server:prod`、生产容器和 Tailscale Serve 映射未替换；原 tailnet API readiness 200。候选 API 一次进程重启后 readiness 恢复 200。

候选 API 的本机 `/health/ready` HTTP 基线：20 并发、200 请求全部 200，p50=40.70ms、p95=87.04ms、max=101.75ms。仅覆盖 readiness 及其依赖检查，不代表解析/下载高并发吞吐或多机容灾。最终后端全量 1738 passed / 2 skipped；新增矩阵选项校验和构建上下文排除回归通过。

矩阵窗口 UTC `2026-09-12T15:53:16Z` 至 `16:08:24Z`，按现有 fixed_public_cases 串行执行 23 平台 × metadata/media 共 46 次，结果保存在正常 provider_canary_results。使用同版源码候选 Runner、固定引擎、相同出口、默认首项格式，metadata 30 秒、单次下载 120 秒、输出 256 MiB、工作空间 512 MiB；调用端预算为 35/125 秒。结果为 24 次成功、18 次依赖缺失、4 次媒体预算未完成；全矩阵未通过。随后镜像重建复测单列，不混入此统计。

| 平台 | metadata | media（耗时含重新解析） |
| --- | --- | --- |
| 哔哩哔哩 | 成功 | 成功，25.567s |
| TikTok | 成功 | 成功，21.939s |
| 快手 | 成功 | 成功，6.244s |
| Twitch | 成功 | 成功，68.969s |
| 微博 | 成功 | 成功，20.151s |
| Snapchat | 成功 | 成功，11.241s |
| LinkedIn | 成功 | 成功，43.431s |
| Telegram | 成功 | 成功，41.960s |
| Tumblr | 成功 | 成功，69.798s |
| 红果官方分享 | 成功 | 成功，14.461s |
| Vimeo | 成功 | download_timeout，125.125s |
| 优酷 public | 成功 | output_limit_exceeded，114.712s |
| 腾讯视频 public | 成功 | download_timeout，131.166s |
| Kick | 成功 | download_timeout，133.019s |
| YouTube、抖音、小红书、X、Instagram、Facebook、Reddit、Pinterest、微信视频号 | runner_unavailable | runner_unavailable |

上述九个受控端点对应的 profile 当前未启用；生产 Compose 十个 manual_file 平台的来源文件均未准备，视频号另需动态来源。未读取真实 Cookie 内容，也未自动启用空 profile。微信公众号文章不在 Runner 矩阵，发现/选择/原生媒体链仍未验收。每个平台只有固定单项样本，不能扩展为全部视频、全部能力或会员内容通过。

agent-browser：无模拟响应的公开页/登录页与镜像公开页检查正常；真实账号未登录，所以未执行用户 API→队列→MinIO→客户端交付。单独合成夹具会话验证策略选项、禁用未配置项、冷却提示、管理员按需诊断、390px 和深浅主题后关闭；不是实际管理员业务。所测页面 axe 无 violations；一次 OKLCH 对比度 incomplete 经浏览器颜色归一测得按钮 15.27:1，后续检查无 incomplete。截图不包含账号或来源内容。

iPhone 16 可识别但 unavailable，未安装新版；新机器未指定。G1 完整应用/引擎对照、G2 新机、G3 自动来源、G5 真机/对象交付仍不通过，不把以上结果写为 035 完整落地或多机 HA 完成。

## 10. 镜像 Runner 三次重建复测

每轮停止并移除本轮自建的 media-runner-035-image 容器，再从相同镜像新建；无源码挂载，容器临时层/tmpfs 重新建立，既有共享 /work 和基础设施保留。使用既有 fixed_matrix 命令、同一 bilibili-public-single 样本与首项格式，三轮各两项均返回 matrix_complete=true：

| 轮次 | metadata | media |
| --- | --- | --- |
| 1 | 2.628s | 38.164s |
| 2 | 2.422s | 25.662s |
| 3 | 2.462s | 33.613s |

没有更换代码、Cookie、样本或降低清晰度；仍是 Runner/Canary 结果，不包含 API 身份、RabbitMQ 下载任务、MinIO 持久制品获取或手机保存。数据库业务下载仍为 44 succeeded / 1 failed / 2 cancelled，路线冷却表 0 行；没有用人工写入真实路线来演示 429。

旧源码挂载候选及只用于 OpenAPI 的本机进程已停止，移除的仅是本轮临时容器，原镜像和业务卷保留。当前镜像候选 Web=127.0.0.1:8137、API=127.0.0.1:8139；按用户 Tailscale 测试要求额外配置 tailnet HTTPS 8445 转发候选 API，健康检查 200，原 443/8111/8443 映射保持。仅限 tailnet，没有启用 Funnel；移除测试映射用 `tailscale serve --https=8445 off`。

候选 API 仍复用项目数据库/消息系统，原业务 Worker 尚未切换新版；因此候选只完成上述验证，不作为整组已升级的生产下载链路。正式开放真实用户创建任务前须排空并整组更新，不能混跑旧 Worker 后声称 I3 冷却已覆盖生产执行。

App 使用显式候选 tailnet HTTPS API 8445 重新构建：Android debug APK 通过，iOS 设备 debug/no-codesign 通过；iOS 未签名且设备 unavailable，未安装。早前 iOS simulator 构建也通过，但两种产物不能互相替代。App 保留全部本轮及用户既有工作区修改，未提交；详见 App 020 验收。

## 11. 用户 YouTube 链接及真实前端路由复测（2026-09-13）

本节纠正此前未由真实登录业务验证的候选路由判断。用户链接 video id=`r4HUsKKZbxo`，原 8137 页面没有策略选择且提示 inspection_failed；镜像 routes-manifest 的目的地址仍是 `http://api:8111`。点击后请求确实出现在旧 video-api 日志，而非候选 API。

修复复用 src/proxy.ts 运行时 BACKEND_ORIGIN，移除构建期 rewrites/ARG。聚焦 14 项（修改前 10 项失败）通过；Web 全量 62 文件/280 项、lint/typecheck/format/build 通过；后端部署契约 23 项通过。review-agent 只读审查完整代码 diff，No findings；不是独立代理审查，也不是全仓安全扫描。

构建 `video-server:035-routing-fixed`（image id `sha256:38d0a08d15a0bfc45524ba6329e088d04788bfe19787f883a1ad1685a36bc86e`），仅替换候选前端 8137。旧候选停止并保留为 video-frontend-035-before-routing-fix；原生产、API/Runner、Worker、数据库、Secret、Tailscale 映射未改动。新 routes-manifest 无构建期 rewrite；health 200，真实用户 auth/me、providers 请求在候选 API 返回 200，登录状态保留。浏览器已出现 YouTube 两种策略。agent-browser 检查实际镜像公开页；当前用户登录在 Codex 内置浏览器，使用该浏览器自动化验证已登录链路，没有复制 Cookie、代造令牌或模拟响应。

同一用户原链接两种策略真实结果：默认 operator 路线服务不可用（对应 Runner 未启动且 `.provider-sessions/youtube/cookies.txt` 不存在）；用户界面显式选择 public 后提示平台额外验证。候选 API 内现有 anonymous client 直接调用同一视频也得到 MediaInspectionVerificationFailed / egress_challenged，与界面一致。未获得可用媒体格式，未创建下载任务、取得 Artifact 或验证播放。需要已授权的单平台来源后再验证，不把此次路由修复当作 YouTube 下载恢复。仍不得使用旧业务 Worker 验收新版任务链路。

本次镜像审计提示已登记 CQ-016，依赖公告未修复；不将该候选升格为通过安全发布验收。

## 12. 8101 正式入口与 Chrome 会话验证（2026-09-13）

用户指出实际使用 `localhost:8101` 而非候选端口。部署前查询项目库：下载 44 succeeded/1 failed/2 cancelled，分析 11 succeeded/1 failed，media_imports 空，document_import_attempts 6 ready/6 cancelled/1 expired，无执行中任务，冷却表已存在；未改 schema、业务记录或 Secret。预检脚本曾误用文档表名/模型名，改为源码确认的 document_import_attempts 后查询成功，不属于数据库缺表故障。

先将已验证运行时代理更新到主服务，再从用户现有 Chrome 的 8101 标签验证，发现 `/api/auth/me/` 返回 307 且 Location 指向 `http://api:8111/api/auth/me`。新增三个回归先失败，代理规范化尾斜杠后通过；全量 Web 62 文件/283 项、lint/typecheck/format/build 通过。按 review-agent 只读复核本次代码和调用方，No findings；未做新一轮全后端、手机或负载测试。

最终统一镜像 `video-server:prod` / `video-server:035-8101-fixed` 的 Docker image id 为 `sha256:2bf02cee478a04fe8e4de8356c047ebb30aac6b2658cbb4e5eeb7b6f6ca8ea47`，frontend/api/media-runner/download-worker/import-worker/report-worker/outbox/provider-canary 八个容器均核对相同 image id 并启动成功。旧生产镜像保留为 video-server:before-8101-update，卷和历史制品保留。仅停止临时 frontend-035-image，不删除容器或镜像；8137 不再作为前端入口。候选 API 8139/Runner 和 tailnet 8445 暂保留供此前候选 App 使用；Tailscale 主 Web 仍转发 8101、API 仍转发 8111，没有改动这些映射。

实际 HTTP 验证：`/health/ready/`=200，无凭据 `/api/auth/me/`=401，均无内部 Location。原 Chrome 页面无需清缓存、复制 Cookie 或重新登录即可恢复工作区；正式 API 日志确认 auth/me 和 providers=200。agent-browser 检查 8101 公开页；真实登录验证使用用户原 Chrome 标签，内置浏览器也已切回 127.0.0.1:8101。

原视频 `r4HUsKKZbxo` 在实际 8101 页面选择 public 并提交，正式 API 返回 503，界面准确提示平台要求额外验证。未取得格式、创建下载任务或生成视频文件；YouTube 下载仍未恢复，须取得批准的单平台来源再继续真实交付。已修复的是入口部署与登录恢复，不关闭 G1/G5；CQ-016 依赖风险继续保留，不将主服务更新等同安全发布验收通过。

## 13. Web 链接入口收敛（2026-09-13）

按用户最新要求移除 Web 策略选择器、结果策略标签、URL 分类提示函数和首页平台状态订阅。Web inspection 仅提交 source.kind 与链接，幂等键保留；服务端读取已有部署覆盖，未覆盖的双模式平台默认 public，不依赖配置了但未运行的受控端点。仅受控平台和显式受控默认仍按原边界拒绝缺少依赖的请求；没有自动取私人会话或增加回退框架。

Red→Green：新入口测试先出现 1 项失败，后端新默认测试先出现 1 项失败；实现后更新 4 个明确测试受控默认的路由夹具和 1 个管理诊断夹具，使其显式设置部署默认。最终后端 1739 passed / 2 skipped（未提供隔离 MinIO、macOS 无 Linux O_PATH），Ruff/format/mypy 通过；Web 62 文件/283 项、lint/typecheck/format/build 通过。按 review-agent 对本次完整代码 diff 与调用方只读复核，No findings；不是独立代理或全仓安全审计。

部署前数据库无活动任务，计数与第 12 节一致；未改 Secret、schema 或业务记录。统一镜像 video-server:simple-entry / prod，八个主业务容器实际 Image 均为 `sha256:b9c1cb2afbfd570ed49380a24d029be765f150096b1843379c3542822c06f233`，Compose 启动通过，8101 readiness=200、无凭据 auth/me/=401 且无内部重定向。上一个主服务镜像保留为 video-server:before-simple-entry；没有删除卷或历史制品。

用户原 Chrome 的 localhost:8101 刷新后保留登录，链接页只有输入和解析主操作（有内容时保留输入内清除按钮），没有 combobox。键盘提交原 YouTube 链接，加载时控件禁用；正式 API POST /api/inspections=503，页面仍提示平台额外验证，没有返回格式、创建任务或交付文件。新容器日志中首页 auth/me=200，没有首页 GET /api/providers。agent-browser 对真实 8101 未登录页执行 axe：0 violations / 0 incomplete；已登录、390px、主题及导航验证使用用户现有 Chrome 自动化，没有复制登录令牌或模拟平台响应。视觉边界见 design-qa.md。

本节只关闭 Web 入口过度暴露和对应默认路由改动；原视频下载、App 收敛、全平台、性能负载、新机/真机与 CQ-016 依赖风险均不因此通过。

## YouTube 按需授权来源与原视频交付（2026-09-13）

用户明确授权本机 Chrome YouTube 会话用于当前部署公开媒体。只读取 YouTube 白名单域，未导出整个 Profile、访问其他平台 Cookie 或修改 Chrome；已有 launchd 按需助手已安装，不重新安装全平台助手。匿名独立 agent-browser 页面要求登录；同出口、同镜像带授权会话的隔离实验返回 public、564 秒、360p H.264/AAC MP4。mweb 与上游默认客户端均仅有这一音视频版本（另四项为 storyboard），因此不宣称高清恢复。

新增可选 `docker-compose-browser.yml`，保留原生产 Linux 文件拓扑；实际合并验证除 YouTube 来源外服务定义与基础生产一致。私有 `.env.prod` 持久选择 Compose 文件、youtube-operator profile 和 operator_public 默认；更新 API、下载 Worker、Canary，并启动独立 YouTube Runner。未修改密码、业务表、引擎、前端镜像或 8101 端口，没有清理旧候选或业务制品。

| 实测阶段 | 证据 |
| --- | --- |
| 8101 原 Chrome 账号 | 输入原 `r4HUsKKZbxo` 链接→返回 640×360/9:24→创建任务；没有会话选择器或 Cookie 输入 |
| 首次真实任务 | `847f12af-8abc-47d0-8a67-31298331c126`，succeeded、attempt=1，51,933,107 bytes、564,431ms |
| Runner 强制重建 | 2026-09-13 01:28:48 UTC 启动新容器，来源 probe healthy；不准备新 Cookie 文件 |
| 重建后新任务 | `2abb1c9a-f317-4ad6-bd8e-e0d771e9eab5`，succeeded、attempt=1，独立新 Artifact，大小/时长/哈希与首次完全一致 |
| 文件验证 | SHA-256 `0ecb953ac0eaf54832d7e17e030db96aec0004e0cdeaec4ff2ed6bc7b2ec4be8`；从私有 Artifact 流式保存到本机 Downloads/FrameFetch-r4HUsKKZbxo-360p.mp4，ffprobe 验证 H.264/AAC、640×360、564.430658 秒，FFmpeg 全片解码退出 0 |
| 浏览器消费 | 已登录 Chrome 任务页显示完整性通过；静音播放 currentTime=24.91219、readyState=4、error=null。已点击“获取视频文件”，但未取得该浏览器动作的落盘路径证据；上行文件记录是独立 Artifact 流式交付，不冒充浏览器保存确认 |
| 会话清理 | 操作结束后 Runner tmpfs 无 Cookie，响应队列为空；健康检查会持续创建短期 probe 请求，不能把任意瞬间请求队列非空判成明文泄漏 |
| 确定性验证 | 新覆盖契约先因文件缺失失败，完成后连同部署、Secret 隔离、Cookie agent/sync/boundary/store 共 68 passed、1 skipped（macOS 不支持 Linux O_PATH）；Ruff/格式、diff check 通过。真实 Compose 5.5.1 合并通过 |
| 浏览器技能与审查 | agent-browser 0.33.0 实际 8101 未登录页 axe 0 violations/0 incomplete；其独立会话没有用户登录，已登录业务验证使用当前 Chrome 自动化，未复制 FrameFetch 登录令牌。review-agent 只读审查未发现本次部署改动的可操作缺陷，非独立子代理复审 |

当前九个主业务/Runner 使用原 `b9c1cb2a…f233` 镜像，不需要为纯 Compose 来源覆盖重建应用镜像。这里只证明当前 macOS 已授权会话、原视频 360p、同机 Runner 重建和真实制品交付；不证明整机断电恢复、Linux 自动浏览器、换机、账号切换、全画质、全平台、手机或多机高可用。既有动态来源的 browser 固定版本不能单独证明主体切换检测，见 CQ-020；CQ-016 依赖风险仍未关闭。

## 18. 当前默认线路全平台复测与音轨缺陷（2026-09-13）

09:46 起在当前生产 b9c1cb2a 镜像执行固定样本。15 平台使用原 fixed_matrix；另外 8 平台沿用同一 URL/target，调用现有 MediaRunnerRouter.resolve_access_policy 选择首页实际默认线路，再交给现有 CanaryService，未改固定样本文件或运行配置。除已授权 YouTube operator 外均为 public；没有导出其他平台会话。探针写入正常 provider_canary_results，media 使用首项真实格式并执行完整下载和 Runner 文件校验。两组有界探针期间 API 及用户下载继续工作；这不是第三方压力测试或 HA 验收。

| 范围 | 当前证据与结论 |
| --- | --- |
| YouTube、哔哩哔哩、TikTok、快手、Twitch、微博、优酷、Snapchat、LinkedIn、Telegram、Kick、Tumblr | 固定样本 metadata/media 均成功；YouTube 使用已批准 operator，其余 anonymous。不能扩展为全部链接、画质或权限通过 |
| 小红书、X、Instagram、Facebook、Pinterest | 首页默认 anonymous 的固定样本 metadata/media 均成功；原固定 operator 样本的端点缺失不能代表这些公开线路失效 |
| Vimeo | 原探针两阶段返回成功，但 8101 真实任务 `00f55eb4-613a-4652-a023-50cc24ac37eb` 为 41,879,482 bytes 的静音文件，不能算完整体验通过。修复和候选文件见下文 |
| 抖音、Reddit、微信视频号 | 首页默认 anonymous 两阶段均返回 provider_auth_required。未启用未授权私人会话；视频号既有来源需要腾讯元宝会话，不是普通微信 Cookie |
| 腾讯视频 | metadata 成功（720p/AAC、2955 秒）；media 在 561.663 秒后由本轮精确取消测试任务，CLI 将取消归一为 runner_failed，不冒充平台自主报错。另一次 60 秒有界传输仅 220,873 bytes、没有捕获 HTTP 错误关键词，未证明根因或完整下载，不能靠提高超时宣称修复 |
| 红果官方分享 | 初轮 metadata 成功/media format_unavailable；直接 Runner 复测曾取得 30,887,133 bytes、197.6 秒、H.264/AAC 文件，SHA-256 b1b1677560c55e4d5bf112f88331c868d264a402f1a513f29c2d3e24d9b033c7。随后两轮固定探针分别 metadata 失败/media 成功、metadata 成功/media 失败；保留不稳定结论，不以单次成功关闭 |

首轮 46 次结果为 38 次 succeeded、8 次 failed；Vimeo 的静音缺陷说明此计数不是完整媒体体验通过数。PeerTube 当前配置实例数为 0；微信公众号文章不在该 23 平台 Runner 视频矩阵，本轮未验收文章内原生视频。

红果补充诊断：同一生产 Runner 的五次直接 inspect 为四次成功、一次失败，另四次带脱敏进程观测为两次成功、两次失败；失败的 ffprobe 均提示上游 5xx，并最终归为 format_unavailable，见 CQ-022。生产 DNS 的 media-runner 只解析到当前主 Runner，旧候选没有同名别名，不将问题误归为候选串线。已登录平台页实际展示 24 项（包含公众号文章）；界面“当前可用”计数不是本轮完整验收结论。

### CQ-021 音轨修复

真实上游格式提供 vcodec=none/acodec=null 的独立 HLS 音轨。通用解析在得到可用纯视频后提前返回，未执行已有稀疏格式探测。修复只对这种未知编码音轨调用现有探测，最多 12 条、并发 4、继续受原总解析 deadline 和出口隔离限制；保留原视频完整时长，不以音轨探测时长替换。不猜 AAC、不新增 extractor、兼容层、备用站点或前端操作。

测试夹具先纠正误用的 response.formats 为真实 options，再得到预期 4 failed/2 passed；实现后聚焦 79 passed。后端全量 1746 passed/2 skipped（隔离 MinIO 未提供、macOS 无 Linux O_PATH），Ruff/format/mypy 通过。review-agent 对完整代码改动及调用方只读审查 No findings；非独立子代理或全仓审计。

候选镜像 `video-server:audio-validation`，config image ID `sha256:0845782cdde77250e4ae4b0278766352ed28502d3e2ace4b57f689ad4bfcd2f7`。隔离容器使用原 egress 网络、没有数据库/队列/存储/平台凭据，按同一源先 inspect 再重新 inspect/download，生成 1920×1080 H.264 + AAC MP4：43,328,716 bytes，60.054 秒，SHA-256 `28fa4bea28c465c2282e9ea4fc942b01c110577fa231d67adfdc57cfe97e011c`。完整 FFmpeg 解码退出 0；复制到本机 `Downloads/FrameFetch-vimeo-1208412630-audio.mp4` 后 ffprobe/哈希一致。它是候选 Runner 文件验证，不是已更新的 8101 用户任务。

agent-browser 0.33.0 实测 8101 公开页面，axe 0 violations/0 incomplete。独立 CLI 没有用户登录，已登录 Vimeo 业务在独立 Chrome 标签复用本地账号；未复制令牌、未修改用户原标签。保留输入框/解析按钮入口，没有增加策略选择。未执行新的手机/新机/全 HTTP 负载测试。

上线前无活动任务检查发现新的用户下载 `2a42f78e-28f6-47ae-8ddd-c4fbc157cf5f`，因此未重启匿名 Runner、替换生产镜像标签或切换主服务。CQ-021 代码及候选通过，部署验收仍待排空；原静音文件未改写。工作区同期出现的前端改动属于其他工作，不纳入本次音轨修复提交。

## 19. 抖音、Reddit 与视频号授权来源修复（2026-09-13）

用户明确同意使用三平台限定来源。抖音、Reddit 的有界本机导出状态均为 ok，但生产只有 YouTube operator 在运行。扩展已有 macOS Compose 覆盖，持久合并抖音/Reddit profile 与 operator_public 默认；视频号仅启动原有来源，不设置未经验证的默认策略。切换 API/下载 Worker/Canary 前数据库无活动下载；未动前端 8101、匿名 Runner、镜像标签或其他工作区改动。宿主代理从本机代码按需加载本轮修复，无需把 Chrome Profile 挂入容器。

初轮抖音/Reddit 四次 metadata/media 全通过。强制重建这两个 Runner 后，抖音两阶段仍成功，但元宝慢导出造成后来 Reddit 超时、抖音页面 503，暴露共享宿主串行阻塞。修复 CQ-024 后，在视频号仍失败的测试期间，Reddit 两阶段恢复（7404/16619 ms），抖音真实页面解析/任务成功；同平台每次一个导出，主线程处理后来 probe，空闲平台持续接收新请求。既有 YouTube metadata/media 回归成功（8794/16800 ms）。这不是整机重启、账号切换、新机或压力容量验收。

8101 已登录 Chrome 实际任务：

| 平台 | 任务 | 完整文件证据 |
| --- | --- | --- |
| 抖音 | `968d6e35-484c-431f-b327-b8fa96fdb86b` | succeeded，第 1 次执行；2,991,195 bytes，14,070 ms，1280×720 H.264/AAC；SHA-256 `b495811a95bddf1332cf3327198016f6f485b691539819c7fb7256870a1d00fc` |
| Reddit | `d87b3c96-d9d9-454a-a5ae-ae735af02e7a` | succeeded，第 1 次执行；2,071,054 bytes，17,686 ms，198×360 H.264/AAC；SHA-256 `f89d230ff0711e57d1e52e8eae8c789125364590481a07033ca07f0f079f5cca` |

页面均显示“下载已完成 / 文件完整性验证通过”，实际播放进度前进；数据库 Artifact 对应大小/时长/哈希已核验。Reddit 获取文件按钮已点击，但未取得 Chrome 本地保存路径，不能宣称浏览器文件落盘已验证。agent-browser 独立访客页面 axe 0 violations/0 incomplete；登录业务在现有 Chrome 新标签执行，不复制应用登录令牌。

视频号未通过：原 Chrome 元宝页面显示已登录，限定 yuanbao.tencent.com Cookie 查询为空；临时浏览器只复制 Cookie，不能恢复此账号状态。现有测试曾以空来源 Fake 出登录，已纠正夹具。缺失来源现在直接结束，不再启动无登录浏览器等待 15 秒；实际探针仍为 provider_auth_required（1302/1242 ms），不是解决了下载。CQ-025 保留：需要单独确定当前网页存储的受控来源或获得专用持久浏览器会话方案批准；不要求用户重复授予同一读取权限，不复制整个私人 Profile、不接入历史凭据恢复兼容层。临时 Chrome 继承外层导出进程组，补上超时清理边界 CQ-026。

回归：Compose 三项、晚到 probe/refresh 两项、浏览器进程组及空来源两项均先失败后通过。最终 Ruff/format/mypy 全通过，1752 passed/2 skipped（隔离 MinIO 未提供、macOS 无 Linux O_PATH）；生产合并 Compose、开发 Compose 静态解析及 diff --check 通过。review-agent 对本次完整代码 diff、队列/取消调用方和测试只读审查 No findings；不是独立子代理全仓审计。前端/App/新机/高并发容量未重新验收。全平台任务继续保持未完成。

## 20. 视频号专用会话与真实下载（2026-09-13）

> 后续用户截图确认该任务及 Reddit 的浏览器本地保存被组织策略屏蔽；下述服务器制品与播放证据仍成立，但不能视为设备交付完成。解除拦截见第 21 节，尚未通过。

用户明确批准本机独立持久元宝来源，并在专用窗口完成首次登录。按 agent-browser 的持久 Profile 思路复用已有 ChromeDevTools，不添加运行时浏览器框架依赖；agent-browser 实际核验专用登录窗口，常规 Chrome 仅用于已有帧取登录的业务测试，未复制应用登录凭据。

来源目录权限 0700、独占锁 0600；浏览器只在登录/导出期间运行，结束关闭、保留目录。缺失来源不创建浏览器，登录和后台导出互斥，竞争失败不能关闭持有者窗口，清理异常也释放锁。原临时来源类删除，不保留旧类名或兼容分支。登录/导出只接受正确类型的元宝身份字段，错误类型回归先失败后通过。

专用登录结束后两次独立宿主导出状态 ok，其中一次 3.02 秒；Runner 的实际加密队列读取 ok（6.01 秒）。首次固定矩阵仍失败（metadata 21003 ms、media 19329 ms，均 provider_temporarily_unavailable）；安全分段诊断随后确认第一方页面、两次 feed API 和元宝解析均成功，未打印 Cookie、身份、动态头或返回媒体地址。再次完整矩阵通过：metadata 12063 ms、media 18693 ms。初次失败原因尚未确定，不能从后来成功反推已消除间歇故障，登记 CQ-027。

完整探针通过后，仅在本机私有配置合并 wechat_channels=operator_public。确认无活动下载，再重建 API、下载 Worker、Canary 和视频号 Runner；保持原镜像 sha256:b9c1cb2afbfd570ed49380a24d029be765f150096b1843379c3542822c06f233，不采用已被其他工作更新的 prod 标签，不重建前端。API readiness 通过。

重建后在已登录 Chrome 的 localhost:8101，粘贴 `https://weixin.qq.com/sph/A9znfitafp`，未设置任何访问策略，解析显示 576×576 H264/AAC、约 25 秒，并创建任务：

- 任务 `707a2a06-a2c6-4a7d-9cbd-fd1eaae0e0eb`，succeeded，第 1 次执行。
- 制品 1,525,279 bytes，24,448 ms；数据库确认 1 video stream + 1 audio stream。
- SHA-256 `da103fe327267c55e0a47046c44183483f885162c50484f0b141a4812ec515dd`。
- 从对象存储实际读取制品，大小与 SHA-256 均匹配；ffprobe exit 0，H.264 576×576 + AAC，时长 24.448 秒。
- 页面显示下载完成、文件完整性验证通过；点击播放后实际进度到 0:24、Seek=100。已点击获取文件，但未验证 Chrome 的本地保存路径，不将按钮点击当作本地文件落盘证据。

其他来源回归：YouTube metadata/media 9897/15565 ms、Reddit 5065/12760 ms 成功；抖音 media 7991 ms 成功，但同轮 metadata 3185 ms inspection_failed，单独复测 4055 ms 成功，故该轮六项矩阵整体为失败，不能只记录最终绿色结果。

最终后端 1760 passed、2 skipped（隔离 MinIO、Linux O_PATH），Ruff/check-format/mypy、生产/开发 Compose 静态解析和 diff --check 通过；源文件相关 12 项单测通过。review-agent 只读审查发现登录字段类型误判，退出审查后补充 Red→Green 修复；最终代码复核无新增可操作发现，不是独立子代理全仓审计。文档清除旧临时来源与不适用的退出说明。本轮无前端/App 代码变更，已有改动保留；没有整机断电、新机、账号切换、全平台持续性或高并发容量验收。CQ-025 仅本机来源修复通过，CQ-027 保留，全任务仍未完成。

## 21. Chrome 组织策略阻断设备保存（2026-09-13）

当前 No-Go。原浏览器显示视频号和 Reddit 的文件均被组织屏蔽。已核对 Web 通过 use_browser_download_proxy 选择鉴权同源 /file 流，具备 Content-Length、Content-Disposition、ETag 和 Range；前端 anchor.click 不能取得浏览器下载管理器的最终结果。没有证据证明具体哪项策略造成阻断，不将 DownloadRestrictions=3 当作已确认事实。

本次工具访问 chrome://policy 被安全策略拒绝，未改用其他接口绕过。已请求用户手动提供下载限制、URL 限制及下载扫描条目的值和来源；未修改 Chrome 策略、扩展、网络、账户或平台来源。

候选修复仅将服务端终态改为“服务端已完成”，就绪说明明确设备保存由浏览器处理，并提示组织策略屏蔽的处理责任。未建立虚构的设备保存成功状态，也未新增 Blob、下载器或兼容通道。三个媒体类型的组件回归先失败后通过；相关 20 项、Web 全量 286 项、lint/类型检查和生产构建通过，新增测试格式问题修正后复验。已有前端视觉改动保留，不随本次提交；候选文案未单独部署至 8101，未声称原 Chrome 拦截已修复。

完成门禁：取得实际策略来源，由有权限的管理者确认并纠正误配置，原 Chrome 正常保存用户报告的两个文件，本地文件存在、大小及 SHA-256 与对象存储一致且可播放。没有原浏览器落盘证据前 CQ-028 保持开放。

## 22. 当前重启故障实证（2026-09-15）

完整报告：[021 下载可靠性与重启会话故障深度调研](../research/021-下载可靠性与重启会话故障深度调研.md)。Server 基线 `6b50c83dcb07f15e8471a79f047a1b050cbee8a8`；业务运行镜像 `b9c1cb2afbfd`，不把两者视为全部代码等同。

- 代理实际 restarting，退出码 1，详细快照计数 795，后续 798；日志反复指向 `/tmp/squid.pid` 的 PID 1。
- 五个运行 Runner 的 egress TCP probe 均 false；四个 Operator 的无 Cookie 来源 probe 均 true。只能证明代理响应，不证明上游会话有效。
- 同代理镜像、无网络临时容器：原可写层在 SIGKILL/start 后 exited 1；tmpfs 候选 running 且 `squid -k check` 退出 0。临时容器及自动卷已清理，生产服务未变更。
- 运行 Canary 的目标数 0；三个业务容器记录的临时镜像覆盖文件已不存在；九个 Operator 端点仅四个对应实例运行。
- 本地 Fresh cookies 加 JSON 失败在 authenticated true/false 两种输入都映射为 credential_required，已复现误分类。相关五个测试文件共 114 passed；其中旧错误期待需要修正。

本次状态：研究与隔离实验完成；生产恢复、完整 ACL 转发、真实媒体下载、全平台矩阵、Docker Desktop/整机重启、Web/App 本地保存均未验收。旧平台成功记录不覆盖当前共同出口故障。

## 23. CQ-029 公共出口候选修复（2026-09-15）

按 [009 SOP](../operations/009-下载故障修复与持续可用性SOP.md) 执行 P0 切片。开始时 Server 为 `21fa68fe`、工作树无既有改动、`main` 领先远端 2 个文档提交；数据库定向查询确认 queued/running/retry_wait 下载任务均为 0。实际代理仍以镜像 `sha256:8a3baed477e2c282ab8aa5edad442f69873246964f225c5c2ae8364b6610963c` 反复退出，快照 restart_count 822，日志仍为 `/tmp/squid.pid` PID 1。

Red：新增测试要求两个业务 Compose 的 `egress-proxy.tmpfs` 精确为 `/tmp:rw,noexec,nosuid,size=16m,mode=1777`，初次运行 1 failed、3 passed，失败为 `KeyError: tmpfs`。Green：仅为两个代理服务增加该挂载，保留现有镜像、启动参数、健康检查、网络和 ACL。`test_egress_config.py`、`test_provider_supply_chain.py`、`test_database_bootstrap.py` 共 34 passed；本机和生产浏览器来源两套 Compose `config --quiet` 均通过。

使用同一代理镜像、完整生产 `squid.conf` 和 `blocked-destinations.conf` 创建无业务网络临时候选；正常 TERM/start 3 轮、异常 KILL/start 3 轮均恢复 running，六轮 `squid -k check` 均通过，临时容器及匿名卷已删除。

生产变更前确认 queued/running/retry_wait 下载任务均为 0，仅以 `--no-deps --force-recreate --no-build --pull never` 替换 `egress-proxy`。替换后镜像 ID 保持不变，`/tmp` 为预期 tmpfs；经代理访问公网成功，访问 `127.0.0.1:8111` 返回 403，证明既有私网 ACL 仍生效。media-runner 与 YouTube、抖音、Reddit、微信视频号四个当前运行的 Operator Runner 全部恢复 healthy。

随后在生产容器完成 `docker restart --time 35` 3 轮，以及 `docker kill --signal KILL` 后 `docker start` 3 轮；每轮均恢复 healthy，未再出现陈旧 PID。真实 fixed_matrix 结果：B站 anonymous metadata 2525 ms、media 7280 ms；抖音 operator_managed metadata 3461 ms、media 7528 ms，四项均 succeeded。由此关闭 CQ-029 的公共出口重启循环；这组证据没有覆盖浏览器本地文件保存、所有九个平台或 CQ-031 的实例/配置一致性。

## 24. CQ-030 登录误归因候选修复（2026-09-15）

Red：将抖音 `Fresh cookies (not necessarily logged in) are needed` 分别放入 anonymous 与 authenticated 失败上下文，两项都得到 credential_required；包含明确 HTTP 429 的 `rate-limit reached or login required` 及只有该模糊提示的输入也都误报 credential_required。四项失败与生产症状一致。

Green：在通用 Fresh cookies 认证规则之前增加抖音限定规则，将这类缺少会话失效证据的上游空响应映射为 provider_temporarily_unavailable；混合提示在有明确 429 时映射 provider_rate_limited，没有额外证据时映射 provider_temporarily_unavailable。明确的 credential_expired、登录要求、私密/付费内容和 verification challenge 规则未放宽。四项红测转绿；Runner 命令、错误转换、下载执行与 Canary 相关 127 项通过，后端全量单元测试 1616 passed、1 skipped，目标文件 Ruff 和 mypy 通过。全量 mypy 的 3 个既存 Pydantic Settings call-arg 另行保留。

生产部署前活动下载任务为 0。由提交 `29d2cf05` 构建并保留回滚标签 `video-server:29d2cf05`，只重建公共及 YouTube、抖音、Reddit、微信视频号五个当前运行的 Runner；五个容器全部 healthy，镜像 ID 一致，并且 Compose 标签只引用仓库内的 prod/browser 文件。部署容器内输入相同错误：抖音 anonymous/authenticated 都得到 provider_temporarily_unavailable 503，明确 `Account cookies are no longer valid` 仍为 credential_expired 422。

部署后的真实 fixed_matrix：B站 anonymous metadata 2760 ms、media 6911 ms；抖音 operator_managed metadata 3989 ms、media 7602 ms，均 succeeded。再单独重启抖音 Runner，健康恢复后没有重新导出或替换浏览器会话，metadata 3560 ms、media 8736 ms 再次 succeeded。由此关闭 CQ-030 的服务端误归因和 Runner 重启会话复用；未通过真实失败页面触发浏览器提示，前端文案体验仍按实际 UI 验收边界报告。

## 25. CQ-031 本机生产拓扑收敛（2026-09-15）

生产私有配置原声明 9 个 Operator endpoint，COMPOSE_PROFILES 和实际容器只有 YouTube、抖音、Reddit、微信视频号 4 个；其余小红书、X、Instagram、Facebook、Pinterest 在本机没有可用来源目录。Canary targets 为空；API、Worker、Canary 等旧容器标签还引用已删除的 `/tmp/framefetch-yuanbao-deploy.../images.yml`。

先在内存候选中将 Operator routes 收敛为上述 4 个已运行实例，从固定样本选择这 4 个平台及 B站，共 10 个 metadata/media Canary 目标；Pydantic 目标校验和完整 prod/browser Compose 解析通过后，才原子更新 `.env.prod` 对应两项，不输出或复制其他私有配置。无来源的五个平台没有启动空 Runner，也没有被静默切换到另一个账号或匿名策略。

部署前活动下载任务为 0。当前应用镜像保留 `video-server:29d2cf05`，旧后端与前端镜像分别保留回滚标签；随后使用 prod/browser 两份仓库内 Compose 重建 API、前端、Outbox、三个 Worker、Canary，以及上一节已重建的五个 Runner。所有当前应用容器镜像 ID 一致，标签中不再出现临时 images.yml；API `/health/ready` 和前端首页均返回 200，各容器运行或健康。

Canary 运行态解析到 10 个目标、5 个平台。最近结果全部 succeeded：YouTube 11719/21106 ms、B站 2760/6911 ms、抖音 3560/8736 ms、Reddit 6141/13556 ms、微信视频号 17282/16361 ms（每组依次为 metadata/media）。本机生产的配置/实例一致性与空监测问题由此关闭；该结果只覆盖当前合法来源和固定样本，未准备来源的五个平台及其受控访问不能记为通过。

## 26. CQ-032 Web 下载页面保持（2026-09-15）

用户实际点击“获取视频文件”时页面刷新，并重新显示“正在恢复登录状态”。代码路径确认下载按钮先取得鉴权同源 `/api/downloads/{job_id}/file`，随后使用无 target 的临时 anchor 导航；认证恢复提示只在 `AuthProvider` 初次挂载期间出现，因此该现象说明下载响应替换了当前文档，而不是一次正常的文件获取需要重新认证。

修复使用隐藏 iframe 发起附件请求，保留服务端的鉴权、Content-Disposition、Range 与浏览器下载管理器，不把完整媒体读进前端内存。Red 用例在旧实现上因 anchor click 被调用一次而失败；Green 覆盖下载通道创建、当前 `/downloads/detail/` 路径保持、顶层 anchor 未触发和 60 秒节点回收。聚焦 4 个文件 30 项通过；前端全量 63 个文件 287 项、format、lint/typecheck 和生产构建通过。

提交 `567e3215` 已推送 main，并构建为镜像 `sha256:d22a49bdd906bdd10928bb28f65a6a8df97f3f284698cbc7afdbdd776094d11e`；只重建 frontend，健康检查与 8101 首页通过，后端、Worker、Runner 和平台会话未重启。原已登录 Chrome 在下载记录与任务详情分别点击获取文件，两个页面的 URL、账户菜单和业务内容均保持，未重新出现认证恢复。API 日志对应两组 download-url 200 与 file 200，整个点击区间只有进入下载记录时原有的一次 auth/me 200，没有因下载新增 auth/me。由此关闭 CQ-032 的页面刷新和认证重取行为。

浏览器下载管理器是否因组织策略拦截，以及文件最终是否落盘、哈希一致并可播放，继续按 CQ-028 验收；本项不把 file 200 解释为设备保存成功。

## 27. Chrome 来源挂载与失败阶段（2026-09-15）

`a38925752c314365e311161a0c887a0e83dcdd48` 为 browser Compose 的小红书、X、Instagram、Facebook、Pinterest 补齐单平台加密队列挂载，与原有 YouTube、抖音、Reddit 合计八个。31 项聚焦契约、后端 1769 passed / 2 skipped、Ruff/mypy 通过；远端 [CI 成功](https://github.com/StephenQiu30/video-server/actions/runs/34931385910)。这些是代码和配置证据，不能关闭平台文件验收。

小红书候选 Runner healthy，但 fixed metadata 返回 `provider_temporarily_unavailable`；隔离来源操作进一步确认在提取器命令启动前返回 `provider_session_unavailable`。八个 Chrome 来源有界导出均失败。原机 Chrome Cookies 文件存在，项目 SQLite 打开失败，直接读取返回 OS `Operation not permitted`；没有证明具体 TCC 责任进程，也没有证明再次登录或给 Python 授权必能恢复。

失败的小红书候选已停止，未加入生产默认路线；本机仍使用四条已配置来源与十个 Canary 目标。缺失五平台的来源与完整文件验证继续未通过。此记录不能据此推断八个平台的上游提取器都不可用。

## 28. 可迁移架构新增门禁（2026-09-15）

依据：[022 调研](../research/022-可迁移下载服务与平台会话架构调研.md)；以下均为待执行验收，不是本轮测试结果。

| ID | 操作 | 必须观察到 | 需求 | 状态 |
| --- | --- | --- | --- | --- |
| T22 | 新电脑登录同一服务，解析/获取已有文件 | 不导入平台 Cookie，不触发平台交互登录；当前业务页面不刷新；设备保存单独核验 | R16 | 未执行 |
| T23 | 干净目标机没有日常 Chrome，启动并重建三次 | 核心服务/历史可用；选定匿名与有效 file 样本完整媒体可取；不提示广泛磁盘授权 | R17 | 未执行 |
| T24 | 合成权限拒绝、密钥错误、文件缺失与上游账号过期；读取状态 | 分别归因；普通 GET 无 Cookie 读取、无登录副作用；单平台不阻塞核心 readiness | R17 | 未执行 |
| T25 | 专用来源首次授权后多次解析/下载，维护换版及 kill 来源进程 | 不访问日常 Chrome；持久源保留；只领取精确版本；旧解析拒新修订；延续 T07–T11 撤销/原子性 | R18 | 未执行 |
| T26 | 恢复撤销前备份、双机误启动、缺恢复密钥、换出口，再回滚 | 旧授权/租约不复活，旧机停写；未知授权待验证；最新冷却/撤销保留；历史制品仍可按业务权限取回 | R19 | 未执行 |
| T27 | 故意遗漏挂载/实例或制造 default/Canary 路线差异；执行默认产品入口 | 发布预检拒绝不完整声明；显式测试与默认路线各有证据，不隐式换账号；敏感分享参数不泄漏 | R20 | 未执行 |
| T28 | 固定来源/出口/链接对照主引擎和候选并回滚 | 真实媒体画质、音轨、完整性和样本身份一致；无增量不接入；生产启动不在线升级 | R21 | 未执行 |

文档交付与运行验收分开：本轮只更新研究、设计、需求、计划、验收及索引/缺陷；未执行第二机器、专用普通平台来源或最新 23 平台矩阵。G1/G2/G3/G5 不因此整体转为通过。

本轮文档静态检查：8 份 Markdown 的 182 个相对链接均存在；R16–R21、T22–T28 新增编号存在且映射已复核；暂存 diff 空白检查通过。没有业务代码变化，未重复运行本地全量业务测试；第 27 节测试与 CI 属于 a3892575，不能作为新来源实现证据。

## 29. S0 来源诊断与路线预检（2026-09-15）

先增加失败用例，确认旧实现缺少新来源状态和运维命令；随后完成以下行为：

- Chrome Profile/Cookies 数据库不存在返回 `provider_session_source_missing`，宿主 OS 拒绝读取返回 `provider_session_permission_denied`；Cookie 可读但不含平台必需字段才返回 `credential_required`。稳定状态经 bounded child、一次性加密响应和 Runner/API 分类保持，不传递异常路径或 Cookie。
- `provider_cookie_agent doctor [--provider key]` 并行、有界诊断来源，只打印平台和稳定状态。Agent `status` 与 Runner readiness 仍保持无秘密 probe，不因健康查询访问 Chrome。
- Settings 启动时拒绝默认 operator 策略没有同平台 Runner endpoint；Canary 构建运行时前拒绝 operator 目标没有同平台 endpoint。额外 endpoint 仍可作为未启用候选，不因存在而提升默认权限。

最终 121 项聚焦测试通过；此前同一实现的后端全量 1783 passed / 2 skipped，最终 Canary 映射变更另由聚焦用例覆盖；Ruff/格式及 mypy 534 个源文件通过，两套业务 Compose 配置解析通过。跳过项分别为未提供隔离 MinIO、macOS 不具备 Linux `O_PATH`，没有本切片新增失败。本机 doctor 退出码 5：抖音、Facebook、Instagram、Pinterest、Reddit、X、小红书、YouTube 均准确返回 `provider_session_permission_denied`；专用元宝视频号返回 `ok`。命令输出没有原始路径、Cookie 或账号。

这项结果关闭 T24 中“来源错误分类与无副作用状态”的代码部分，并完成 T27 的 endpoint 关系预检部分；没有关闭来源可迁移、平台可接受、完整文件、第二机器或全 Compose 实例声明。八平台当前仍不可用，下一切片为 S1 项目独立 file 来源基线及 S2 小红书来源/提取器对照。

生产发布前查询 `download_jobs` 的 `queued/running/retry_wait` 数量均为 0。提交 `31f0811e` 构建镜像 `sha256:53b4c2ab234b20456e6f19baae35549653d1fef18792fa3a4e694e33491b0f29`，新镜像预检读取 4 个 operator endpoint、4 个默认策略、10 个 Canary 目标并通过关系校验；随后只重建 API、下载 Worker、Canary、YouTube/抖音/Reddit/视频号四个当前启用 Runner。提交 `cd1570b4` 增加 Canary 最终错误映射，构建 `sha256:620fdb724098a5179ed681947e2b6ea6660e82e314e85eb5df39fc752d8b1419` 并只重建 Canary。API 与四个 Runner healthy，Worker/Canary running，8111 readiness 和 8101 首页均为 200。

生产 YouTube operator metadata 固定探针在 1466 ms 失败，稳定结果为 `provider_configuration_missing`，不再保存为需要登录。该结果验证新诊断已生效，同时确认来源没有恢复；未通过真实媒体、浏览器错误文案或设备文件保存，不能关闭 T23/T25 或宣称 YouTube 可下载。

## 30. 小红书匿名固定路线对照（2026-09-15）

在不修改生产默认策略、不启用已停止小红书 operator Runner 的前提下，读取仓库同一固定样本并只将访问方式改为 anonymous。生产匿名 Runner 的 metadata 在 2962 ms succeeded，完整 media 在 6214 ms succeeded，稳定错误均为空；这证明该标准 explore 样本及当前出口/引擎不需要 Chrome 来源。

固定矩阵的两条小红书目标由 `operator_managed` 改为 `anonymous`，增加专门回归防止重新绑回浏览器来源。目录、固定矩阵和 Runner 相关 67 项通过，Ruff/格式及 mypy 保持通过。

提交 `f0184c2d` 构建镜像 `sha256:f5a54168779b462ee250dd6bee2d9dfe3a0d20d051f4e7124363c48325acdd55`。活动任务检查通过后，Canary 和匿名 Runner 均由该镜像重建；容器运行后标准 `fixed_matrix --provider xiaohongshu` 取得 metadata 2159 ms、media 5224 ms，两个阶段均为 `anonymous`、`succeeded`，稳定错误为空。由此关闭 CQ-035 的固定路线与重建复测范围。

这项证据不改变小红书 `degraded` 目录状态，也不证明短链接、图集、不同画质、账号可见内容或所有笔记；访客来源和专用适配器仍按 S2 单独对照。固定样本 URL 含必要分享参数，运行输出和验收记录继续只保留脱敏 target/provider 信息。

## 31. 会话能力平台的公开路线分组（2026-09-15）

固定公开矩阵原先把除小红书外的所有浏览器会话能力平台都指定为 `operator_managed`。这把“可以使用 Cookie”误当成“公开样本必须使用 Cookie”，导致 X、Instagram、Facebook、Pinterest 在本机 Chrome 来源权限拒绝时失去持续验证。用户默认入口没有为这四个平台配置 operator 默认策略，实际仍为 public；缺陷集中在固定验证和生产周期监控。

使用生产匿名 Runner 对同一固定 URL 执行 metadata/media，结果为：X 12837/27449 ms、Instagram 7861/16667 ms、Facebook 5461/9870 ms、Pinterest 2473/25288 ms，八项均 succeeded 且稳定错误为空。Reddit 同样对照的两个阶段均为 `provider_auth_required`；YouTube、抖音、视频号匿名 metadata 分别为 `provider_verification_failed`、`provider_temporarily_unavailable`、`provider_auth_required`。因此候选只把已通过的四个平台改为 anonymous，后四个平台继续保留 operator；不做请求期自动回退。

固定矩阵测试现在要求全部九个浏览器会话能力平台必须显式属于“已证明匿名”或“当前保留 operator”集合；新增会话平台未归组会直接失败。候选聚焦测试 25 项、后端全量 1785 passed / 2 skipped，Ruff/格式及 mypy 534 个源文件通过。跳过项仍为隔离 MinIO 未提供和 macOS 不具备 Linux `O_PATH`。

提交 `2cca58b1` 构建镜像 `sha256:76639ce0f41d579827b25e11189d859bcc396978623af44553223abd49629a6e`。候选设置预检读取 20 个目标、10 个平台、4 个 Operator endpoint；在活动下载任务为 0 后，生产 Canary 与公开 Runner 均由该镜像重建，Runner 恢复 healthy。生产周期目标由原 10 条扩为 20 条，新增小红书、X、Instagram、Facebook、Pinterest 的匿名 metadata/media。

重建后的标准矩阵再次取得八项成功：X 15619/31679 ms、Instagram 6047/14185 ms、Facebook 5046/10966 ms、Pinterest 3334/26716 ms；访问方式均为 anonymous，稳定错误均为空。由此关闭 CQ-036。此结论只覆盖四个平台各一个固定公开样本；私人内容、账号内容、图集以及样本外形态继续按平台目录状态与后续矩阵验收。

## 2026-09-15 现有 Chrome 采集、文件来源与生产交付

初始阻塞阶段：来源安装代码已实现，尚未恢复真实下载；下文分别记录来源恢复、生产交付及再次失效，不能从候选成功关闭 G1/G3 或 CQ-034。用户明确要求使用已登录 Chrome，不在测试/专用浏览器再次登录。试开的独立登录进程已停止并清理，没有保存新登录来源；最终代码不包含新登录入口。

### 复现实验

- 当前生产固定 YouTube metadata：operator_managed，1319 ms，provider_configuration_missing；同进程 doctor：provider_session_permission_denied。
- 同 media-runner、同出口、固定 jNQXAC9IVRw，mweb 和上游 default 各一次：进程退出 0，但 duration=null、formats=0、要求 Sign in。没有把退出码当作成功，也没有批量切到匿名。
- 运行栈：yt-dlp 2026.08.19、固定 commit 3a08beaf031ab68f966401ead017ac81fe8486cf、Node v24.21.0、yt-dlp-ejs 0.8.0、bgutil 1.3.2；PO /ping 返回 200/version=1.3.2。没有缺 Node/EJS 的证据。
- 当前 Chrome 可见账号菜单和正常播放器；只核对可见登录状态，不导出全浏览器 Cookie。
- 来源目录 0700、Cookie 0600，属主 501 与当前用户相同；文件 stat 成功而 open 返回 EPERM。2026-09-15 16:59:45 CST 的 TCC 记录把访问 Python 归属 com.microsoft.VSCode，AllFiles 与 AppDataDetailed 拒绝。系统设置可见 ChatGPT FDA=on、Visual Studio Code FDA=off。该结论仅针对本次执行链；其他 LaunchAgent/机器须独立核对责任进程。
- 该阶段按工具要求请求实际执行宿主的 OS 权限确认；用户后续回复“继续”时设置已开启，见下文，代理没有操作开关。

### 平台固定 metadata 矩阵

本轮同一入口 23 targets，19 succeeded；YouTube/抖音/Reddit 为 provider_configuration_missing，腾讯视频 inspection_timeout（120077 ms）。通过平台：Bilibili、TikTok、小红书、快手、视频号、Vimeo、X、Instagram、Facebook、Twitch、Pinterest、微博、优酷、Snapchat、LinkedIn、Telegram、Kick、Tumblr、红果。这里只是固定单链接的 metadata，不是 19 平台完整下载验收。

### 实现边界

新增部署侧 provider_session_setup capture-chrome/import/check。capture-chrome 复用现有按 Provider 域 allowlist 的有界读取，显式指定既有 Profile，只在部署者执行时采集。无页面跳转、Cookie 日志或普通 API 上传；校验后持锁并原子替换 0600 文件。下载继续沿用既有 file keyed revision 与临时操作副本。来源被拒绝、过期、跨域、文件损坏或原子替换前失败不覆盖旧文件；文件格式有效不等于平台接受账号。

聚焦 56 项来源、Chrome、租约边界与文件重启/换版回归通过。最终后端全量 1793 passed / 2 skipped；跳过项为未提供隔离 MinIO、macOS 不具备 Linux O_PATH。Ruff/格式与 mypy 535 个源文件通过。这些代码检查不替代后续生产切换、真实文件和重启验收；实际证据见下文。

### 上游核对

已用 GitHub、Firecrawl 和 Context7 核对。yt-dlp 官方说明 YouTube 会轮换浏览器 Cookie；PO Token 与账号/出口挑战是不同依赖。[官方会话说明](https://github.com/yt-dlp/yt-dlp/wiki/Extractors#exporting-youtube-cookies)、[PO Token Guide](https://github.com/yt-dlp/yt-dlp/wiki/PO-Token-Guide)。公开 issue 17376 被维护者标记为 17368 的重复问题；issue 的用户报告只能作为同类症状，不能替代本机实验或宣称升级可解决。[上游问题](https://github.com/yt-dlp/yt-dlp/issues/17376)。

### 既有 Chrome 授权恢复后的候选证据

用户回复“继续”后，系统设置已显示 VS Code FDA=on；没有再次切换或重新登录。capture-chrome 成功采集 YouTube、抖音、Reddit 单平台候选并按 Runner UID/GID 10001 安装只读文件。候选实际镜像 3f3ee8296931c1653703bf484df059ee7a5fb2f201b66ed4f5dc1384d26dbc17，沿用生产引擎、隔离出口和单平台参数。

| 平台/样本 | 完整媒体 | SHA-256 | 重启后 |
| --- | --- | --- | --- |
| YouTube jNQXAC9IVRw | 632006 bytes / 18.948 s / H264 AAC 320×240 | 1c335af874a305f810d2b0d6634c8e36a0b538578010bc5b49e50fc91d95ab2a | 原长视频承担重启门禁 |
| YouTube r4HUsKKZbxo | 51933107 bytes / 564.431 s / H264 AAC 640×360 | 0ecb953ac0eaf54832d7e17e030db96aec0004e0cdeaec4ff2ed6bc7b2ec4be8 | 新任务 0020124c-f739-421a-a380-edc1d55ffb7d，同哈希 |
| 抖音固定样本 | 2991195 bytes / 14.070 s | b495811a95bddf1332cf3327198016f6f485b691539819c7fb7256870a1d00fc | 新任务 0fc1a12d-0fec-4718-98db-2d8d966d760e，同哈希 |
| Reddit 固定样本 | 2071054 bytes / 17.686 s | f89d230ff0711e57d1e52e8eae8c789125364590481a07033ca07f0f079f5cca | 新任务 1225c2d7-5dd4-4564-b800-7b87a8001f36，同哈希 |

所有上述文件 FFmpeg 全片解码退出 0。首轮原 YouTube HTTP 客户端下载曾在 21.26 s 返回 download_failed；相同来源/引擎的直接下载、同服务下载与重启后 HTTP 下载随后通过，首次失败的具体传输原因未捕获，保留 CQ-027 偶发失败，不声称零失败。Reddit 在容器重启后立即请求曾暂时不可用；三个 /health/ready 200 后复验通过。没有在重启时重新采集 Chrome。

Compose 三层合并只改变三个目标 Runner，文件 mount 均只读，sync key 不存在，其他业务服务相同；标量 !reset 实验未清除中间来源，完整 environment !override 已通过真实合并和 11 项部署契约/进程 Secret 测试。生产切换前 active_download_jobs=0。

### 生产切换及来源轮换实证

`2394541c` 已推送 main，CI [34952352381](https://github.com/StephenQiu30/video-server/actions/runs/34952352381) 成功。私有 `.env.prod` 仅更新 COMPOSE_FILE 为 prod/browser/session-files 三份受控配置；三个 Runner 由当前 `video-server:prod` 镜像 3f3ee829… 重建，其他业务容器保持。实际容器 sync 字段缺失、单平台来源只读挂载；三者 `/health/ready` 200。这里的“只读”指会话挂载，不是整个生产容器根文件系统。

第一轮生产固定矩阵：抖音 metadata/media 1682/5128 ms 成功，Reddit 4333/14243 ms 成功；YouTube metadata/media 4316/4241 ms 返回 provider_session_expired，单独复测 5292 ms 同样失败。文件本地校验仍通过，上游安全标记明确包含 cookies are no longer valid 与 sign in to confirm。这证明有效 Cookie 快照可被平台轮换而失效，不能把磁盘持久化宣传为永久来源。

显式再次从用户已登录的同一 Chrome Profile 采集 YouTube，校验后原子替换该平台文件；没有新登录、账号切换、镜像重建或请求期回退。网页解析原样本 r4HUsKKZbxo 后创建任务 `e657507d-cb08-489b-a911-389bc57fe539`，正常 API → Outbox → 下载 Worker → Runner → MinIO 首次 attempt 成功。持久制品 51933107 bytes / 564431 ms，SHA-256 为 0ecb953ac0eaf54832d7e17e030db96aec0004e0cdeaec4ff2ed6bc7b2ec4be8；从对象存储重新取回后大小、哈希相符，FFmpeg 全片解码退出 0。默认格式为 640×360 H264/AAC，不是高画质或所有内容能力证明。

17:30 CST 在原已登录 Chrome 点击“获取视频文件”，详情路由、账户菜单和播放器保留。API 记录 download-url 200 与 file 200，观察窗口 auth/me 新请求为 0。工具政策禁止打开 chrome://downloads，未绕过；默认 Downloads 中没有本次标题匹配的文件，因此只通过服务端交付与无登录重建，设备落盘继续未通过，不关闭 CQ-028。


活动任务再次为 0 后，仅用 `docker compose --env-file .env.prod up -d --no-deps --force-recreate` 重建三个 Runner，验证保存在环境中的三文件列表确实生效；没有再采集 Chrome。就绪后固定矩阵六项全部 succeeded：YouTube metadata/media 8253/11402 ms，抖音 2905/4223 ms，Reddit 3599/7887 ms。初次过期失败仍保留，重启复测通过不关闭 CQ-044。三个候选容器已删除，生产只保留既有业务拓扑。


重建后从同一已登录 Chrome 的普通首页重新解析原长视频，创建新任务 `5c572d15-5d65-4a07-b441-6852c7d43bed`，首次 attempt succeeded；持久制品仍为 51933107 bytes / 564431 ms，SHA-256 与重建前一致。没有再次更新来源，证明本次有效版本跨生产重建后仍能由正常 Worker 完成新下载。三个正式文件来源保留，三个临时候选来源副本、候选容器及七个已记录候选工作目录已清理。API/前端 8111/8101 均为 200，生产 Runner healthy，Worker/Canary running。

当前结论：本机来源权限归因、按平台采集安装、生产 Compose 来源覆盖、有效文件的重建复用和原 YouTube 网页服务端交付通过。后端来源实现全量 1793 passed / 2 skipped，Compose 增量 11 项通过，两个代码提交远端 CI 成功；验收文档相对链接 77 个均存在，diff 空白检查通过。首次 YouTube 下载偶发失败、实际 Cookie 轮换、腾讯视频 120 秒解析超时、浏览器设备落盘、第二机器、全平台完整矩阵与持续高可用仍未通过；035 整体保持进行中。

## 32. agent-browser 跨平台路线复核（2026-09-15）

按 `agent-browser` 的匿名独立浏览器会话访问固定公开样本，并与同一时段生产 Canary 分开对照。浏览器只用于观察访客页面，不导入用户 Chrome 登录态、不接受登录弹窗，也不把页面可见当作下载成功。

| 平台组 | agent-browser 访客页面 | 生产固定探针 | 判定 |
| --- | --- | --- | --- |
| Bilibili、TikTok、小红书、快手 | 内容页、标题或视频区域可到达；Bilibili 有登录/试看提示，快手可访问树稀疏 | metadata 与完整 media 均 succeeded | 保持 anonymous，不增加会话来源 |
| X、Instagram、Facebook、Pinterest | X 两次 HTTP 403；Instagram Reel 可见；Facebook 内容标题在登录覆盖层下可见；Pinterest 首次连接关闭后重试进入公开页 | 四平台 metadata 与完整 media 均 succeeded | 保持 anonymous；网页登录/403 不触发 Cookie 路线 |
| YouTube | 标题可见，播放器要求登录确认非机器人 | 本轮 metadata `inspection_failed` 9488 ms；此前重建后 metadata/media 通过 | 保持单平台文件来源，继续 CQ-027/CQ-044 的间歇失败与轮换修复 |
| 抖音 | 标题、播放器和时长可见，高清/互动要求登录 | operator metadata succeeded；近期完整 media succeeded | 当前单平台文件来源保持，不能仅凭网页可见改 anonymous |
| Reddit | 两次均显示 network security block，只提供登录或工单入口 | operator metadata/media succeeded | 单平台文件来源必要 |
| 微信视频号 | 分享标题和互动数据可见 | 专用 operator metadata/media succeeded | 保持元宝专用来源，不套用普通 Chrome 文件 |
| 腾讯视频 | 页面、剧集和播放器可见，并明确展示会员全集 | metadata succeeded 18007 ms；media `inspection_timeout` 120048 ms | 新增 CQ-045，按提取器/权益/DRM 分段诊断，不增加通用 Cookie |

同期完整 metadata 矩阵共 23 项，22 succeeded，只有 YouTube 失败。选定九个平台 media 中 Bilibili、TikTok、小红书、快手、X、Instagram、Facebook、Pinterest 八项 succeeded，腾讯视频失败。由此确认其他匿名平台目前不需要复制 YouTube 的来源结构；失败路线按平台单独治理。

矩阵结束后对 YouTube 再执行一次同样 metadata，5410 ms 返回 `provider_session_expired`。同一生产文件先通过重建后的完整 media，随后依次出现 `inspection_failed` 和明确过期，证明静态快照缺少持续维护能力。不得继续通过反复手工 capture-chrome 伪装稳定；下一修复须由独立单平台维护者在用户请求之外验证、原子发布新 revision，Runner 继续只读精确版本。该维护者尚未实现，CQ-044 保持 P1 未修复。

本次本地 dogfood 报告和 17 张截图保存在忽略目录 `dogfood-output/platform-adaptation/`，不作为源码或长期凭据提交。没有执行私人内容、会员内容、图集、直播、全部画质或第二机器验证；完整矩阵的单样本结果不能扩张为全平台永久支持。
