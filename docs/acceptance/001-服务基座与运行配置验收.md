# Acceptance 001 服务基座与运行配置

## 状态与冻结输入

- 状态：Blocked（2026-07-20）；自动化门禁与真实依赖健康已执行，冷构建/覆盖率/SBOM 证据仍缺失。
- Design：[001 服务边界与总体架构](../design/001-服务边界与总体架构设计.md)。
- PRD：[001 服务基座与运行配置需求](../prd/001-服务基座与运行配置需求.md)。
- Plan：[001 服务基座与运行配置计划](../plans/001-服务基座与运行配置计划.md)。
- 冻结规则：实现开始后只填写实际证据、结果和结论；标准变化必须先重新确认上游文档。

## 1. 阶段开始门禁

- 用户已明确授权实现，Plan 001 仍为 Ready，当前 branch、基线 commit 和已有工作区改动已记录。
- Python、uv、Docker/Compose 可用；PostgreSQL、RabbitMQ、MinIO 镜像及非敏感环境变量名称确定。
- `S001-T01` 的目录/依赖禁止项检查可先产生预期 Red；任务范围不包含业务表、下载逻辑或业务 API。
- 任一项不满足时本阶段为 `blocked`，不得创建业务代码。

## 2. 逐任务验收

| 任务 | 必须通过 | 最低证据 |
| --- | --- | --- |
| `S001-T01` | 直接 `src/`/`tests/`、锁文件和依赖清单符合边界，无包装层 | 文件树、依赖扫描、Red/Green 测试 |
| `S001-T02` | Settings 对缺失/非法/环境覆盖行为确定，无硬编码 Endpoint/Secret | 配置负例、`.env.example` 对照、Secret 扫描 |
| `S001-T03` | API/Worker 可独立启动、停止并正确管理生命周期 | 进程测试、启动/关闭日志和退出码 |
| `S001-T04` | 仅使用 PostgreSQL/RabbitMQ/MinIO 官方客户端，连接和断连准确 | 真实连接、逐依赖停启和源码扫描 |
| `S001-T05` | Compose 只有约定五个单元、受控卷、健康依赖且无明文 Secret | `docker compose config/ps`、卷与镜像扫描 |
| `S001-T06` | 依赖版本、checksum、许可证、SBOM 和两次冷构建可重复 | lockfile、SBOM、许可证与构建记录 |
| `S001-T07` | 无非 MVP 目录、候选基础设施、适配层和模板残留 | 文件/依赖/源码禁止项扫描 |

当前任务未 `passed`，下一任务不得开始。

## 3. DAC/AC 验收矩阵

| 标准 | 通过条件与证据 |
| --- | --- |
| `S001-DAC-01` / `S001-AC-05` | 供应链记录完整且两次冷构建一致 |
| `S001-DAC-02` / `S001-AC-01`、`S001-AC-02` | API/Worker/Compose 架构与进程行为一致 |
| `S001-DAC-03` / `S001-AC-06` | 目录、依赖、API 和禁止项扫描零违规 |
| `S001-DAC-04` | runtime、lockfile 和可回滚镜像全部冻结 |
| `S001-DAC-05`、`S001-DAC-06` / `S001-AC-03` | 只有 PostgreSQL/RabbitMQ/MinIO 且无适配层 |
| `S001-DAC-07` / `S001-AC-03`、`S001-AC-04` | 配置负例/覆盖通过，镜像与源码无 Endpoint/Secret 硬编码 |

## 4. 阶段验证

- `uv lock --check`、`uv sync --frozen`。
- `uv run ruff check .`、`uv run ruff format --check .`、`uv run mypy --strict src`。
- `uv run pytest tests/unit tests/integration --cov=src --cov-branch --cov-fail-under=90`。
- 两次 `docker compose up -d --build` 冷启动，执行 `docker compose config --quiet` 与 `docker compose ps`。
- 逐项移除必填变量并扫描配置、镜像、日志、依赖与许可证。

## 4.1 实际执行证据与结论

- `uv lock --check`、`uv sync --frozen`、`uv run --frozen pytest -q`、Ruff、格式、严格 mypy、`compileall`：退出码 0；pytest `43 passed`，证据为本次工作区终端输出。
- `docker compose --env-file .env.example config --quiet`：退出码 0；API/Worker/PostgreSQL/RabbitMQ/MinIO 均启动，`/health/live` 与 `/health/ready` 返回 200，readiness 三依赖均为 `ok`；RabbitMQ ping 与 MinIO health 返回 0/200。
- 代码扫描确认服务端源码直接位于 `src/`，未引入 Redis/Celery/第二对象存储；密钥只从 Settings/.env 读取。
- `pytest --cov --cov-fail-under=90`、两次冷构建一致性、许可证/SBOM 和必填配置负例未形成可复核证据，故 `S001-T06` 及相关 DAC/AC 为 `blocked`，阶段结论为 `Blocked`，不得宣称 `Accepted`。

## 5. 证据与结论填写规则

执行后为每个任务和 DAC/AC 填写：实际命令/操作、退出码、证据位置、`passed/failed/blocked`、复核人。任一 `failed` 则阶段 `rejected`；存在 `blocked` 则阶段 `blocked`；全部强制项 `passed` 才能改为 `Accepted` 并允许开始 S003。
