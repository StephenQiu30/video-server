# Acceptance 002 视频解析、下载与文件交付

## 状态与冻结输入

- 状态：Blocked（2026-07-20）；媒体策略、单元门禁与 MinIO 文件交付已执行，真实受控视频全链路证据缺失。
- Design（已归档）：[002 视频解析、下载与文件交付](../design/archive/002-视频解析下载与文件交付设计.md)。
- PRD（已归档）：[002 视频解析、下载与文件交付需求](../prd/archive/002-视频解析下载与文件交付需求.md)。
- Plan：[002 视频解析下载与文件交付计划](../plans/002-视频解析下载与文件交付计划.md)。
- 冻结规则：实现开始后只填写实际证据、结果和结论；标准变化必须先重新确认上游文档。

## 1. 阶段开始门禁

- Acceptance 001、003 当前均为 `Blocked`；Plan 002 为 Implemented，但本阶段不能据此宣称前置验收已通过。
- 受控合法公开视频矩阵、固定 yt-dlp/FFmpeg/ffprobe 和真实 MinIO 可用；平台样例与许可来源已记录。
- 当前 branch、基线 commit、已有改动和 `S002-T01` URL 攻击矩阵 Red 已记录。
- 任一项不满足时本阶段为 `blocked`，不得开始 S002 或 S004。

## 2. 逐任务验收

| 任务 | 必须通过 | 最低证据 |
| --- | --- | --- |
| `S002-T01` | 协议、用户信息、端口、DNS 和重定向 SSRF 全部被正确处理 | 攻击矩阵、未访问私网证据 |
| `S002-T02` | 只解析支持的公开单视频，live/playlist/login/DRM/超时准确失败 | 真实 yt-dlp 输出与错误码 |
| `S002-T03` | 格式去重、排序、单/分离流和未知大小标准化稳定 | 格式快照和排序断言 |
| `S002-T04` | 会话、过期 source/format 和 selector 不泄露/不可伪造 | 仓储/服务测试与源码扫描 |
| `S002-T05` | 单文件/分离流下载、stream copy、注入防护和资源上限准确 | FFmpeg 命令数组、退出码和流信息 |
| `S002-T06` | ffprobe、checksum、MinIO 私有上传和预签名交付准确 | 真实下载、SHA-256、过期/越权测试 |
| `S002-T07` | 成功、失败、超时和退出后临时目录均确定清理 | 残留目录检查 |
| `S002-T08` | 固定版本失效能被发现，升级烟测和镜像回滚可执行 | 失败记录、升级与回滚结果 |

当前任务未 `passed`，下一任务不得开始。

## 3. DAC/AC 验收矩阵

| 标准 | 通过条件与证据 |
| --- | --- |
| `S002-DAC-01` / `S002-AC-01` | 真实 inspect 与标准化格式快照一致 |
| `S002-DAC-02` / `S002-AC-02` | URL/SSRF/媒体攻击矩阵全部通过 |
| `S002-DAC-03` / `S002-AC-04` | 合并产物流信息、音视频和播放器检查通过 |
| `S002-DAC-04` / `S002-AC-03` | selector/provider ID 不进入前端契约或日志 |
| `S002-DAC-05` / `S002-AC-05` | 资源上限、故障注入和清理准确 |
| `S002-DAC-06` / `S002-AC-06` | MinIO 文件、授权、期限和 SHA-256 一致 |
| `S002-DAC-07` / `S002-AC-07` | 版本失败、升级烟测与回滚证据完整 |

## 4. 阶段验证

- `uv run pytest tests/unit/media -q`。
- `uv run pytest tests/integration/yt_dlp tests/integration/ffmpeg tests/integration/minio -q`。
- 完整覆盖率、Ruff、格式和 mypy 门禁全部通过。
- 对单文件、分离流、未知大小和失败样例记录工具版本、退出码、流信息和 SHA-256。
- 攻击样例证明未访问私网；Mock 不替代 yt-dlp、FFmpeg、MinIO 真实证据。

## 4.1 实际执行证据与结论

- `uv run --frozen pytest -q`：退出码 0，媒体/策略/状态/应用测试合计 `82 passed`；覆盖率门禁、Ruff、格式、严格 mypy、compileall 均退出码 0。
- SSRF、端口、userinfo/fragment、格式归一化、文件名、大小/时长限制、FFmpeg/FFprobe 参数和 SHA-256 逻辑均有单元覆盖。
- Docker API/Worker 镜像成功构建并包含 FFmpeg；真实 MinIO 上传、预签名 GET、SHA-256 对照和删除已在临时 Compose 环境执行并通过，但公开 yt-dlp 受当前网络/提供方限制，尚无可复核的真实受控视频样例完成“链接→文件”全链路，故 `S002-T02`、`S002-T06` 及真实交付 DAC/AC 仍为 `blocked`，阶段结论为 `Blocked`。
- MinIO 真实证据输出：`upload=0 http=200 bytes=31 sha256=a5c38f5ad5ecda305e190f516cb96f54eb2e12b8d186e50555c7925a738c7c54 remove=0`。

## 5. 证据与结论填写规则

执行后逐项填写命令、退出码、证据、结果和复核人。全部任务及 S002 DAC/AC 为 `passed` 才能改为 `Accepted` 并允许开始 S004；任一失败为 `Rejected`，真实样例/依赖/证据缺失为 `Blocked`。
