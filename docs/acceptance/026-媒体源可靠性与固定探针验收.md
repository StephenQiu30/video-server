# 026 媒体源可靠性与固定探针验收

- 状态：Passed；22 个注册 Provider 的 metadata/media 固定矩阵全部通过
- 日期：2026-08-18

## 自动化

- [x] 封面认证 Blob、加载/失败、object URL 回收单元测试。
- [x] 封面保持官方 Next.js `Image` + shadcn `AspectRatio`，受保护资源
  只在 service 层完成鉴权适配。
- [x] 业务 TSX 无原生交互控件或直接 Radix import，组件边界测试锁定。
- [x] CSP 允许 `img-src blob:`，其他来源策略不扩大。
- [x] 下载重试完全匹配、1080→当前兼容规格改选和不相关画幅拒绝测试。
- [x] readiness 覆盖所有已配置 Operator Runner。
- [x] 固定矩阵与 22 个 Registry key 双向覆盖，metadata/media 成对。
- [x] canary 共享 `/work` 与本地/生产 Compose 一致。
- [x] canary 对格式轮换最多重检三次，DRM 等非轮换错误不重试。
- [x] 无当前 canary 证据的静态 verified 基线降为 unknown。
- [x] 平台状态分别展示最近真实下载与最近完整 Analysis 证据，不混淆探针成功和
  `verified` 恢复门槛。

## 2026-08-18 本地真实证据

- 全矩阵：重建后真实执行 22 Provider × metadata/media，共 44 条；metadata
  22/22、media 22/22 全部成功，两次矩阵命令均返回 0。
- 最终重启：最新镜像重建并强制重建 API、Worker、Canary、POT、出口代理和五个
  物理 Operator Runner 后，API readiness 通过，metadata 22/22 再次通过。
- media 不是模拟：22 个平台都执行最低兼容规格下载，并通过共享工作区路径、
  文件大小、SHA-256、时长、容器、视频流、音频流与 ffprobe 校验。
- YouTube、TikTok、抖音、小红书与 Reddit 均从物理隔离的单 Provider Operator
  Runner 读取桥接后的最小浏览器会话；匿名 Runner 不携带这些 Cookie。
- 恢复项：Vimeo 升级为当前非 DRM 官方样本；Reddit 使用当前会话；抖音修正
  landscape `download_addr` 尺寸语义；小红书升级为带官方 `xsec_token` 的当前
  Explore Feed URL；Tumblr 直接解析当前 `www.tumblr.com` 公开页，避免上游强制
  改写到持续 429 的旧 blog 子域路径。
- 浏览器：红果官方分享文本可解析为“佳偶错成 第1集”；认证 Blob
  由官方 Next.js `Image` 成功解码为 1920×1080，页面无“封面不可用”。可视证据
  为 `artifacts/hongguo-cover-authenticated-fixed.png`。
- 端到端：同一官方单集新建任务完成下载、ffprobe 与文件完整性校验；
  “导演拉片” AI Skill 分析成功达到 100%，生成 39 个连续分镜、24 个视觉
  资产、结构化视觉摘要和 Markdown/DOCX 可下载报告。证据截图为
  `artifacts/hongguo-analysis-completed.png`。
- YouTube 浏览器端到端：登录态自动复用后，从首页解析固定公开样本、渲染官方
  `Image` 封面、选择 MP4、创建任务、下载与完整性校验均成功；随后“导演拉片”
  Skill 完成分析并生成连续分镜、视觉摘要和 5 个视觉资产，页面控制台无错误。
  完整页面证据为 `artifacts/youtube-download-analysis-completed-full.png`。
- 平台状态：补齐 `provider_catalog_entries` 的 `hongguo_web` 幂等种子后，
  `/api/providers` 返回 23 个可见项（含微信视频号 unsupported），红果官方分享
  在页面中明确显示“当前单集”能力边界。证据为
  `artifacts/provider-status-hongguo-visible.png`。
- 媒体证据展示：平台页已分别呈现最近真实下载和最近完整 Analysis 时间；22 个
  注册 Provider 均显示 2026-08-18 真实下载证据。截图为
  `artifacts/provider-status-real-media-evidence.png`。

## 发布结论

公共下载链路、封面、会话桥接和固定矩阵修复可交付。22 个注册 Provider 在本次
环境与时点均真实可用；这不是对第三方永久可用性的承诺，发布后仍由持续 canary、
稳定错误分类与恢复迟滞防止静态状态掩盖未来回归。
