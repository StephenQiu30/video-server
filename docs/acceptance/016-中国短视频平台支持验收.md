# 016 中国短视频平台支持验收

- 状态：Passed
- 日期：2026-08-11
- 关联 Design：`docs/design/016-中国短视频平台支持设计.md`
- 关联 Research：`docs/research/004-GitHub短视频提取方案调研.md`

## 1. 功能与安全

- [x] 抖音现有 `Douyin + share_page` 插件保持不变，没有因官方 Open API 能力判断而降级。
- [x] 快手桌面公开作品 URL 规范化到第一方移动分享页。
- [x] 快手 App/PC 短链可解析，最终重定向限制在第一方域名族。
- [x] `share_info.photoId` 与最终 URL 作品 ID 必须一致。
- [x] `VIDEO` 返回完整 MP4 格式；图集稳定拒绝，不截断为单图或伪装成视频。
- [x] Provider 为 anonymous，不接收 Cookie、账号、公共解析 API 或任意 yt-dlp 参数。
- [x] DRM、会员、private 和客户端签名绕过继续禁止。

## 2. 代码契约

- [x] `KuaishouPublicIE` 由固定 `--plugin-dirs app/runner` 加载。
- [x] `kuaishou-public-v1` Profile 固定 Android impersonation、能力、重试和 canary 名称。
- [x] 下载来源统计识别 `KuaishouPublic`。
- [x] 失效短链映射 `provider_link_unavailable`；图集映射 `provider_unsupported`。
- [x] `GET /api/providers` 返回快手 registered/extractor/verified。
- [x] 单元、Runner 命令、静态路由和 API 契约定向测试通过：46 passed。
- [x] Next 静态导出的 `downloads/detail.txt` 正确返回 route payload，任务 ID 查询参数不再被改写为 `detail.txt`。

## 3. 真实媒体证据

- [x] 公开直链 inspect：作品 ID 一致，`extractor_key=KuaishouPublic`，72.533 秒，2 个 H.264/HEVC + AAC 格式。
- [x] 公开短链 inspect：有效短链分别解析出 21.766 秒与 567.566 秒视频；审核中旧样本稳定拒绝。
- [x] 真实 CDN ffprobe：MP4 包含 720×1280 H.264 视频与 AAC 音频。
- [x] Compose Media Runner 完成完整媒体下载、re-inspect、remux、ffprobe 与 SHA-256：21.781 秒、2,661,003 bytes、1 路 H.264 视频、1 路 AAC 音频。
- [x] 当前 Compose API → RabbitMQ → Worker → MinIO E2E 成功；数据库与 MinIO object stat 均确认 2,661,003 bytes `video/mp4` 产物。

## 4. 页面与发布门禁

- [x] agent-browser 确认 Provider 状态页显示快手“已验证”；桌面 1440px 与移动 390px 的 header/main 左右边界一致，均无横向溢出。
- [x] agent-browser 确认注册后关闭并恢复浏览器会话仍保持登录；快手解析、创建任务、WebSocket 状态更新至下载完成均通过。
- [x] 后端 ruff check、ruff format --check、mypy、pytest 全量通过：455 passed，1 个 POSIX-only 用例在 Windows 按预期跳过。
- [x] 前端 lint、format、test、build 全量通过：27 个测试文件、101 passed。
- [x] 开发/生产 Compose 配置解析通过。
- [x] main 推送后 GitHub Actions 全部通过：Repository rules、Backend quality、Frontend quality、Unified image 均成功。

## 5. 当前结论

快手公开单视频的解析、完整 Runner 下载、Compose API → MinIO、浏览器验收、本地全量门禁与 main CI 均已通过，且真实 metadata/媒体流证据成立。Phase 1 验收通过；自动 canary 聚合属于后续持续演进，不阻塞本次发布。
