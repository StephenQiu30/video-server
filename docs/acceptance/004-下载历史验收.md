# 004 下载历史验收

- 状态：In Progress
- 日期：2026-08-07
- 关联 Design：`docs/design/004-下载历史设计.md`
- 关联 PRD：`docs/prd/004-下载历史需求.md`

## 冻结前置条件

- [ ] 当前登录账户可稳定派生并复用 `owner_hash`。
- [ ] `download_jobs`、`media_inspections`、`media_formats` 已由当前 schema 初始化。
- [ ] API 与前端均通过现有项目依赖启动，不引入第二套路由或数据库访问方式。

## 逐项验收

- [ ] A1：同一账户可创建任务并在历史中看到；另一账户看不到该任务。
- [ ] A2：默认排序、分页、状态筛选和标题搜索均返回正确 `total`。
- [ ] A3：inspection 过期后历史仍返回标题和格式，且响应不包含敏感字段。
- [ ] A4：历史页统计、空状态、加载失败、重试和窄屏布局可用；页面符合 009 方案 3 的 `#FAFAFA` 画布、Geist、72px 无边框 Header、1456/1376px 两级隐形网格与 40/80/32/16px 响应式 gutter、近黑主操作、无边框填充控件与发丝 Separator；页内无可见 PageShell/Card/ring 外壳，完整数据态浏览器证据待补齐。
- [ ] A5：成功记录可进入任务详情，并沿用短时 URL 获取文件。
- [ ] A6：后端测试、前端测试、lint、格式、类型检查和构建通过。

## 证据记录

### 自动化证据

- `uv run pytest tests/unit/infrastructure/test_download_store.py tests/integration/api/test_download_routes.py tests/contract/test_swagger_docs.py -q`：退出码 0，14 passed；覆盖 owner 隔离、标题/状态筛选、公开字段、解析记录过期后仍可读和 OpenAPI operation。
- `uv run ruff check app tests`：退出码 0。
- `npm run lint`：退出码 0，包含 Biome lint 与 TypeScript typecheck。
- `npm run format:check`：退出码 0。
- `npm test -- --run`：退出码 0，11 files / 34 tests passed。
- `npm run build`：退出码 0，产出 Next.js 静态导出目录 `frontend/out`。

### 当前结论

历史功能的后端查询、API 契约、Next.js 页面和旧基线自动化验收已完成。页面使用 Radix UI + Tailwind CSS 构建响应式工具区和记录视图，并经 `@umijs/openapi` 生成客户端读取服务端分页事实。方案 3 视觉/浏览器回归以及真实 PostgreSQL、MinIO 与浏览器端到端验收尚未在本文记录，故整体状态保持 `In Progress`；不得用旧方案截图、SQLite、组件测试或构建结果替代当前真实依赖/浏览器证据。

全量后端测试另有 9 个与本功能无关的既有失败，集中在 Windows 进程组信号、workspace/analysis 运行时和未更新前的 OpenAPI 合同；本次已修正合同项并确认下载历史相关测试通过。
