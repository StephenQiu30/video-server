# 下载故障修复与持续可用性 SOP

## 1. 适用范围与当前状态

本 SOP 用于帧取的“项目重启后无法下载”“解析时反复要求登录”“单个平台间歇失效”等问题，从接报、定位、设计、实现一直执行到部署、回滚和真实交付验收。

**状态：公共出口、登录误分类、部署拓扑和 Web 下载页面刷新已分项修复；来源独立与换机仍未通过。** 详细设计见 [035 设计](../design/035-平台访问与会话恢复能力设计.md)。执行本 SOP 时必须重新采集运行状态，不能把历史记录当实时状态。

本 SOP 是执行入口，长期技术决策与架构规范继续维护在既有设计文档中：

| 资料 | 唯一职责 |
| --- | --- |
| [Server AGENTS.md](../../AGENTS.md)、[App AGENTS.md](../../../video-app/AGENTS.md) | 仓库边界、开发、验证、提交规则 |
| [035 Design](../design/035-平台访问与会话恢复能力设计.md) | 策略、来源所有权、修订与恢复设计 |
| [030 设计](../design/030-运行故障隔离与恢复设计.md)、[031 设计](../design/031-Linux无人值守运行设计.md) | 进程隔离、排空、重启与文件来源恢复 |

初期共同出口、部署输入和误分类已有分项交付，不重复执行已关闭缺陷。后续按 **S0 来源诊断 → S1 无日常 Chrome 恢复基线 → S2 平台增量实验 → S3 单来源维护 → S4 真正换机 → S5 持续适配** 推进；未通过候选不得加入默认路线。历史调研与方案演进通过 Git 追溯。

先判断用户是换客户端访问同一服务，还是迁移整个服务。前者不搬平台来源，后者按规范恢复业务数据、制品、配置、密钥与来源，防止旧备份复活撤销授权。新恢复包/维护者尚未实现，当前命令沿用 [008 手册](008-个人部署重启与换机手册.md)，不把目标流程当成已经可执行的工具。

## 2. 执行规则

1. `Video/` 是父目录；`video-server` 和 `video-app` 分别检查、修改、验证与交付。开始前保留已有未提交改动，不清理用户工作树。
2. 先对齐 Design → PRD → Plan → Acceptance，再执行本切片的 Red → Green → Refactor。既有文档已定义的内容直接复用，不复制一套新设计。
3. PostgreSQL 是任务状态事实来源；复用已有数据库、RabbitMQ、Redis/Valkey、MinIO。不能为本机验证启动 CI 基础设施夹具，也不能用清库恢复服务。
4. 只有 schema 确实变化才更新并应用 `backend/sql/schema.sql`；同时验证空库和已有当前态库，使用现有服务中的隔离测试空间。纯 Compose 或错误分类修复不执行数据库结构变更。
5. Provider 来源与 FrameFetch 登录分开。普通业务请求、App、日志、截图、Git 均不接收原始 Cookie/Token；只读取已授权平台来源。
6. 保留匿名/受控 Runner 隔离、出口 ACL、下载前重解析、严格 context、Outbox、任务幂等与制品验证。不通过换账号、放开私网或忽略媒体失败来获得绿色结果。
7. 已有用户授权继续有效，不机械重复询问。实施过程中新增目标机器、账号、破坏性操作或超出现有范围的发布动作，按当时授权范围处理；本 SOP 本身不表示这些动作已经执行。
8. 每个可独立说明和回滚的小切片单独交付。Server 按 AGENTS 完成后本地提交；App 仅在用户明确要求提交时提交。推送、分支、PR 与外部发布按各仓库授权规则执行。

## 3. 总流程与执行记录

| 步骤 | 输入 | 主要产出 | 转入下一步的条件 |
| --- | --- | --- | --- |
| S00 接报与基线 | 现象、当前仓库/环境 | 故障记录、影响范围、已有改动清单 | 目标和现象可复现或缺失信息明确 |
| S01 分层定位 | 基线、脱敏探测 | 故障层级、证据、CQ 对应项 | 不把未知原因当成账号失效 |
| S02 固定切片契约 | 根因、035 文档链 | 本切片 Design/PRD/Plan/Acceptance | 修改位置、反例、通过条件、回滚均明确 |
| S03 最小共同出口修复 | CQ-029 | tmpfs 修复、异常重启实验 | 完整配置与隔离恢复通过，具备恢复部署条件 |
| S04 固化部署与预检 | CQ-031 | 持久部署输入、路线一致性、版本清单 | 不依赖消失的临时文件 |
| S05 分类和来源改造 | CQ-030/020、依赖项 | 正确动作、来源修订、候选依赖组合 | 确定性测试和单平台必要性验证通过 |
| S06 候选验证 | 本切片代码/镜像 | 模块检查、契约、真实小样本结果 | 本切片必需项均有结果 |
| S07 受影响服务部署 | 候选版本、回滚资料 | 实际运行版本、恢复状态 | 排空和依赖条件满足，运行版本核对完成 |
| S08 真实业务与重启验收 | 已部署版本 | 原链接/固定矩阵/文件/重启证据 | 声明范围内所有必需项通过 |
| S09 持续监测 | 已验证的平台能力 | 定时目标、失败诊断与退化状态 | 监测实际运行，证据年龄可信 |
| S10 关闭与交付 | 全部证据 | CQ 状态、Acceptance、提交与残余项 | 修复范围和证据边界一致 |

表格是依赖顺序，不要求每个小切片都等待 S05 全部结束。S03 通过后可单独走 S06 → S07 → S08，先恢复共同出口；再推进其余切片。任何必需项失败，进入第 12 节处理，不把失败改成跳过。

在 035 Acceptance 中为本次执行新增一个记录，使用以下模板；原始材料只存入受控、Git 忽略目录，文档只保留脱敏摘要和校验值：

```text
执行编号/时间/执行者：
目标机器/系统架构/部署入口：
症状/影响平台与能力/原始错误码：
Server HEAD/App HEAD/已有改动：
当前业务镜像与代理镜像 ID/候选镜像 ID：
Compose 文件清单/启用 profiles/平台默认策略：
本次 CQ/范围/预期结果/不涉及项：
复现步骤与 Red 结果：
修复 commit/Green 命令与退出码：
真实任务与样本版本/阶段结果/制品长度与 SHA-256：
重启方式/次数/恢复耗时/来源是否要求交互登录：
客户端与设备保存/播放结果：
最终状态 passed|failed|blocked/残余原因/下一动作：
回滚版本/回滚执行与验证结果（如发生）：
```

一个人可以承担执行和复核，但要分别记录“观察结果”和“结论”。没有独立审查就不能写成独立审查通过。

## 4. S00：接报、保护现场与确定入口

### 操作

1. 记录发生在哪个页面/客户端、哪一个平台、具体动作、可见错误和时间。用户原始链接私下保留，公共记录使用样本 ID；确认是 FrameFetch 登录页跳转，还是平台授权提示。
2. 标明触发方式：普通进程 restart、Compose 重建、Docker Desktop 重启、整机重启、部署升级或无重启。未知时先记未知，不自行归因为 Cookie 过期。
3. 分别记录两个仓库状态。以下命令从 `Video/` 执行：

```bash
git -C video-server status --short --branch
git -C video-server rev-parse HEAD
git -C video-app status --short --branch
git -C video-app rev-parse HEAD
docker ps --format '{{.Names}}\t{{.Image}}\t{{.Status}}'
```

4. 从当前容器标签和已有运维配置核对真正生效的 Compose 文件，而不是凭 `.env.prod` 文件名猜测。用定向字段读取，不输出完整 `docker inspect` 的 Env：

```bash
docker inspect --format '{{index .Config.Labels "com.docker.compose.project.config_files"}}' video-api
docker inspect --format '{{.Image}}' video-api
docker inspect --format '{{.Image}}' egress-proxy
docker inspect --format '{{.State.Status}} {{.State.ExitCode}} {{.RestartCount}}' egress-proxy
```

5. 记录活动下载、待处理任务和正在登录/导出的会话。尚不清空队列、重启 Worker、关闭用户登录窗口或重建全栈。

### 通过与失败处理

通过：目标仓库、运行入口、影响层级和已有改动已经记录。若缺少用户原链接，可继续共同基础设施诊断；最终原链接验收保持待补。不得把前次研究中的 795 次重启或四个平台来源探测结果写成本次新证据。

## 5. S01：按层诊断，先排除共同故障

按下表从上到下检查，避免对每个平台重复刷新账号。

| 层级 | 操作 | 判定与下一动作 |
| --- | --- | --- |
| 核心 API | 访问 `/health/ready`，核对核心依赖 | 核心不可用先诊断 DB/MQ/Redis/MinIO/schema；不能直接改 Provider |
| 公共出口 | 代理状态、退出码、定向日志、Runner 到代理的 TCP/协议探测 | 多平台共同失败且出口不可达，优先 CQ-029/部署；保留来源 |
| Runner | 容器、DNS、端口、二进制、workspace、readiness | 缺实例/旧镜像/权限错误归运行配置，不归登录 |
| 会话来源 | 文件存在/权限或无 Cookie 的代理 probe；记录 source kind | 来源通道可用不等于账号有效；离线/锁定/超时不注销账号 |
| 平台解析 | 固定来源和上下文的小样本 metadata；保留阶段错误 | 明确区分空响应、429、挑战、权限、页面结构变化 |
| 媒体与制品 | 下载、ffprobe、完整解码、MinIO 长度/哈希 | 解析成功但媒体失败，定位 CDN/音轨/传输/校验 |
| 客户端 | 实际文件保存、播放、站内登录状态 | 后台成功不覆盖 Chrome 策略、手机 HTTPS 和设备保存失败 |

核心 API 与代理只读检查：

```bash
curl --fail --silent --show-error --max-time 5 http://127.0.0.1:8111/health/ready
docker logs --tail 50 egress-proxy
```

日志在本地检查，写入文档前脱敏。涉及平台/业务容器的日志不能直接整段复制；不要输出 Cookie、带签名的媒体地址、完整请求头或 token。

**诊断决策：**

- 看到残留 `/tmp/squid.pid`、PID 1、`egress-proxy` 连续退出或端口策略异常，进入 S03；不要要求所有平台重新登录。
- 端点缺失、来源方式与部署不符、引用临时文件，进入 S04。
- 已带来源仍因 Fresh cookies/空 JSON 提示登录，进入 S05-A。
- 来源明确不存在/失效才按来源类型恢复；涉及账号变更、修订不明，进入 S05-C。
- 出口正常、来源正常、某平台媒体失败，进入平台专项；保存首轮失败结果。

## 6. S02：先固定本次切片的四份文档

对本切片检查以下内容，缺少的直接补入 035 对应文档，不新建平行规则：

| 文档 | 开始编码前要明确的内容 |
| --- | --- |
| Design | 根因、状态所有者、受影响模块、保留边界；复用现有能力的理由 |
| PRD | 具体触发与预期行为，例如“来源仍有效，重启后不要求交互登录” |
| Plan | 修改/测试文件、依赖、候选部署、服务范围、回滚步骤 |
| Acceptance | 失败反例、真实样本、环境、文件校验、重启场景、失败状态 |

在 CQ 登记中写明“已确认待修复/方案就绪”。已有 114 项基线测试通过不能关闭 CQ-029/030，因为它们没有覆盖正确的重启与错误语义。文档准备完成只允许进入实现，不表示上线或平台能力已验收。

## 7. S03：共同出口最小修复

### Red：稳定复现

1. 用当前已存在的 Squid 镜像 ID 创建临时容器，`network none`、不发布端口、不接业务网络或凭据；只操作本次创建的容器及自动卷。
2. 保留当前前台启动和 PID 文件路径，验证首次启动成功。
3. 对临时容器执行强制终止后同容器 start，记录原配置退出码及残留 PID 错误。正常 stop/start 单独记录，不能替代强杀场景。
4. 复现结束清理本次临时资源，不执行 Docker 全局 prune。

### Green：候选实现

同时修改两个业务 Compose 文件中 `egress-proxy`：

```yaml
tmpfs:
  - /tmp:rw,noexec,nosuid,size=16m,mode=1777
```

保持固定 Squid 镜像、`pid_filename /tmp/squid.pid`、PID 健康检查、受控网络和目标 ACL。不要仅删除一次 PID 文件，也不要把 PID 禁用后保留已失效的 `squid -k check`。

### 验证与转出

- 原配置失败、候选成功的对照成立。
- 使用完整实际 Squid 配置验证权限、启动、转发及私网目标拒绝；最小 `deny all` 实验不代替此项。
- 正常停止恢复和强杀恢复分别至少 3 轮，保持同一候选镜像、配置与测试身份。
- 两个业务 Compose 均通过静态解析；停止宽限仍覆盖既有预算。

完成后以本切片走 S06/S07/S08；真实媒体验收之前 CQ-029 保持“已修复待验收”。如果原镜像无法复现，先重新核对日志、PID 路径和启动参数，不能直接修改无关登录代码。

## 8. S04：部署固化、预检和恢复配置

### 8.1 固化最终输入

1. 列出现行 Compose 文件、profiles、默认策略、Operator 实例和对应来源方式。
2. 对照当前容器镜像 ID，重建缺失临时覆盖所表达的最终配置，写到持久部署配置中。不能凭当前可变 `prod` tag 推断历史镜像，也不能随手删掉覆盖层。
3. 记录无 Secret 的发布清单：代码 commit、镜像摘要、引擎/插件/JS/FFmpeg/PO Provider 版本、宿主来源代理代码版本、配置文件清单。
4. `.env.prod`、平台来源和敏感配置继续私有存放；不把展开全部变量的 Compose 配置提交 Git。

### 8.2 固定本次命令入口

以下示例从 `video-server/`、zsh 执行，适用于当前单文件生产拓扑。来源方式由 `docker-compose-prod.yml` 内各平台 Runner 固定；不要临时增加覆盖文件改变来源。

```bash
FF_COMPOSE=(docker compose --env-file .env.prod -f docker-compose-prod.yml)
"${FF_COMPOSE[@]}" config --quiet
"${FF_COMPOSE[@]}" config --services
"${FF_COMPOSE[@]}" ps
```

`FF_COMPOSE` 只是本次 shell 的明确命令参数，不是新部署服务。`.env.prod` 同样固定 `COMPOSE_FILE=docker-compose-prod.yml`，显式参数和日常入口必须解析为同一拓扑。

### 8.3 预检清单

- 每个默认/允许受控策略都有对应 Runner 和正确 source kind；缺失项选择恢复已声明路线，或明确移出允许集合，不能静默换匿名/账号。
- 平台文件挂载与权限正确；动态来源用无 Cookie probe；不批量导出全部浏览器凭据。
- 镜像摘要与发布清单一致；不混用旧 Worker、新 Runner 和不兼容 RPC。
- workspace、依赖、必要 schema 完整；基础设施直接复用。
- 正式宣称启用的 Canary 有真实目标；空列表显示“监测未配置”。

未来如补自动预检 CLI，复用现有 Python 工具入口，并先写对应测试。本 SOP 不提供一个尚未实现的 `doctor` 命令冒充现有能力。当前可用定向 Docker 检查、管理员运行诊断和既有健康接口完成清单。

## 9. S05：分类、依赖、会话和平台专项

### S05-A：修复登录误分类

**修改位置：** `provider_errors.py`、`integrations/media_runner.py`、相应测试；只有用户动作/文案发生变化才调整 Web/App 对应映射。

先增加或修改这些反例：

| 输入/场景 | 必须断言 |
| --- | --- |
| 已认证 + 空 JSON + Fresh cookies | 不直接宣判未登录；无明确原因时保留临时失败 |
| 未认证 + 同样模糊错误 | 不能只凭字符串要求账号登录，访客验证与账号区分 |
| 明确没有来源 | 来源缺失准确提示，不自动寻找其他账号 |
| 明确过期/撤销 | 准确失效，旧 context 不继续执行 |
| 429 与混合登录提示 | 优先明确限流，遵守冷却，不循环刷新账号 |
| 来源锁定/代理断连 | 运行暂不可用，保留已有授权 |
| 挑战页/验证码 | 平台验证失败，不自动反复尝试 |
| 平台 422/429/503 | FrameFetch 登录保持；不跳转站内登录页 |

实现遵循证据优先并复用既有稳定错误码，旧测试中不正确的期望同步修正。额外重试必须共享绝对 deadline、同一 context 和总预算，避免 Worker/HTTP/yt-dlp/plugin 多层重试放大；冷却不得因重启、换 Cookie 或删除任务清空。

### S05-B：bgutil 配套依赖升级

按 021 的来源记录复核目标版本和发布变化，在候选环境成对更新插件、Provider 与项目 supervisor 的版本约束。保持专用网络，验证默认监听变化不会阻断 Runner，确认错误响应仍按正确原因分类。

验证顺序为：依赖/监听契约 → 原网络可达 → token 获取 → 同样本 metadata/media → Provider 重启后重测。本机未发布宿主端口的事实不能替代依赖修复，也不能直接套用默认公开监听部署的受影响结论。此项独立提交，不混入代理 tmpfs 修复。

### S05-C：会话来源修订与复用

1. 先读现有 `provider_sessions.py`、`provider_cookie_file.py`、来源协议、宿主代理与 `yuanbao_session.py`。保留已实现的专用元宝持久化。
2. 明确来源持久保存什么，操作副本销毁什么；`source_id/subject_epoch/revision_id` 归来源所有者，普通任务和 API 只持不透明引用。
3. 针对 CQ-020 先补“解析后切换账号必须拒绝旧 context”的 Red 测试，不能仅增加缓存降低读取次数。
4. 实现精确修订领取、单写者/并发约束、原子发布与撤销；领取失败不能自动改用最新修订或其他主体。
5. 文件来源保持只读；下载操作继续用一次领取的 jar 完成重解析和下载。来源内的更新与业务操作领取分开。
6. 连续操作验证有效授权可复用；账号变更、文件替换、来源锁定、进程崩溃、过期、撤销、重启逐项测试。
7. 仅有平台允许且可验证的更新方法时才实现自动更新；没有该证据的来源维持人工授权入口和准确状态。不要新增空的通用 Session Broker 或默认启动全平台浏览器。

**通过条件：** 一个选定平台完整通过来源语义和真实媒体验证，再逐平台扩展。`probe=true`、Cookie 文件存在、字段名齐全都不构成授权有效证明。

### S05-D：平台专项

保持 021 的 23 平台矩阵；视频号间歇失败、Vimeo 音轨、红果 ffprobe 5xx、优酷/腾讯完整个人下载分别对应已有 CQ/专项。每项固定样本、策略、引擎和出口，先复现再修复。

同一批次先失败后成功，必须保留两次结果。探测接口的 5xx 不归永久无格式；有音轨声明但元数据不足时不能静音误成功；试看不能算全片。某平台通过不得关闭其他平台问题。

## 10. S06：代码、契约和候选验证

### 10.1 按影响范围执行检查

纯 SOP/研究文档仅检查内容、链接、事实和 diff，不要求为文档跑全套业务测试。代码切片先执行针对性 Red/Green，交付时执行相应模块规范；不相关模块写明未运行及原因。

后端，从 `video-server/backend/` 执行：

```bash
uv sync --frozen --dev
uv run ruff check app tests
uv run ruff format --check app tests
uv run mypy app
uv run pytest
```

Web 有变更时，从 `video-server/frontend/` 执行：

```bash
npm ci
npm run format:check
npm run lint
npm test
npm run build
```

API 契约变化时，先从候选 API 的 `/openapi.json` 验证 operationId、schema 和错误契约。Web 按 `frontend/openapi2ts.config.ts` 的真实来源生成；App 按已核实的候选地址配置 `OPENAPI_SCHEMA_URL`，使用 `dart run tool/openapi.dart`。禁止手改生成代码或并行 DTO。

Web 的实际生成入口为 `npm run openapi`，接受本切片生成结果后用 `npm run openapi:check` 检查；App 对应 `dart run tool/openapi.dart --check`。各自在自己的仓库目录执行，先核实生成配置指向本次候选契约。

生成器的 `--check` / `openapi:check` 以已接受的生成基线检查漂移；有预期生成改动或 App 既有改动时，先区分本切片变化再判断，不把工作树已有差异当成本切片失败或顺带提交。

App 有变更时，从 `video-app/` 按其 AGENTS 执行：

```bash
flutter pub get
dart format --output=none --set-exit-if-changed .
flutter analyze
flutter test
flutter test integration_test
flutter build apk --debug
flutter build ios --simulator --no-codesign
```

真实设备测试需要已选设备和目标 API；缺少时保持该项 blocked，不冒充通过。格式检查不能用于清理其他人的既有修改。

### 10.2 容器与候选门禁

- 解析受影响的本机与生产 Compose，使用 `config --quiet`，不打印完整配置/Secret。
- 锁定候选镜像与依赖组合；验证需要的候选服务，复用现有基础设施。
- 先做本切片的小样本与失败注入，再进入发布范围的固定矩阵。
- 有 UI 变化时，按实际组件验证状态、键盘、深浅主题、390px/桌面表现；纯后端修复仍需原用户流程验证。
- 最后执行 `git diff --check`，复核源码、测试和四份文档状态一致。

**转出条件：** 本切片必需的确定性检查、配置和候选验证通过；所有未执行项有明确边界。接口变化涉及多个服务时，以兼容的整组版本为最小发布单位。

## 11. S07–S08：部署、真实交付与重启演练

### 11.1 部署前

1. 核对本切片已经获得的实施/目标环境授权，记录实际服务范围；不重复请求已经给出的授权。
2. 记录当前可回退镜像和持久配置，验证回退文件实际存在。只在数据/来源变化需要时准备受控备份，不复制整个 Chrome Profile。
3. 核实活动任务，使用既有排空/停止宽限。没有全局维护开关就不能假装执行了维护模式；在已授权窗口暂停新请求或等待排空，避免中断用户正在下载的任务。
4. schema 未变化则跳过 schema 操作；有变化按仓库当前态 SQL 规则完成验证后再应用。

### 11.2 只部署受影响的服务

公共代理切片在已固定 `FF_COMPOSE`、确认本地镜像 tag 与批准 ID 一致后执行：

```bash
"${FF_COMPOSE[@]}" up -d --no-deps --force-recreate --no-build --pull never --wait --wait-timeout 180 egress-proxy
```

命令非零即失败，不附加 `|| true`。`--no-deps` 适合这个已核实的单代理切片；不照抄用于尚未启动依赖的完整部署。

其他切片先把候选镜像写入持久发布配置，再显式列出受影响服务。API/RPC 变化通常涉及 `api`、`worker-download`、`provider-canary` 与相应 Runner；是否包含其他 Worker/前端，由本次调用关系决定。不要为解决一个代理故障重建所有业务镜像。

`restart` 仅用于镜像、配置、schema 均未变化的生命周期测试；配置更新必须重建才能生效。完整拓扑部署使用完整持久文件清单和 profiles，不能重新引用 `/tmp` 覆盖文件。

### 11.3 部署后逐层验证

1. 实际容器的镜像 ID、配置来源、source kind 与发布清单一致。
2. API 核心依赖、代理、Runner、来源各自探测通过；不能把 API 200 当成下载通过。
   抖音标准访客线路可在部署机读取脱敏准备状态：

```bash
docker exec provider-guest python -m app.workers.runner.provider_guest_manager status
```

输出仅包含 `state`、`stored_usable`、`published_lease_usable`、稳定 `reason_code`、`retry_at` 和有效期。`stored_usable` 表示数据库中的材料尚在有效期内；`published_lease_usable` 还要求当前容器共享卷中的短租约通过作用域、版本、权限和期限校验。退出码 0 仅表示已发布租约可供 Runner 读取，4 表示尚未准备；实际媒体是否可下载仍以 canary 为准。容器 healthy 只表示维护进程在运行。`cooling` 时按 `retry_at` 判断下次维护时间，禁止通过重启或增加身份／出口绕过平台限制。

访客线路遇到 `Fresh cookies ... needed` 时，应分类为 `guest_context_required`，不能因为使用了 Cookie 文件就标记为账户授权过期。API、下载 Worker 与 Canary 按响应上下文中的 `guest-N` 作废匹配版本；并发产生的新版本不受影响。访客维护服务在 30 秒冷却后重新准备，解析意图沿用 15 秒等待、最多 3 次媒体尝试及 180 秒总预算。平台明确的限流、验证或内容限制仍按各自错误处理，不通过会话轮换绕过。

3. 从固定矩阵选择本切片平台并跑 metadata/media。以下是**实际访问平台、创建诊断证据的操作**，仅在来源/样本和成本范围已明确后执行：

```bash
docker exec video-provider-canary python -m app.workers.canary.fixed_matrix --provider youtube --stage metadata
docker exec video-provider-canary python -m app.workers.canary.fixed_matrix --provider youtube --stage media
```

按实际平台替换 `youtube`。完整发布矩阵使用同一入口去掉 `--provider`，失败保持非零；结果不得通过换样本、降低默认格式或删除首轮失败来变绿。固定公共诊断样本不自动成为自有授权发布样本，区别见 [007 手册](007-固定Provider探针运行手册.md)。

4. 在正式 `http://localhost:8101`（API 8111）复测原用户链接：解析 → 创建任务 → Worker 执行 → 服务端制品完成 → 浏览器实际保存 → 播放。若本次是远程部署，用发布清单里的实际 HTTPS 入口，不使用内部容器地址。
5. 对真正保存到设备的文件执行校验。将下面变量替换成实际文件路径，不读取或展示媒体内容：

```bash
FF_MEDIA_FILE='/absolute/path/to/downloaded.mp4'
ffprobe -v error -show_entries format=duration,size:stream=codec_type,codec_name,width,height -of json "$FF_MEDIA_FILE"
ffmpeg -v error -xerror -i "$FF_MEDIA_FILE" -map '0:v?' -map '0:a?' -f null -
shasum -a 256 "$FF_MEDIA_FILE"
```

长度/哈希与对象存储记录比较，音视频轨与来源预期比较。真实无音轨内容与丢失音轨区分；合集/图集按其 ZIP/manifest 规则校验，不套单视频命令。浏览器点击保存和播放预览都不能替代本地文件存在。

### 11.4 重启矩阵

每一场景都记录前后任务状态、来源修订、恢复耗时和完整文件结果；会话仍有效的前提下，交互登录次数应为 0。

| 场景 | 验证内容 |
| --- | --- |
| 代理正常停止/start，至少 3 轮 | 无 PID 残留循环，协议转发和 ACL 正常 |
| 代理异常终止/start，至少 3 轮 | 先在隔离环境演练；目标环境演练需排空且处于本次范围 |
| Runner 重启 | 来源恢复、tmpfs 临时副本清理、同路线重新解析与下载 |
| Worker 重启/消息重投 | lease/heartbeat、幂等、重试预算和 cooldown 保留；无重复制品/扣额 |
| 整套业务容器重建 | 持久文件和镜像组合足够重放，不依赖临时输入 |
| Docker Desktop/整机重启 | 在目标机执行；来源代理、宿主登录/Keychain 条件、服务恢复明确 |
| 连续 20 次有效来源解析 | 无交互登录；读取/更新次数符合来源复用契约；失败单独记录 |
| 解析后来源更新/切换账号/撤销 | 精确修订领取或要求重新解析，不静默跨主体 |

App 另验真实 HTTPS、登录、下载、权限/保存和播放。模拟器、Web 或服务端成功不替代真机。24–72 小时持续观察记录平台波动、来源状态与请求预算；没有完成观察窗口就不能宣称持续性通过。

## 12. 失败处理与回滚

| 失败位置 | 立即动作 | 后续条件 |
| --- | --- | --- |
| Red 未复现 | 停止扩大修改，核对真实执行对象与版本 | 有稳定反例或明确证据再修 |
| 单测/契约/配置失败 | 留在候选阶段修复 | 不部署、不删除检查 |
| 候选真实样本失败 | 保留原错误与第一轮结果，判断内容/来源/网络 | 不通过换账号或降低校验放行 |
| 部署后新增核心回归 | 停止推广候选，回退受影响服务的已验证镜像/配置 | 回退后重新验证核心和原路线 |
| 来源失效/撤销 | 停发旧 epoch；一次准确通知来源所有者 | 显式恢复授权后重新解析，不恢复已撤销备份 |
| 单平台上游变化 | 降级对应能力，保留其他能力 | 该平台候选修复通过后再恢复 |
| 服务端成功、设备保存失败 | 保留制品，定位客户端/网络/浏览器策略 | 实际文件保存验证；不重复抓取平台掩盖 |

回滚步骤：

1. 记录失败版本、任务与证据；停止该候选继续接收受影响工作，保留任务和消息事实。
2. 从发布清单取出上一个已验证镜像与完整持久配置；确认文件/镜像存在。
3. 排空后重建受影响服务；回滚镜像若与当前 schema/RPC 不兼容，先制定安全前向修复，不能盲目降级。
4. 不清库、不 `down -v`、不删除冷却、不改写任务成功、不恢复已撤销凭据。全栈清理不属于本 SOP 的恢复手段。
5. 重新执行核心健康、受影响路线和文件交付验证，在 Acceptance 中记录回滚结果。

Generic 长尾线路发布后，待处理或在途的 `generic` 意图／下载任务要求支持 Generic 按 key 上下文与单媒体归属校验的 Runner。更早镜像会将其误判为不可重试的平台不支持；只有确认该类任务已排空，且维护窗口内已停止新接单，才能向更早镜像降级。当前没有单平台维护开关，不能以为仅暂停某个 Worker 就阻止了新意图。保留首个支持该契约的镜像作为后续最低回滚版本；无法满足排空条件时采用安全的前向修复，不直接降级。

对于出口代理故障，回到旧版本不等于恢复完成。应保留准确的不可用状态，修正候选
权限、版本或策略并再次验证，不能用单次 TCP 健康检查替代真实代理策略验收。

## 13. S09：持续平台维护

1. 复用现有 Canary 表、固定矩阵与真实任务证据，不新建平行状态系统。每个承诺的能力配置自有/授权样本、样本版本、access mode、预算与预期结果。
2. 先按已有 metadata/media 周期运行，确认目标数量和实际执行结果。再根据限流、时延和成本调整，不用高频探针制造平台失败。
3. 发布/重启后触发受影响能力验证；展示证据时间与过期状态，空目标/过期证据不能显示“刚刚验证可用”。
4. 每轮保留全部 non-pass，复测另起记录。按平台、能力、策略统计首试解析率、有效文件率、恢复时间、来源交互登录次数。
5. 上游新版本和平台回归进入独立候选镜像，固定样本与同一来源对照，再按本 SOP 发布；启动和用户请求中不在线自动升级引擎。
6. 支持范围以真实能力为单位。23 个 Registry 平台不等于所有视频/图集/直播/地区/账号内容都已支持。增加能力时仍按 035/平台专项更新文档和验收。

## 14. S10：关闭、提交与交付

### 缺陷关闭清单

- [ ] 根因与修复一致，Red/Green 及失败注入有结果。
- [ ] 本切片必需的模块检查、配置、契约已通过。
- [ ] 实际运行版本与候选一致，部署和回滚资料可重放。
- [ ] 受影响平台、原链接、真实文件和对应重启场景通过。
- [ ] Web/App 仅对实际完成的客户端范围声明通过。
- [ ] CQ、Design、PRD、Plan、Acceptance 的状态同步，残余项独立保留。

只有与该缺陷相关的必需项通过才关闭该 CQ；总体 035 仍可保持未完成。全部平台/新机未通过时，不能把单代理修复写成全项目高可用验收。

### Git 与文档交付

Server 提交前检查 `git status --short`、暂存文件清单、`git diff --cached --check` 和实际暂存差异；只暂存当前切片，使用中文 Conventional Commits。提交后记录 SHA 与工作区状态。App 按其独立授权规则处理提交，不搭车处理已有改动。

最终交付至少写明：本次改变、根因、执行检查与结果、部署是否完成、真实文件/重启证据、未验证边界、Server/App 分支与提交、推送状态、残余改动。未执行 remote CI 或推送时如实写明，不能引用旧绿色流水线冒充当前结果。

完成真实验收后按 AGENTS 判断四份文档是否需要长期保留；仍有价值则按各自类型归档，纯实施记录已被长期事实吸收则整体删除并用 Git 历史追溯。未完成的 035 不提前归档，也不创建集中 `docs/archive/`。

## 15. 本次执行起点

当前建议从 **S00 → S01 → S02 → S03** 开始，把 CQ-029 作为首个独立修复切片，然后完成对应 S06/S07/S08；再推进部署固化、分类、来源和持续监测。

本次 SOP 编写没有修改 Compose、来源或业务代码，也没有重启线上容器。流程文档可审阅，不代表上述修复、发布或验收已经完成。


### 11.4 抖音冷媒体 CDN 的端口拒绝

2026-09-25 实测公开分享链先经 `aweme.snssdk.com`，再由 `*.douyinvod.com` 跳转到 `*.wmzfylgdsz.com:8889`。页面元数据可读取，但 ffprobe 的时长探测被 Squid 的 CONNECT 端口规则拒绝，从而表现为 `inspection_failed`。已为这一域名单独允许 TLS 8889；其他域名的 8889、IP 字面量和已有私网限制维持拒绝。不要将所有 403 都归因于登录失效，先区分代理 CONNECT 拒绝和上游 HTTP 响应，也不要把时长校验删掉来制造解析成功。

本次验证：原分享链接在 Web 实际解析成功，显示 1254×720、MP4、H264/AAC、实测 3:08；新访客元数据中的 184 秒未替代真实媒体校验。代理配置校验和热加载通过，非白名单域名的 8889、127.0.0.1/localhost 的 443、其他域名的 22 仍返回 403。相关后端与 PostgreSQL 回归共 148 项通过，Ruff 与 mypy 通过。本次未创建下载任务，不作为完整文件交付验收。
