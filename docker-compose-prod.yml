x-app-build: &app-build
  context: .
  dockerfile: Dockerfile
  args:
    SITE_URL: "${SITE_URL:?SITE_URL is required}"

x-provider-session-runner: &provider-session-runner
  build: *app-build
  image: video-server:prod
  restart: unless-stopped
  networks:
    - runner_rpc_net
    - runner_egress_net
  tmpfs:
    - /tmp:rw,noexec,nosuid,size=64m
    - /run/provider-session:rw,noexec,nosuid,size=16m,mode=0700,uid=10001,gid=10001
  depends_on:
    egress-proxy:
      condition: service_healthy
  healthcheck:
    test:
      [
        "CMD",
        "python",
        "-c",
        "import urllib.request; urllib.request.urlopen('http://127.0.0.1:19100/health/ready', timeout=3)",
      ]
    interval: 5s
    timeout: 5s
    retries: 24
    start_period: 5s
  command:
    [
      "python",
      "-m",
      "uvicorn",
      "app.runner.main:create_app",
      "--factory",
      "--host",
      "0.0.0.0",
      "--port",
      "19100",
      "--no-access-log",
    ]

x-provider-session-environment: &provider-session-environment
  APP_ENV: "${APP_ENV:-production}"
  RUNNER_HMAC_SECRET: "${RUNNER_HMAC_SECRET:-development-runner-secret-change-me}"
  RUNNER_EGRESS_PROXY: http://egress-proxy:3128
  RUNNER_PROVIDER_EGRESS_PROXIES: "${RUNNER_PROVIDER_EGRESS_PROXIES:-{}}"
  RUNNER_WORKSPACE_ROOT: /work
  RUNNER_ACCESS_MODE: operator_managed
  RUNNER_MAX_ACTIVE_TASKS: "1"
  RUNNER_DOWNLOAD_TIMEOUT_SECONDS: "${RUNNER_DOWNLOAD_TIMEOUT_SECONDS:-7200}"
  RUNNER_INSPECT_TIMEOUT_SECONDS: "${RUNNER_INSPECT_TIMEOUT_SECONDS:-120}"
  RUNNER_MAX_DURATION_SECONDS: "${RUNNER_MAX_DURATION_SECONDS:-86400}"
  RUNNER_MAX_OUTPUT_BYTES: "${RUNNER_MAX_OUTPUT_BYTES:-21474836480}"
  RUNNER_MAX_WORKSPACE_BYTES: "${RUNNER_MAX_WORKSPACE_BYTES:-42949672960}"
  RUNNER_OPERATOR_ACCOUNT_BASELINE_ATTESTED: "true"
  RUNNER_PROVIDER_SESSION_TEMP_ROOT: /run/provider-session
  RUNNER_PROVIDER_COOKIE_SYNC_ROOT: /run/provider-cookie-agent
  PEERTUBE_ALLOWED_INSTANCES: "${PEERTUBE_ALLOWED_INSTANCES:-[]}"

x-media-runtime-dependencies: &media-runtime-dependencies
  media-runner:
    condition: service_healthy
  youtube-operator-runner:
    condition: service_healthy
  douyin-operator-runner:
    condition: service_healthy
  xiaohongshu-operator-runner:
    condition: service_healthy
  reddit-operator-runner:
    condition: service_healthy
  x-operator-runner:
    condition: service_healthy
  instagram-operator-runner:
    condition: service_healthy
  facebook-operator-runner:
    condition: service_healthy
  pinterest-operator-runner:
    condition: service_healthy
  wechat-channels-operator-runner:
    condition: service_healthy
  egress-proxy:
    condition: service_healthy

services:
  api:
    build: *app-build
    image: video-server:prod
    container_name: video-api
    restart: unless-stopped
    environment:
      ANALYSIS_ENABLED: '${ANALYSIS_ENABLED:-false}'
      ANALYSIS_MANUAL_RETRIES_PER_DAY: '${ANALYSIS_MANUAL_RETRIES_PER_DAY:-20}'
      ANALYSIS_MANUAL_RETRY_MIN_INTERVAL_SECONDS: '${ANALYSIS_MANUAL_RETRY_MIN_INTERVAL_SECONDS:-30}'
      ANALYSIS_MAX_RUNS_PER_JOB: '${ANALYSIS_MAX_RUNS_PER_JOB:-10}'
      ANALYSIS_QUEUE: '${ANALYSIS_QUEUE:-video.analysis}'
      ANALYSIS_REPORT_MAX_BYTES: '${ANALYSIS_REPORT_MAX_BYTES:-16777216}'
      ANALYSIS_REPORT_QUEUE: '${ANALYSIS_REPORT_QUEUE:-video.analysis-report}'
      ANALYSIS_REPORT_ROUTING_KEY: '${ANALYSIS_REPORT_ROUTING_KEY:-analysis.report.publish.requested}'
      ANALYSIS_ROUTING_KEY: '${ANALYSIS_ROUTING_KEY:-analysis.requested}'
      ANALYSIS_WORKER_STALE_SECONDS: '${ANALYSIS_WORKER_STALE_SECONDS:-45}'
      APP_ENV: '${APP_ENV:-production}'
      APP_VERSION: '${APP_VERSION:-0.1.0}'
      ARTICLE_DISCOVERY_MAX_ITEMS: '${ARTICLE_DISCOVERY_MAX_ITEMS:-24}'
      ARTICLE_DISCOVERY_MAX_RESPONSE_BYTES: '${ARTICLE_DISCOVERY_MAX_RESPONSE_BYTES:-4194304}'
      ARTICLE_DISCOVERY_MIN_INTERVAL_SECONDS: '${ARTICLE_DISCOVERY_MIN_INTERVAL_SECONDS:-1}'
      ARTICLE_DISCOVERY_TIMEOUT_SECONDS: '${ARTICLE_DISCOVERY_TIMEOUT_SECONDS:-12}'
      ARTICLE_DISCOVERY_TTL_SECONDS: '${ARTICLE_DISCOVERY_TTL_SECONDS:-900}'
      ARTIFACT_DELETE_TIMEOUT_SECONDS: '${ARTIFACT_DELETE_TIMEOUT_SECONDS:-30}'
      ARTIFACT_DOWNLOAD_URL_TTL_SECONDS: '${ARTIFACT_DOWNLOAD_URL_TTL_SECONDS:-300}'
      AUTH_ACCESS_COOKIE_NAME: '${AUTH_ACCESS_COOKIE_NAME:-video_access_token}'
      AUTH_ACCESS_TOKEN_TTL_SECONDS: '${AUTH_ACCESS_TOKEN_TTL_SECONDS:-900}'
      AUTH_BOOTSTRAP_ADMIN_EMAIL: '${AUTH_BOOTSTRAP_ADMIN_EMAIL:-}'
      AUTH_BOOTSTRAP_ADMIN_SECRET: '${AUTH_BOOTSTRAP_ADMIN_SECRET:-development-admin-bootstrap-secret-change-me}'
      AUTH_JWT_AUDIENCE: '${AUTH_JWT_AUDIENCE:-video-web}'
      AUTH_JWT_ISSUER: '${AUTH_JWT_ISSUER:-video-server}'
      AUTH_JWT_SECRET: '${AUTH_JWT_SECRET:-development-jwt-secret-change-me-32-bytes}'
      AUTH_REFRESH_COOKIE_NAME: '${AUTH_REFRESH_COOKIE_NAME:-video_refresh_token}'
      AUTH_REFRESH_TOKEN_TTL_SECONDS: '${AUTH_REFRESH_TOKEN_TTL_SECONDS:-2592000}'
      DOCUMENT_IMPORT_ENABLED: '${DOCUMENT_IMPORT_ENABLED:-true}'
      DOCUMENT_IMPORT_MAX_BYTES: '${DOCUMENT_IMPORT_MAX_BYTES:-52428800}'
      DOCUMENT_NORMALIZED_MAX_CHARACTERS: '${DOCUMENT_NORMALIZED_MAX_CHARACTERS:-2000000}'
      DOCUMENT_PREVIEW_MAX_BYTES: '${DOCUMENT_PREVIEW_MAX_BYTES:-1048576}'
      DOCUMENT_PREVIEW_MAX_CHARACTERS: '${DOCUMENT_PREVIEW_MAX_CHARACTERS:-1000000}'
      DOWNLOAD_QUEUE: '${DOWNLOAD_QUEUE:-video.download}'
      DOWNLOAD_ROUTING_KEY: '${DOWNLOAD_ROUTING_KEY:-download.requested}'
      DOWNLOAD_THUMBNAIL_MAX_BYTES: '${DOWNLOAD_THUMBNAIL_MAX_BYTES:-2000000}'
      DOWNLOAD_TIMEOUT_SECONDS: '${DOWNLOAD_TIMEOUT_SECONDS:-7200}'
      IMPORT_QUEUE: '${IMPORT_QUEUE:-video.import}'
      IMPORT_RIGHTS_STATEMENT_VERSION: '${IMPORT_RIGHTS_STATEMENT_VERSION:-content-rights}'
      IMPORT_ROUTING_KEY: '${IMPORT_ROUTING_KEY:-content.import.verify.requested}'
      IMPORT_UPLOAD_MAX_CONCURRENCY: '${IMPORT_UPLOAD_MAX_CONCURRENCY:-4}'
      IMPORT_UPLOAD_MAX_PARTS: '${IMPORT_UPLOAD_MAX_PARTS:-1000}'
      IMPORT_UPLOAD_PART_SIZE_BYTES: '${IMPORT_UPLOAD_PART_SIZE_BYTES:-33554432}'
      IMPORT_UPLOAD_SESSION_TTL_SECONDS: '${IMPORT_UPLOAD_SESSION_TTL_SECONDS:-900}'
      INSPECT_TIMEOUT_SECONDS: '${INSPECT_TIMEOUT_SECONDS:-150}'
      INSPECTION_TTL_SECONDS: '${INSPECTION_TTL_SECONDS:-900}'
      MAX_ANALYSIS_ATTEMPTS: '${MAX_ANALYSIS_ATTEMPTS:-3}'
      MAX_DOWNLOAD_ATTEMPTS: '${MAX_DOWNLOAD_ATTEMPTS:-3}'
      MAX_FILE_SIZE_BYTES: '${MAX_FILE_SIZE_BYTES:-21474836480}'
      MAX_VIDEO_DURATION_SECONDS: '${MAX_VIDEO_DURATION_SECONDS:-86400}'
      MAX_WORKSPACE_SIZE_BYTES: '${MAX_WORKSPACE_SIZE_BYTES:-42949672960}'
      MEDIA_IMPORT_ENABLED: '${MEDIA_IMPORT_ENABLED:-true}'
      MEDIA_IMPORT_MAX_BYTES: '${MEDIA_IMPORT_MAX_BYTES:-2147483648}'
      METRICS_ACCESS_KEY: '${METRICS_ACCESS_KEY:-development-metrics-access-key-change-me}'
      MINIO_ACCESS_KEY: '${MINIO_ACCESS_KEY:-video-access}'
      MINIO_PUBLIC_SECURE: '${MINIO_PUBLIC_SECURE:-false}'
      MINIO_REGION: '${MINIO_REGION:-us-east-1}'
      MINIO_SECRET_KEY: '${MINIO_SECRET_KEY:-video-secret-change-me}'
      PEERTUBE_ALLOWED_INSTANCES: '${PEERTUBE_ALLOWED_INSTANCES:-[]}'
      PROVIDER_VERIFIED_KEYS: '${PROVIDER_VERIFIED_KEYS:-[]}'
      QUOTA_LIMITS: '${QUOTA_LIMITS:-{"max_active_per_owner":5,"max_active_global":200,"daily_tasks":50,"daily_bytes":107374182400,"storage_bytes":107374182400,"daily_analysis_attempts":60}}'
      RABBITMQ_CONNECTION_TIMEOUT_SECONDS: '${RABBITMQ_CONNECTION_TIMEOUT_SECONDS:-10}'
      RABBITMQ_EXCHANGE: '${RABBITMQ_EXCHANGE:-video.events}'
      RABBITMQ_HEARTBEAT_SECONDS: '${RABBITMQ_HEARTBEAT_SECONDS:-60}'
      RABBITMQ_PUBLISH_TIMEOUT_SECONDS: '${RABBITMQ_PUBLISH_TIMEOUT_SECONDS:-10}'
      RABBITMQ_RECONNECT_INTERVAL_SECONDS: '${RABBITMQ_RECONNECT_INTERVAL_SECONDS:-5}'
      RABBITMQ_VHOST: '${RABBITMQ_VHOST:-video}'
      RATE_LIMIT_POLICIES: '${RATE_LIMIT_POLICIES:-{}}'
      READINESS_TIMEOUT_SECONDS: '${READINESS_TIMEOUT_SECONDS:-2.0}'
      REQUEST_FINGERPRINT_SECRET: '${REQUEST_FINGERPRINT_SECRET:-development-fingerprint-secret-change-me}'
      REQUEST_MAX_BYTES: '${REQUEST_MAX_BYTES:-262144}'
      REQUEST_TIMEOUT_SECONDS: '${REQUEST_TIMEOUT_SECONDS:-180}'
      RUNNER_HMAC_SECRET: '${RUNNER_HMAC_SECRET:-development-runner-secret-change-me}'
      RUNNER_OPERATOR_BASE_URLS: '${RUNNER_OPERATOR_BASE_URLS:-{}}'
      RUNNER_SIGNATURE_TTL_SECONDS: '${RUNNER_SIGNATURE_TTL_SECONDS:-30}'
      SCREENPLAY_ANALYSIS_ENABLED: '${SCREENPLAY_ANALYSIS_ENABLED:-false}'
      URL_ENCRYPTION_KEY: '${URL_ENCRYPTION_KEY:-ZGV2ZWxvcG1lbnQtdXJsLWtleS0zMi1ieXRlcyEhISE=}'
      URL_ENCRYPTION_KEY_ID: '${URL_ENCRYPTION_KEY_ID:-fernet}'
      WEBSOCKET_AUTH_RECHECK_SECONDS: '${WEBSOCKET_AUTH_RECHECK_SECONDS:-15}'
      WEBSOCKET_MAX_CONNECTIONS: '${WEBSOCKET_MAX_CONNECTIONS:-1000}'
      WEBSOCKET_MAX_CONNECTIONS_PER_OWNER: '${WEBSOCKET_MAX_CONNECTIONS_PER_OWNER:-4}'
      SERVICE_ROLE: api
      APP_HOST: 0.0.0.0
      APP_PORT: 8111
      TRUSTED_PROXY_CIDRS: '${TRUSTED_PROXY_CIDRS:-[]}'
      TRUSTED_FRONTEND_PROXY_IP: '${FRONTEND_IPV4_ADDRESS:-10.251.0.10}'
      DATABASE_URL: "postgresql+asyncpg://${POSTGRES_USER:-video}:${POSTGRES_PASSWORD:-video}@${POSTGRES_HOST:-host.docker.internal}:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-video}"
      RABBITMQ_URL: "amqp://${RABBITMQ_API_USER:-video-api}:${RABBITMQ_API_PASS:-video-api-secret}@${RABBITMQ_HOST:-host.docker.internal}:${RABBITMQ_PORT:-5672}/${RABBITMQ_VHOST:-video}"
      VALKEY_URL: "redis://${VALKEY_HOST:-host.docker.internal}:${VALKEY_PORT:-6379}/0"
      MINIO_ENDPOINT: "${MINIO_HOST:-host.docker.internal}:${MINIO_PORT:-19190}"
      MINIO_PUBLIC_ENDPOINT: "${MINIO_PUBLIC_ENDPOINT:-127.0.0.1:19190}"
      MINIO_LOCAL_BROWSER_ENDPOINT: "${MINIO_LOCAL_BROWSER_ENDPOINT:-}"
      MINIO_LOCAL_BROWSER_SECURE: "${MINIO_LOCAL_BROWSER_SECURE:-false}"
      MINIO_BUCKET: "${MINIO_BUCKET:-video-artifacts}"
      MINIO_INTERNAL_SECURE: "${MINIO_INTERNAL_SECURE:-false}"
      RUNNER_BASE_URL: http://media-runner:19100
      RUNNER_EGRESS_PROXY: http://egress-proxy:3128
      ARTICLE_DISCOVERY_PROXY_URL: http://egress-proxy:3128
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - app_net
      - runner_rpc_net
      - runner_egress_net
    ports:
      - "${APP_HOST_BIND:-127.0.0.1}:${APP_HOST_PORT:-8111}:8111"
    depends_on: *media-runtime-dependencies
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8111/health/ready', timeout=3)",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 15s
    command: ["python", "-m", "app.main"]

  frontend:
    build: *app-build
    image: video-server:prod
    container_name: video-frontend
    restart: unless-stopped
    environment:
      BACKEND_ORIGIN: http://api:8111
      HOSTNAME: 0.0.0.0
      MINIO_PUBLIC_ENDPOINT: "${MINIO_PUBLIC_ENDPOINT:-127.0.0.1:19190}"
      MINIO_PUBLIC_SECURE: "${MINIO_PUBLIC_SECURE:-false}"
      MINIO_LOCAL_BROWSER_ENDPOINT: "${MINIO_LOCAL_BROWSER_ENDPOINT:-}"
      MINIO_LOCAL_BROWSER_SECURE: "${MINIO_LOCAL_BROWSER_SECURE:-false}"
      NODE_ENV: production
      PORT: 8101
      SITE_URL: "${SITE_URL:?SITE_URL is required}"
    networks:
      app_net:
        ipv4_address: "${FRONTEND_IPV4_ADDRESS:-10.251.0.10}"
    ports:
      - "${APP_HOST_BIND:-127.0.0.1}:${FRONTEND_HOST_PORT:-8101}:8101"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:8101/', {redirect: 'manual'}).then((response) => process.exit(response.status >= 200 && response.status < 400 ? 0 : 1)).catch(() => process.exit(1))",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s
    working_dir: /app/frontend/.next/standalone
    command: ["node", "server.js"]

  outbox:
    build: *app-build
    image: video-server:prod
    container_name: video-outbox
    restart: unless-stopped
    environment:
      ANALYSIS_QUEUE: '${ANALYSIS_QUEUE:-video.analysis}'
      ANALYSIS_REPORT_QUEUE: '${ANALYSIS_REPORT_QUEUE:-video.analysis-report}'
      ANALYSIS_REPORT_ROUTING_KEY: '${ANALYSIS_REPORT_ROUTING_KEY:-analysis.report.publish.requested}'
      ANALYSIS_ROUTING_KEY: '${ANALYSIS_ROUTING_KEY:-analysis.requested}'
      APP_ENV: '${APP_ENV:-production}'
      APP_VERSION: '${APP_VERSION:-0.1.0}'
      DOWNLOAD_QUEUE: '${DOWNLOAD_QUEUE:-video.download}'
      DOWNLOAD_ROUTING_KEY: '${DOWNLOAD_ROUTING_KEY:-download.requested}'
      IMPORT_QUEUE: '${IMPORT_QUEUE:-video.import}'
      IMPORT_ROUTING_KEY: '${IMPORT_ROUTING_KEY:-content.import.verify.requested}'
      OUTBOX_BATCH_SIZE: '${OUTBOX_BATCH_SIZE:-50}'
      OUTBOX_POLL_INTERVAL_SECONDS: '${OUTBOX_POLL_INTERVAL_SECONDS:-1.0}'
      RABBITMQ_CONNECTION_TIMEOUT_SECONDS: '${RABBITMQ_CONNECTION_TIMEOUT_SECONDS:-10}'
      RABBITMQ_EXCHANGE: '${RABBITMQ_EXCHANGE:-video.events}'
      RABBITMQ_HEARTBEAT_SECONDS: '${RABBITMQ_HEARTBEAT_SECONDS:-60}'
      RABBITMQ_PUBLISH_TIMEOUT_SECONDS: '${RABBITMQ_PUBLISH_TIMEOUT_SECONDS:-10}'
      RABBITMQ_RECONNECT_INTERVAL_SECONDS: '${RABBITMQ_RECONNECT_INTERVAL_SECONDS:-5}'
      RABBITMQ_VHOST: '${RABBITMQ_VHOST:-video}'
      SERVICE_ROLE: outbox
      DATABASE_URL: "postgresql+asyncpg://${POSTGRES_USER:-video}:${POSTGRES_PASSWORD:-video}@${POSTGRES_HOST:-host.docker.internal}:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-video}"
      RABBITMQ_URL: "amqp://${RABBITMQ_OUTBOX_USER:-video-outbox}:${RABBITMQ_OUTBOX_PASS:-video-outbox-secret}@${RABBITMQ_HOST:-host.docker.internal}:${RABBITMQ_PORT:-5672}/${RABBITMQ_VHOST:-video}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - app_net
    command: ["python", "-m", "app.workers.outbox.main"]

  worker-download:
    build: *app-build
    image: video-server:prod
    container_name: video-worker-download
    restart: unless-stopped
    environment:
      ANALYSIS_QUEUE: '${ANALYSIS_QUEUE:-video.analysis}'
      ANALYSIS_REPORT_QUEUE: '${ANALYSIS_REPORT_QUEUE:-video.analysis-report}'
      ANALYSIS_REPORT_ROUTING_KEY: '${ANALYSIS_REPORT_ROUTING_KEY:-analysis.report.publish.requested}'
      ANALYSIS_ROUTING_KEY: '${ANALYSIS_ROUTING_KEY:-analysis.requested}'
      APP_ENV: '${APP_ENV:-production}'
      APP_VERSION: '${APP_VERSION:-0.1.0}'
      DOWNLOAD_QUEUE: '${DOWNLOAD_QUEUE:-video.download}'
      DOWNLOAD_QUEUED_RECOVERY_SECONDS: '${DOWNLOAD_QUEUED_RECOVERY_SECONDS:-60}'
      DOWNLOAD_ROUTING_KEY: '${DOWNLOAD_ROUTING_KEY:-download.requested}'
      DOWNLOAD_THUMBNAIL_FFMPEG_BINARY: '${DOWNLOAD_THUMBNAIL_FFMPEG_BINARY:-ffmpeg}'
      DOWNLOAD_THUMBNAIL_MAX_BYTES: '${DOWNLOAD_THUMBNAIL_MAX_BYTES:-2000000}'
      DOWNLOAD_THUMBNAIL_TIMEOUT_SECONDS: '${DOWNLOAD_THUMBNAIL_TIMEOUT_SECONDS:-15}'
      DOWNLOAD_TIMEOUT_SECONDS: '${DOWNLOAD_TIMEOUT_SECONDS:-7200}'
      DOWNLOAD_WORKER_THREADS: '${DOWNLOAD_WORKER_THREADS:-4}'
      HEARTBEAT_INTERVAL_SECONDS: '${HEARTBEAT_INTERVAL_SECONDS:-15}'
      IMPORT_QUEUE: '${IMPORT_QUEUE:-video.import}'
      IMPORT_ROUTING_KEY: '${IMPORT_ROUTING_KEY:-content.import.verify.requested}'
      INSPECT_TIMEOUT_SECONDS: '${INSPECT_TIMEOUT_SECONDS:-150}'
      JOB_LEASE_SECONDS: '${JOB_LEASE_SECONDS:-60}'
      MAX_FILE_SIZE_BYTES: '${MAX_FILE_SIZE_BYTES:-21474836480}'
      MAX_VIDEO_DURATION_SECONDS: '${MAX_VIDEO_DURATION_SECONDS:-86400}'
      MAX_WORKSPACE_SIZE_BYTES: '${MAX_WORKSPACE_SIZE_BYTES:-42949672960}'
      MINIO_ACCESS_KEY: '${MINIO_ACCESS_KEY:-video-access}'
      MINIO_REGION: '${MINIO_REGION:-us-east-1}'
      MINIO_SECRET_KEY: '${MINIO_SECRET_KEY:-video-secret-change-me}'
      PEERTUBE_ALLOWED_INSTANCES: '${PEERTUBE_ALLOWED_INSTANCES:-[]}'
      RABBITMQ_CONNECTION_TIMEOUT_SECONDS: '${RABBITMQ_CONNECTION_TIMEOUT_SECONDS:-10}'
      RABBITMQ_EXCHANGE: '${RABBITMQ_EXCHANGE:-video.events}'
      RABBITMQ_HEARTBEAT_SECONDS: '${RABBITMQ_HEARTBEAT_SECONDS:-60}'
      RABBITMQ_PUBLISH_TIMEOUT_SECONDS: '${RABBITMQ_PUBLISH_TIMEOUT_SECONDS:-10}'
      RABBITMQ_RECONNECT_INTERVAL_SECONDS: '${RABBITMQ_RECONNECT_INTERVAL_SECONDS:-5}'
      RABBITMQ_VHOST: '${RABBITMQ_VHOST:-video}'
      RUNNER_HMAC_SECRET: '${RUNNER_HMAC_SECRET:-development-runner-secret-change-me}'
      RUNNER_OPERATOR_BASE_URLS: '${RUNNER_OPERATOR_BASE_URLS:-{}}'
      RUNNER_SIGNATURE_TTL_SECONDS: '${RUNNER_SIGNATURE_TTL_SECONDS:-30}'
      URL_ENCRYPTION_KEY: '${URL_ENCRYPTION_KEY:-ZGV2ZWxvcG1lbnQtdXJsLWtleS0zMi1ieXRlcyEhISE=}'
      URL_ENCRYPTION_KEY_ID: '${URL_ENCRYPTION_KEY_ID:-fernet}'
      SERVICE_ROLE: download-worker
      DATABASE_URL: "postgresql+asyncpg://${POSTGRES_USER:-video}:${POSTGRES_PASSWORD:-video}@${POSTGRES_HOST:-host.docker.internal}:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-video}"
      RABBITMQ_URL: "amqp://${RABBITMQ_DOWNLOAD_USER:-video-download}:${RABBITMQ_DOWNLOAD_PASS:-video-download-secret}@${RABBITMQ_HOST:-host.docker.internal}:${RABBITMQ_PORT:-5672}/${RABBITMQ_VHOST:-video}"
      MINIO_ENDPOINT: "${MINIO_HOST:-host.docker.internal}:${MINIO_PORT:-19190}"
      MINIO_BUCKET: "${MINIO_BUCKET:-video-artifacts}"
      MINIO_INTERNAL_SECURE: "${MINIO_INTERNAL_SECURE:-false}"
      RUNNER_BASE_URL: http://media-runner:19100
      RUNNER_WORKSPACE_ROOT: /work
      RUNNER_EGRESS_PROXY: http://egress-proxy:3128
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - app_net
      - runner_rpc_net
    volumes:
      - runner_work:/work
    depends_on: *media-runtime-dependencies
    command: ["python", "-m", "app.workers.download.main"]

  worker-import:
    build: *app-build
    image: video-server:prod
    container_name: video-worker-import
    restart: unless-stopped
    environment:
      ANALYSIS_QUEUE: '${ANALYSIS_QUEUE:-video.analysis}'
      ANALYSIS_REPORT_QUEUE: '${ANALYSIS_REPORT_QUEUE:-video.analysis-report}'
      ANALYSIS_REPORT_ROUTING_KEY: '${ANALYSIS_REPORT_ROUTING_KEY:-analysis.report.publish.requested}'
      ANALYSIS_ROUTING_KEY: '${ANALYSIS_ROUTING_KEY:-analysis.requested}'
      APP_ENV: '${APP_ENV:-production}'
      APP_VERSION: '${APP_VERSION:-0.1.0}'
      DOCUMENT_IMPORT_MAX_BYTES: '${DOCUMENT_IMPORT_MAX_BYTES:-52428800}'
      DOCUMENT_NORMALIZED_MAX_CHARACTERS: '${DOCUMENT_NORMALIZED_MAX_CHARACTERS:-2000000}'
      DOWNLOAD_QUEUE: '${DOWNLOAD_QUEUE:-video.download}'
      DOWNLOAD_ROUTING_KEY: '${DOWNLOAD_ROUTING_KEY:-download.requested}'
      DOWNLOAD_THUMBNAIL_FFMPEG_BINARY: '${DOWNLOAD_THUMBNAIL_FFMPEG_BINARY:-ffmpeg}'
      DOWNLOAD_THUMBNAIL_MAX_BYTES: '${DOWNLOAD_THUMBNAIL_MAX_BYTES:-2000000}'
      DOWNLOAD_THUMBNAIL_TIMEOUT_SECONDS: '${DOWNLOAD_THUMBNAIL_TIMEOUT_SECONDS:-15}'
      HEARTBEAT_INTERVAL_SECONDS: '${HEARTBEAT_INTERVAL_SECONDS:-15}'
      IMPORT_ARTIFACT_ORPHAN_GRACE_SECONDS: '${IMPORT_ARTIFACT_ORPHAN_GRACE_SECONDS:-3600}'
      IMPORT_FFPROBE_BINARY: '${IMPORT_FFPROBE_BINARY:-ffprobe}'
      IMPORT_FFPROBE_TIMEOUT_SECONDS: '${IMPORT_FFPROBE_TIMEOUT_SECONDS:-30}'
      IMPORT_MAX_MEDIA_STREAMS: '${IMPORT_MAX_MEDIA_STREAMS:-32}'
      IMPORT_MAX_PROBE_OUTPUT_BYTES: '${IMPORT_MAX_PROBE_OUTPUT_BYTES:-262144}'
      IMPORT_MAX_VIDEO_HEIGHT: '${IMPORT_MAX_VIDEO_HEIGHT:-8192}'
      IMPORT_MAX_VIDEO_WIDTH: '${IMPORT_MAX_VIDEO_WIDTH:-8192}'
      IMPORT_QUARANTINE_RETENTION_DAYS: '${IMPORT_QUARANTINE_RETENTION_DAYS:-1}'
      IMPORT_QUEUE: '${IMPORT_QUEUE:-video.import}'
      IMPORT_RECOVERY_BATCH_SIZE: '${IMPORT_RECOVERY_BATCH_SIZE:-50}'
      IMPORT_RECOVERY_INTERVAL_SECONDS: '${IMPORT_RECOVERY_INTERVAL_SECONDS:-5}'
      IMPORT_RIGHTS_STATEMENT_VERSION: '${IMPORT_RIGHTS_STATEMENT_VERSION:-content-rights}'
      IMPORT_ROUTING_KEY: '${IMPORT_ROUTING_KEY:-content.import.verify.requested}'
      IMPORT_UPLOAD_MAX_CONCURRENCY: '${IMPORT_UPLOAD_MAX_CONCURRENCY:-4}'
      IMPORT_UPLOAD_MAX_PARTS: '${IMPORT_UPLOAD_MAX_PARTS:-1000}'
      IMPORT_UPLOAD_PART_SIZE_BYTES: '${IMPORT_UPLOAD_PART_SIZE_BYTES:-33554432}'
      IMPORT_UPLOAD_SESSION_TTL_SECONDS: '${IMPORT_UPLOAD_SESSION_TTL_SECONDS:-900}'
      IMPORT_WORKSPACE_GRACE_SECONDS: '${IMPORT_WORKSPACE_GRACE_SECONDS:-1800}'
      IMPORT_WORKSPACE_ROOT: '${IMPORT_WORKSPACE_ROOT:-.import-work}'
      JOB_LEASE_SECONDS: '${JOB_LEASE_SECONDS:-60}'
      MAX_VIDEO_DURATION_SECONDS: '${MAX_VIDEO_DURATION_SECONDS:-86400}'
      MEDIA_IMPORT_MAX_BYTES: '${MEDIA_IMPORT_MAX_BYTES:-2147483648}'
      MINIO_ACCESS_KEY: '${MINIO_ACCESS_KEY:-video-access}'
      MINIO_REGION: '${MINIO_REGION:-us-east-1}'
      MINIO_SECRET_KEY: '${MINIO_SECRET_KEY:-video-secret-change-me}'
      RABBITMQ_CONNECTION_TIMEOUT_SECONDS: '${RABBITMQ_CONNECTION_TIMEOUT_SECONDS:-10}'
      RABBITMQ_EXCHANGE: '${RABBITMQ_EXCHANGE:-video.events}'
      RABBITMQ_HEARTBEAT_SECONDS: '${RABBITMQ_HEARTBEAT_SECONDS:-60}'
      RABBITMQ_PUBLISH_TIMEOUT_SECONDS: '${RABBITMQ_PUBLISH_TIMEOUT_SECONDS:-10}'
      RABBITMQ_RECONNECT_INTERVAL_SECONDS: '${RABBITMQ_RECONNECT_INTERVAL_SECONDS:-5}'
      RABBITMQ_VHOST: '${RABBITMQ_VHOST:-video}'
      WORKER_PREFETCH: '${WORKER_PREFETCH:-2}'
      SERVICE_ROLE: import-worker
      DATABASE_URL: "postgresql+asyncpg://${POSTGRES_USER:-video}:${POSTGRES_PASSWORD:-video}@${POSTGRES_HOST:-host.docker.internal}:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-video}"
      RABBITMQ_URL: "amqp://${RABBITMQ_IMPORT_USER:-video-import}:${RABBITMQ_IMPORT_PASS:-video-import-secret}@${RABBITMQ_HOST:-host.docker.internal}:${RABBITMQ_PORT:-5672}/${RABBITMQ_VHOST:-video}"
      MINIO_ENDPOINT: "${MINIO_HOST:-host.docker.internal}:${MINIO_PORT:-19190}"
      MINIO_BUCKET: "${MINIO_BUCKET:-video-artifacts}"
      MINIO_INTERNAL_SECURE: "${MINIO_INTERNAL_SECURE:-false}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - app_net
    command: ["python", "-m", "app.workers.imports.main"]

  worker-report:
    build: *app-build
    image: video-server:prod
    container_name: video-worker-report
    restart: unless-stopped
    environment:
      ANALYSIS_QUEUE: '${ANALYSIS_QUEUE:-video.analysis}'
      ANALYSIS_REPORT_GC_BATCH_SIZE: '${ANALYSIS_REPORT_GC_BATCH_SIZE:-50}'
      ANALYSIS_REPORT_GC_INTERVAL_SECONDS: '${ANALYSIS_REPORT_GC_INTERVAL_SECONDS:-300}'
      ANALYSIS_REPORT_MAX_BYTES: '${ANALYSIS_REPORT_MAX_BYTES:-16777216}'
      ANALYSIS_REPORT_ORPHAN_GRACE_SECONDS: '${ANALYSIS_REPORT_ORPHAN_GRACE_SECONDS:-3600}'
      ANALYSIS_REPORT_QUEUE: '${ANALYSIS_REPORT_QUEUE:-video.analysis-report}'
      ANALYSIS_REPORT_ROUTING_KEY: '${ANALYSIS_REPORT_ROUTING_KEY:-analysis.report.publish.requested}'
      ANALYSIS_ROUTING_KEY: '${ANALYSIS_ROUTING_KEY:-analysis.requested}'
      APP_ENV: '${APP_ENV:-production}'
      APP_VERSION: '${APP_VERSION:-0.1.0}'
      ARTIFACT_DELETE_TIMEOUT_SECONDS: '${ARTIFACT_DELETE_TIMEOUT_SECONDS:-30}'
      DOWNLOAD_QUEUE: '${DOWNLOAD_QUEUE:-video.download}'
      DOWNLOAD_ROUTING_KEY: '${DOWNLOAD_ROUTING_KEY:-download.requested}'
      IMPORT_QUEUE: '${IMPORT_QUEUE:-video.import}'
      IMPORT_ROUTING_KEY: '${IMPORT_ROUTING_KEY:-content.import.verify.requested}'
      MINIO_ACCESS_KEY: '${MINIO_ACCESS_KEY:-video-access}'
      MINIO_REGION: '${MINIO_REGION:-us-east-1}'
      MINIO_SECRET_KEY: '${MINIO_SECRET_KEY:-video-secret-change-me}'
      RABBITMQ_CONNECTION_TIMEOUT_SECONDS: '${RABBITMQ_CONNECTION_TIMEOUT_SECONDS:-10}'
      RABBITMQ_EXCHANGE: '${RABBITMQ_EXCHANGE:-video.events}'
      RABBITMQ_HEARTBEAT_SECONDS: '${RABBITMQ_HEARTBEAT_SECONDS:-60}'
      RABBITMQ_PUBLISH_TIMEOUT_SECONDS: '${RABBITMQ_PUBLISH_TIMEOUT_SECONDS:-10}'
      RABBITMQ_RECONNECT_INTERVAL_SECONDS: '${RABBITMQ_RECONNECT_INTERVAL_SECONDS:-5}'
      RABBITMQ_VHOST: '${RABBITMQ_VHOST:-video}'
      WORKER_PREFETCH: '${WORKER_PREFETCH:-2}'
      SERVICE_ROLE: report-worker
      DATABASE_URL: "postgresql+asyncpg://${POSTGRES_USER:-video}:${POSTGRES_PASSWORD:-video}@${POSTGRES_HOST:-host.docker.internal}:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-video}"
      RABBITMQ_URL: "amqp://${RABBITMQ_REPORT_USER:-video-report}:${RABBITMQ_REPORT_PASS:-video-report-secret}@${RABBITMQ_HOST:-host.docker.internal}:${RABBITMQ_PORT:-5672}/${RABBITMQ_VHOST:-video}"
      MINIO_ENDPOINT: "${MINIO_HOST:-host.docker.internal}:${MINIO_PORT:-19190}"
      MINIO_BUCKET: "${MINIO_BUCKET:-video-artifacts}"
      MINIO_INTERNAL_SECURE: "${MINIO_INTERNAL_SECURE:-false}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - app_net
    command: ["python", "-m", "app.workers.report.main"]

  provider-canary:
    build: *app-build
    image: video-server:prod
    container_name: video-provider-canary
    restart: unless-stopped
    environment:
      APP_ENV: '${APP_ENV:-production}'
      APP_VERSION: '${APP_VERSION:-0.1.0}'
      DOWNLOAD_TIMEOUT_SECONDS: '${DOWNLOAD_TIMEOUT_SECONDS:-7200}'
      INSPECT_TIMEOUT_SECONDS: '${INSPECT_TIMEOUT_SECONDS:-150}'
      MAX_FILE_SIZE_BYTES: '${MAX_FILE_SIZE_BYTES:-21474836480}'
      MAX_VIDEO_DURATION_SECONDS: '${MAX_VIDEO_DURATION_SECONDS:-86400}'
      MAX_WORKSPACE_SIZE_BYTES: '${MAX_WORKSPACE_SIZE_BYTES:-42949672960}'
      MINIO_ACCESS_KEY: '${MINIO_ACCESS_KEY:-video-access}'
      MINIO_REGION: '${MINIO_REGION:-us-east-1}'
      MINIO_SECRET_KEY: '${MINIO_SECRET_KEY:-video-secret-change-me}'
      PEERTUBE_ALLOWED_INSTANCES: '${PEERTUBE_ALLOWED_INSTANCES:-[]}'
      PROVIDER_CANARY_MEDIA_INTERVAL_SECONDS: '${PROVIDER_CANARY_MEDIA_INTERVAL_SECONDS:-86400}'
      PROVIDER_CANARY_METADATA_INTERVAL_SECONDS: '${PROVIDER_CANARY_METADATA_INTERVAL_SECONDS:-21600}'
      PROVIDER_CANARY_POLL_SECONDS: '${PROVIDER_CANARY_POLL_SECONDS:-60}'
      PROVIDER_CANARY_TARGETS: '${PROVIDER_CANARY_TARGETS:-[]}'
      RUNNER_HMAC_SECRET: '${RUNNER_HMAC_SECRET:-development-runner-secret-change-me}'
      RUNNER_OPERATOR_BASE_URLS: '${RUNNER_OPERATOR_BASE_URLS:-{}}'
      RUNNER_SIGNATURE_TTL_SECONDS: '${RUNNER_SIGNATURE_TTL_SECONDS:-30}'
      URL_ENCRYPTION_KEY: '${URL_ENCRYPTION_KEY:-ZGV2ZWxvcG1lbnQtdXJsLWtleS0zMi1ieXRlcyEhISE=}'
      URL_ENCRYPTION_KEY_ID: '${URL_ENCRYPTION_KEY_ID:-fernet}'
      SERVICE_ROLE: provider-canary
      DATABASE_URL: "postgresql+asyncpg://${POSTGRES_USER:-video}:${POSTGRES_PASSWORD:-video}@${POSTGRES_HOST:-host.docker.internal}:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-video}"
      MINIO_ENDPOINT: "${MINIO_HOST:-host.docker.internal}:${MINIO_PORT:-19190}"
      MINIO_BUCKET: "${MINIO_BUCKET:-video-artifacts}"
      MINIO_INTERNAL_SECURE: "${MINIO_INTERNAL_SECURE:-false}"
      RUNNER_BASE_URL: http://media-runner:19100
      RUNNER_WORKSPACE_ROOT: /work
      RUNNER_EGRESS_PROXY: http://egress-proxy:3128
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - app_net
      - runner_rpc_net
    volumes:
      - runner_work:/work
    depends_on: *media-runtime-dependencies
    command: ["python", "-m", "app.workers.canary.main"]

  media-runner:
    build: *app-build
    image: video-server:prod
    container_name: media-runner
    restart: unless-stopped
    environment:
      APP_ENV: "${APP_ENV:-development}"
      RUNNER_HMAC_SECRET: "${RUNNER_HMAC_SECRET:-development-runner-secret-change-me}"
      RUNNER_EGRESS_PROXY: http://egress-proxy:3128
      RUNNER_PROVIDER_EGRESS_PROXIES: "${RUNNER_PROVIDER_EGRESS_PROXIES:-{}}"
      RUNNER_WORKSPACE_ROOT: /work
      RUNNER_ACCESS_MODE: anonymous
      RUNNER_MAX_ACTIVE_TASKS: "${RUNNER_MAX_ACTIVE_TASKS:-4}"
      RUNNER_DOWNLOAD_TIMEOUT_SECONDS: "${RUNNER_DOWNLOAD_TIMEOUT_SECONDS:-7200}"
      RUNNER_INSPECT_TIMEOUT_SECONDS: "${RUNNER_INSPECT_TIMEOUT_SECONDS:-120}"
      RUNNER_MAX_DURATION_SECONDS: "${RUNNER_MAX_DURATION_SECONDS:-86400}"
      RUNNER_MAX_OUTPUT_BYTES: "${RUNNER_MAX_OUTPUT_BYTES:-21474836480}"
      RUNNER_MAX_WORKSPACE_BYTES: "${RUNNER_MAX_WORKSPACE_BYTES:-42949672960}"
      RUNNER_YOUTUBE_POT_BASE_URL: http://youtube-pot-provider:4416
      RUNNER_YOUTUBE_POT_PROVIDER_VERSION: bgutil-http-1.3.2
      PEERTUBE_ALLOWED_INSTANCES: "${PEERTUBE_ALLOWED_INSTANCES:-[]}"
    networks:
      - runner_rpc_net
      - runner_egress_net
      - youtube_pot_net
    volumes:
      - runner_work:/work
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=64m
    depends_on:
      workspace-init:
        condition: service_completed_successfully
      egress-proxy:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:19100/health/ready', timeout=3)",
        ]
      interval: 5s
      timeout: 5s
      retries: 24
      start_period: 5s
    command:
      [
        "python",
        "-m",
        "uvicorn",
        "app.runner.main:create_app",
        "--factory",
        "--host",
        "0.0.0.0",
        "--port",
        "19100",
        "--no-access-log",
      ]

  egress-proxy:
    image: ubuntu/squid:6.6-24.04_edge
    container_name: egress-proxy
    restart: unless-stopped
    stop_grace_period: 35s
    entrypoint: ["squid"]
    command: ["-N", "-f", "/etc/squid/squid.conf"]
    ports:
      - "127.0.0.1:${EGRESS_PROXY_HOST_PORT:-13128}:3128"
    volumes:
      - ./backend/egress/squid.conf:/etc/squid/squid.conf:ro
      - ${EGRESS_DESTINATION_POLICY_FILE:-./backend/egress/blocked-destinations.conf}:/etc/squid/blocked-destinations.conf:ro
    networks:
      - runner_egress_net
      - youtube_pot_net
      - proxy_uplink_net
    healthcheck:
      test: ["CMD", "squid", "-k", "check", "-f", "/etc/squid/squid.conf"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 5s

  workspace-init:
    build: *app-build
    image: video-server:prod
    container_name: workspace-init
    restart: "no"
    network_mode: none
    user: "0:0"
    command: ["sh", "-c", "install -d -o 10001 -g 10001 -m 0700 /work"]
    volumes:
      - runner_work:/work

  douyin-operator-runner:
    <<: *provider-session-runner
    container_name: douyin-operator-runner
    environment:
      <<: *provider-session-environment
      RUNNER_OPERATOR_SESSION_VERSIONS: '{"douyin":"browser"}'
    volumes:
      - runner_work:/work
      - type: bind
        source: "${PROVIDER_COOKIE_AGENT_RUNTIME_DIR:-${HOME}/Library/Caches/FrameFetch/provider-cookie-agent}/douyin"
        target: /run/provider-cookie-agent
        read_only: false
        bind:
          create_host_path: false

  xiaohongshu-operator-runner:
    <<: *provider-session-runner
    container_name: xiaohongshu-operator-runner
    environment:
      <<: *provider-session-environment
      RUNNER_OPERATOR_SESSION_VERSIONS: '{"xiaohongshu":"browser"}'
    volumes:
      - runner_work:/work
      - type: bind
        source: "${PROVIDER_COOKIE_AGENT_RUNTIME_DIR:-${HOME}/Library/Caches/FrameFetch/provider-cookie-agent}/xiaohongshu"
        target: /run/provider-cookie-agent
        read_only: false
        bind:
          create_host_path: false

  reddit-operator-runner:
    <<: *provider-session-runner
    container_name: reddit-operator-runner
    environment:
      <<: *provider-session-environment
      RUNNER_OPERATOR_SESSION_VERSIONS: '{"reddit":"browser"}'
    volumes:
      - runner_work:/work
      - type: bind
        source: "${PROVIDER_COOKIE_AGENT_RUNTIME_DIR:-${HOME}/Library/Caches/FrameFetch/provider-cookie-agent}/reddit"
        target: /run/provider-cookie-agent
        read_only: false
        bind:
          create_host_path: false

  x-operator-runner:
    <<: *provider-session-runner
    container_name: x-operator-runner
    environment:
      <<: *provider-session-environment
      RUNNER_OPERATOR_SESSION_VERSIONS: '{"x":"browser"}'
    volumes:
      - runner_work:/work
      - type: bind
        source: "${PROVIDER_COOKIE_AGENT_RUNTIME_DIR:-${HOME}/Library/Caches/FrameFetch/provider-cookie-agent}/x"
        target: /run/provider-cookie-agent
        read_only: false
        bind:
          create_host_path: false

  instagram-operator-runner:
    <<: *provider-session-runner
    container_name: instagram-operator-runner
    environment:
      <<: *provider-session-environment
      RUNNER_OPERATOR_SESSION_VERSIONS: '{"instagram":"browser"}'
    volumes:
      - runner_work:/work
      - type: bind
        source: "${PROVIDER_COOKIE_AGENT_RUNTIME_DIR:-${HOME}/Library/Caches/FrameFetch/provider-cookie-agent}/instagram"
        target: /run/provider-cookie-agent
        read_only: false
        bind:
          create_host_path: false

  facebook-operator-runner:
    <<: *provider-session-runner
    container_name: facebook-operator-runner
    environment:
      <<: *provider-session-environment
      RUNNER_OPERATOR_SESSION_VERSIONS: '{"facebook":"browser"}'
    volumes:
      - runner_work:/work
      - type: bind
        source: "${PROVIDER_COOKIE_AGENT_RUNTIME_DIR:-${HOME}/Library/Caches/FrameFetch/provider-cookie-agent}/facebook"
        target: /run/provider-cookie-agent
        read_only: false
        bind:
          create_host_path: false

  pinterest-operator-runner:
    <<: *provider-session-runner
    container_name: pinterest-operator-runner
    environment:
      <<: *provider-session-environment
      RUNNER_OPERATOR_SESSION_VERSIONS: '{"pinterest":"browser"}'
    volumes:
      - runner_work:/work
      - type: bind
        source: "${PROVIDER_COOKIE_AGENT_RUNTIME_DIR:-${HOME}/Library/Caches/FrameFetch/provider-cookie-agent}/pinterest"
        target: /run/provider-cookie-agent
        read_only: false
        bind:
          create_host_path: false

  wechat-channels-operator-runner:
    <<: *provider-session-runner
    container_name: wechat-channels-operator-runner
    environment:
      <<: *provider-session-environment
      RUNNER_OPERATOR_SESSION_VERSIONS: '{"wechat_channels":"browser"}'
    volumes:
      - runner_work:/work
      - type: bind
        source: "${PROVIDER_COOKIE_AGENT_RUNTIME_DIR:-${HOME}/Library/Caches/FrameFetch/provider-cookie-agent}/wechat_channels"
        target: /run/provider-cookie-agent
        read_only: false
        bind:
          create_host_path: false

  youtube-operator-runner:
    <<: *provider-session-runner
    container_name: youtube-operator-runner
    environment:
      <<: *provider-session-environment
      RUNNER_OPERATOR_SESSION_VERSIONS: '{"youtube":"browser"}'
      RUNNER_YOUTUBE_POT_BASE_URL: http://youtube-pot-provider:4416
      RUNNER_YOUTUBE_POT_PROVIDER_VERSION: bgutil-http-1.3.2
    networks:
      - runner_rpc_net
      - runner_egress_net
      - youtube_pot_net
    volumes:
      - runner_work:/work
      - type: bind
        source: "${PROVIDER_COOKIE_AGENT_RUNTIME_DIR:-${HOME}/Library/Caches/FrameFetch/provider-cookie-agent}/youtube"
        target: /run/provider-cookie-agent
        read_only: false
        bind:
          create_host_path: false
    depends_on:
      egress-proxy:
        condition: service_healthy
      youtube-pot-provider:
        condition: service_started

  youtube-pot-provider:
    image: brainicism/bgutil-ytdlp-pot-provider:1.3.2@sha256:9a96e6385ce1928da87dea07b1cab0413d2cf8c07a3b8a8bd419f53df2c3843c
    container_name: youtube-pot-provider
    restart: unless-stopped
    read_only: true
    entrypoint:
      ["/usr/local/bin/node", "/opt/video/youtube-pot-supervisor.mjs"]
    environment:
      YOUTUBE_POT_EXPECTED_VERSION: "1.3.2"
      RUNNER_EGRESS_PROXY: http://egress-proxy:3128
      RUNNER_PROVIDER_EGRESS_PROXIES: "${RUNNER_PROVIDER_EGRESS_PROXIES:-{}}"
    networks:
      - youtube_pot_net
    volumes:
      - type: bind
        source: ./backend/ops/youtube-pot-supervisor.mjs
        target: /opt/video/youtube-pot-supervisor.mjs
        read_only: true
        bind:
          create_host_path: false
    cap_drop: [ALL]
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=16m
    security_opt:
      - no-new-privileges:true

volumes:
  runner_work:

networks:
  app_net:
    ipam:
      config:
        - subnet: "${APP_NETWORK_SUBNET:-10.251.0.0/24}"
          ip_range: "${APP_NETWORK_IP_RANGE:-10.251.0.128/25}"
  runner_rpc_net:
    internal: true
  runner_egress_net:
    internal: true
  youtube_pot_net:
    internal: true
  proxy_uplink_net:
