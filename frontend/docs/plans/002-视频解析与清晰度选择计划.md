# Plan 002 视频解析与清晰度选择计划

## 状态与输入

- 状态：Implemented；实现已合并到 `main`，对应 Acceptance 当前为 Blocked，故暂不归档。
- 阶段验收：[002 视频解析与清晰度选择验收](../acceptance/002-视频解析与清晰度选择验收.md)；已执行，当前结论为 Blocked。
- Design（已归档）：[002 视频解析与清晰度选择交互](../design/archive/002-视频解析与清晰度选择交互设计.md)。
- PRD（已归档）：[002 视频解析与清晰度选择需求](../prd/archive/002-视频解析与清晰度选择需求.md)。
- 依赖：Web Plan 001、004 已实现，但对应 Acceptance 仍为 Blocked；真实 Server inspect/create operation 尚需完整验收。

## 1. 任务边界

实现 `/download` 的 URL 输入、inspect 状态、视频摘要、分辨率列表、到期处理和创建任务跳转。只使用生成 Service，不创建 DTO、裸 URL、Mock fallback 或本地业务缓存。

## 2. Test-first 执行拆分

| 顺序 | 先失败的约束 | 最小实现 | 主要产出 |
| --- | --- | --- | --- |
| `W002-T01` | 空值、非法协议、键盘提交和单次请求测试 | URL 表单与 inspect mutation | 输入卡 |
| `W002-T02` | idle/inspecting/inspected/failed/expired 状态矩阵 | feature hook 与页面状态组合 | 确定状态机 |
| `W002-T03` | 新请求竞态、旧结果/错误清除测试 | 取消/版本化响应处理 | 无陈旧数据 |
| `W002-T04` | 摘要、不可信标题、未知大小、排序和 merge DOM 断言 | MediaSummary、FormatList | 真实结果展示 |
| `W002-T05` | 可控时间到期、重新解析和 source/format 更新测试 | 到期判断与重新解析 | 期限行为 |
| `W002-T06` | 三字段 payload、重复点击、稳定幂等键和路由测试 | create mutation | 唯一任务创建 |
| `W002-T07` | Problem 错误、viewport、Chrome/Safari、键盘和 live region | 恢复提示与响应式样式 | 可恢复体验 |
| `W002-T08` | DOM/storage/network/source 敏感与非 MVP 扫描 | 移除多余字段和持久化 | 隐私边界 |

## 3. 预期文件边界

- `src/pages/Download/{index.tsx,index.less,hooks.ts}`。
- `src/pages/Download/components/{MediaSummary,FormatList}.tsx`。
- `src/utils/{format,time}.ts` 中纯展示工具。
- `tests/unit/Download/`、`tests/e2e/download-inspect.spec.ts`。

## 4. 验收映射

| 标准 | 负责任务与证据 |
| --- | --- |
| `W002-DAC-01` / `W002-AC-01` | T01：表单参数、请求计数和网络记录 |
| `W002-DAC-02` / `W002-AC-02` | T02、T03：状态矩阵与截图 |
| `W002-DAC-03` / `W002-AC-03` | T04：真实 API、DOM 和排序断言 |
| `W002-DAC-04` / `W002-AC-04` | T05：Fake Timer 与新请求记录 |
| `W002-DAC-05` / `W002-AC-05` | T06：payload、幂等、mutation 和路由 |
| `W002-DAC-06` / `W002-AC-06` | T08：DOM、storage、network 和源码扫描 |
| `W002-DAC-07` / `W002-AC-07` | T07：故障注入与浏览器/键盘矩阵 |

## 5. 验证命令

- `npm run test -- Download`、`npm run test:coverage`。
- `npm run lint`、`npm run format:check`、`npm run typecheck`、`npm run build`。
- 连接真实 Server 验证成功、非法、禁止、不支持、登录、DRM、超限、超时和过期。
- Playwright Chromium/WebKit 加实际 Safari 复核 1440/768/375px 与键盘；实际 Safari 环境缺失时不得通过该项。

## 6. 提交顺序

`test: 输入与状态约束 → impl: inspect流程 → test: 摘要与格式约束 → impl: 结果选择 → test: 到期创建错误约束 → impl: 创建与恢复`

## 7. 完成条件

全部 W002 DAC/AC 为 `passed`，真实 Server 矩阵、竞态、到期、响应式和隐私扫描通过后，在已定义的 Acceptance 002 中填写证据与结论。
