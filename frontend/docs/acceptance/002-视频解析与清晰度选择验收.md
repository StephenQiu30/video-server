# Acceptance 002 视频解析与清晰度选择

## 状态与冻结输入

- 状态：Blocked（2026-07-20）；下载页/清晰度选择单元证据已执行，真实服务与浏览器 E2E 缺失。
- Design（已归档）：[002 视频解析与清晰度选择交互](../design/archive/002-视频解析与清晰度选择交互设计.md)。
- PRD（已归档）：[002 视频解析与清晰度选择需求](../prd/archive/002-视频解析与清晰度选择需求.md)。
- Plan：[002 视频解析与清晰度选择计划](../plans/002-视频解析与清晰度选择计划.md)。
- 冻结规则：实现开始后只填写实际证据、结果和结论；标准变化必须先重新确认上游文档。

## 1. 阶段开始门禁

- Acceptance 001、004 当前均为 `Blocked`；Plan 002 为 Implemented，但本阶段不能据此宣称前置验收已通过。
- 真实 Server inspect/create operation、受控合法媒体和全部冻结错误场景可用。
- Chrome/WebKit/实际 Safari、1440/768/375px 和键盘验证环境已准备。
- 任一项不满足时本阶段为 `blocked`，不得开始解析选择页面实现。

## 2. 逐任务验收

| 任务 | 必须通过 | 最低证据 |
| --- | --- | --- |
| `W002-T01` | 空值/非法协议/键盘/重复提交正确，一次动作一次请求 | 表单测试与网络计数 |
| `W002-T02` | idle/inspecting/inspected/failed/expired 状态唯一确定 | 状态矩阵与截图 |
| `W002-T03` | 新 inspect 不被旧响应/错误覆盖 | 取消/竞态测试 |
| `W002-T04` | 标题安全、摘要、未知大小、格式排序和 merge 展示准确 | 真实 API、DOM 与排序断言 |
| `W002-T05` | source/format 到期后禁止创建并可重新解析 | Fake Timer 与新请求记录 |
| `W002-T06` | payload 三字段、重复点击、幂等键和 job 路由准确 | mutation 与路由记录 |
| `W002-T07` | 错误可恢复，响应式、Chrome/Safari、键盘/live region 通过 | 故障与浏览器矩阵 |
| `W002-T08` | DOM/storage/network/source 无敏感或非 MVP 信息 | 隐私与源码扫描 |

当前任务未 `passed`，下一任务不得开始。

## 3. DAC/AC 验收矩阵

| 标准 | 通过条件与证据 |
| --- | --- |
| `W002-DAC-01` / `W002-AC-01` | 表单参数、提交和请求计数准确 |
| `W002-DAC-02` / `W002-AC-02` | inspect 状态和竞态矩阵通过 |
| `W002-DAC-03` / `W002-AC-03` | 真实摘要与格式排序展示准确 |
| `W002-DAC-04` / `W002-AC-04` | 到期和重新解析行为准确 |
| `W002-DAC-05` / `W002-AC-05` | payload、幂等、mutation 与路由准确 |
| `W002-DAC-06` / `W002-AC-06` | DOM/storage/network/源码隐私扫描零违规 |
| `W002-DAC-07` / `W002-AC-07` | 故障、viewport、浏览器、键盘通过 |

## 4. 阶段验证

- 执行 Download 单元/组件测试、覆盖率、lint、format、typecheck 和 build。
- 连接真实 Server 验证成功、非法、禁止、不支持、登录、DRM、超限、超时和过期。
- Playwright Chromium/WebKit 加实际 Safari 复核 1440/768/375px 与键盘。
- 检查 DOM、storage、network、控制台和源代码敏感字段。

## 4.1 实际执行证据与结论

- Download flow 与页面交互测试通过（解析成功/失败/过期、竞态、重复创建、清晰度选择、表单校验和创建错误）；全量 `npm test -- --run` 为 8 文件/42 测试通过，`npm run test:coverage` 退出码 0（Statements/Lines 99.50%、Branches 93.24%、Functions 96.07%），lint、format、typecheck、build 均通过。
- 已接入生成的 Umi Service、credentials 和 Problem Details 映射；但真实 Server 的成功/非法/DRM/超限样例及 Chromium/WebKit/Safari 视口与键盘复核尚未执行，故 W002 强制项为 `blocked`，阶段结论为 `Blocked`。

## 5. 证据与结论填写规则

执行后逐项填写命令、退出码、证据、结果和复核人。全部任务及 W002 DAC/AC 为 `passed` 才能改为 `Accepted` 并允许 W003；任一失败为 `Rejected`，真实 Server/Safari/证据缺失为 `Blocked`。
