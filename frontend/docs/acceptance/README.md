# Acceptance 文档

本目录在每个 Plan 实现前定义对应阶段的验收门禁，并在实现完成后原地补充证据与结论；不另建执行标准文件。

## 当前文档

- [001 Web 应用基座与全局体验验收](001-Web应用基座与全局体验验收.md)：Blocked；质量门禁与覆盖率通过，官方脚手架来源/浏览器矩阵待补。
- [004 API 请求与异常恢复验收](004-API请求与异常恢复验收.md)：Blocked；真实 schema 生成有历史证据，文件边界、真实故障恢复与服务端前置验收待补。
- [002 视频解析与清晰度选择验收](002-视频解析与清晰度选择验收.md)：Blocked；组件测试通过，真实服务/浏览器 E2E 待补。
- [003 下载任务状态与文件获取验收](003-下载任务状态与文件获取验收.md)：Blocked；状态测试通过，真实 artifact/浏览器恢复待补。

固定执行顺序为 `001 → 004 → 002 → 003`；`004` 还必须等待 video-server 004 accepted。任一阶段未 `accepted`，下一阶段不得开始。

## 文档生命周期

1. `Defined`：实现前冻结开始门禁、逐任务标准、DAC/AC、命令和证据；没有执行结论。
2. `In Verification`：Plan 全部任务完成，在同一文档中填写实际 branch、commit、环境、命令、退出码和证据。
3. `Accepted/Rejected/Blocked`：依据逐项结果形成阶段结论；不得创建另一份报告替代原定义。

实现开始后不得直接修改验收标准。确需改变时必须先更新并重新确认 Design/PRD/Plan，将 Acceptance 恢复为 `Defined`，再重新授权执行。

## 强制结论规则

1. 验收项只能来自 accepted Design 的 `Wxxx-DAC-*`、accepted PRD 的 `Wxxx-AC-*` 和 Ready Plan，不得在本目录临时创造、删除或降级标准。
2. 每项结论只允许 `passed`、`failed`、`blocked`；缺少证据等同未通过。
3. 任一项 `failed` 时整体为 `rejected`；无失败但存在 `blocked` 时整体为 `blocked`；只有全部强制项 `passed` 才能为 `accepted`。
4. 不允许 `accepted_with_risk`、条件通过、部分通过、后补证据或人工口头豁免。

## 必备内容

- Design、PRD、Plan 链接及验收编号完整清单。
- 当前 branch、完整 commit SHA、工作区状态、执行时间、浏览器/设备、执行环境和依赖版本。
- 每项的预期、完整命令或操作、退出码、证据位置和结论。
- 全量测试、Biome、TypeScript、覆盖率、生产构建、真实 Server/RabbitMQ/MinIO、失败路径、安全、响应式、键盘和浏览器端到端结果。
- 独立复核人及逐项复核结论。
- 只记录已确认非门禁风险；任何影响验收标准的风险必须判定 `failed` 或 `blocked`。

## 适合放入

1. 实现前冻结的阶段/任务验收标准。
2. 实现后填写的测试记录、验证证据和结论。
3. 回归证据和不影响门禁的残余风险说明。

## 不适合放入

1. 产品需求或执行计划。
2. 架构设计草案。
3. 发布流程说明。
4. 没有验收结论或长期复用价值的临时测试过程记录。

## 命名建议

使用与 Plan 相同的编号和主题，例如 `001-Web应用基座与全局体验验收.md`。
