# Acceptance 003 下载任务状态与文件获取

## 状态与冻结输入

- 状态：Blocked（2026-07-20）；任务状态组件单元证据已执行，真实 artifact 与浏览器恢复证据缺失。
- Design（已归档）：[003 下载任务与文件获取交互](../design/archive/003-下载任务与文件获取交互设计.md)。
- PRD（已归档）：[003 下载任务状态与文件获取需求](../prd/archive/003-下载任务状态与文件获取需求.md)。
- Plan：[003 下载任务状态与文件获取计划](../plans/003-下载任务状态与文件获取计划.md)。
- 冻结规则：实现开始后只填写实际证据、结果和结论；标准变化必须先重新确认上游文档。

## 1. 阶段开始门禁

- Acceptance 001、004、002 及全部服务端 Acceptance 当前均为 `Blocked`；Plan 003 为 Implemented，但本阶段不能据此宣称前置验收已通过。
- 真实 PostgreSQL/RabbitMQ/MinIO/yt-dlp/FFmpeg、受控视频和完整 API 状态机可用。
- Chrome/WebKit/实际 Safari、1440/768/375px、键盘和真实下载校验环境已准备。
- 任一项不满足时本阶段为 `blocked`，不得开始最终页面或全旅程验收。

## 2. 逐任务验收

| 任务 | 必须通过 | 最低证据 |
| --- | --- | --- |
| `W003-T01` | loading/queued/running/succeeded/failed/expired/未知状态视图准确 | 状态测试与截图 |
| `W003-T02` | 只在 active 且可见/在线时轮询，终态/隐藏/离线/卸载停止 | Fake Timer 与请求计数 |
| `W003-T03` | 临时 GET 失败保留最后状态，403/404/410/未知状态不伪成功 | 故障注入与恢复记录 |
| `W003-T04` | 刷新/重开仅凭 job ID+Cookie 恢复，storage 为空 | 浏览器刷新与 storage 扫描 |
| `W003-T05` | 点击前不签发 URL，重复点击和签发失败受控 | 网络记录与 mutation 测试 |
| `W003-T06` | 预签名地址 no-referrer 导航且不进入 cache/DOM/log，过期准确 | 浏览器与敏感扫描 |
| `W003-T07` | 长文本、进度语义、viewport、Safari 和键盘通过 | 浏览器/辅助技术证据 |
| `W003-T08` | 真实“链接→分辨率→文件”及 W001–W004 全量矩阵通过 | E2E、SHA-256 与全量报告 |

当前任务未 `passed`，下一任务不得开始。

## 3. DAC/AC 验收矩阵

| 标准 | 通过条件与证据 |
| --- | --- |
| `W003-DAC-01` / `W003-AC-01` | 所有任务状态组件与截图准确 |
| `W003-DAC-02` / `W003-AC-02` | 轮询、visibility、online/offline 行为准确 |
| `W003-DAC-03` / `W003-AC-03` | Cookie 刷新恢复且无客户端持久状态 |
| `W003-DAC-04` / `W003-AC-04` | GET 故障、状态和重试计数准确 |
| `W003-DAC-05` / `W003-AC-05` | 按需签发、真实 MinIO 下载和 checksum 一致 |
| `W003-DAC-06` / `W003-AC-06` | cache/DOM/URL/log 与过期扫描零泄露 |
| `W003-DAC-07` / `W003-AC-07` | 浏览器、viewport、键盘与辅助技术通过 |

## 4. 阶段验证

- 执行 DownloadJob 测试、覆盖率、lint、format、typecheck 和 build。
- 使用真实全部依赖执行完整 E2E，并以 SHA-256 对照 API artifact。
- 检查 Cookie、storage、cache、DOM、network 和控制台。
- Chromium/WebKit 加实际 Safari 验证 1440/768/375px、键盘和刷新恢复。
- 重跑 W001–W004 全部 DAC/AC，不合并或降级。

## 4.1 实际执行证据与结论

- DownloadJob 数据/状态/签发测试与页面状态交互测试通过（queued/running/succeeded/failed/expired/未知、离线、恢复、临时地址过期和失败）；全量 `npm test -- --run` 为 8 文件/42 测试通过，覆盖率门禁退出码 0（Statements/Lines 99.50%、Branches 93.24%、Functions 96.07%），lint、format、typecheck、build 均通过。
- 真实 MinIO artifact、SHA-256 对照、Cookie/storage/cache 刷新恢复以及 Chromium/WebKit/Safari E2E 尚未执行，故 W003 强制项为 `blocked`，阶段结论为 `Blocked`。

## 5. 证据与结论填写规则

执行后逐项填写命令、退出码、证据、结果和复核人。全部任务、W003 DAC/AC、T08 和 W001–W004 全量矩阵为 `passed` 才能改为 `Accepted`；任一失败为 `Rejected`，真实依赖/Safari/样例缺失为 `Blocked`。
