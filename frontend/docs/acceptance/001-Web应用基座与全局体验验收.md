# Acceptance 001 Web 应用基座与全局体验

## 状态与冻结输入

- 状态：Blocked（2026-07-20）；基座质量门禁和覆盖率已通过，官方脚手架来源与浏览器矩阵证据缺失。
- Design（已归档）：[001 Web 边界与总体架构](../design/archive/001-Web边界与总体架构设计.md)。
- PRD（已归档）：[001 Web 应用基座与全局体验需求](../prd/archive/001-Web应用基座与全局体验需求.md)。
- Plan：[001 Web 应用基座与全局体验计划](../plans/001-Web应用基座与全局体验计划.md)。
- 冻结规则：实现开始后只填写实际证据、结果和结论；标准变化必须先重新确认上游文档。

## 1. 阶段开始门禁

- 用户已明确授权并完成实现；Plan 001 为 Implemented，Acceptance 当前为 Blocked，不能据此宣称阶段已通过。
- 当前未形成可复核的 Ant Design Pro 官方 tag/commit 初始化记录；Node/npm 可用，但 Chrome/WebKit/Safari 验证矩阵尚未完成。
- `W001-T01` 可在临时目录执行官方初始化并产生预期检查；不得覆盖现有 docs/治理文件或引入业务 Mock。
- 任一项不满足时本阶段为 `blocked`，不得创建应用代码。

## 2. 逐任务验收

| 任务 | 必须通过 | 最低证据 |
| --- | --- | --- |
| `W001-T01` | 官方 tag/commit、`npm run simple`、lockfile 与环境配置可追溯/重复 | 初始化记录、`npm ci`、依赖树 |
| `W001-T02` | 根重定向、两条业务路由和 404 准确，无额外正式页面 | 路由测试与页面清单 |
| `W001-T03` | 菜单、标题、合法提示和唯一主内容准确 | 组件测试与截图 |
| `W001-T04` | Demo、账号、AI、Mock 和无关依赖/资源清零 | 文件、依赖和资源扫描 |
| `W001-T05` | 1440/768/375px、键盘、焦点、语义和不可信文本安全 | 浏览器矩阵和可访问证据 |
| `W001-T06` | storage、路由、日志、上报不泄露敏感信息 | storage/URL/DOM/日志扫描 |
| `W001-T07` | lint、format、type、unit、90% coverage 和 build 可执行 | 完整命令与退出码 |

当前任务未 `passed`，下一任务不得开始。

## 3. DAC/AC 验收矩阵

| 标准 | 通过条件与证据 |
| --- | --- |
| `W001-DAC-01` / `W001-AC-01` | 官方基线、lockfile、冷安装和构建可重复 |
| `W001-DAC-02` / `W001-AC-02` | 路由、请求和状态体系没有平行实现 |
| `W001-DAC-03` / `W001-AC-03` | 目录、Demo、越界功能与依赖扫描零违规 |
| `W001-DAC-04` / `W001-AC-04` | viewport、Chrome/Safari、键盘与语义通过 |
| `W001-DAC-05` / `W001-AC-05` | storage、URL、网络和日志无敏感持久化 |
| `W001-DAC-06` / `W001-AC-06` | 只使用冻结技术栈且无基础设施选择界面 |
| `W001-DAC-07` / `W001-AC-07` | 环境覆盖通过且无 Endpoint/Secret 硬编码 |

## 4. 阶段验证

- 两次 `npm ci` 并确认 lockfile 无差异。
- `npm run lint`、`npm run format:check`、`npm run typecheck`。
- `npm run test:coverage`，新增代码综合覆盖率不低于 90%。
- `npm run build`。
- 检查路由、1440/768/375px、Chrome/Safari、键盘、控制台和持久存储。

## 4.1 实际执行证据与结论

- `npm test -- --run`：退出码 0，8 个测试文件/42 个测试通过；`npm run lint`、`npm run format:check`、`npm run typecheck`、`npm run build` 均退出码 0。
- `npm run test:coverage`：退出码 0；Statements/Lines `99.50%`、Branches `93.24%`、Functions `96.07%`，均达到 90% 门禁。覆盖配置明确排除 Umi/OpenAPI 生成目录与生成 Service，不隐藏业务源文件；结果仅输出到终端，不保留 `coverage/` HTML/JSON 生成报告。
- `.gitattributes` 已冻结文本文件 LF 规则；工作区规范化后 `npm run format:check` 退出码 0。GitHub Actions 已加入 npm 冷安装、lint、format、coverage 与 build 门禁，远端执行结果须在本轮提交后补充核对。
- Ant Design Pro/Umi 仍通过标准 `npm run build` 生成静态产物；新增多阶段 Web 镜像、Nginx SPA/`/api/` 代理以及默认/`prod` Compose。默认与生产组合的 `docker compose ... config --quiet`、镜像构建和 `nginx -T` 均退出码 0；真实容器 `/healthz` 与首页返回 200，代理请求到达真实 API，临时容器和网络已清理。
- 仓库历史只记录了“建立 AntDesignPro 官方脚手架基线”的本地提交，缺少可回溯到上游官方 tag/完整 commit SHA 的初始化记录，也未保留 `npm run simple` 证据；因此 `W001-T01`、`W001-DAC-01` 与 `W001-AC-01` 仍为 `blocked`。
- 新增 JSDOM Testing Library 交互证据覆盖表单校验/回车提交、解析竞态、清晰度选择、任务状态、失败恢复、临时地址按需签发、过期保护和 no-referrer 导航；`crypto.randomUUID` 不可用时的请求 ID fallback 亦有回归测试。
- 1440/768/375px、Chrome/Safari、键盘、控制台和持久存储尚未完成真实浏览器复核，故 W001 强制项为 `blocked`，阶段结论为 `Blocked`。

## 5. 证据与结论填写规则

执行后逐项填写命令、退出码、证据、结果和复核人。全部任务及 W001 DAC/AC 为 `passed` 才能改为 `Accepted`；任一失败为 `Rejected`，实际 Safari 或环境证据缺失为 `Blocked`。
