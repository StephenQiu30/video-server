# Copy to .env.prod and replace every placeholder before deployment.
VIDEO_WEB_IMAGE=replace-with-registry/video-web:0.1.0
VIDEO_API_BASE_URL=
VIDEO_API_UPSTREAM=replace-with-private-api-host:19090
WEB_BIND=127.0.0.1
WEB_PORT=8080

# Only needed when regenerating the committed Umi OpenAPI client.
VIDEO_API_PROXY_TARGET=
OPENAPI_SCHEMA_URL=
