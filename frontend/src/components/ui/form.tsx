'use client';

import { Form as RadixForm } from 'radix-ui';

/**
 * Project form boundary. Business views use this Radix primitive instead of
 * rendering a native form element directly.
 */
export const Form = RadixForm.Root;
